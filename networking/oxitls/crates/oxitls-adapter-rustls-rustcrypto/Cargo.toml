[package]
name = "oxitls-adapter-rustls-rustcrypto"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "OxiTLS adapter: rustls + RustCrypto CryptoProvider (pure Rust TLS)"
readme = "README.md"
categories = ["cryptography", "network-programming"]
keywords = ["tls", "rustls", "rustcrypto", "pure-rust", "cooljapan"]

[features]
# Post-quantum hybrid key exchange (X25519MLKEM768, draft-ietf-tls-ecdhe-mlkem)
# — fully wired (Wave 6 complete): see src/kx.rs for the real
# encapsulate/decapsulate implementation (ml-kem + x25519-dalek), exported
# from lib.rs and prepended to the provider's supported KX groups.
post-quantum = ["dep:ml-kem", "dep:x25519-dalek"]
# RFC 9180 HPKE base-mode provider for Encrypted Client Hello (ECH).
ech = ["dep:hkdf", "dep:x25519-dalek", "dep:aes-gcm", "dep:chacha20poly1305", "p256/ecdh"]
# RFC 8879 TLS certificate compression backed by OxiARC pure-Rust zlib.
cert-compression = ["dep:oxiarc-deflate"]

[dependencies]
oxitls-core = { workspace = true }
oxitls-webpki-roots = { workspace = true }
rustls = { workspace = true, features = ["std", "logging"] }
rustls-pemfile = { workspace = true }
rustls-rustcrypto = { workspace = true }
rustls-pki-types = { workspace = true }
tokio-rustls = { workspace = true, features = ["early-data"] }
tokio = { workspace = true }
p256 = { workspace = true }
p384 = { workspace = true }
ed25519-dalek = { workspace = true, features = ["alloc"] }
rsa = { workspace = true }
sha2 = { workspace = true }
# SHA-1 is required to recompute OCSP CertID issuer hashes: RFC 6960 responders
# overwhelmingly use id-sha1 as the CertID hashAlgorithm. Pure Rust (RustCrypto).
sha1 = { workspace = true }
signature = { workspace = true }
x509-ocsp = { workspace = true }
der = { workspace = true }
spki = { workspace = true }
tracing = { workspace = true }
x509-parser = { workspace = true }
x509-cert = { workspace = true }
ml-kem = { workspace = true, optional = true }
x25519-dalek = { workspace = true, optional = true }
hkdf = { workspace = true, optional = true }
aes-gcm = { workspace = true, optional = true }
chacha20poly1305 = { workspace = true, optional = true }
# NOTE: rand_core/rand are intentionally NOT direct deps of this crate. The
# getrandom-backed `oxitls_core::OsRng` (rand_core 0.6) feeds x25519-dalek's
# `EphemeralSecret::random_from_rng`; ml-kem's keygen takes no RNG argument.
getrandom = { workspace = true }
oxiarc-deflate = { workspace = true, optional = true }

[dev-dependencies]
rcgen = { workspace = true, features = ["crypto", "pem", "ring"] }
tokio = { workspace = true, features = ["full", "macros"] }
oxitls-webpki-roots = { path = "../oxitls-webpki-roots" }
sha2 = { workspace = true }
oxitls-rcgen = { path = "../oxitls-rcgen" }
criterion = { workspace = true }
p256 = { workspace = true }
rsa = { workspace = true }
x509-ocsp = { workspace = true, features = ["builder"] }
x509-cert = { workspace = true }
der = { workspace = true }

[[bench]]
name = "builder"
harness = false

[[bench]]
name = "cert_pin_lookup"
harness = false

[[bench]]
name = "handshake_full_vs_resumed"
harness = false

[[bench]]
name = "crl_check"
harness = false

[[bench]]
name = "deep_chain_validation"
harness = false

[[bench]]
name = "memory"
harness = false

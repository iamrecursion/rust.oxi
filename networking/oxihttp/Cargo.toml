[workspace]
resolver = "2"
members = [
    "crates/oxihttp-core",
    "crates/oxihttp-client",
    "crates/oxihttp-server",
    "crates/oxihttp",
]

[workspace.package]
version = "0.2.2"
edition = "2021"
# Covers the DEFAULT feature set only (client + server, no tls/h3/
# compression/decompression). Some optional features pull in sibling
# COOLJAPAN crates with a higher declared floor of their own — oxitls (tls
# feature, published rust-version 1.89), oxiarc-deflate/oxiarc-core
# (compression/decompression, 1.85). oxiquic-h3/oxiquic-crypto (h3) publish
# rust-version 1.80, same as this default floor. cargo's resolver enforces
# each floor automatically when that feature is enabled; see the MSRV table
# in README.md (verified against `cargo tree --features <feature>`, not
# just each sibling repo's current — possibly ahead-of-published — Cargo.toml)
# for the full per-feature breakdown before raising this value.
rust-version = "1.80"
authors = ["COOLJAPAN OU (Team Kitasan)"]
license = "Apache-2.0"
repository = "https://github.com/cool-japan/oxihttp"
description = "OxiHTTP is the COOLJAPAN Pure-Rust HTTP stack — a reqwest-compatible facade over hyper."

[workspace.dependencies]
oxihttp-core   = { path = "crates/oxihttp-core",   version = "0.2.2" }
oxihttp-client = { path = "crates/oxihttp-client", version = "0.2.2" }
oxihttp-server = { path = "crates/oxihttp-server", version = "0.2.2" }
# external — re-read from crates.io 2026-05-25
http            = { version = "1.4" }
http-body       = { version = "1.0" }
bytes           = { version = "1.12" }
http-body-util  = { version = "0.1" }
hyper           = { version = "1.10", features = ["client", "http1", "server", "http2"] }
hyper-util      = { version = "0.1", features = ["client", "client-legacy", "http1", "http2", "tokio", "server", "server-auto"] }
tokio           = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util", "signal", "sync", "time"] }
tower           = { version = "0.5", features = ["util"] }
tower-service   = { version = "0.3" }
tower-layer     = { version = "0.3" }
tower-http      = { version = "0.7" }
thiserror       = { version = "2.0" }
serde           = { version = "1.0", features = ["derive"] }
serde_json      = { version = "1.0" }
percent-encoding = { version = "2.3" }
pin-project-lite = { version = "0.2" }
futures-core    = { version = "0.3" }
# New in this pass — TLS (M2)
oxitls           = { version = "0.3.0", default-features = false, features = ["pure", "webpki-roots"] }
oxitls-core      = { version = "0.3.0" }
tokio-rustls     = { version = "0.26", default-features = false, features = ["tls12"] }
rustls           = { version = "0.23", default-features = false, features = ["std", "tls12"] }
rustls-pki-types = { version = "1" }
rustls-pemfile   = { version = "2" }
# New in this pass — server features (M3 completion)
oxiarc-deflate   = { version = "0.4.2" }
# CRC-32 verification for the bounded gzip decompression path (client-side
# decompression-bomb fix) — same version oxiarc-deflate itself resolves to.
oxiarc-core      = { version = "0.4.2" }
futures-util     = { version = "0.3", default-features = false, features = ["alloc", "async-await"] }
sha2             = { version = "0.11", default-features = false }
mime_guess       = { version = "2", default-features = false }
# WebSocket support (M4 Block D)
sha1             = { version = "0.11", default-features = true }
base64           = { version = "0.23", default-features = false, features = ["alloc"] }
# Benchmark infrastructure (M6 Block A)
criterion        = { version = "0.8", features = ["async_tokio"] }
# TCP socket tuning (M7 Block C)
socket2          = { version = "0.6", features = ["all"] }
# HTTP/3 via oxiquic
oxiquic-h3       = { version = "0.2.1", default-features = false, features = ["h3-compat"] }
oxiquic-crypto   = { version = "0.2.1" }
# Testing
proptest         = { version = "1" }

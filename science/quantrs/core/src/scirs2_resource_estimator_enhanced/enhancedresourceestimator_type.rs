//! Auto-generated module
//!
//! 🤖 Generated with [SplitRS](https://github.com/cool-japan/splitrs)

use crate::parallel_ops_stubs::*;
use crate::platform::PlatformCapabilities;

use super::types::{
    ComparativeAnalyzer, CostAnalyzer, EnhancedResourceConfig, HardwareRecommender,
    MLResourcePredictor, OptimizationEngine, RealtimeResourceTracker, ScalingPredictor,
    VisualResourceGenerator,
};

/// Enhanced quantum resource estimator
pub struct EnhancedResourceEstimator {
    pub(super) config: EnhancedResourceConfig,
    pub(super) ml_predictor: MLResourcePredictor,
    pub(super) cost_analyzer: CostAnalyzer,
    pub(super) optimization_engine: OptimizationEngine,
    pub(super) comparative_analyzer: ComparativeAnalyzer,
    pub(super) realtime_tracker: RealtimeResourceTracker,
    pub(super) visual_generator: VisualResourceGenerator,
    pub(super) hardware_recommender: HardwareRecommender,
    pub(super) scaling_predictor: ScalingPredictor,
    pub(super) platform_capabilities: PlatformCapabilities,
}

# Normalize line endings to LF for all text files (this is a Rust project; LF everywhere).
* text=auto eol=lf

# Treat these as binary (no conversion).
*.png binary
*.jpg binary
*.pdf binary
*.wasm binary

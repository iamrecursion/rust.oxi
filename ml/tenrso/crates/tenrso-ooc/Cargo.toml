[package]
name = "tenrso-ooc"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
description = "Out-of-core processing with Arrow/Parquet for TenRSo"
categories = ["filesystem", "caching", "concurrency", "science"]
keywords = ["tenrso", "out-of-core", "arrow", "parquet", "memory-mapped"]

[dependencies]
tenrso-core.workspace = true
# Streaming MTTKRP delegates all per-chunk math to the in-core kernels.
# (tenrso-kernels does NOT depend on tenrso-ooc: no cycle.)
tenrso-kernels.workspace = true
scirs2-core.workspace = true
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing = { workspace = true, optional = true }
tracing-subscriber = { workspace = true, optional = true }
arrow = { workspace = true, optional = true }
parquet = { workspace = true, optional = true }
memmap2 = { workspace = true, optional = true }
oxiarc-lz4 = { workspace = true, optional = true }
oxiarc-zstd = { workspace = true, optional = true }
opentelemetry = { workspace = true, optional = true }
opentelemetry_sdk = { workspace = true, optional = true }
opentelemetry-otlp = { workspace = true, optional = true }
opentelemetry-semantic-conventions = { workspace = true, optional = true }
prometheus = { workspace = true, optional = true }
tokio = { workspace = true, optional = true }
parking_lot = { workspace = true, optional = true }
lazy_static = { workspace = true, optional = true }
tikv-jemallocator = { workspace = true, optional = true }
tikv-jemalloc-ctl = { workspace = true, optional = true, features = ["stats"] }
mimalloc = { workspace = true, optional = true }
num_cpus.workspace = true
crossbeam = { workspace = true, optional = true }
dashmap = { workspace = true, optional = true }
tonic = { workspace = true, optional = true }
prost = { workspace = true, optional = true }
oxicode = { workspace = true, optional = true }
bytes = { workspace = true, optional = true }
futures = { workspace = true, optional = true }
# Real GPU compute backend (default-OFF; only pulled in by `cuda-compute`).
oxicuda-driver = { workspace = true, optional = true }
oxicuda-memory = { workspace = true, optional = true }
oxicuda-blas = { workspace = true, optional = true }
crc32fast.workspace = true
xxhash-rust.workspace = true
blake3.workspace = true

[features]
default = ["arrow", "parquet", "mmap", "parallel", "compression", "lz4-compression", "zstd-compression", "tracing", "lock-free"]
arrow = ["dep:arrow"]
parquet = ["dep:parquet"]
mmap = ["dep:memmap2"]
parallel = ["scirs2-core/parallel", "tenrso-kernels/parallel"]
compression = ["dep:oxiarc-lz4", "dep:oxiarc-zstd"]
lz4-compression = ["compression", "dep:oxiarc-lz4"]
zstd-compression = ["compression", "dep:oxiarc-zstd"]
tracing = ["dep:tracing", "dep:tracing-subscriber"]
# GPU device enumeration (src/gpu.rs). All default-off: a plain build never
# links a vendor SDK and never probes for hardware.
#
# `gpu` is the base switch that pulls in scirs2-core's GPU abstraction
# (`scirs2_core::gpu`); it alone enables no vendor-specific detection.
# Enable the vendor-specific features below to opt into real hardware
# enumeration for that backend (see `DeviceManager::available_backends`).
gpu = ["scirs2-core/gpu"]
# NVIDIA CUDA device enumeration via `nvidia-smi` (scirs2-core has no
# dedicated `cuda` feature in 0.6.x; the base `gpu` feature is sufficient).
cuda = ["gpu"]
# AMD ROCm device enumeration via `rocm-smi`.
rocm = ["gpu", "scirs2-core/rocm"]
# Apple Metal device enumeration via the real Metal API (macOS only; the
# feature compiles everywhere else but finds nothing on non-macOS hosts).
metal = ["gpu", "scirs2-core/metal"]
# Cross-platform GPU compute adapter detection (Vulkan / DirectX12 / Metal /
# GL) via scirs2-core's `wgpu` backend.
vulkan = ["gpu", "scirs2-core/wgpu"]
# REAL CUDA elementwise dispatch (src/gpu_cuda.rs). Off by default. Builds on
# top of the `cuda` enumeration feature and additionally pulls in the pure-Rust
# oxicuda driver/memory/BLAS crates so that `Device::add` / `Device::mul` for
# f32 execute on an actual NVIDIA GPU (H2D -> kernel -> D2H). No CUDA SDK is
# needed at build time; libcuda is loaded at runtime. This is a capability /
# correctness path, NOT a speedup for single host-buffer ops (see src/gpu.rs).
cuda-compute = ["cuda", "dep:oxicuda-driver", "dep:oxicuda-memory", "dep:oxicuda-blas"]
opentelemetry = ["dep:opentelemetry", "dep:opentelemetry_sdk", "dep:opentelemetry-otlp", "dep:opentelemetry-semantic-conventions", "dep:tokio", "dep:parking_lot"]
prometheus-metrics = ["dep:prometheus", "dep:parking_lot", "dep:lazy_static"]
jemalloc = ["dep:tikv-jemallocator", "dep:tikv-jemalloc-ctl"]
mimalloc-allocator = ["dep:mimalloc"]
lock-free = ["dep:crossbeam", "dep:dashmap", "dep:parking_lot"]
distributed = ["dep:tonic", "dep:prost", "dep:oxicode", "dep:bytes", "dep:futures", "dep:tokio", "dep:parking_lot", "lock-free"]

# NUMA memory policies (src/numa.rs) are a Linux kernel facility; libc is only
# needed there, so it is not pulled in on other platforms.
[target.'cfg(target_os = "linux")'.dependencies]
libc.workspace = true

[dev-dependencies]
criterion.workspace = true
proptest.workspace = true
tenrso-decomp.workspace = true
tonic-build.workspace = true

[[example]]
name = "distributed_demo"
required-features = ["distributed"]

[[bench]]
name = "large_tensors"
harness = false

[[bench]]
name = "advanced_features"
harness = false

[[bench]]
name = "allocator_comparison"
harness = false

[[bench]]
name = "ml_eviction_benchmarks"
harness = false

[[bench]]
name = "lockfree_prefetch_benchmarks"
harness = false

[[bench]]
name = "distributed_benchmarks"
harness = false
required-features = ["distributed"]

[[bench]]
name = "integrity_and_autoselect"
harness = false

[[bench]]
name = "ooc_benchmarks"
harness = false

[[bench]]
name = "micro"
harness = false

[[bench]]
name = "mttkrp_stream"
harness = false

# OxiBonsai TODO

## Production-Release Audit (2026-07-20) — 91 verified findings, 23 implementation packages

> Source: 109-agent audit (15 domain sweeps + Opus deep triage + adversarial per-finding verification).
> Raw 98 → deduped 93 → **confirmed 91** (P0:4, P1:11, P2:59, P3:17), 2 rejected as false positives.
> Work orders with full evidence per package: session scratchpad `pkg/*.json`.
> Baseline before fixes: 4763 tests green, clippy clean except 5 example warnings.

> **Status update (2026-07-20, post-fix pass): all 23 implementation packages below are DONE.** Checkboxes flipped to `[x]` with an outcome summary per package (verified against the actual working tree, not just the work orders). Two items were newly discovered/surfaced during this pass; one (the FP8 CUDA split-KV-cache bug) was mitigated the same day in a residual-fix pass, one remains open — see "Newly discovered, unresolved" below.

### Batch 1 — hard/correctness (Opus agents)

- [x] **cuda-prefill** (P0) — DONE. Unified `PREFILL_STATE`/`FULL_LAYER_STATE` KV caches so batch prefill writes the same device buffer decode reads (`cuda_prefill/state.rs` now delegates to `cuda_full_layer::acquire_kv_cache`); added the missing per-token `d_pos_seqlen` upload to the Q1 prefill loop; **re-enabled ternary CUDA batch prefill** (dropped the `#[allow(dead_code)]` guard in `forward_cuda/q1.rs`). Q1 and ternary CPU↔CUDA parity now ~0.001 logit delta (was ~7.3), identical greedy sequences. Auditing FP8/Q-std/K-quant prefill for the same split-cache bug found it present in all three; **Q4_0/Q8_0 (`q_std`) and K-quant (Q2K–Q8K), which this package owns, are now intentionally disabled by default** (unconditional `Err` → correct sequential fallback) pending the same KV-cache unification. **FP8 was audited and found to have the identical bug** (out of this package's file ownership) — flagged as a new P0 item below and **mitigated the same day** in a residual-fix pass (both FP8 CUDA batch-prefill entry points now disabled-by-default, matching q_std/k_quant). Findings #0/#11/#43/#74 fixed. 195 tests pass, clippy clean.
- [x] **pipeline** (P1) — DONE. `InferencePipeline::run_autoregressive`/`run_beam_search` now honor `GenerationStrategy` (Greedy/Sampling via a real `SamplerChain`), apply the attached `TokenConstraint` (masking + `StopReason::ConstraintComplete`), wire token healing against the real model (real `vocab_size`, real prefix logits, not the previous hardcoded `vocab_size=0` no-op), and no longer panic on engine errors (new `try_run() -> RuntimeResult<PipelineOutput>`, additive `StopReason::Error(String)`; `run()` keeps its old signature for the CLI integration test). Findings #5/#6/#15/#38 fixed; 17/17 pipeline tests pass.
- [x] **speculative** (P2) — DONE. Added a real two-engine `SpeculativeDecoder::generate_verified(target: &mut InferenceEngine, ...)`: drafts K tokens via the shipped delta-KV path and verifies them against a **separate target engine** through `InferenceEngine::verify_batch`/`forward_prefill_verify` — no synthetic logits; output is token-identical to plain greedy target decode. The old `generate_speculative`/`verify` mock harness (1/vocab_size draft probability, synthesized target logits) is now `#[doc(hidden)]` and documented as a synthetic test harness only — no public API advertises fabricated results. Also fixed the draft-reuse OOB panic and capped `Vec::with_capacity(max_tokens)` preallocations at 65536. Findings #16/#25/#36 fixed; 762 runtime tests pass.
- [x] **tokenizer** (P1) — DONE. Special/added-token protection at the `encode()` dispatch level (leftmost-longest match before any BPE/Unigram/WordPiece segmentation); GPT-2 byte-level bytes↔unicode map wired into `encode()` (CJK/emoji/accented Latin now correct, whitespace runs survive round-trip); `BpeMerges` merge-rank lookup changed from an O(total-merges) `Vec::position` scan to O(1) `HashMap`. Findings #8/#9/#14/#34 fixed; 366 tests pass (21 new).
- [x] **constrained** (P2) — DONE. `JsonConstraint`/`RegexConstraint` gained real `with_decoder(Fn(u32)->Option<String>, vocab_size)` modes operating on actual token TEXT (new `TokenTextIndex`) instead of interpreting raw token IDs as Unicode codepoints; the old ID-as-codepoint constructors are retained but now loudly documented as demonstration-only. Finding #17 fixed.
- [x] **kernels-cpu** (P2) — DONE. `AlignedBuffer`/`AlignedBlocks` guarded against `usize` allocation-size wraparound; lib.rs's SIMD-feature-gate doc table corrected (features are informational; tiers are runtime-selected); dead AVX-512 gather/scatter helpers removed and the previously-dead AVX-512 streaming-store GEMV wired in behind the AVX-512 tier; `PlatformProfile::global_thresholds()` now genuinely governs the live GEMV/GEMM parallel-dispatch decision; Q4_0/Q8_0 got full AVX2+AVX-512 tiered GEMV kernels plus Rayon row-parallel wrappers, K-quant (Q2K..Q8K) got Rayon row-parallelism (SIMD deferred as the stated stretch goal). Findings #78/#85/#41/#20/#42 fixed; 433 tests pass. **Correction (2026-07-21, `kernels-cpu-02`):** the "`PlatformProfile::global_thresholds()` now genuinely governs the live GEMV/GEMM parallel-dispatch decision" claim above was only true for `parallel.rs`'s row/batch-parallel entry points at the time — `parallel_tiled.rs`'s adaptive strategy selector (`select_gemv_strategy`) and `tiled.rs`'s L1 tile sizing (`optimal_tile_rows`) still used hardcoded, platform-oblivious constants. **Now fully true as of the Round-2 fix** — see the Production-Release Audit Round 2 section below (new `TunedThresholds::par_tiled_min_rows`, `optimal_tile_rows` now reads the real `l1_cache_bytes`). Also: NEON GEMV kernels for Q4_0/Q8_0 (closing the CPU/SIMD half of the K-quant/Q_std gap this entry left implicit) shipped in a follow-up `oxibonsai-kernels` package the same day, dispatch-wired in wave 2 — see Round 2 below.
- [x] **server-core** (P0/P1) — DONE. Up-front chat-request validation (`max_tokens` ≤ 8192, temperature/top_p/`n` bounds) returning OpenAI-style 400s; `/rag/query` now does real encode→generate→decode on the retrieved context (was a hardcoded 1-token fabrication) via a pooled `RagState`; `/v1/models` reports the actually-loaded model; `/admin/config` reports the real running config + model info, `/admin/cache-stats` reports an honest null/not-enabled structure instead of fake zeros; the admin router, chat web UI (`/ui`), CORS, request logging, and an opt-in rate limiter are now mounted into the served router. Findings #1/#7/#27/#52/#53/#54/#68/#81/#88/#90 fixed; 17 new regression tests.

### Batch 2 — API/robustness (Sonnet agents)

- [x] **server-api** (P2) — DONE. Real `finish_reason` for the extended chat endpoint (derived from tool_calls/hit_stop/output length, was a hardcoded `"stop"` literal); `completions.rs` now threads the request's real `max_tokens` (was hardcoded 16); `frequency_penalty`/`presence_penalty` are rejected with an honest 400 in both handlers rather than silently discarded (`sampling.rs`'s `SamplingParams`/`Sampler` have no penalty seam to wire into — out of this package's file ownership); the extended endpoint's `logprobs` now honestly returns `None` instead of a fabricated non-null-but-empty `ChoiceLogprobs`; `n>4` on the extended endpoint now 400s. Findings #28/#29/#30/#31/#55 fixed. The Phase 5 "OpenAI API extensions" line further down in this file has been annotated to match (penalties validated-and-rejected, logprobs honestly null — the package's report explicitly asked for this but didn't touch TODO.md per its own ownership scope; done here instead). **Superseded 2026-07-21:** a follow-up `oxibonsai-runtime` package built the missing penalty seam (`Sampler::sample_with_history` + `PenaltyParams`) and a logits-capturing `generate_with_logprobs`, then `w2-server-base` wired both into the base **and** extended chat endpoints for real — `frequency_penalty`/`presence_penalty` are now genuinely applied (the 400-rejection is retired) and `logprobs`/`top_logprobs` return real per-token values on both endpoints. See the Production-Release Audit Round 2 section below.
- [x] **gguf-core** (P1) — DONE. Bounded Array-nesting recursion depth in both metadata parsers; checked/saturating arithmetic for tensor offset/size and shape products; alignment validation (nonzero power-of-two); `compat.rs`'s `check_gguf_header`/`build_compat_report` wired into a real production call path (`GgufFile::parse` + new tolerant `GgufFile::probe_compat`). Findings #10/#18/#33/#59/#60 fixed.
- [x] **quant-export** (P1) — DONE. Fixed the Q1_0_g128 export sign-bit convention inversion (bit=1⇒+scale, matching the kernels/loader — a genuine correctness bug, verified end-to-end through the real GGUF reader); `Int8PerChannel` export now refuses with `ExportError::NoLoaderForFormat` instead of silently mislabeling packed `[i8][f32 scales]` bytes as `TensorType::F32`; `quantize_per_channel` guards `num_channels==0`; `Checkpoint::read_from` bounds all eager pre-allocations against a corrupted/adversarial header (`MAX_EAGER_CAPACITY`); `load_f32_tensor` extended to dequantize Q4_K/F8_E4M3/F8_E5M2 for the export/loader round-trip. Findings #4/#22/#46/#47/#79 fixed.
- [x] **serve-bin** (P1) — DONE. `limits.max_concurrent_requests`/`per_request_timeout_ms` now genuinely enforced via a tower admission stack (overload→503, timeout→408); `tokenizer.kind` validated against a real backend whitelist at startup (rejected otherwise); `quantization_hint` cross-checked against real GGUF tensor-type names (kept non-fatal/logging-only — the shipped example config's own doc comment establishes this as intentional); CLI-override precedence fixed via an explicit "which flags were literally present in argv" tracker (was comparing against `ServerArgs::default()`, so a CLI flag matching the built-in default silently lost to a non-default TOML/env value); bearer-token comparison is now constant-time. Findings #12/#61/#62/#89 fixed.
- [x] **grammar** (P2) — DONE. JSON-Schema compiler now rejects `minimum`/`maximum`/`minLength`/`maxLength`/`minItems`/`maxItems`/`uniqueItems` (previously silently ignored despite not being on the documented "out of scope" list) and implements real `const` support; `select_tool` now validates arguments against the matched tool's schema (was JSON-well-formedness only); `compile_object_type` now supports optional (non-required) properties; `AllowedTokensCache` is now keyed by `(state_hash, vocab_size)` instead of `state_hash` alone (closes a latent wrong-length-mask risk). Findings #32/#56/#57/#82 fixed.
- [x] **engine-misc** (P2) — DONE. `PrefixCachedEngine` now owns a persistent seeded `Sampler` across calls (was rebuilt fresh every call with seed 0, discarding RNG state); `SemanticCache::refit_embedder` re-embeds existing entries instead of leaving stale-dimension "zombie" entries, `max_entries==0` is clamped to 1; all server-reachable `.lock().expect("poisoned")` sites (26 total across `semantic_cache.rs`/`embeddings.rs`) replaced with poison-recovering locks. Findings #26/#51/#70/#71 fixed.
- [x] **cli** (P2) — DONE. `oxibonsai quantize` no longer fabricates a result — it dequantizes every source tensor and re-encodes through the real `oxibonsai_model::export::export_to_gguf` pipeline, writing real bytes and real stats; docs/CLI.md's "simulates the operation" language and stale format list have been corrected to match (see docs/CLI.md). `oxibonsai serve --rag` mounts the RAG HTTP API alongside the OpenAI-compatible routes (previously unreachable from any binary). Findings #35/#13 fixed. **Known test regression flagged by this package, tracked below, not fixed here:** `server_integration_tests::test_models_endpoint_returns_bonsai_8b` now fails because `/v1/models` (per the `server-core` fix above) reports the real loaded-model name instead of the hardcoded `"bonsai-8b"` the test still expects.
- [x] **eval-rag** (P2) — DONE. TruthfulQA MC1/MC2 and the shared `McLogitEvaluator` (also used by ARC/HellaSwag/Winogrande/MMLU) no longer default to index 0 (silently "correct") on empty/all-NaN logits — they now skip via an explicit NaN-safe best-match tracker and report a new `skipped` count; `ChunkConfig::validate()` now rejects `min_chunk_size > chunk_size` (previously guaranteed permanent zero-chunk ingestion) on every indexing path (`add_document`/`add_documents`/`RagPipeline::index_document(s)`). Findings #63/#64 fixed.

### Batch 3 — platform/infra (Sonnet + Opus for image)

- [x] **model-misc** (P3) — DONE. `KvCache::store_key`/`store_value` gained real release-mode bounds checking (`try_store_key`/`try_store_value`; the old `debug_assert!`-only checks were a no-op in release builds and could index out of range); `PagedKvCache::sequence_length()` now returns the actual highest-written position instead of block-rounded capacity. Findings #80/#87 fixed.
- [x] **wasm** (P2) — DONE. Restored the advertised `wasm32-unknown-unknown` build. Root cause verified as `engine_pool` (unconditionally `use`s `tokio`, which is confined to non-wasm target deps) not being cfg-gated like the sibling `async_engine` module — the original finding's `memmap2` hypothesis was checked and found false. `cargo check -p oxibonsai-runtime --target wasm32-unknown-unknown --no-default-features` now passes; a regression test shells out to the exact acceptance command. Finding #65 fixed.
- [x] **facade-release** (P0) — DONE. `oxibonsai`'s `server` feature now also enables `dep:oxibonsai-serve` (previously only enabled `oxibonsai-runtime/server`, so `--features server`/`--features full` failed to compile); `scripts/publish.sh` gained the missing `oxibonsai-image` entry in a re-derived, verified topological publish order; `oxibonsai-cli`'s root `Cargo.toml` gained `homepage`/`documentation`/`keywords`/`categories` metadata matching every other workspace crate. Findings #2/#3/#72 fixed.
- [x] **ci** (P2) — DONE. Authored a real `.github/workflows/ci.yml` (previously empty): fmt, clippy `--all-features --all-targets -D warnings`, build+nextest, a wasm32-unknown-unknown compile-check, a macOS `--features metal` compile-only job, a native-cuda compile-check job, and a manually-triggered self-hosted-A4000 GPU job running the real GPU test commands. Findings #37/#39/#40 fixed. **Policy note (recorded 2026-07-21, `release-infra-03`):** `.github/workflows/ci.yml` existing at all is a deliberate, user-acknowledged exception to the general "`.github/workflows`: only `pypi-publish.yml`/`npm-publish.yml` allowed" policy — this is a pure Rust/crates.io project (no PyPI/npm workflows exist or apply), CI here is build/test/lint only with no publish/credentialed step, and its restoration was already an intentional prior decision (see the Phase 14 "CI re-enabled" entry below, predating this audit). No action taken; kept as-is.
- [x] **kernels-meta** (P2) — DONE. `build.rs`'s embedded-metallib fast path (permanently dead — pointed at a deleted single-file module) rewired to read every file under `kernel_sources/`; the `ACTIVE_KERNELS` whitelist reconciled against the actual combined-MSL builder (with a loud panic on future drift); stale 128-thread/4-simdgroup doc comments in `metal_dispatch.rs` corrected to 256 threads/8 simdgroups (a third stale copy in `kernel_sources/dit_attention_flash.rs` was left untouched — outside this package's file ownership). Findings #21/#86 fixed.
- [x] **tests-quant** (P3) — DONE. Added property/roundtrip test coverage for Q3_K/Q5_K/Q6_K/Q8_K/TQ2_0(_g128)/FP8 E4M3+E5M2/Q4_0/Q8_0 (49 new test cases, test-file-only). **Surfaced a real, previously-uncovered production defect while doing so (not fixable by this test-only package — see the new P1 item below):** `BlockFP8E5M2::quantize` (`oxibonsai-core/src/quant_fp8.rs`) can round its block scale such that the block's maximum-magnitude element overflows to actual IEEE `Infinity` after dequantize — reproducible in ~49% of random 32-element blocks. A `#[ignore]`d regression test documents and reproduces it so the defect isn't silently hidden while the default suite stays green. Finding #84 fixed (test coverage); the FP8 E5M2 bug itself is tracked separately below.
- [x] **determinism** (P3) — DONE. Added `cuda_cross_backend_determinism_tests.rs`: an always-on (given `native-cuda` + a live device) CPU-vs-CUDA engine-level greedy-sequence determinism guard spanning both sides of the 16-token batch-prefill boundary, on both TQ2_0_g128 and Q1_0G128 synthetic fixtures. Finding #77 fixed.
- [x] **image** (P2) — DONE. `TileBoundary::AfterMid` no-op, TE GPU weight residency on CUDA (name-keyed cache), and golden-free pipeline tests all implemented for real and covered by regression tests. Findings #58/#73/#76 fixed.

### Batch 4 — honest documentation of deliberate deferrals

- [x] **docs-honesty** — DONE (this pass). README.md/TODO.md/docs/CLI.md now state the honest current reality for all 13 audited claims-vs-reality findings: scirs2 "CUDA" tier is a CPU fallback (#67, scirs2-core retired its cudarc CUDA backend in 0.6.x — the old "incomplete `func.launch()` stub" doc comment was itself stale); "distributed serving" is in-memory consistent-hash-ring topology only, no networking (#69); "multi-GPU" is a rayon CPU simulation of NCCL-style collectives, honestly self-disclosed in the module's own rustdoc (#83); Metal FP8 batch-forward integration (Phase 28.B) and Metal Q4_0/Q8_0/K-quant kernels remain platform gaps pending macOS hardware (#44/#45); PagedAttention (#23), KV-cache-quantization application (#24), attention-variant layers — sink/sparse/flash-decode/cross-attention/MoD/MoE (#48) — and RoPE-scaling variants — YaRN/DynamicNTK/LLaMA-3.1/LongRoPE (#75) — are documented as implemented-and-tested standalone building blocks *not* wired into the default forward path; `KvCachePolicy`'s tier decision is documented as advisory/telemetry-only, with the `Fp8` tier noted as structurally unreachable (#50); the (already independently fixed) speculative-decoding mock-vs-real distinction is reflected accurately (#19, see the `speculative` entry above); `gpu_cache.rs`'s handle-0 fallback (#49) and its `:224` placeholder are recorded below as a deferred, macOS-only hygiene item with the enum-split plan from this session's triage. See "Known Limitations" in README.md for the consolidated user-facing version.

### Newly discovered during this audit round, unresolved (tracked here so they aren't lost)

- [x] **FP8 CUDA batch prefill had the same prefill/decode split-KV-cache bug the Q1 fix above closed — now guarded (disabled-by-default)** (P0, `crates/oxibonsai-model/src/model/types/forward_cuda_fp8.rs`). **RESOLVED 2026-07-20 (residual-fix pass).** `try_cuda_prefill_with_lm_head_fp8`/`try_cuda_prefill_verify_fp8` write the prompt K/V into `cuda_fp8_prefill.rs`'s private `FP8_PREFILL_STATE.kv_cache` — a *separate* cache from the CPU `self.kv_cache` that per-token FP8 decode reads (`model/types/mod.rs:740`, `block.forward(..., &mut self.kv_cache, ...)`). Confirmed structurally (the batch path never writes `self.kv_cache`) and empirically (after a forced batch prefill, every prompt-position key in `self.kv_cache` reads back zero). This is the identical bug class fixed for Q1/ternary and disabled-by-default for Q4_0/Q8_0/K-quant. **Fix:** both FP8 entry points now carry the same `SPLIT-CACHE GUARD` used in `q_std.rs`/`k_quant.rs` — they return `Err` by default so `forward_prefill`/`forward_prefill_verify` fall back to the bit-correct sequential per-token path that populates `self.kv_cache`; `OXIBONSAI_FORCE_CUDA_SPLIT_PREFILL=1` re-enables the decode-incorrect GPU batch path for throughput microbenchmarks only. Also added the missing context-length guard (`pos_start + batch_size > max_seq_len` → `Err` → sequential fallback → clean `SequenceTooLong`, no out-of-bounds RoPE-slice panic). **Regression gate:** new `cuda_synthetic_fp8_e4m3_prefill_decode_parity` / `_e5m2_` tests (synthetic in-memory FP8 GGUF, no external file) drive a 20-token prompt and assert, weight-independently, that every prompt position has a non-zero key in the CPU `self.kv_cache` (this assertion FAILS under the env override, i.e. exactly when the guard is bypassed) plus full CPU↔CUDA decode logit/greedy parity. `cargo clippy`/`nextest -p oxibonsai-kernels -p oxibonsai-model -p oxibonsai-runtime --features native-cuda` green.
- [x] **FP8 E5M2 block-scale rounding can overflow to `Infinity`** (P1, `crates/oxibonsai-core/src/quant_fp8.rs`, `BlockFP8E5M2::quantize`) — **RESOLVED 2026-07-20 (residual-fix pass).** The block scale `d = f16::from_f32(max_abs / FP8_E5M2_MAX)` could round down such that the element that defined `max_abs` computed `scaled = max_abs / d_rounded > FP8_E5M2_MAX`, which `fp8_e5m2_encode` maps to actual `Infinity` (reproducible in ~49% of random 32-element blocks). **Fix:** each finite scaled value is now clamped to `±FP8_E5M2_MAX` before encode (round-to-nearest scale kept — optimal for the block's bulk); genuine `±Inf` *input* still encodes as Inf per the E5M2 contract. `BlockFP8E4M3::quantize` was audited (already safe via encoder saturation) and given the symmetric explicit clamp; the SmoothQuant channel-scale paths (`quantize_fp8_e4m3_smooth`/`_e5m2_smooth`) inherit the fix by delegation. The regression test `fp8_e5m2_block_scale_rounding_can_overflow_to_infinity` is un-`#[ignore]`d and asserts the fixed behavior; new never-non-finite proptests cover both formats + channel-scale variants (9 new tests total).
- [x] **`test_models_endpoint_returns_bonsai_8b` stale-test/real-behavior mismatch** (P3, `tests/server_integration_tests.rs` in `oxibonsai-cli`) — **RESOLVED.** The test was renamed to `test_models_endpoint_reports_loaded_model` and rewritten to assert against `Qwen3Config::tiny_test().model_name` (the real dynamic id) instead of the hardcoded `"bonsai-8b"` literal — exactly the fix this item prescribed. Tracking-only staleness; no code defect.

### Explicitly deferred (macOS validation required — do NOT ship blind changes from this Linux box)

- ~~`gpu_cache.rs:224` — Q1 `final_norm`/`lm_head` placeholder handles.~~ **RESOLVED 2026-07-21 (`w2-metal-correctness`, validated on real M3/Metal hardware).** `CachedModelWeights` (`metal_full_layer/types.rs`) is now an enum — `CachedModelWeights::Q1(CachedQ1Weights) | CachedModelWeights::Ternary(CachedTernaryWeights)` — across all six touch points (struct, both builders in `functions_3.rs`, the Q1 consumer, and the three `forward_metal.rs` consumers). The ternary cache builder no longer uploads the dummy `[0.0]` `final_norm`/`lm_head` placeholder handles (IDs `4_000_000`/`4_000_001` are gone) — the dead cross-type fields this item flagged as a latent wrong-field-read risk no longer exist; a future mismatch is now a compile error, not a silent zero-buffer read. Q1 + ternary decode parity preserved (metal suite green).
- ~~`gpu_cache.rs:82` — four `.map(|hnd| hnd.id()).unwrap_or(0)` GPU-handle fallbacks.~~ **RESOLVED 2026-07-21 (`w2-metal-correctness`, same package as above).** The four Q1-branch fallbacks (`fused_qkv_handle`/`attn_proj_handle`/`gate_up_handle`/`down_handle`) are now `.ok_or_else(|| format!("missing GPU handle for layer {} <field>", block.layer_index()))?` hard errors instead of `.unwrap_or(0)`; callers fall back to CPU forward on `Err`. The silent-buffer-aliasing risk this item flagged is closed.
- ~~Metal FP8 forward-path integration (Phase 28.B)~~ **RESOLVED 2026-07-21 (stretch-goal package, `w3-metal-fp8`).** New `BonsaiModel::try_metal_prefill_with_lm_head_fp8` (`model/types/forward_metal_fp8.rs`) runs the heavy linear projections (fused QKV, attn-output, fused gate/up SwiGLU, FFN down) as batched FP8 GEMMs on the GPU via the previously-zero-caller `metal_fp8_prefill` kernels, while q/k-norm, RoPE, attention, and the K/V store stay on the CPU against `self.kv_cache` — the same cache the per-token FP8 decode path reads, so this hybrid design avoids the split-KV-cache bug class by construction (contrast the CUDA FP8 prefill note in Phase 26 above, which writes a GPU-private cache and stays disabled for exactly that reason). Parity: prefill KV population + CPU-sequential-vs-Metal-batch logit cos=1.000000 + identical greedy continuation for both E4M3/E5M2; context-length guard; chunked prefill matches single-shot. Wired into `forward_prefill` with a clean sequential fallback on any GPU error. This was the last documented Metal platform gap in this section.
- ~~Metal Q4_0/Q8_0/K-quant kernels~~ **RESOLVED 2026-07-21, end-to-end (two packages: kernel implementation in wave 1's `oxibonsai-kernels`/`kernels-metal-04`, model-layer wiring in wave 2's `w2-last-mile`).** New MSL GEMV kernels + host dispatch exist for all 8 formats (`kernel_sources/q_std.rs`, `kernel_sources/k_quant.rs`; `metal_q_std_kernels.rs`, `metal_k_quant_kernels.rs`; public fns `oxibonsai_kernels::metal_gemv_{q4_0,q8_0,q2k,q3k,q4k,q5k,q6k,q8k}`), parity-validated on Apple-Silicon M3 within 1e-4 rel / 5e-3 abs. `LinearQ4_0`/`LinearQ8_0`/`LinearQ2K`/`LinearQ3K`/`LinearQ4K`/`LinearQ5K`/`LinearQ6K`/`LinearQ8K::forward` (`oxibonsai-model`) now try these Metal kernels first (mirroring the pre-existing CUDA short-circuit pattern) and fall back to CPU on any `Err`. With `--features metal` on macOS + a Metal device, Q4_0/K-quant GGUFs now genuinely dispatch to the GPU — this item is closed end-to-end (kernel existence *and* runtime routing), not just at the kernel level.
- **NEW (2026-07-21, `model-forward-02`):** `TransformerBlock::forward_with_sliding_window` (`block/types/forward_sw.rs`) is a complete, unit-tested ~330-line implementation (RoPE, QK-norm, windowed KV lookup, parallel/sequential attention, fused FFN) but has zero call sites outside its own smoke test — `BonsaiModel::forward`/`forward_prefill` call only the dense/full-attention `block.forward(...)`. `Qwen3Config` has no `sliding_window`-related field at all, so there is no metadata-driven path that could ever select the windowed variant for any loaded GGUF model today. This matches the same standalone-primitive pattern as `sparse_attention_forward`/`yarn_rope.rs`/`rope_scaling.rs` above, but was missing from both this list and README's "Known Limitations" until now — see README for the user-facing disclosure. **Fix (not applied, effort: medium):** either wire `forward_with_sliding_window` behind a config-driven trigger (add `sliding_window: Option<SlidingWindowConfig>` to `Qwen3Config`, populated from GGUF `llm.attention.sliding_window` metadata when present), or leave it as a documented standalone building block.

## Production-Release Audit Round 2 (2026-07-21) — 14 implementation packages across 3 waves

> Source: a second production-readiness pass on this session's branch, following up on Round 1 (2026-07-20 above). Baseline entering this round: 4947 tests green, clippy clean (including `--all-targets`). Covers: real penalty/logprobs application on the OpenAI-compatible server (Round 1 had shipped an honest 400-rejection because no penalty seam existed yet), GPU-tier parity for Metal FP8/Q4_0/Q8_0/K-quant (closing every remaining Metal platform gap listed in Round 1's "Explicitly deferred" section), CPU NEON coverage for Q4_0/Q8_0, threshold-driven kernel dispatch (closing a Round-1 overclaim), GGUF/checkpoint/LoRA/SmoothQuant hardening, tokenizer fidelity fixes, and CLI/server hygiene (bearer-auth, RAG top_k, base64 embeddings, batch completions). Every stale Round-1 claim this round corrected is annotated in place above rather than silently rewritten.

### Wave 1 — runtime, kernels, tokenizer, GGUF, model, release-infra, image (10 packages)

- [x] **oxibonsai-runtime (sampler/penalty seam)** — DONE. `repetition_penalty` (default 1.1) now actually applies in the native decode loop (was a 100%-silent no-op despite being validated/preset/builder-exposed). New `Sampler::sample_with_history(logits, recent_tokens)`, `PenaltyParams { presence_penalty, frequency_penalty }` (OpenAI formula `logit -= presence*I(count>0) + frequency*count`), `apply_frequency_presence_penalty`. Engine gained `eos_token_id()` resolved from the loaded GGUF's `tokenizer.ggml.eos_token_id` (fallback 151645, was always hardcoded — non-Qwen3-EOS fine-tunes now stop correctly) and, server-gated, `generate_with_logprobs(prompt, max_tokens, top_k, id_to_token)`. +21 tests.
- [x] **oxibonsai-kernels (Metal Q_std/K-quant GEMV, `kernels-metal-04`)** — DONE. New MSL GEMV kernels + host dispatch for Q4_0, Q8_0, Q2_K–Q8_K (`kernel_sources/q_std.rs`, `k_quant.rs`; `metal_q_std_kernels.rs`, `metal_k_quant_kernels.rs`), closing the Apple-Silicon GPU-decode gap for these 8 formats at the kernel level (model-layer routing landed in wave 2, see below). CPU-vs-Metal parity ≤1e-4 rel / 5e-3 abs across rows {1,7,32,33,256}. +16 tests.
- [x] **w1-serve-embed-rag** — DONE, 7 findings. Real RFC 4648 base64 for `/v1/embeddings`'s `encoding_format: "base64"` (was silently-wrong hex mislabeled as base64); `/rag/query`'s `top_k` now genuinely drives both retrieval and the actual generation prompt (was a dead-code override — the handler always used the pipeline's fixed default `top_k=5` regardless of the request); `/rag/index`'s `chunk_overlap` must now satisfy `<= chunk_size / 2` in addition to `< chunk_size` (previously unbounded, e.g. size=512/overlap=511 → ~500× storage amplification); `RateLimitConfig::trusted_proxies: Vec<IpAddr>` closes the spoofable `X-Forwarded-For`/`X-Real-IP` rate-limit bypass (headers now only honored from an allowlisted proxy peer); TruthfulQA MC1/MC2 out-of-range gold indices now correctly count as `skipped` instead of silently mis-scoring. +20 tests.
- [x] **w1-serve-extended** — DONE, 3 findings. Extended chat endpoint (`/v1/chat/completions/extended`) reports the real loaded-model id for both `model` and `system_fingerprint` (was hardcoded `"bonsai-8b"`); `stream: true` now produces real token-by-token SSE on the extended endpoint (was silently ignored — a request with `stream:true` got a normal blocking response); `/v1/completions` batch prompts (`"prompt": [...]`) now all generate — one choice per prompt, `index` matching position, aggregated `usage` (previously only the first prompt effectively generated regardless of documented behavior), capped at `MAX_COMPLETION_BATCH_SIZE = 8`. +15 tests.
- [x] **oxibonsai-cli (root binary, package `oxibonsai-cli`) + oxibonsai-image (`RenderParams`)** — DONE, 4 findings. `oxibonsai serve` now mounts the same hardening shape as the standalone `oxibonsai-serve` binary: `--bearer-token`/env `OXIBONSAI_BEARER_TOKEN` (constant-time comparison, exempts `/health`+`/metrics`), `--max-concurrent-requests` (default 32, 503 on overload), `--request-timeout-ms` (default 60000, 408 on timeout) — a `tracing::warn!` now fires explicitly when no bearer token is configured (previously silent); `mmap_gguf_file` errors on `run`/`chat`/`info`/`quantize`/`validate` now include the offending path (e.g. `failed to open model '/path/x.gguf': No such file or directory`); new `oxibonsai eval` subcommand (Cargo feature `eval`, off by default — enable with `--features eval`): loads a JSONL dataset (`{"input":...,"expected_output":...}` lines) before touching the model, greedy-generates per prompt, scores the corpus with ROUGE-1/2/L, optional `--report-json`/`--report-markdown`; `--guidance`/`:guidance` removed entirely from `image`/`repl`/REPL commands (Bonsai-Image is CFG-free — `guidance_embeds: false` — so the flag was provably inert). +25 tests.
- [x] **w1-gguf-hardening** — DONE, 6 findings. GGUF metadata/tensor-array eager allocation is now capped (`min(declared_count, 4096)`) independent of the attacker-declared count, in both the batch (`metadata.rs`) and streaming (`streaming.rs`) parsers; string parsing (`metadata.rs`/`tensor_info.rs`) uses a bounded 64 KiB-chunk incremental read instead of an upfront `vec![0u8; len]`; duplicate metadata keys / tensor names are now a hard parse error (`BonsaiError::InvalidMetadata`) in both the batch and streaming parsers (was silent last-wins); `GgufWriter::write` validates `alignment` (0 / non-power-of-two → `WriteError::InvalidAlignment`) and guards shape-product/cumulative-offset arithmetic with `checked_mul`/`checked_add` (`WriteError::ShapeOverflow`) instead of silently wrapping. Verified against 3 real production GGUF files (399/311/399 tensors) with zero false-positive duplicate-key rejections. +11 tests.
- [x] **oxibonsai-model (checkpoint/smoothquant/lora)** — DONE, 3 findings. `Checkpoint::read_from` cross-validates each tensor's declared shape against its actual data length (new `CheckpointError::ShapeDataMismatch`), closing a path to a shape/data-inconsistent tensor flowing into downstream shape-driven consumers such as structured pruning; `SmoothQuantCalibrator::record_activation` returns `Result<(), SmoothQuantError>` instead of panicking on an `in_features` mismatch for a previously-seen layer; `LoraAdapter::apply`/`merge_into_weights` return `Result<_, LoraError>` (new `LoraError::DimensionMismatch`) instead of silently truncating via `.take(d_in)` / panicking with an out-of-bounds index; `LoraRegistry::apply_adapter` propagates the new `Result`. All three are **breaking API changes** (`smoothquant`/`lora` signatures), but LoRA adapters are not wired into the live inference forward path today, so there is no runtime behavior impact, only API-surface impact. +13 tests.
- [x] **oxibonsai-kernels (packing hygiene + NEON Q4_0/Q8_0 kernels)** — DONE, across 2 sibling packages. `packing::AlignedBuffer::new` guarded against `usize` allocation-size overflow with `checked_mul`/`checked_add` (mirroring the pre-existing `aligned::AlignedBuffer::new` guard); `packing.rs`'s dead duplicate `prefetch_read`/`prefetch_write` removed (only reachable from its own unit test; the crate's one supported prefetch abstraction lives in `crate::prefetch`, re-exported from `lib.rs` and exercised by `tests/tuning_tests.rs`); new NEON GEMV kernels for Q4_0 and Q8_0 (`simd_q_std_neon.rs`: `gemv_q4_0_neon`, `gemv_q8_0_neon`), closing the AArch64 SIMD gap that previously left Apple-Silicon CPUs silently falling to the scalar reference for these two formats — kernels only in this wave (parity-verified within 1e-4 rel across shapes (1,32)..(1,1024)); dispatch wiring landed in wave 2 (`w2-last-mile`, below). +13 tests.
- [x] **oxibonsai-tokenizer** — DONE, 4 findings. Non-special `added_tokens` (e.g. Qwen's `<tool_call>`, `</tool_call>`, `<|fim_prefix|>`, `<|fim_middle|>`, `<|fim_suffix|>`, `<|fim_pad|>`, `<|repo_name|>`, `<|file_sep|>`, `<tool_response>`) now encode atomically to their trained IDs via a new `protected_tokens` superset on both `HfTokenizerJson` and `Vocabulary` — previously only tokens with the JSON `special: true` flag were protected (a gap in the original Round-1 Findings #8/#9/#14/#34 fix, which this closes); `detect_byte_level` now recurses into HF's real `{"type":"Sequence","pretokenizers":[...]}`/`decoders` shape (the old `Value::Array` arm was unreachable dead code — a byte-level model whose signal is only inside a Sequence wrapper previously silently mis-detected as non-byte-level, corrupting CJK/emoji/accented-Latin output via the legacy Ġ-stripping decode fallback); the GPT-2 unicode↔byte inverse map is now cached once per process via `OnceLock` (was rebuilt + linear-scanned on every `decode_id_into` call — a per-token server streaming hot-path cost); BPE vocab construction now tracks `seen_ids` and rejects two distinct token strings claiming the same numeric ID (was silently letting randomized `HashMap` iteration order pick a non-deterministic decode winner across process restarts). Validated end-to-end against the real bundled `models/tokenizer.json`. +9 tests.
- [x] **w1-release-infra** — DONE. New workspace-root `deny.toml` enforcing the full COOLJAPAN banned-crate list (`openblas`/`bincode`/`rustfft`/`z3`/`rusqlite`/`zip`/`flate2`/`zstd`/`bzip2`/`lz4`/`tar`/`snap`/`brotli`/`miniz_oxide` and their `-sys`/`-src` variants) plus an OSS license allowlist (including `CDLA-Permissive-2.0` for `webpki-root-certs`' CA-bundle data license, the one real rejection found against the live `Cargo.lock`); `cargo deny check bans licenses sources` wired into both `scripts/publish.sh` (new Step 2/6, all subsequent steps renumbered) and `.github/workflows/ci.yml` (new `deny` job, `EmbarkStudios/cargo-deny-action@v2`); `bytes` moved from a stray root `[dev-dependencies]` pin to `[workspace.dependencies]`.
- [x] **oxibonsai-image (VAE tiling test rigor, `image-02`)** — DONE. Strengthened the default `AfterConvNormOut` VAE-tiling parity test in `vae/tiling.rs` from a `0.99999` cosine-similarity check to exact `assert_eq!` byte-identity, matching the module's own documented bit-identity contract and the rigor of the sibling `AfterMid` tests; added a non-multiple-of-4 tile-row-count regression test (11×9 spatial, tile_px=3) to specifically exercise the batched-4-vs-tail GEMM kernel routing. No product code changed — the tiling computation was already correct, only the test's assertion rigor was the gap; this is the regression guard for the pre-existing (uncommitted) `gemm.rs` `dot4_neon` bit-identity fix already in the tree. +1 test.

### Wave 2 — Metal correctness, server API, kernel dispatch wiring, converter (4 packages)

- [x] **w2-metal-correctness** — DONE, 5 sub-items, validated on real M3/Metal hardware against the real 1.7B model. (1) Context-length guards on all six Metal prefill/greedy entry points in `forward_metal.rs` (was an out-of-bounds `RopeTable::cos_at` panic on an over-long prompt; now a clean `SequenceTooLong`-style `Err`, mirroring the existing CUDA guards). (2) A coherent Metal→CPU mid-stream fallback in `InferenceEngine::generate_greedy_gpu` via a new `greedy_decode_token_with_fallback` helper that rebuilds the CPU KV cache by replaying the committed sequence through a scalar reference forward before continuing on CPU (was silent corruption — the naive fallback read an all-zero CPU `kv_cache`); new env var `OXIBONSAI_FORCE_CPU_DECODE_AFTER=<n>` for triage/testing. (3) `gpu_cache.rs`'s four `.unwrap_or(0)` GPU-handle fallbacks are now hard `Err`s with a CPU fallback — closes the Round-1 "Explicitly deferred" `gpu_cache.rs:82` item (see above, now annotated resolved). (4) `CachedModelWeights` split into a `Q1(CachedQ1Weights)`/`Ternary(CachedTernaryWeights)` enum, deleting the dead cross-type fields (dummy `final_norm`/`lm_head` handles gone) — closes the Round-1 "Explicitly deferred" `gpu_cache.rs:224` item (see above, now annotated resolved). (5) A stale `dit_attention_flash.rs` thread-count doc comment (`[128,1,1]` → `[256,1,1]`) fixed. +11 tests.
- [x] **w2-server-base** — DONE, 6 findings + 3 additional items, all landed on `oxibonsai-runtime`. Base `/v1/chat/completions` now declares and genuinely applies `frequency_penalty`/`presence_penalty` (`[-2.0, 2.0]`, validated) via `generate_with_params_and_penalties` (non-stream) / `set_penalties` + `generate_streaming_with_params` (stream) — see the superseded-claim annotations on the `server-api`/"OpenAI API extensions" entries above; the extended endpoint's honest-400 penalty rejection is retired the same way. Real `logprobs`/`top_logprobs` (clamped 0..=20) via `engine.generate_with_logprobs` on **both** the base and extended endpoints. A mid-stream SSE generation failure now emits a terminal `{"error":{...}}` OpenAI-style event instead of a bogus clean finish (`serve-api-02`). The base endpoint now parses `tool_calls` from generated text for non-streaming requests (previously extended-endpoint-only) and both endpoints report honest `finish_reason` (`stop`/`length`/`tool_calls`) on streaming and non-streaming paths. `<|...|>` ChatML-family markers in user/system message content are stripped by default before prompt assembly in both `build_prompt`/`build_extended_prompt` (closes a turn-boundary-forgery prompt-injection vector, `security-03`; opt-out via env `OXI_DISABLE_PROMPT_SANITIZATION`). Every route (chat/completions/extended/embeddings/rag) now returns a unified `{"error":{"message","type","param","code"}}` envelope via new `src/http_error.rs` (`serve-api-10`). The serve entrypoint now serves via `into_make_service_with_connect_info::<SocketAddr>()`, so the wave-1 `trusted_proxies` rate-limit peer-IP logic (previously plumbed but inert — see the Round-1 `security-05`/`serve-api-07` deviation) is now live end-to-end for direct clients. `/v1/completions` now also honors `temperature`/`top_p`/penalties for real (gated so a vanilla request stays bit-identical) and reports the real loaded-model id (was hardcoded `"bonsai-8b"`); it still does not honor `seed`. Known residual limitation: `logprobs` requests sample with the engine's ambient temperature/top_p (no per-call override seam in `generate_with_logprobs`), though penalties still apply and the returned logprobs are real. +27 tests.
- [x] **w2-last-mile** — DONE, 2 of 3 items (3rd investigated, found no in-scope fix — see the `kernels-cpu-02` entry in wave 3 below, which resolves it). (1) NEON dispatch wiring: `KernelTier::Neon` match arms added to `StandardQuantKernel::gemv_q4_0`/`gemv_q8_0` in `dispatch.rs`, calling wave-1's `simd_q_std_neon::gemv_q4_0_neon`/`gemv_q8_0_neon` (previously fell through to the scalar `_` arm on AArch64 — every Q4_0/Q8_0 GEMV silently ran scalar even though a validated NEON kernel existed). (2) Model-layer Metal short-circuit: `LinearQ4_0`/`LinearQ8_0` (`linear_standard.rs`), `LinearQ2K`/`LinearQ3K`/`LinearQ4K`/`LinearQ8K` (`linear_kquant_full.rs`), `LinearQ5K`/`LinearQ6K` (`linear_kquant_ext.rs`) now try the wave-1 `oxibonsai_kernels::metal_gemv_*` kernels first (mirroring the pre-existing `cuda_gemv_*` short-circuit pattern) and fall back to CPU on any `Err`; the 8 `metal_gemv_*` fns were also re-exported at the `oxibonsai-kernels` crate root (`lib.rs`) so the model crate could reach them. This closes the Round-1 "Explicitly deferred" `Metal Q4_0/Q8_0/K-quant kernels` item (see above, now annotated resolved) end-to-end — kernel existence *and* runtime routing. +12 tests (incl. 8 real-Metal-GPU parity tests).
- [x] **w2-convert-q1** — DONE. `oxibonsai convert` (both the HF-safetensors and ONNX `MatMulNBits` paths, `crates/oxibonsai-model/src/convert/{mod.rs,onnx/mod.rs}`) now genuinely supports `--quant q1_0_g128` end-to-end, reusing the shipping `quantize_q1_0_g128` encoder and its canonical sign convention (bit=1⇒+scale). `general.quantization_version` GGUF metadata now records the actually-written format instead of being hard-coded to `"TQ2_0_G128"`. This makes docs/CLI.md's pre-existing `--quant tq2_0_g128 (default) or q1_0_g128` table entry factually true again (it had only been true of `--help`'s text since Round 1, not the converter's actual behavior). +8 tests.

### Wave 3 — stretch goals (2 packages)

- [x] **Metal FP8 batch prefill (Phase 28.B)** — DONE. Closes the last documented Metal platform gap from Round 1 — see the "Explicitly deferred" annotation above for the full design writeup (hybrid GPU-linear-projections / CPU-attention-and-KV-store, avoiding the split-KV-cache bug class by construction). New `BonsaiModel::try_metal_prefill_with_lm_head_fp8` (`model/types/forward_metal_fp8.rs`, new module), wired into `forward_prefill` with a clean sequential fallback. Parity: prefill KV population + CPU-sequential-vs-Metal-batch logit cos=1.000000 + identical 16-token greedy continuation for both E4M3/E5M2 on real M3 hardware; context-length guard returns a clean `Err`; chunked prefill (`pos_start > 0`) matches single-shot. +11 tests.
- [x] **kernels-cpu-02 (threshold-driven dispatch, closing the gap)** — DONE. The real Q1/Ternary CPU forward hot path — `select_gemv_strategy`/`gemv_parallel_tiled`/`gemm_parallel_tiled` in `parallel_tiled.rs`, `optimal_tile_rows` in `tiled.rs` — now genuinely reads `PlatformProfile::global_thresholds()` instead of hardcoded constants (`DIRECT_DISPATCH_MAX_ROWS`/`MEDIUM_PARALLEL_MAX_ROWS`/a hardcoded 32KB-L1 assumption, all removed as fully superseded). New `TunedThresholds::par_tiled_min_rows` field (`tuning.rs`, scaled from the platform's real L2 cache size, clamped ≥ `par_gemv_min_rows` so the Direct→ParallelRow→ParallelTiled tiers stay correctly ordered on any platform); new pure/injectable `select_gemv_strategy_with_thresholds` for host-independent testing. This closes the caveat flagged by both the Round-1 `kernels-cpu` Batch-1 entry (now annotated as a correction above) and the `w2-last-mile` deviation note (wave 2 above): `PlatformProfile::global_thresholds()` now governs *all* of the GEMV/GEMM parallel-dispatch decision, including the tiled tier and L1 tile sizing (previously only true for `parallel.rs`'s row/batch-parallel entry points). +7 tests.

## Stubs to implement (added 2026-06-12 by /cooljapan-stub-check)

- [x] `oxibonsai-model`: `crates/oxibonsai-model/src/model/types/forward_cuda/q1.rs:329` — re-enable Q1 prefill→decode KV handoff once the fix is verified (done 2026-07-20 by the `cuda-prefill` audit package: `cuda_prefill/state.rs` now delegates to `cuda_full_layer::acquire_kv_cache`, unifying the prefill and decode KV caches; ternary batch prefill re-enabled alongside it; synthetic-model CPU↔CUDA parity gate passes at ~0.001 logit delta, was ~7.3)
  - Priority: P2 | Scope: medium | Hint: none
- [x] `oxibonsai-model`: `crates/oxibonsai-model/src/model/types/gpu_cache.rs:224` — implement real Q1 `final_norm`/`lm_head` handles (currently placeholders)
  - Priority: P2 | Scope: medium | Hint: none | **DONE 2026-07-21** (`w2-metal-correctness`, validated on real Apple-Silicon Metal hardware): `CachedModelWeights` split into a `Q1`/`Ternary` enum, deleting the dummy-handle placeholders and the dead cross-type fields — see "Explicitly deferred" under the Production-Release Audit section above.
- [x] `oxibonsai-runtime`: `crates/oxibonsai-runtime/src/speculative.rs` — implement delta-KV-cache sync so draft model processes only newly-committed tokens instead of re-prefilling the full history every drafted token (done 2026-06-17)
  - **Goal:** Keep the draft engine's KV cache warm across rounds. Each round commits only the *delta* (tokens accepted since last round) with one `prefill_from_pos`, then drafts K tokens incrementally via `decode_step` — turning the current O(K·N) per-round cost into O(N_delta + K). Output is token-identical to the old full-reprefill path.
  - **Design:** Add `committed_len: usize` cursor to `SpeculativeDecoder` (init 0 in `new`/`with_adaptive`). New `draft_delta(context, committed_len, k) -> RuntimeResult<Vec<u32>>`: (a) `prefill_from_pos(&context[committed_len..], committed_len)?` for the delta; (b) loop K times `sample → decode_step`, guard `pos >= max_seq_len`; (c) `rewind_cache(context.len())` to drop speculative KV. Public `draft` becomes a thin wrapper. New primitives: `KvCache::truncate(&mut self, new_len)` (clamps down, no buffer zero) + `InferenceEngine::rewind_cache`. `generate_speculative`: `reset()` at entry; empty-acceptance fallback replaced with cursor-aware `draft_one_committed`. New `pub fn reset()` + `pub fn committed_position() -> usize`.
  - **Files:** `crates/oxibonsai-model/src/kv_cache.rs`, `crates/oxibonsai-runtime/src/engine.rs`, `crates/oxibonsai-runtime/src/speculative.rs`. ~185 LoC.
  - **Tests:** (A) `test_delta_draft_token_identical_to_full_reprefill` — baseline vs new `draft`, `assert_eq!`, engine constructed with `SamplingParams { temperature: 0.0, .. }`; (B) `test_delta_draft_commits_incrementally` — `committed_position() == kv_cache().seq_len()` after `generate_speculative`; (C) `test_reject_all_rolls_back_and_next_round_correct` — reject-all → cursor at `context.len()` → re-draft identical tokens. All in inline `mod tests`, `Qwen3Config::tiny_test()`.
  - **Risk:** `SpeculativeDecoder` uses mock target logits (the feature still targets the real draft engine). Test A pins token-identity, so the change is behavior-preserving by construction.

> Pure Rust 1-bit / Ternary LLM inference engine for PrismML Bonsai models
> 683 source files, ~197,000 lines of Rust code, 5,158 tests passing across workspace — verified 2026-07-21 (0.2.3)

## Bonsai-Image Port (FLUX.2 Text-to-Image, Pure Rust) — COMPLETE

> **Status: ✅ DONE (shipped in 0.1.5)** — full pure-Rust text→image pipeline works end-to-end and is GPU-accelerated (CUDA steps=4 ≈31.7s on A4000-class, Metal ≈52–62s on Apple Silicon M3, cos≥0.999 throughout). Runs natively via `pipeline::text_to_image` and the `oxibonsai image` CLI subcommand.

**Overview** — Bonsai-Image 4B is PrismML's text-to-image diffusion model (same team as Bonsai LLM). It is based on Black Forest Labs' FLUX.2 Klein 4B (rectified-flow DiT). The DiT's matmul-heavy layers are quantized to ternary g128 `{-1,0,+1}` (128-weight groups with bf16 scale per group), which maps exactly to OxiBonsai's native `BlockTQ2_0_g128` format. The text encoder is Qwen3-4B (already supported by OxiBonsai — reused). The VAE is `AutoencoderKLFlux2` (convolution-based; the largest new subsystem).

### Confirmed Architecture (config.json)

- **DiT (`Flux2Transformer2DModel`)** — hidden 3072 = 24 heads × 128 head_dim; 5 double-stream + 20 single-stream blocks; `mlp_ratio` 3.0 (intermediate 9216); `in_channels` 128 (latent 32ch × 2×2 patch); `joint_attention_dim` 7680 (= 3×2560); `axes_dims_rope` [32,32,32,32] (4-axis RoPE = 128 total; differs from FLUX.1's 3-axis); `rope_theta` 2000; `guidance_embeds` false (no CFG → one forward per step); `timestep_guidance_channels` 256; `eps` 1e-6.
- **VAE (`AutoencoderKLFlux2`)** — `latent_channels` 32; in/out 3/3; `block_out_channels` [128,256,512,512]; 4× UpDecoderBlock2D; `layers_per_block` 2; `norm_num_groups` 32 (GroupNorm); `mid_block_add_attention` true; `patch_size` [2,2]; `act_fn` silu; `force_upcast` true (fp32 execution); spatial downsampling ×8 + patch ×2 = ×16 (512px → 32×32 = 1024 tokens).
- **Scheduler (`FlowMatchEulerDiscreteScheduler`)** — `num_train_timesteps` 1000; `shift` 3.0; `use_dynamic_shifting` true (resolution-dependent); `base_shift`/`max_shift` 0.5/1.15; `base/max_image_seq_len` 256/4096; `time_shift_type` exponential. Default inference: 4 denoising steps, guidance 1.0.
- **Text encoder** — standard Qwen3-4B (hidden 2560, 36 layers, 32 heads, 8 kv_heads GQA, head_dim 128, intermediate 9728, rope_theta 1e6). Existing Qwen3 implementation reused as a hidden-state extractor.

### Source Format

- Conversion source: `prism-ml/bonsai-image-ternary-4B-mlx-2bit` (all safetensors). PyTorch `.pt` (pickle) rejected per COOLJAPAN Pure Rust policy. Safetensors supported via existing dependency (Pure Rust).
- Quantization metadata (`quantization_config.json`): `format = "mlx-packed-affine"`, `bits 2`, `group_size 128`, `scale_dtype = "bfloat16"` (bf16, not FP16 — important for the converter).
- Skip patterns (kept bf16, < 5% params): `proj_out, x_embedder, context_embedder, time_text_embed, time_guidance_embed, norm_out, double_stream_modulation_{img,txt}, single_stream_modulation`. Quantized targets are attn/MLP linears in double/single blocks (matches whitepaper Figure 3).

### Phased Bring-Up Checklist (historical record)

- [x] **Phase 0 — Golden reference & harness** — DiT/VAE/scheduler/TE config confirmed; MLX ternary model downloaded; Python golden dumper created (text embeddings → per-step latents → decoded image saved as `.npy`).
- [x] **Phase 1 — Converter + single-tensor dequant parity [primary gate]** — MLX `mlx-packed-affine` 2-bit bit layout identified from mflux/MLX source → converter (MLX safetensors → GGUF; quantized layers = `BlockTQ2_0_g128`, skip/VAE/TE = bf16) → Rust dequant validated against Python dequant dump.
- [x] **Phase 2 — Text encoder (Qwen3-4B feature extraction)** — existing Qwen3 forward adapted for hidden-state extraction (LM head / sampling removed); 7680-wide conditioning vector assembled; TE weights loaded from MLX int4 affine format; hidden states compared against Python dump (te_parity cos≥0.999999).
- [x] **Phase 3 — DiT forward (1 block → 25 blocks)** — double-stream (×5): AdaLN → QKV (img/txt separate) → joint attention (concat→split) → out proj → AdaLN → MLP; single-stream (×20): AdaLN → fused QKV+MLP-in → attn ∥ MLP act → cat → fused out; 4-axis RoPE / RMSNorm / mlp_ratio 3.0; quantized linears reuse ternary GEMM; skip_patterns use bf16 dense; velocity prediction compared vs Python dump (dit_parity 59 taps cos≥0.999).
- [x] **Phase 4 — Flow-match sampler** — `FlowMatchEulerDiscreteScheduler` + dynamic shift (seq_len→μ, base 0.5/max 1.15, exponential); 4 steps, no CFG (`guidance_embeds=false`); initial bring-up injected Python latents to isolate cross-framework RNG differences.
- [x] **Phase 5 — VAE decoder (`AutoencoderKLFlux2`) [largest new subsystem]** — Conv2d (im2col+GEMM), GroupNorm(32), SiLU, ResNet blocks (layers_per_block 2), mid-block attention, 4-stage upsample (128/256/512/512), post_quant_conv, patch[2,2] unpack; latent(32ch)→RGB, fp32 (force_upcast); decoded image compared against Python dump (vae_parity 11 taps cos≥0.999).
- [x] **Phase 6 — PNG output + CLI (end-to-end)** — oxiarc-deflate Pure Rust PNG (COOLJAPAN compression policy; no flate2/zstd/zip); `oxibonsai image --prompt … --seed N --out x.png`; native Threefry RNG byte-matches MLX/mflux reference at `--seed 42`.
- [x] **Phase 7 — GPU acceleration & optimization** — Metal (default-on): DiT flash-attention 5.47×, ternary GEMM v10 1.89×, VAE GPU 3.2×; CUDA (native-cuda): total 3.2× (gemm_tq2 ~6×, flash-attn ~6.3×, stage0 context_embedder 59×); all cos≥0.999.

### Open Questions (resolved during bring-up)

All open questions from the planning phase were resolved during implementation:
- `joint_attention_dim=7680` = concatenated hidden states from 3 Qwen3 layers (×2560 each).
- 4-axis RoPE position IDs: `[0, h//32, w%32, 0]` for image tokens; `[0, 0, 0, j]` for text tokens.
- AdaLN modulation: double-stream blocks use 6 parameters (scale/shift/gate × 2 paths); single-stream blocks use 3.
- VAE `scaling_factor`/`shift_factor`: applied at pipeline level (latent packing/unpacking), not inside the VAE decoder.
- Latent token order: `noise[c, s] = packed[s, c]` where s = h*32+w (raster scan).

## Phase Status

| Phase | Status | Summary |
|-------|--------|---------|
| Phase 0: Foundation | ✅ Complete | Workspace, GGUF loader, metadata extraction |
| Phase 1: 1-Bit Kernels | ✅ Complete | dequant/gemv/gemm + property tests (20 kernel tests) |
| Phase 2: Transformer Engine | ✅ Complete | RMSNorm, SwiGLU, RoPE, GQA, 36-layer Qwen3 forward pass |
| Phase 3: Inference Runtime | ✅ Complete | KV cache, sampling, tokenizer, CLI (run/chat/serve/info), OpenAI-compatible server |
| Phase 4: Production Hardening | ✅ Complete | AVX2 SIMD, NEON, AVX-512, rayon, benchmarks, tracing, TOML config, proptests, integration tests, circuit breaker, health, metrics, recovery, Prometheus metrics, memory profiling, fuzz testing |
| Phase 5: Ecosystem Integration | ✅ Complete | All major subsystems implemented (see below) |
| Phase 6: Advanced Infrastructure | ✅ Complete (see caveats) | Multi-GPU (rayon simulation — not real GPU collectives), CUDA/Metal stubs, PagedAttention (standalone primitive, not wired into `BonsaiModel`), GGUF v3, dynamic quant, profiler — see the detailed Phase 6 section below and README "Known Limitations" |
| Phase 7: Production Features | ✅ Complete | Model merging, checkpointing, flash decoding, attention sink, compression, ROUGE, MoD, adaptive sampling, oxibonsai-serve |
| Phase 8: Final Polish | ✅ Complete | K-quant formats, streaming GGUF, kernel tuning, comprehensive testing |
| Phase 9: Ternary Bonsai | ✅ Complete | TQ2_0_g128 block type, SIMD kernels, model variants, GGUF surface, export |
| Phase 10: Ternary CPU SIMD tiers | ✅ Complete | AVX2 / AVX-512 / NEON TQ2 GEMV with pos_mask+neg_mask decomposition |
| Phase 11: Metal TQ2 GEMV + dispatch | ✅ Complete | Per-kernel Metal TQ2_0_g128 dispatch in `gpu_backend/metal_*` |
| Phase 12: Native CUDA backend | ✅ Complete | NVRTC kernels, fused Q1 + TQ2 full-forward; ~21.9 tok/s on Ternary-Bonsai-1.7B |
| Phase 13.x: Fused Metal TQ2 full-forward | ✅ Complete | Single command buffer per token, ~13× speedup; ~50 tok/s on Ternary-Bonsai-1.7B |
| Phase 13.y: Ternary LM head on GPU | ✅ Complete | All 7 `OutputWeight::Ternary` guard sites closed (4 Metal + 3 CUDA); Metal ~54.5 tok/s avg on Ternary-Bonsai-1.7B |
| Phase 14: Runtime controllers (0.1.4) | ✅ Complete | `KvCachePolicy`, `AdaptiveLookahead`, `RequestRateTracker`, `RequestId`, expanded Prometheus surface |
| Phase 15: Advanced Inference Capabilities | ✅ Complete | FP8 quantization (E4M3/E5M2), FP8 kernels + `Fp8Kernel` trait, `AllowList`/`Sequence`/`LengthConstraint`, BNF grammar engine + Earley-based `GrammarConstraint` |
| Phase 15.x: FP8 SIMD + Grammar Cache | ✅ Complete | AVX2/AVX-512/NEON gather-based FP8 kernels; parallel FP8; LUT decode tables; Earley `allowed_tokens` LRU cache (O(1) on repeated state) |
| Phase 16: FP8 Export + Criterion Benchmarks | ✅ Complete | `ExportFormat::FP8E4M3/E5M2` in `oxibonsai-model`; 4 export tests; `bench_fp8_kernels` in `kernel_benchmarks.rs`; 14 benchmark cases |
| Phase 16A: FP8 Model Layers + Integration | ✅ Complete | `LinearFP8E4M3<'a>` + `LinearFP8E5M2<'a>` in `oxibonsai-model`; `OutputWeight::FP8E4M3/E5M2` variants; `ModelVariant::FP8Bonsai8B/4B/1_7B` with spec builders; FP8 block loaders in `weight_loaders.rs`; GPU paths return graceful Err; 6 integration tests |
| Phase 16B: Token-byte precomputation + first-byte index | ✅ Complete | `GrammarConstraint` precomputes all token byte sequences at construction; `first_byte_index: Box<[Vec<u32>; 256]>` eliminates per-step decode calls; `empty_token_ids` for EOS/special tokens; `vocab_size()` + `index_memory_bytes()` accessors; 5 new integration tests |
| Phase 17: FP8 KV cache + JSON Schema BNF + ARC/GSM8K + SmoothQuant + Unigram | ✅ Complete | `Fp8KvLayer`/`Fp8KvCache`/`KvCacheLevel::Fp8`; JSON Schema→Grammar compiler; ARC/GSM8K evaluators; SmoothQuant FP8 calibrator + channel-aware quant; Unigram tokenizer with HF format support |
| Phase 18: Standard GGUF formats + K-quant ext + WordPiece + WinoGrande/BoolQ + Regex→BNF | ✅ Complete | Q4_0/Q8_0 full stack; Q5_K/Q6_K K-quant layers; WordPiece tokenizer; WinoGrande/BoolQ evaluators; Regex→BNF compiler (Thompson NFA→Subset DFA→Grammar) |
| Phase 19: Q2_K/Q3_K/Q4_K/Q8_K + MMLU + GBNF + HellaSwag/TruthfulQA + Tool Calling | ✅ Complete | Full Q2_K/Q3_K/Q4_K/Q8_K block types, GEMV kernels, linear layers, model integration; MMLU evaluator with per-subject breakdown; GBNF parser (two-pass, `*`/`+`/`?` expansion); HellaSwag/TruthfulQA evaluators; Tool calling API (select_tool, build_tool_constraint, ToolRegistry, validate_tool_arguments) |
| Bonsai-Image Phases 0–7: text-to-image pipeline | ✅ Complete | oxibonsai-image crate; DiT TQ2 + VAE + TE 4-bit + PNG + MLX RNG; Metal ≈52–62s, CUDA ≈31.7s, steps=4 512² |
| Phase 33: Split block/types.rs (Policy Compliance Milestone) | ✅ Complete | Split `crates/oxibonsai-model/src/block/types.rs` (1853 lines, 147-line margin) into `block/types/` directory module with 10 sub-files (max 424 lines): `mod.rs` (35), `upload.rs` (41), `layer_stats.rs` (48), `scratch.rs` (67), `block_def.rs` (104, `pub struct TransformerBlock<'a>` + `new()`), `forward_stats.rs` (283), `helpers.rs` (283, layer_idx + try_full_layer_gpu/cuda), `forward_sw.rs` (330, sliding-window forward), `forward.rs` (419, main forward), `accessors.rs` (424, all ~85 quant-family accessor methods grouped by family). The single big `impl<'a> TransformerBlock<'a>` block split across 5 sub-files. Visibility widened: 19 `TransformerBlock` fields + 16 `ScratchBuffers` fields from private to `pub(super)` (siblings destructure them); 2 cfg-gated helpers (`try_full_layer_gpu`, `try_full_layer_cuda`) from `fn` to `pub(super) fn`. **Milestone**: after Phase 33, every Rust source file in the workspace has ≥ 227 lines of margin to the 2000-line policy ceiling. Top file is now `cuda_k_quant_prefill_kernels.rs` at 1773 (kernel-source-string container, no host code). 4625 tests pass; clippy clean. |
| Phase 34: oxibonsai repl (Resident Image Session) | ✅ Complete | `ImageSession` loads DiT/VAE/TE once, renders many prompts without re-paying load/dequant cost. `TeWeights::set_resident()` for 4-bit resident caching. `oxibonsai repl` CLI with Ghostty inline kitty-protocol display. `RenderParams`/`RenderOutcome`/`StageTimings` types. Byte-identical output with `text_to_image` (shared `decoded_chw_to_rgb8`). |
| Phase 32: Split forward_cuda.rs (Policy Compliance) | ✅ Complete | Split `crates/oxibonsai-model/src/model/types/forward_cuda.rs` (1801 lines, 199-line margin) into `forward_cuda/` directory module with 6 sub-files: `mod.rs` (34, cfg-gate + decls), `byte_helpers.rs` (119, 8 free byte-cast helpers), `q1.rs` (580, Q1 builders + 4 top-level dispatch entry points), `ternary.rs` (285, TQ2 prefill methods), `q_std.rs` (328, Q4_0/Q8_0 methods), `k_quant.rs` (424, K-quant methods). Max sub-file 580. The single big `impl<'a> BonsaiModel<'a>` block split into 5 sibling impl blocks; private inherent methods widened to `pub(super)` (sibling-sub-module helpers) or `pub(in super::super)` (entry points called from `types/mod.rs`). Inner-attribute `#![cfg(...)]` on `mod.rs` eliminates 19 repeated per-method cfg attributes. After Phase 32: every CUDA-related file at policy edge is now split (cuda_prefill, cuda_k_quant_prefill, forward_cuda all done). New top of file ranking is `block/types.rs` at 1853 (147-line margin). 4625 tests pass; clippy clean. |
| Phase 31: Cap-of-8 Batch-Kernel Regression Audit | ✅ Complete | New `tests/capof8_audit.rs` integration test in `oxibonsai-kernels` enumerates every batch-GEMM kernel by name and asserts both (a) the kernel entry-point appears in its source string and (b) the source contains the `col_base += 8` (CUDA) / `col_base += 8u` (MSL) outer-loop pattern. Audits 36 CUDA batch kernels (6 in `cuda_prefill_kernels` for Q1+TQ2 × {gemm, gemm_residual, fused_gate_up_swiglu}; 18 in `cuda_k_quant_prefill_kernels` for Q2_K/Q3_K/Q4_K/Q5_K/Q6_K/Q8_K × 3 variants; 6 in `cuda_fp8_prefill_kernels` for E4M3/E5M2 × 3 variants; 6 in `cuda_q_std_prefill_kernels` for Q4_0/Q8_0 × 3 variants) plus 10 Metal MSL batch kernels (4 in `kernel_sources::prefill` for Q1+TQ2; 6 in `kernel_sources::fp8_prefill` for E4M3/E5M2 × 3 variants). Two defensive host-only sanity tests confirm the assertion helper rejects sources missing the pattern or the kernel name. All 46 batch kernels currently audit-clean. Host-only; no GPU required; the assertions run on every CI build that enables `native-cuda` (Linux/Windows) or `metal` (macOS) features. Phase Status: 4625 workspace tests passing (4619 + 6). |
| Phase 30: Metal + Runtime Refactor (Policy Compliance) | ✅ Complete | Split `metal_graph.rs` (1948 lines → 7 files in `metal_graph/{mod, error, reformat, pipelines, buffers, graph, tests}.rs`, max 670 lines) and `constrained_decoding.rs` (1966 lines → 8 files in `constrained_decoding/{mod, error_trait, regex, json, sampler, allow_list, sequence, length}.rs`, max 724 lines). All public surface preserved: `MetalGraph`/`MetalGraphError`/`MetalWeightHandle` (gpu_backend re-export) plus the 11 constraint types (`ConstraintError`, `TokenConstraint`, `NoConstraint`, `RegexConstraint`, `JsonConstraint`, `JsonParseState`, `ConstrainedSampler`, `ConstrainedSamplerBuilder`, `AllowListConstraint`, `SequenceConstraint`, `LengthConstraint`). Internal helpers (`alloc_buf`, `div_ceil`, `set_scalar`, `upload_f32`, `download_f32`) re-exported with `pub(crate)`; NFA internals (`NfaState`, `RegexNfa`, `Fragment`) tightened from previously-private to `pub(super)` to satisfy nested test modules. After Phase 29 + Phase 30: zero files within 100 lines of the 2000-line policy; the top-ranked file is now 1853 (147 lines of margin). 4619 tests still passing; clippy clean. |
| Phase 29: CUDA Prefill Module Refactor (Policy Compliance) | ✅ Complete | Split `cuda_prefill.rs` (1989 lines → 6 files) and `cuda_k_quant_prefill.rs` (1941 lines → 5 files) into directory modules mirroring the `cuda_full_layer/` template. New layout: `cuda_prefill/{mod, state, launchers, encode_q1, encode_ternary, try_apis, tests}.rs` and `cuda_k_quant_prefill/{mod, state, launchers, encode, try_api, tests}.rs`. All public surface preserved (`try_cuda_prefill`, `try_cuda_prefill_ternary`, `init_prefill_modules`, `CudaPrefillBuffers`, `CudaPrefillModules`, `try_cuda_prefill_k_quant`, `CudaKQuantPrefillLayerParams`, `KQuantFormat`, `CudaKQuantPrefillModules`, `init_k_quant_prefill_modules`). Internal helpers gated to `pub(super)`. Largest CUDA host files now ≤ 1801 lines; the two files at policy risk (1989 / 1941) are gone. Zero changes to runtime behaviour: 4619+ workspace tests still green; clippy clean. |
| Phase 28: Metal FP8 Batch GEMM Primitives | ✅ Complete | 8 new MSL kernels (`MSL_GEMM_FP8_E4M3_V1`, `_RESIDUAL`, `MSL_FUSED_GATE_UP_SWIGLU_GEMM_FP8_E4M3_V1`, `MSL_GEMV_FP8_E4M3_PF_V1`, plus 4 E5M2 variants) in `kernel_sources/fp8_prefill.rs` mirroring Phase 26 CUDA design: column-major batch I/O, AoS 34-byte FP8 blocks, cap-of-8 outer loop, one simdgroup per row. New `metal_fp8_prefill.rs` module with own singleton (Device + CommandQueue + 6 lazy pipelines) exposes 6 public host fns: `metal_gemm_fp8_e4m3`/`e5m2`, `metal_gemm_fp8_e4m3_residual`/`e5m2_residual`, `metal_fused_gate_up_swiglu_fp8_e4m3`/`e5m2`. 7 CI-GPU-gated CPU↔Metal parity tests (incl. `batch_size=12` cap-of-8 discriminator + non-mul-of-8 row boundary) + host-only kernel-source-string assertions. ~~Forward-path integration (5-stage encoder + LM-head dispatch) deferred to Phase 28.B.~~ **Phase 28.B DONE 2026-07-21** — see the Production-Release Audit Round 2 section below (hybrid GPU-linear/CPU-attention batch prefill, `forward_metal_fp8.rs`). |
| Phase 27: Metal FP8 GEMV (E4M3/E5M2) | ✅ Complete | `MSL_GEMV_FP8_E4M3_V1` + `MSL_GEMV_FP8_E5M2_V1` MSL kernels (AoS 34B blocks, simdgroup-per-row reduction); `metal_fp8_kernels.rs` with `metal_gemv_fp8_e4m3`/`metal_gemv_fp8_e5m2` host functions (own singleton Device + CommandQueue, lazy pipeline init); `impl Fp8Kernel for KernelDispatcher` extended with Metal-first GPU dispatch (Metal → CUDA → CPU SIMD); `BonsaiModel::forward_prefill` and `forward_prefill_verify` skip fused Metal prefill for FP8 and fall through to per-token sequential (which now uses Metal GPU GEMV via dispatcher); 2 CI-GPU-gated parity tests + host-only MSL source-string assertions. |
| Phase 26: CUDA FP8 E4M3/E5M2 Batch Prefill | ✅ Complete (kernels); **⚠️ disabled-by-default in dispatch since 2026-07-20** | 8 NVRTC kernels (E4M3/E5M2 × gemm/gemm_residual/fused_gate_up_swiglu/gemv_pf); `try_cuda_prefill_fp8`; `CudaFP8PrefillLayerParams`; `forward_cuda_fp8.rs` with `try_cuda_prefill_with_lm_head_fp8`/`try_cuda_prefill_verify_fp8`; handle namespaces E4M3 26M-31M, E5M2 28M-33M; replaces sequential FP8 CUDA GEMV loop for prefill. **The `cuda-prefill` audit found this path writes into its own private `FP8_PREFILL_STATE.kv_cache`, separate from the `FULL_LAYER_STATE` cache per-token decode reads — the identical split-cache bug fixed for Q1/ternary and disabled-by-default for Q4_0/Q8_0/K-quant (Phase 24/25 above).** Both FP8 entry points now unconditionally return `Err` (sequential fallback) plus a context-length guard, mitigated the same day in a residual-fix pass; `OXIBONSAI_FORCE_CUDA_SPLIT_PREFILL=1` re-enables the decode-incorrect fused path for throughput microbenchmarks only. Verified by new `cuda_synthetic_fp8_e4m3_prefill_decode_parity`/`_e5m2_` regression tests; see the resolved item in "Newly discovered during this audit round" at the top of this file. |
| Phase 25: K-quant CUDA Batch Prefill | ✅ Complete (kernels); **⚠️ disabled-by-default in dispatch since 2026-07-20** | 18 NVRTC kernels (Q2K/Q3K/Q4K/Q5K/Q6K/Q8K × gemm/gemm_residual/fused_gate_up_swiglu); `try_cuda_prefill_k_quant`; `KQuantFormat` enum; 42 K-quant block accessor methods — kernels are correct and tested, but `try_cuda_prefill_with_lm_head`/`_verify` in `forward_cuda/k_quant.rs` now unconditionally return `Err` (sequential fallback) because the `cuda-prefill` audit found the same prefill/decode split-KV-cache bug fixed for Q1/ternary was also present here; re-enable once the same KV-cache unification is applied to this path |
| Phase 24   | CUDA Q4_0/Q8_0 Batch Prefill | ✅ Complete (kernels); **⚠️ disabled-by-default in dispatch since 2026-07-20** | Q4_0/Q8_0 batch GEMM prefill (CPU-only tests pass) — kernels are correct, but `forward_cuda/q_std.rs`'s `try_cuda_prefill_with_lm_head`/`_verify` now unconditionally return `Err` (sequential fallback): same split-KV-cache bug class as Phase 20/25, found by the `cuda-prefill` audit package and disabled as a mitigation pending KV-cache unification |
| Phase 23: K-quant CUDA GEMV kernels | ✅ Complete | Six NVRTC kernels (Q2K/Q3K/Q4K/Q5K/Q6K/Q8K) added; `LinearQ*K::forward` dispatch to GPU; `BonsaiModel::forward`/`forward_prefill` K-quant early-dispatch added. |
| Phase 22: Q4_0/Q8_0 CUDA full-forward dispatch | ✅ Complete | `LinearQ4_0::forward`/`LinearQ8_0::forward` dispatch to `cuda_gemv_q4_0`/`cuda_gemv_q8_0` NVRTC kernels when CUDA available; `BonsaiModel::forward`/`forward_prefill` early-dispatch for Q4_0/Q8_0 skips the Q1 graph attempt. Compile-time `BlockQ4_0`/`BlockQ8_0` layout assertions added. |
| Phase 21: CUDA FP8 dispatch wiring + Q4_0/Q8_0 GEMV kernels | ✅ Complete | FP8 CUDA early-dispatch in `BonsaiModel::forward`/`forward_prefill` (skips Q1 graph, uses `KernelTier::Gpu` GEMV via block dispatch); 14 FP8 E4M3/E5M2 + 14 Q4_0/Q8_0 block accessor methods added to `TransformerBlock`; `cuda_gemv_q4_0`/`cuda_gemv_q8_0` NVRTC kernels (AoS 18B/34B blocks, warp-per-row, nibble decode for Q4_0, int8 for Q8_0). |
| Phase 20: CUDA Ternary Batch Prefill + FP8 GEMV | ✅ Complete | `try_cuda_prefill_ternary`: TQ2_0_g128 fused batch GEMM kernels (`gemm_tq2_g128_v7`, `gemm_tq2_g128_v7_residual`, `fused_gate_up_swiglu_gemm_tq2`) with cap-of-8 outer loop, SoA weight layout, per-token `encode_attn_phase_tq2`; replaces sequential CUDA prefill fallback for Ternary models. **History:** this path was disabled between roughly 2026-06 and 2026-07-20 after a prefill/decode KV-cache handoff bug was found (prompts >16 tok produced corrupted decode, logit Δ≈7.3); it was re-enabled 2026-07-20 once the `cuda-prefill` audit package unified the prefill and decode KV caches (parity restored to Δ≈0.001) — see the Production-Release Audit section at the top of this file. `cuda_gemv_fp8_e4m3`/`cuda_gemv_fp8_e5m2`: NVRTC FP8 GEMV kernels with AoS block decode, warp-per-row reduction; `KernelDispatcher::Gpu` tier dispatches to GPU path for both E4M3 and E5M2; host-only + CI-GPU-gated parity tests. |

## Phase 4 — Complete

- [x] **NEON (ARM) SIMD kernels** -- Implemented `simd_neon.rs` with dequant/gemv/gemm
- [x] **AVX-512 SIMD kernels** -- Implemented `simd_avx512.rs` (compile-checked, untested on ARM)
- [x] **Property-based tests** -- proptest roundtrip, linearity, cross-tier in `proptest_kernels.rs`
- [x] **Integration tests** -- `tests/integration_pipeline.rs` + `tests/cross_tier.rs` + `tests/feature_flags.rs`
- [x] **Tracing enhancement** -- EnvFilter + `#[instrument]` spans on generate, forward, block, server, sampling
- [x] **TOML configuration system** -- `OxiBonsaiConfig` with layered loading, `--config` CLI arg
- [x] **Circuit breaker** -- Overload protection with configurable thresholds
- [x] **Health checks** -- `/health` endpoint with readiness probes
- [x] **Inference metrics** -- tokens/sec, latency, request counts via `InferenceMetrics`
- [x] **Builder pattern API** -- `EngineBuilder`, `ConfigBuilder`, `SamplerBuilder`
- [x] **Sampling presets** -- Greedy, Balanced, Creative, Code presets
- [x] **Batch engine** -- Batched inference support
- [x] **Async engine** -- Tokio-based async inference
- [x] **Recovery strategies** -- Error classification and automatic recovery
- [x] **Prometheus metrics** -- Self-contained Prometheus-compatible metrics; `/metrics` endpoint via `InferenceMetrics` + `metrics_text()` renderer
- [x] **Memory profiling** -- `MemoryProfiler` + `MemorySnapshot`; RSS via Mach (macOS) / `/proc/self/statm` (Linux); peak tracking
- [x] **Fuzz testing** -- Property-based kernel + GGUF parsing fuzz tests; cargo-fuzz harnesses being added

## Phase 5 — Complete

- [x] **Streaming chat completions (SSE)** -- Implemented with `generate_streaming` + SSE handler
- [x] **WASM compilation target** -- Compiles to `wasm32-unknown-unknown`; `wasm_api.rs` provides WASM-safe generate API
- [x] **Bonsai-4B and Bonsai-1.7B model support** -- `Qwen3Config::bonsai_4b()` / `bonsai_1_7b()`, `ModelVariant::Bonsai4B` / `Bonsai1_7B`, `model_variants.rs` with full `ModelSpec` + `CapabilityProfile`
- [x] **OxiRAG integration** -- Pure Rust RAG pipeline (`oxibonsai-rag`): chunker, IdentityEmbedder, TfIdfEmbedder, VectorStore, Retriever, RagPipeline
- [x] **MeCrab/OxiTokenizer** -- Native BPE tokenizer stub (`oxibonsai-tokenizer`): Vocabulary, BpeMerges, OxiTokenizer, char-level fallback, ChatTemplate/chatml
- [x] **Advanced sampling** -- Mirostat v1/v2, Locally Typical, Eta, Min-P samplers; `SamplerChain` composable pipeline; `LcgRng`
- [x] **Speculative decoding** -- `SpeculativeDecoder` with draft/verify loop, acceptance rate tracking. **Updated 2026-07-20:** the production entry point is now `generate_verified(target: &mut InferenceEngine, ...)`, which drafts against the delta-KV path and verifies against a real, separate target engine (token-identical to plain greedy target decode, no synthetic logits). The originally-shipped `generate_speculative`/`verify` (mock 1/vocab_size draft probability, synthesized target logits) is retained only as a `#[doc(hidden)]` synthetic test harness, not a production API — see the `speculative` entry in the Production-Release Audit above.
- [x] **LoRA adapter support** -- `LoraAdapter`, `LoraRegistry`, `BonsaiLoraSet`; apply/merge into weights
- [x] **Context window management** -- `ContextWindow` with TruncateLeft/Right/SlidingWindow/Summarize strategies; system-prompt preservation
- [x] **Beam search** -- `BeamSearchEngine` with configurable `beam_width`, length penalty, no-repeat n-gram, early stopping
- [x] **Token healing** -- `TokenHealer`, `TokenHealingConfig`, `HealingDecoder`
- [x] **Inference pipeline API** -- `InferencePipeline`, `PipelineBuilder`, `GenerationStrategy`, `StopReason`, `PipelineOutput`; convenience constructors (`chat_pipeline`, `code_pipeline`, `greedy_pipeline`)
- [x] **INT8 quantization** -- Per-tensor and per-channel INT8 quantization; `Int8Tensor` with `dequantize()` and `matvec()`
- [x] **GGUF writer** -- `GgufWriter` producing valid GGUF byte streams with metadata and tensor entries
- [x] **Model export** -- `export_to_gguf` supporting Float32, Q1_0_g128, Int8PerChannel formats; FP32 exception layers; `ExportStats`
- [x] **Prefix caching** -- `PrefixCache`, `CacheBlock`, `CacheSession`, `PrefixAwarePrefill` for KV reuse
- [x] **Semantic cache** -- `SemanticCache` with cosine-similarity lookup for repeated prompts
- [x] **MoE architecture** -- Mixture-of-Experts router and expert networks (`moe_router.rs`, `moe_expert.rs`). **Standalone primitive:** not called from `TransformerBlock::forward` or any shipping Bonsai variant — the forward path uses a single dense SwiGLU FFN with no MoE branch. `moe_router`/`moe_expert` are not even re-exported from `lib.rs` (declared as `pub mod` only). See README "Known Limitations".
- [x] **Tensor parallelism** -- `TensorParallelConfig`, column/row parallel linear; `AllReduceOp`
- [x] **Grammar-constrained decoding** -- `TokenConstraint` trait; `JsonConstraint`, `RegexConstraint`, `AllowListConstraint`, `SequenceConstraint`
- [x] **OpenAI API extensions** -- Function calling, JSON mode, `n` completions, response format (`api_extensions.rs`, `api_types.rs`). **Updated 2026-07-20 (`server-api` audit package):** ~~frequency/presence penalties are validated-and-**rejected** with an honest 400, not applied — `sampling.rs`'s `SamplingParams`/`Sampler` have no penalty seam to wire them into; the extended endpoint's `logprobs` honestly returns `None` rather than a fabricated empty placeholder. Real per-token logprobs and real penalty application both remain future work (would need a logits-capturing generate variant in `engine.rs` plus a sampler-level penalty seam).~~ **Superseded 2026-07-21:** both gaps are closed. `oxibonsai-runtime` gained the penalty seam (`Sampler::sample_with_history`, `PenaltyParams`) and a logits-capturing `InferenceEngine::generate_with_logprobs`; `w2-server-base` wired both into base `/v1/chat/completions` **and** the extended endpoint. `frequency_penalty`/`presence_penalty` (`[-2.0, 2.0]`) are now genuinely applied on both endpoints (the honest-400 rejection is retired); `logprobs`/`top_logprobs` (0..=20) return real per-token log-probabilities from the model's actual distribution on both endpoints. Known residual limitation: `generate_with_logprobs` has no per-call temperature/top_p override seam, so a `logprobs:true` request samples with the engine's ambient temperature/top_p rather than a per-request value (penalties still apply correctly). `/v1/completions`' `logprobs` field remains a stub (reflected back as `null`) — not covered by this fix.
- [x] **Embeddings endpoint** -- `POST /v1/embeddings`; `EmbedderRegistry` (IdentityEmbedder + TfIdfEmbedder); float + base64 encoding; dimensions truncation
- [x] **Completions endpoint** -- `POST /v1/completions` with single/batch prompt, echo, logprobs stub
- [x] **RAG server endpoints** -- `/v1/rag/index`, `/v1/rag/query` (feature-gated)
- [x] **Rate limiting + middleware** -- `RateLimiter`, `MiddlewareConfig`; tower-based rate-limit and timeout layers
- [x] **Admin API** -- `/admin/status`, `/admin/config`, `/admin/reset-metrics`, `/admin/cache-stats`
- [x] **Evaluation harness** -- `oxibonsai-eval` crate: HellaSwag, MMLU, TruthfulQA benchmarks; `EvalRunner`, `EvalResult`
- [x] **Optimizer suite** -- SGD (with momentum, Nesterov, weight decay), Adam, AdamW; CosineAnnealing / WarmupConstant / Constant LR schedulers
- [x] **LoRA training scaffold** -- `LoraTrainer` with per-step Adam updates, gradient clipping, warmup; `TrainingStep` metrics
- [x] **Fine-grained gradient computation** -- `GradientContext`, `LayerGradients`, `backward_rms_norm`, `backward_swiglu`, `backward_linear`
- [x] **Continuous batching** -- `ContinuousBatchScheduler` for in-flight request management
- [x] **Model registry** -- `ModelVariant`, `ModelSpec`, `CapabilityProfile`; auto-detection from config dimensions
- [x] **Comprehensive integration tests** -- `tests/full_pipeline_tests.rs` (25 tests), `tests/quantization_pipeline_tests.rs` (10 tests), `tests/server_integration_tests.rs` (8 tests)

## Phase 17 — FP8 KV Cache + JSON Schema BNF + Eval + SmoothQuant + Unigram

- [x] **FP8 KV cache (Slice A)** — `Fp8KvLayer`/`Fp8KvFormat`/`Fp8KvCache` in `kv_cache_quant.rs`; per-row abs-max scale (`max|row|/448.0` E4M3, `/57344.0` E5M2); `push`, `get_key`, `get_value`, `get_keys_at`, `get_values_at`, `memory_bytes`, `memory_bytes_fp32_equivalent`, `clear`; `KvCacheLevel::Fp8` variant (ordinal 2, between Q8 and Q4, `memory_factor=0.5`, `tag="fp8"`); all `ordinal`/`tag`/`memory_factor`/`from_ordinal` match arms extended; 11 model tests + 3 runtime tests; clippy clean
- [x] **JSON Schema → BNF compiler (Slice B)** — `grammar/json_schema_compiler.rs` in `oxibonsai-runtime`; two-pass compiler (pre-allocate `$defs` NTs in pass 1, compile rules in pass 2); supports `string`/`integer`/`number`/`boolean`/`null`, `object` (required properties), `array` (items), `enum`, `anyOf`/`oneOf`/`allOf`, `$ref` to `#/$defs/...` and `#/definitions/...`, empty/boolean schemas; lazy shared NTs for digit/string rules; max depth 32; `JsonSchemaCompileError` (`InvalidSchema`, `UnsupportedKeyword`, `DanglingRef`, `DepthExceeded`, `InvalidJson`); 30 integration tests; re-exported from `grammar::` and `lib.rs`
- [x] **ARC + GSM8K evaluators (Slice C)** — `arc.rs` (ArcEvaluator with `ArcSplit::Easy/Challenge`, delegates to `McEvaluator`/`McLogitEvaluator`, `ArcResult`; handles 4- and 5-choice questions); `gsm8k.rs` (regex-free `extract_final_answer` scanning for `####` + trailing numeric, 1e-6 relative tolerance, `Gsm8kResult` with `accuracy_pct`/`no_answer_rate`; `gsm8k_example` builder); 10 ARC tests + 15 GSM8K tests; re-exported from `oxibonsai-eval::lib`
- [x] **SmoothQuant FP8 calibrator (Slice D)** — `smoothquant.rs` in `oxibonsai-model`: `SmoothQuantCalibrator` with per-channel `running_max_abs` accumulator, `record_activation`, `smooth_factors`, `layer_count`, `has_layer`; `SmoothQuantError` enum; `quantize_fp8_e4m3_smooth` / `quantize_fp8_e5m2_smooth` (apply `smooth_weights` then standard block quantize); `BlockFP8E4M3::quantize_with_channel_scales` / `BlockFP8E5M2::quantize_with_channel_scales` in `oxibonsai-core` (channel-wise divisor before block packing, zero-divisor guards); 12 tests; re-exported from model and core
- [x] **Unigram tokenizer (Slice E)** — `unigram.rs` in `oxibonsai-tokenizer`: `UnigramVocab` (Viterbi forward-pass lattice, `token_to_id` HashMap, UNK_PENALTY=-1e6, UTF-8 char-boundary safe, per-byte UNK fallback, `encode`/`decode`/`token_count`/`unk_id`); `UnigramError` (`EmptyVocab`, `UnkOutOfRange`, `DuplicateToken`); `OxiTokenizer::with_unigram` constructor + `is_unigram()` + `Option<UnigramVocab>` dispatch in `encode()`; `HfModelType` enum + `model.type` branch in `hf_format.rs` (`parse_bpe_model`/`parse_unigram_model` helpers, `into_tokenizer` branches on type); 14 inline tests + 8 integration tests + 4 hf_format tests

## Phase 16 — FP8 Full Stack + Grammar Acceleration

- [x] **FP8 export formats** — `ExportFormat::FP8E4M3/E5M2` in `oxibonsai-model/src/export.rs`; size estimate 34/32 bytes/weight using `div_ceil`; GGUF type ID 43/44 mapping; zero-copy `repr(C)` block serialization via raw pointer cast; 4 tests (roundtrip E4M3, roundtrip E5M2, size estimate, FP32 exceptions)
- [x] **FP8 kernel criterion benchmarks** — `bench_fp8_kernels` in `benches/kernel_benchmarks.rs`; 4 benchmark groups: dequant 8192w, GEMV 128×256, GEMV 512×512 (sequential vs parallel), GEMV dispatch sweep 32/256/1024 rows; throughput reporting in element-ops/sec; E4M3 and E5M2 coverage; scalar vs simd_auto vs rayon-parallel
- [x] **Token-byte precomputation + first-byte index (Phase 16B)** — `GrammarConstraint` now precomputes byte sequences for all vocab tokens at construction time; `first_byte_index: Box<[Vec<u32>; 256]>` accelerates `allowed_tokens` by skipping non-first-byte tokens without calling the decode fn or checking HashSet; `empty_token_ids` handles EOS/special tokens; `vocab_size()` accessor; `index_memory_bytes()` for observability; `advance()` uses precomputed `token_bytes` (no decode call per token); combined with Phase 15.x state cache for O(1) repeated-state hits; 5 new integration tests in `tests/grammar_cache_tests.rs` + 8 new unit tests in `src/grammar/constraint.rs`

## Phase 15 — Advanced Inference Capabilities

- [x] **FP8 E4M3FN quantization** — `BlockFP8E4M3` (32 weights × 1 FP8 byte + FP16 scale = 34 bytes/block); `fp8_e4m3_encode/decode` with bit-exact RNE rounding, saturation to ±448, NaN=0x7f; GGUF type ID 43 (PrismML extension); 56 tests in `oxibonsai-core`
- [x] **FP8 E5M2 quantization** — `BlockFP8E5M2` (same 34 bytes/block); `fp8_e5m2_encode/decode` with ±infinity support; GGUF type ID 44 (PrismML extension)
- [x] **FP8 GGUF surface** — `GgufTensorType::F8_E4M3/F8_E5M2`, `TensorType::F8_E4M3/F8_E5M2`, `ExtendedQuantType::F8_E4M3/F8_E5M2` (8.5 bits/weight), `is_fp8()` predicate
- [x] **FP8 reference kernels** — `dequant_fp8_e4m3/e5m2`, `gemv_fp8_e4m3/e5m2`, `gemm_fp8_e4m3/e5m2` (scalar reference); `Fp8Kernel` trait; `impl Fp8Kernel for KernelDispatcher`; 349 tests in `oxibonsai-kernels`
- [x] **FP8 SIMD kernels (Phase 15.x)** — `fp8_lut.rs` (OnceLock 256-entry decode LUTs); `simd_fp8_avx2.rs` (gather-based 8-wide, `_mm256_i32gather_ps`); `simd_fp8_avx512.rs` (16-wide, `_mm512_i32gather_ps`); `simd_fp8_neon.rs` (LUT + `vfmaq_f32`, 4-wide); tier-aware dispatch in `KernelDispatcher`; parallel FP8 GEMV/GEMM in `parallel.rs`; 18 parity tests in `tests/fp8_simd_parity.rs`
- [x] **Earley `allowed_tokens` cache (Phase 15.x)** — `grammar/cache.rs` `AllowedTokensCache` (LRU, 256-entry default, `Arc<[bool]>` masks); `state_hash()` on `EarleyRecognizer` (sorted chart items → `DefaultHasher`); `Mutex<AllowedTokensCache>` in `GrammarConstraint`; `with_cache_capacity` constructor; `cache_stats()` for observability; 12 integration tests in `tests/grammar_cache_tests.rs`
- [x] **`AllowListConstraint`** — finite-set token-sequence constraint for multiple-choice output; candidate activation bitmask with prefix tracking; 32 tests in `oxibonsai-runtime`
- [x] **`SequenceConstraint`** — exact token-sequence forcing constraint; unconstrained after sequence consumed
- [x] **`LengthConstraint`** — hard `[min_len, max_len]` bounds with optional stop token enforcement
- [x] **BNF grammar engine** (`oxibonsai-runtime/src/grammar/`):
  - `ast.rs` — `Grammar`, `Rule`, `Symbol` (byte-level terminals), `normalise_terminals()` single-byte chain expansion
  - `bnf_parser.rs` — hand-rolled two-phase BNF parser (lexer + parser); forward references, line continuation, escape sequences, `BnfParseError`
  - `earley.rs` — full Earley recognizer; FIRST set computation; predict/complete/scan; `next_byte_set()` lookahead; `clone_state()` for speculative token probing; left-recursive grammar support via set-based memoization
  - `constraint.rs` — `GrammarConstraint` implementing `TokenConstraint`; byte-level probe via `clone_state()` per candidate token; EOS handling
  - `examples.rs` — `arithmetic_grammar()`, `simple_ab_grammar()`, `csv_row_grammar()`, `json_lite_grammar()`, `palindrome_grammar()`
  - 93 tests in `oxibonsai-runtime`

## CI / Publish

- [x] Publish script (`scripts/publish.sh`) -- dry-run by default, `--for-real` for actual publish
- [x] Feature flag tests (`tests/feature_flags.rs`) -- compile-time validation of feature propagation
- [x] GitHub Actions CI pipeline -- `.github/workflows/ci.yml` (build, test, clippy, WASM, feature matrix)
- [ ] crates.io publish (awaiting approval)

## Phase 6 — Complete

- [x] **True multi-GPU inference** -- NCCL-style all-reduce/gather/scatter; `DeviceMesh` (tp×pp); rayon-simulated collectives; column/row weight partition + merge helpers; `multi_gpu.rs` + 27 tests. **Honest label:** this is a **rayon CPU simulation** of NCCL-style collectives, not real GPU-to-GPU communication — the module's own doc comment says so directly ("implemented over rayon thread pools as a CPU simulation... a real GPU backend would swap in NCCL/cuBLAS calls"), and `DeviceMesh`/`multi_gpu.rs` is referenced only by its own tests, wired into no actual multi-device inference dispatch path. No supported configuration (CUDA, Metal, or CPU) performs real inter-GPU communication today. See README "Known Limitations".
- [x] **CUDA/Metal backend** -- GPU kernel dispatch stubs: `GpuBackend` trait, `CpuBackend` (always-on), `CudaBackend` (feature = "cuda"), `MetalBackend` (feature = "metal"); `select_backend`, `gpu_matmul`; 18 integration tests; cuBLAS / Metal Performance Shaders left as future implementation. **Separately:** the `cuda` (scirs2-core) acceleration tier (`gpu_backend/scirs2_backend.rs`, distinct from the above) is a **CPU fallback in practice** — `is_accelerated()` always returns `false` for CUDA because scirs2-core retired its cudarc-based CUDA backend in 0.6.x, so the dispatcher always falls through to CPU SIMD when built with `--features cuda`. Correct but silent: no garbage output, just no GPU acceleration. Use `native-cuda` for real NVIDIA GPU acceleration. See README Acceleration Tiers / "Known Limitations".
- [x] **Production GGUF with real model weights** -- Streaming `TensorChunkIter`; memory budget estimation (`estimate_memory_bytes`, `fits_in_budget`); lazy tensor metadata loading; `validate_gguf_file`; `gguf_loader.rs` + 25 tests
- [x] **Web UI** -- Chat interface; model management dashboard
- [x] **Distributed serving** -- Multi-node coordinator; consistent hashing for request routing. **Honest label: in-memory topology only.** `distributed.rs` implements FNV-1a consistent hashing (`ConsistentHashRing`/`VNode`) and an in-memory `NodeRegistry`/`DistributedCoordinator` — its own doc comment states "All state is in-memory — no actual TCP connections are made" and that it is "designed for unit testing and single-process simulation." There is no TCP/HTTP/gRPC transport anywhere in the module, and no cross-node request forwarding exists in this codebase. See README "Known Limitations".
- [x] **Full MeCrab tokenizer integration** -- `NativeTokenizerBridge` added to `oxibonsai-runtime`; wraps `OxiTokenizer` with encode/decode/chat-format API; pure Rust, WASM-compatible, zero C/FFI; 10 integration tests all passing
- [x] **PagedAttention / vLLM-style KV management** -- Block-based KV cache for higher utilisation; `PagedKvCache`, `BlockPool`, `BlockTable`, `KvPage` in `oxibonsai-model/src/paged_kv_cache.rs`. **Standalone, tested library primitive; not consumed by `BonsaiModel`/`oxibonsai-runtime`/`oxibonsai-serve`.** The shipping inference engine exclusively uses the contiguous `kv_cache::KvCache` (pre-allocated to `max_seq_len`, no block-based reuse) — the "higher utilisation" benefit this module targets is not realized in the default forward/serving path. Usable today as an external library type for consumers of `oxibonsai-model` who wire it in themselves. See README "Known Limitations".
- [x] **GGMLv4 / GGUF v3 support** -- Forward compat layer: `GgufVersion`, `ExtendedQuantType`, `GgufCompatReport`, `check_gguf_header`, `build_compat_report` in `gguf/compat.rs`; 20 integration tests all passing
- [x] **Sparse attention patterns** -- `SparseAttentionMask` with Local Window, Strided, Bigbird, Longformer, and Dilated patterns; `sparse_attention_forward`; memory reduction estimate; `sparse_attention.rs` + tests in `oxibonsai-model`. **Standalone primitive:** `sparse_attention_forward` has zero call sites outside its own tests — `TransformerBlock::forward`/`forward_sw` use only dense `compute_gqa_attention`. See README "Known Limitations".
- [x] **Weight tying** -- `TiedEmbedding` with tied embedding/unembedding pair; `embed()`, `lm_head_logits()`, `set_embedding()` API; `TyingError` type; `weight_tying.rs` + tests in `oxibonsai-model`
- [x] **Dynamic quantization** -- `DynamicQuant` trait; `DynamicQ8_0`, `DynamicQ4_0`, `DynamicQ4_1` quantizers; `QuantizedTensor`, `quantize_model_dynamic`, `calibrate_scale`; `dynamic_quant.rs` + 21 tests
- [x] **Runtime profiler** -- `InferenceProfiler` with span tracking, `SpanRecord`, `ProfileReport`, stats (mean/min/max/p95/p99); flame-graph text renderer; `profiler.rs` + 20 tests
- [x] **Advanced RAG chunking** -- `SemanticChunker` with cosine-similarity boundary detection; `HierarchicalChunker` with multi-level chunking; `SlideWindowChunker`; 31 tests in `oxibonsai-rag`
- [x] **Tokenizer serialization** -- `save_json`/`load_json` for `OxiTokenizer`; vocabulary and BPE merge round-trip; `TokenizerSerializer`; 16 tests in `oxibonsai-tokenizer`
- [x] **GGUF model card** -- `ModelCard` with metadata (author, license, description, tags, datasets); `CardFormat` enum; `render_card`/`parse_card`; embedded in GGUF metadata via `oxibonsai-core`
- [x] **Token streaming metrics** -- `StreamingSession` with per-token timing; `TokenEvent`, `StreamingStats` (mean/p50/p95 TTFT + TPOT); `StreamingProfiler`; 22 tests in `oxibonsai-runtime`
- [x] **KV cache quantization** -- `QuantizedKvCache` with Q8 and Q4 formats; per-head scale factors; `KvCacheQuantizer`, `store_quantized`, `load_dequantized`; `kv_cache_quant.rs` + 18 tests. **Primitive only — never applied.** `QuantizedKvCache`/`Fp8KvCache`/`Fp8KvLayer` are never constructed by `BonsaiModel` or the runtime; separately, `oxibonsai-runtime`'s `KvCachePolicy::observe()` computes an adaptive target level but its only consumers are a Prometheus gauge and an admin JSON endpoint — nothing swaps the model's actual KV-cache representation. "Adaptive KV cache quantization under memory pressure" reports a level; it does not enforce one. See README "Known Limitations" and the Phase 14 `KvCachePolicy` entry below.

## Phase 7 — Complete

- [x] **KV cache quantization (INT8)** -- `QuantizedKvCache` with Q8/Q4 formats; per-head scale factors; `kv_cache_quant.rs`. (Same integration gap as the Phase 6 entry above — primitive + telemetry only, never applied to the model's live KV cache.)
- [x] **Model merging (SLERP, TIES, DARE, task vector)** -- `model_merge.rs` with multi-strategy merge support
- [x] **Checkpoint save/load (OXCK binary format)** -- `checkpoint.rs` with serialization/deserialization
- [x] **YaRN extended RoPE** -- `yarn_rope.rs` with YaRN-style RoPE scaling for extended context. **Standalone primitive:** the production `RopeTable` (built in `model/types/mod.rs`, used by every forward pass) never calls into `yarn_rope.rs`; `Qwen3Config::from_metadata` has no field to carry a scaling strategy, and no shipped native Bonsai GGUF carries `llm.rope.scaling.*` metadata to select one. See README "Known Limitations".
- [x] **Flash Decoding** -- `flash_decode.rs` for optimized single-token decoding
- [x] **Attention Sink / StreamingLLM** -- `attention_sink.rs` with sink tokens for infinite context windows. **Standalone primitive:** not called from `TransformerBlock::forward` — no sink-token retention happens in the shipping decode path. See README "Known Limitations".
- [x] **Dynamic activation quantization** -- `dynamic_quant.rs` with DynamicQ8_0/Q4_0/Q4_1 quantizers
- [x] **Inference profiler with FLOP counting** -- `profiler.rs` with span tracking, p95/p99 stats, flame-graph renderer
- [x] **GGUF model card generation** -- `ModelCard` with metadata; `render_card`/`parse_card` in `oxibonsai-core`
- [x] **Token streaming metrics (TTFT, TBT)** -- `StreamingSession`, `StreamingStats`, `StreamingProfiler` in `oxibonsai-runtime`
- [x] **Weight importance analysis and pruning** -- `pruning.rs` with structured/unstructured pruning
- [x] **PTQ calibration pipeline** -- `calibration.rs` with post-training quantization calibration
- [x] **BPE tokenizer training** -- `trainer.rs` in `oxibonsai-tokenizer` with BPE merge learning
- [x] **Tokenizer serialization** -- `save_json`/`load_json` for `OxiTokenizer`; vocabulary and BPE merge round-trip
- [x] **Advanced RAG chunking** -- Sentence, recursive, sliding window, markdown chunking in `oxibonsai-rag`
- [x] **Loss functions (CE, focal, distillation, contrastive)** -- `losses.rs` with full training loss suite
- [x] **Advanced LR schedulers (OneCycle, ReducePlateau, CyclicLR)** -- `lr_schedulers.rs` with advanced scheduling
- [x] **Sparse attention patterns (local window, BigBird, strided)** -- `sparse_attention.rs` with multiple patterns. (Same standalone-primitive caveat as the Phase 6 entry above.)
- [x] **Weight tying (embedding/LM head sharing)** -- `weight_tying.rs` with `TiedEmbedding`
- [x] **Model compression pipeline (prune+quantize)** -- `compression.rs` with end-to-end compression
- [x] **ROUGE evaluation metrics (ROUGE-1/2/L/S)** -- `rouge.rs` in `oxibonsai-eval`
- [x] **Mixture of Depths (adaptive compute)** -- `mixture_of_depths.rs` with `ModRouter`. **Standalone primitive:** no MoD/`ModRouter` branch exists in `TransformerBlock::forward` — every layer always runs at full depth. See README "Known Limitations".
- [x] **Token budget tracking** -- `token_budget.rs` in `oxibonsai-runtime`
- [x] **RoPE scaling variants (linear, DynamicNTK, LLaMA 3.1, LongRoPE)** -- `rope_scaling.rs` with multiple strategies. **Standalone primitive:** same integration gap as YaRN above — `RopeScalingStrategy`/`compute_rope_frequencies` have zero call sites outside their own tests; the production `RopeTable` uses plain unscaled RoPE. See README "Known Limitations".
- [x] **N-best hypothesis tracking** -- `nbest.rs` in `oxibonsai-runtime`
- [x] **Adaptive sampling (entropy cooling, repetition adaptation)** -- `adaptive_sampling.rs` with `AdaptiveChain`
- [x] **Generation quality metrics (repetition, diversity, BLEU)** -- `quality_metrics.rs` in `oxibonsai-runtime`
- [x] **oxibonsai-serve standalone binary crate** -- `crates/oxibonsai-serve` with CLI args, banner, OpenAI-compatible server
- [x] **Cross-attention layers** -- `cross_attention.rs` for encoder-decoder architectures. **Standalone primitive:** no cross-attention call site exists in the shipping Bonsai (decoder-only) forward path. See README "Known Limitations".
- [x] **Gradient checkpointing** -- `gradient_checkpoint.rs` for memory-efficient training
- [x] **Request deduplication** -- `dedup.rs` with FNV-1a hashing for duplicate request detection
- [x] **Model hot-reload** -- `hot_reload.rs` for live model swapping without restart
- [x] **Performance auto-tuning engine** -- `auto_tuner.rs` with CPU detection, SIMD tier selection, memory budget estimation, kernel benchmarking
- [x] **JSON Schema structured output constraint** -- `json_schema.rs` with schema parsing, validation state machine, constrained token generation
- [x] **Multi-model serving with LoRA routing** -- `multi_model.rs` / `lora_router` with dynamic model selection and LoRA adapter dispatch

## Phase 8 — Complete

- [x] **Q2_K and Q4_K quantization formats** — `BlockQ2K`/`BlockQ4K` with dequant/quantize in `oxibonsai-core/quant_k.rs`; 21 tests
- [x] **Streaming GGUF reader** — `GgufStreamParser` state machine for progressive network-loaded GGUF parsing; 22 tests
- [x] **Kernel performance tuning** — `PlatformProfile` detection, `TunedThresholds` auto-computation, cache-line aligned buffers, software prefetch hints; 29 tests
- [x] **Layer-level correctness tests** — RMSNorm, SwiGLU, RoPE, Attention, TransformerBlock reference comparisons; 23 tests
- [x] **Numerical stability tests** — Extreme inputs, overflow/underflow, long sequences, KV cache stress; 25 tests
- [x] **Full generate() pipeline integration tests** — End-to-end generation, determinism, sampling parameter effects, edge cases; 26 tests
- [x] **Sampling distribution statistical tests** — Chi-square goodness-of-fit, temperature/top-k/top-p/repetition penalty validation; 22 tests

## Phase 9 — Ternary Bonsai

- [x] **Quant block types** — `crates/oxibonsai-core/src/quant_ternary.rs`: `BlockTQ2_0_g128` (34B/128w), `BlockTQ2_0` (66B/256w), `TernaryCode`, scalar dequant/quantize, zero-copy `slice_from_bytes`, 11 tests
- [x] **GGUF surface** — `GgufTensorType::TQ2_0=37/TQ2_0_g128=42`, `writer::TensorType`, `KNOWN_QUANT_TYPES`, `compute_tensor_size_bytes`, `is_ternary()` helper, ID verification against ggml-quants.h, 6 tests
- [x] **Core re-exports** — `pub mod quant_ternary` + re-export `BlockTQ2_0{_g128}`, `TernaryCode`, constants from `oxibonsai_core`
- [x] **Scalar ternary kernels** — `dequant_ternary.rs`, `gemv_ternary.rs`, `gemm_ternary.rs` correctness reference; 9 tests per file
- [x] **SIMD ternary kernels** — NEON / AVX2 / AVX-512 `gemv_tq2_0_g128` + `dequant` + `gemm`; pos_mask+neg_mask decomposition; sign-canary tests
- [x] **`TernaryKernel` trait + dispatcher** — `traits.rs` + `dispatch.rs` impl; GPU tier falls back to scalar; 2 dispatcher tests
- [x] **`Qwen3Config::ternary_bonsai_*()` constructors** — three named constructors in `config.rs`, co-drift via 1-bit ctor call; 2 tests
- [x] **`ModelVariant::TernaryBonsai*` + detection** — three enum variants + `from_config_and_sample_tensor_type()`; all-ternary byte budget; 4 tests
- [x] **`ternary_bonsai_*_spec()` + capability profiles** — six-entry `all_specs()`, ±5 % byte budget tests; 4 tests
- [x] **`weight_loaders.rs` + `LinearTernary` layer** — `load_ternary_blocks`, `load_ternary_embedding`, `OutputWeight::Ternary`; sum-type for Attn/MLP; 3 tests
- [x] **`ExportFormat::TernaryG128`** — `quantize_ternary.rs`, norm-only FP32 exceptions, `default_fp32_exceptions_ternary_excludes_embedding` guard; 5 tests
- [x] **Phase 9 docs** — TODO.md Phase Status table + Memory/Performance rows + crate-local entries; README.md Ternary Bonsai subsection

## Phase 10 — Complete

- [x] **NEON TQ2 GEMV** — `simd_neon.rs` ternary GEMV with pos_mask + neg_mask decomposition over TQ2_0_g128 blocks
- [x] **AVX2 TQ2 GEMV** — `simd_avx2.rs` ternary GEMV over 128-weight groups with FP16 scale broadcast
- [x] **AVX-512 TQ2 GEMV** — `simd_avx512.rs` ternary GEMV with 512-bit lanes
- [x] **Ternary dispatch** — `TernaryKernel` routes via `KernelDispatcher::auto_detect()` across NEON / AVX2 / AVX-512 / scalar tiers
- [x] **Ternary dequant SIMD** — per-tier `dequant_ternary` for TQ2_0_g128 (mirrors the Q1 dequant layout)
- [x] **Apple Silicon CPU baseline** — NEON TQ2 path measured at ~7–8 tok/s end-to-end on Ternary-Bonsai-1.7B via `scripts/cli_ternary.sh`

## Phase 11 — Complete

- [x] **Metal TQ2_0_g128 GEMV kernel** — per-kernel TQ2 dispatch path in `crates/oxibonsai-kernels/src/gpu_backend/metal_dispatch.rs`
- [x] **`blocks_as_bytes_ternary` helper** — zero-copy TQ2 block upload to the Metal backend
- [x] **Per-kernel dispatch parity with Q1** — Metal TQ2 path validated against scalar reference so CPU and Metal produce byte-identical output at `--temperature 0 --seed 42`
- [x] **Metal graph groundwork** — `gpu_backend/metal_graph.rs` and `metal_prefill.rs` cover prefill + per-step GEMV composition ahead of the Phase 13.x fused path

## Phase 12 — Complete

- [x] **NVRTC kernel pipeline** — `gpu_backend/cuda_kernels.rs`, `cuda_attn_kernels.rs`, `cuda_prefill_kernels.rs`; runtime-compiled PTX in `kernel_sources/`
- [x] **CUDA graph capture** — `gpu_backend/cuda_graph/` groups every layer op (RMSNorm, gemv v7/v8/v9 with residual, SwiGLU, gate+up, Q1 AoS→SoA and TQ2 block→SoA reformat) into a reusable CUDA graph
- [x] **Fused Q1 + TQ2 full-forward** — `cuda_full_layer.rs` encodes a full Qwen3 layer into one graph launch for both quant families
- [x] **Prefill path** — `gpu_backend/cuda_prefill.rs` shares kernel sources with decode to keep the prefill/decode invariants identical
- [x] **`native-cuda` feature flag** — opt-in backend selected via `KernelDispatcher` alongside `cuda` (scirs2) and `metal`
- [x] **Measured** — ~21.9 tok/s on Ternary-Bonsai-1.7B end-to-end via `scripts/cli_ternary.sh cuda`

## Phase 13.x — Complete

- [x] **Fused Metal TQ2 full-forward** — `gpu_backend/metal_full_layer/` encodes the 14-dispatch sequence (pre-attn RMSNorm → fused QKV → QK-norm + RoPE → KV-store → batched attention → attn output + residual → FFN RMSNorm → fused gate+up → SwiGLU → down + residual) into a single GPU command buffer per token
- [x] **Fused Metal Q1 full-forward** — same command-buffer layout reused for 1-bit weights; shared with the TQ2 code path via a weight-family-generic encoder
- [x] **Command buffer reuse** — per-layer argument buffers cached in `metal_full_layer/types.rs` to avoid per-token allocations
- [x] **`scripts/bench_ternary.sh`** — 3-run CPU-vs-Metal averaging harness with best-of-N reporting
- [x] **`scripts/download_ternary.sh`** — HF `hf` CLI fetch + safetensors → GGUF conversion wrapper
- [x] **Measured** — ~50 tok/s (best ~57) on Ternary-Bonsai-1.7B on Apple Silicon (M-series), ~13× speedup vs the per-GEMV dispatch of Phase 11

## Phase 13.y — Complete (shipped in 0.1.3)

Closes all 7 `OutputWeight::Ternary(_)` CPU-fallback guard sites in `crates/oxibonsai-model/src/model/types.rs`.

### Milestone 1 — Metal ternary LM head (4 guard sites)

- [x] Pre-split `model/types.rs` (1857 lines → >2000 after M1 additions) via `splitrs` (completed 2026-04-24)
  - **Goal:** Keep every file under 2000 lines. Split into `types.rs` + `types_gpu_cache.rs` + `types_forward_metal.rs`.
  - **Files:** `crates/oxibonsai-model/src/model/types/mod.rs` (541), `gpu_cache.rs` (233), `forward_metal.rs` (1054), `forward_cuda.rs` (454).
  - **Tests:** `cargo nextest run -p oxibonsai-model --all-features` green after split.

- [x] Add `MetalGraph::encode_tail_and_commit_ternary` + update `encode_full_forward_ternary` routing + update `try_metal_full_forward_ternary` LM-head upload (completed 2026-04-24)
  - **Goal:** Kernel-layer ternary LM head support. `encode_tail_and_commit_ternary` mirrors `encode_tail_and_commit` but uses `dispatch_gemv_tq2` at the LM-head GEMV site. `encode_full_forward_ternary` routes to it. `try_metal_full_forward_ternary` uploads via `get_or_upload_tq2_weight_soa` with namespace `4_000_000+`.
  - **Files:** `crates/oxibonsai-kernels/src/gpu_backend/metal_full_layer/functions_2.rs`, `functions_3.rs`, `metal_graph.rs` (test).
  - **Tests:** `test_encode_tail_ternary_matches_reference` passes — bit-exact token match, logits within 1e-3.

- [x] Extend `CachedModelWeights` + remove short-circuit + wire ternary cache builder + remove 4 Metal guards (completed 2026-04-24)
  - **Goal:** Model-layer: `CachedModelWeights` extended with ternary fields; ternary cache builder wired; all 4 Metal Err guards replaced with `try_metal_*_ternary` routes; 3 new thin wrappers in `functions_3.rs`.
  - **Files:** `crates/oxibonsai-model/src/model/types/gpu_cache.rs`, `forward_metal.rs`; `crates/oxibonsai-kernels/src/gpu_backend/metal_full_layer/functions_3.rs`.
  - **Tests:** CPU vs Metal greedy-decode output byte-matches on Ternary-Bonsai-1.7B (temp=0, seed=42). Metal avg=54.5 tok/s (best=55.4) — improvement from ~50 tok/s baseline.

### Milestone 2 — CUDA ternary full-forward (3 guard sites)

- [x] Pre-split `cuda_full_layer.rs` (1687 lines → >2000 after M2 additions) via `splitrs` (completed 2026-04-24)
  - **Goal:** Split into `cuda_full_layer/mod.rs` + `encode_q1.rs` + `encode_ternary.rs` (new, M2 target).
  - **Files:** `crates/oxibonsai-kernels/src/gpu_backend/cuda_full_layer.rs` → directory.
  - **Tests:** `cargo check --features cuda` green (execution gated by CI-GPU).

- [x] Implement CUDA ternary full-forward path (completed 2026-04-24)
  - **Goal:** `encode_layer_into_ternary` (14 dispatches/layer, same split-dispatch shape as Metal Phase 13.x), `encode_full_forward_ternary`, `encode_lm_head_gemv_ternary` (single `launch_gemv_tq2_v1`), `try_cuda_full_forward_ternary` + `try_cuda_full_forward_ternary_with_gpu_lm_head`. Remove 3 CUDA guards at `try_cuda_full_forward_with_lm_head`, `try_cuda_prefill_with_lm_head`, `try_cuda_prefill_verify`.
  - **Files:** `crates/oxibonsai-kernels/src/gpu_backend/cuda_full_layer/encode_ternary.rs` (implemented); `crates/oxibonsai-model/src/model/types/forward_cuda.rs` (3 guards removed).
  - **Tests:** `test_encode_lm_head_gemv_ternary_matches_reference`, `test_encode_full_forward_ternary_matches_reference` (CI-GPU-gated). `cargo check --all-features` + clippy + 140 non-CUDA tests all green.
  - **Risk:** `CuGraphHolder` captured-graph pattern — capture+replay must be tested end-to-end, not just isolated kernel launches. Post-merge validation on CUDA hardware required.

## Phase 18 — Standard GGUF Formats + K-quant Extensions + WordPiece + WinoGrande/BoolQ + Regex→BNF

- [x] **Q4_0 + Q8_0 full stack** — `BlockQ4_0` (18 bytes/32w, nibble dequant) + `BlockQ8_0` (34 bytes/32w, i8 dequant) in `oxibonsai-core::quant_std`; scalar GEMV kernels in `oxibonsai-kernels::{gemv_q4_0, gemv_q8_0}`; `LinearQ4_0<'a>` + `LinearQ8_0<'a>` layers in `oxibonsai-model::layers::linear_standard`; `LinearLayer::{Q4_0, Q8_0}` + `OutputWeight::{Q4_0, Q8_0}` variants; Q4_0/Q8_0 paths in `weight_loaders.rs`; 25 block tests + 8 kernel tests each
- [x] **Q5_K + Q6_K K-quant extensions** — `BlockQ5K` (176 bytes/256w, 8 sub-blocks, 5-bit q, K-quant scale/min encoding) + `BlockQ6K` (210 bytes/256w, 16 sub-blocks, 6-bit q, symmetric centered) in `oxibonsai-core::quant_k_ext`; GEMV kernels in `oxibonsai-kernels::{gemv_q5k, gemv_q6k}`; `LinearQ5K<'a>` + `LinearQ6K<'a>` layers; full model integration; 12 integration tests in `tests/q5k_q6k_model_tests.rs`
- [x] **WordPiece tokenizer** — `WordPieceVocab` with greedy longest-match-first segmentation + `##`-prefixed continuation tokens; Unicode-safe char-boundary iteration; `WordPieceError`; `HfModelType::WordPiece` parsing branch in `hf_format.rs`; `OxiTokenizer::with_wordpiece` constructor + dispatch; 20 unit tests + 15 integration tests
- [x] **WinoGrande + BoolQ evaluators** — `WinoGrandeEvaluator` delegating to `McEvaluator`/`McLogitEvaluator` via `as_mc_dataset()` (1-based→0-based answer mapping); `BoolQEvaluator` with `extract_answer()` case-insensitive yes/no prefix matching; 12 WinoGrande + 16 BoolQ integration tests in `crates/oxibonsai-eval/tests/`
- [x] **Regex → BNF compiler** — `compile_regex(pattern) -> Result<Grammar, RegexCompileError>` via full Thompson NFA → Subset DFA (2048-state limit) → Grammar pipeline; `ByteSet` (256-bit bitset); supports literals, `.`, `[...]`/`[^...]`, `*`/`+`/`?`, `{n,m}`, `|`, grouping, `\d\w\s` classes, anchors (silently ignored); unsupported features (backreferences, lookahead, named groups) return `UnsupportedFeature`; re-exported from `grammar::` and `lib.rs`; 38 integration tests

## Phase 14 — Runtime Controllers (0.1.4)

- [x] **`KvCachePolicy`** — runtime KV-cache compression *policy computation*: FP16 → Q8 → Q4 target-level selection driven by EWMA pressure with hysteresis to prevent thrashing; configurable thresholds and `[min, max]` clamps; thread-safe via atomics. **Advisory/telemetry only — the cache itself does not transition.** `observe()`'s output is consumed solely by an admin JSON endpoint and a Prometheus gauge (`oxibonsai_kv_cache_compression_level`); nothing reconfigures `oxibonsai-model`'s actual KV-cache storage precision. The `Fp8` tier (ordinal 2, fully documented) is additionally unreachable — `target_level()`'s threshold logic only ever yields Fp16/Q8/Q4, with no `fp8_threshold` field. See README "Known Limitations".
- [x] **`AdaptiveLookahead`** — speculative-decoding draft-length controller; updates `k` from a running EWMA of accepted tokens per step; cooldown to suppress oscillation; clamped to a configured `[min, max]`
- [x] **`RequestRateTracker` + `RequestRateAggregator`** — per-request EWMA tokens/sec, p50/p95 inter-token latency (TBT), queue-wait time; workload rollup aggregator with sliding-window ring buffer
- [x] **`RequestId`** — RFC 4122 UUIDv4-style 128-bit identifiers with thread-safe SplitMix64 generator (no external dependencies); hex / UUID / display formatting; round-trip parse
- [x] **Prometheus surface expansion** — five new gauges: `oxibonsai_request_tokens_per_second`, `oxibonsai_inter_token_latency_p50/p95_seconds`, `oxibonsai_queue_wait_seconds`, `oxibonsai_kv_cache_compression_level`
- [x] **`InferenceMetrics::update_request_rate(...)` and `update_kv_cache_level(...)` helpers** — wire the workload aggregator + KV policy directly into Prometheus output
- [x] **`SpeculativeDecoder::with_adaptive(...)`** — wires `AdaptiveLookahead` into the speculative loop; lookahead updates after every step
- [x] **CI re-enabled** — `.github/workflows/ci.yml` restored from `.disabled`; branches updated to `main` / `master` / `0.1.*`

## Performance Targets

| Platform | Target | Status |
|----------|--------|--------|
| x86-64 AVX2 (Zen4) | >= 20 t/s | 🔶 Kernels done, not benchmarked end-to-end |
| x86-64 AVX-512 | >= 30 t/s | 🔶 Kernels done, not benchmarked end-to-end |
| ARM NEON | >= 18 t/s | 🔶 Kernels done, not benchmarked end-to-end |
| WASM | >= 5 t/s | 🔶 Compiles; runtime throughput not benchmarked |
| ARM NEON (ternary) | >= 14 t/s | 🔶 Apple Silicon measured ~7–8 t/s end-to-end on Ternary-Bonsai-1.7B |
| x86-64 AVX2 (ternary) | >= 18 t/s | 🔶 Kernels done, not benchmarked end-to-end |
| x86-64 AVX-512 (ternary) | >= 26 t/s | 🔶 Kernels done, not benchmarked end-to-end |
| Metal fused TQ2 (Apple Silicon) | >= 30 t/s | ✅ Measured avg=54.5 t/s (best=55.4) on Ternary-Bonsai-1.7B (Phase 13.y, 2026-04-24; was ~50 t/s after Phase 13.x) |
| CUDA native TQ2 (NVIDIA GPU) | >= 20 t/s | ✅ Measured ~21.9 t/s on Ternary-Bonsai-1.7B |

## Memory Targets

| Metric | Target | Status |
|--------|--------|--------|
| Model weights (mmap) | 1.15 GB | ✅ mmap zero-copy implemented |
| KV cache (4k context) | ~256 MB | ✅ Implemented |
| Runtime overhead | < 50 MB | 🔶 Profiler available via `MemoryProfiler`; end-to-end not yet characterized |
| Total (4k context) | < 1.5 GB | 🔶 Not profiled end-to-end |
| Ternary-Bonsai-8B | ~1.75 GB | TQ2_0_g128 (34B/128w) |
| Ternary-Bonsai-4B | ~900 MB | TQ2_0_g128 (34B/128w) |
| Ternary-Bonsai-1.7B | ~390 MB | TQ2_0_g128 (34B/128w) |

## Deferred follow-ups from the 2026-06-01 external blog review

Source: technical review at https://nazquadri.com/blog/everyday-thoughts/oxibonsai-1-bit-pure-rust/

All 5 blog-flagged issues were verified against the code; the in-scope ones are fixed (temperature-discard bug, SIMD docs accuracy, CPU↔Metal parity guard, base-server engine pool — plus token_embd Arc-share + CPU default `min(4, cores)`). The 3 items below were deferred: none were raised by the blog, and each is a new feature or large architectural change whose effort + parity risk is disproportionate to a review fix.

> **Item 3 below is now DONE (2026-07-21, `server-core` audit package)** — see the annotation on it.

### 1. De-singletonize `MetalGraph` for true GPU concurrency — LOW priority / high effort / high risk
- **What**: `GLOBAL_METAL_GRAPH` (`crates/oxibonsai-kernels/src/gpu_backend/metal_graph/graph.rs:23`) is a process-global singleton owning ONE `kv_cache` Mutex + shared scratch buffers, so the engine pool clamps GPU→1. True parallel GPU decode would need N command queues / N KV caches / N scratch-buffer sets.
- **Why deferred**: Core Metal-backend rewrite, not a fix. A single GPU serializes compute anyway → the only win is overlapping CPU-side glue / async submission, not matmul throughput (the CPU pool already covers real N-way deployment). Re-architecting KV/scratch risks the byte-identity determinism just validated. High cost, low marginal benefit on single-GPU hardware.

### 2. AVX-VNNI / NEON-UDOT INT8 dot-product kernel tier — LOW priority / medium effort
- **What**: All 1-bit/ternary kernels currently expand weights to ±scale and accumulate in FP32 FMA. An INT8 dot-product tier (AVX-VNNI on x86, NEON-UDOT on ARM) could speed up CPU decode.
- **Why deferred**: New kernel + dispatch entry + parity validation (INT8 accumulation differs numerically from FP32 FMA → risks the `cos≥0.999` gate), not a fix. The blog only asked for accurate SIMD docs (DONE). x86 VNNI can't be benchmarked on this ARM Mac (NEON-UDOT could). Noted as a possible future enhancement.
- **Best entry if pursued**: the NEON-UDOT path first (testable on this Mac).

### 3. Pool the `rag_server` `RagState` for concurrent RAG serving — ~~LOW priority / low effort~~ **DONE**
- **What**: `crates/oxibonsai-runtime/src/rag_server.rs` has its own separate `RagState` Mutex (distinct from the base `/v1/chat/completions` path that was pooled). Apply the same `EnginePool` pattern.
- **Status (2026-07-21):** implemented as part of the `server-core` Production-Release Audit package (see the top of this file, and the "server-core" Batch-1 entry). `RagState` now holds `engines: Arc<EnginePool>` (`rag_server.rs`), `RagState::new(engines, tokenizer)` takes the pool, `rag_query` does `state.engines.acquire().await` — the same lease pattern as the base chat path, sharing the same GPU→1/CPU→N clamp. `create_rag_router_with_pool` wires a pre-built pool in; `create_rag_router` remains as a single-engine convenience wrapper (1-element pool). This deferred note is kept for history; no further action needed.

## imagen CUDA GPU optimization follow-ups (2026-06-02)

Context: this session optimized the `oxibonsai image` (FLUX.2 Klein) CUDA path from steps=4 ≈101s → ≈31.7s (**3.2×**), cos=1.0 throughout (FP32, **no tensor cores**): TE attention AVX2 + 4-bit dequant parallelised; DiT `gemm_tq2` retiled 32×32/2×2 → 128×128/8×8 transposed-shared (~6× kernel); DiT flash-attention rewritten warp-cooperative (lane-split head_dim + `__shfl_xor_sync`, ~6.3× kernel); DiT stage0 `context_embedder` ported CPU→GPU (`encode_gemm_f32`, ~59× on that op). The two items below are **deferred levers**, each established by a code-read (no benchmark). Both are recorded so a future session need not re-derive them.

### TE persistent GPU weight cache — design-confirmed lever; implement only when multi-image throughput can be benchmarked
- **Static finding (code-read, no bench): the TE (Qwen3-4B 4-bit, ~2.1 GB) re-dequantises (CPU) AND re-uploads (HtoD) every Linear weight on EVERY image — nothing persists across `text_to_image` calls.**
  - `te::cuda_gpu::te_matmul_gpu` (`crates/oxibonsai-image/src/te/cuda_gpu.rs:93`): `get_or_upload_f32_weight(key, weight)` (`:115`) → `encode_gemm_f32` (`:116`) → **`evict_f32_weight(key)` (`:117`)**. The evict drops the device-weight cache entry after every GEMM, so the next call re-uploads. (The evict exists to fix a pointer-key collision — the 4-bit dequant yields transient host buffers whose address is recycled across Linears.) Called per Linear from `matmul` (`crates/oxibonsai-image/src/te/forward.rs:494`, GPU branch `:544`).
  - `TeWeights::get` (`crates/oxibonsai-image/src/te/weights.rs:157`), `Source::Mlx4bit` arm (`:174`): returns `Rc::new(model.load_tensor(name)?)` — a **fresh CPU dequant** (`mlx4bit::dequantize_mlx_4bit_affine`) on every call, explicitly **No-cache** (`:173`). `linear()` (`:182`) wraps `get()`. (The `NpyDir` source *does* cache (`:160`–`:170`); the 4-bit MLX source we actually use does not.)
  - Path is **per-image, not one-time init**: `pipeline::compute_cond` (`crates/oxibonsai-image/src/pipeline.rs:325`) opens `TeWeights::open_mlx_4bit` (`:334`) + `encoder.forward` (`:338`), invoked once per `text_to_image` generation (`:355`, cond arm `:417`); `TeWeights` is constructed fresh each call (no process-lifetime persistence).
- **Conclusion: YES — the lever structurally exists.** A process-lifetime cache of the dequantised-f32 + GPU-resident TE weights would let the **2nd-and-later image's TE skip both the CPU dequant and the HtoD upload (GEMM only)**.
- **Caveat / why not now**: **no gain for a single image** — each weight is used exactly once per forward, so it is uploaded once either way; the win is purely **multi-image throughput** (batch/server), which this single-image CLI cannot benchmark. A real implementation must also (a) restore stable cache keys — cache the dequantised f32 with a stable identity and drop the per-call `evict_f32_weight` — and (b) trade back the no-cache policy's ~16 GB → ~2.5 GB RAM frugality plus GPU residency of the f32 weights. **Implement when a multi-image throughput benchmark environment exists.**

### VAE back-stage residency — net-new, large; revisit only when VAE latency is the priority
- **2026-06-02 evaluation**: after the kernel + stage0 wins, nsys (steps=1) shows the imagen GPU time is now **transfer-bound** (cuMemcpyDtoH ≈5.0 s + HtoD ≈4.0 s ≫ all kernels ≈4.3 s). The VAE decode (~7.8 s) bounces its large 512×512 feature maps (up to ~400 MB) CPU↔GPU **per op**: the per-op `conv2d_gpu` / `groupnorm_gpu` / `silu_gpu` / `upsample_gpu` are all host-in/host-out (`crates/oxibonsai-image/src/vae/cuda_gpu.rs`). The conv itself already keeps im2col+GEMM device-resident (`CudaGraph::encode_conv2d_f32_im2col`), so the per-op cost is the input-HtoD + output-DtoH + host bias-scatter between ops.
- **Right fix = residency, NOT implicit-GEMM** (the conv compute / im2col is small): keep the map device-resident across the whole decode (upload latents + weights once, download RGB once). This is the discrete-GPU inversion of Metal's unified-memory wash — and unlike the DiT residual `h` (small ⇒ residency measured WASH this session), VAE maps are huge ⇒ a real ~4–5 s win.
- **Why deferred = net-new, no template**: both `vae/gpu.rs` (Metal) and `vae/cuda_gpu.rs` (CUDA) are per-op; there is **no resident VAE decode to mirror**. It needs new device kernels (conv-output transpose+bias scatter, residual-add) + device-in/out conv (tiled) / groupnorm / silu / upsample + a resnet/upsample/conv-norm/conv-out orchestration = the session's largest single implementation. ROI is **one-shot** (~12 % of steps=4: 7.8 → ~4 s). **Revisit only when VAE / low-step-count latency becomes the priority.**

### DiT — parity-safe (FP32) floor reached this session; negative results (do not re-derive)
- **DiT residual `h` / q/k/v residency = measured WASH.** The resident tensors are small (≪ the VAE's ~400 MB maps), so the PCIe bounce is cheap; the real stage0 glue was the CPU `context_embedder` GEMM (now on GPU, 59×). Full single-block residency moved steps=4 only **45.6 → 45.4 s** (within noise). **Do not re-attempt DiT residency** — the discrete-GPU residency lever pays off ∝ resident-tensor size, and the DiT's tensors are too small (the opposite of the VAE maps in the subsection above).
- **gemm_tq2 (6×) + flash-attention (6.3×) are at the FP32 ceiling** (~2.3 s kernels/step; DiT sample ~3.3 s/step, the remaining glue ~0.5 s/step is small). Further kernel speedup would require **TF32/FP16 tensor cores — deliberately NOT used** to hold cos≥0.999 parity. So ~3.3 s/step is the parity-safe DiT floor; **don't re-attempt FP32 kernel micro-opt** (mirrors the Metal v10→v11 "local optimum" result).

### Cross-backend note (macOS side)
- The TE per-image **re-dequant** (`crates/oxibonsai-image/src/te/weights.rs:173`, `Source::Mlx4bit` No-cache) is **backend-agnostic** — it re-runs on Metal too. So the *dequant-cache* half of the TE lever above would also help **Metal** multi-image throughput; only the *HtoD GPU-residency* half is CUDA-specific (on Metal the upload is free via unified memory, so Metal's multi-image win from this lever would be dequant-only).

## Stubs to implement (added 2026-06-22 by /cooljapan-stub-check)

- [x] **oxibonsai** `oxibonsai-model`: `crates/oxibonsai-model/src/model/types/forward_cuda/q1.rs:329` — `TODO`: `re-enable once the prefill→decode KV handoff is fixed and a CPU↔CUDA parity gate covers it` (ternary CUDA batch prefill returns `Err` to force the sequential fallback; corrupts prompts >16 tokens) — **DONE 2026-07-20**: the prefill-private GPU KV cache was unified with the per-token decode cache (`cuda_prefill/state.rs` delegates to `cuda_full_layer::acquire_kv_cache`); the `Err` guard for `OutputWeight::Ternary` was dropped; a synthetic-model CPU↔CUDA parity test (Q1 and ternary) confirms decode logit Δ collapsed from ≈7.3 to ≈0.001 at a 17-token+ prompt, with identical greedy sequences. **Note:** the same audit found the identical split-cache bug in the FP8/Q-std/K-quant CUDA batch-prefill paths; Q4_0/Q8_0/K-quant were disabled-by-default as a mitigation (see the Production-Release Audit section above), and **FP8 CUDA batch prefill was likewise disabled-by-default in a same-day residual-fix pass** — see the resolved item above ("Newly discovered during this audit round").
  - **Priority:** P2  **Scope:** large  **Cross-project:** oxicuda
  - **Approach:** Unify the prefill-private GPU KV cache (`prefill_state().kv_cache`) with the per-token decode cache (`FULL_LAYER_STATE`) so batch-prefilled KV is what decode attends over; then drop the `Err` guard for `OutputWeight::Ternary` and gate re-enablement on the `cuda_ternary_forward_parity.rs` CPU↔CUDA parity test (decode logit Δ must collapse from ≈7.3 to ≈sequential ≈0.002 at a 17-token prompt).
  - **Risk:** Touches the CUDA KV-cache lifetime shared with oxicuda kernels; a wrong handoff silently corrupts long-prompt decode (the exact bug this guard prevents). Must not regress the already-correct Q1/Q4_0/Q8_0/FP8 batch paths. Requires CUDA hardware to validate — keep the sequential fallback until the parity gate is green.

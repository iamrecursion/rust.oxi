[package]
name = "tenflowers-dataset"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Data pipeline and dataset utilities for TenfloweRS"
documentation = "https://docs.rs/tenflowers-dataset"
readme = "README.md"

[lints]
workspace = true

[dependencies]
tenflowers-core = { workspace = true }
scirs2-core = { workspace = true, features = ["random"] }
thiserror = { workspace = true }
rayon = { workspace = true, optional = true }
serde = { workspace = true, optional = true }
serde_json = { workspace = true, optional = true }
oxicode = { workspace = true, optional = true }
image = { workspace = true, optional = true }
memmap2 = { workspace = true, optional = true }
arrow = { workspace = true, optional = true }
parquet = { workspace = true, optional = true }
regex = { workspace = true, optional = true }
csv = { workspace = true, optional = true }
bytemuck = { workspace = true }
oxiarc-archive = { workspace = true, optional = true }
oxiarc-lz4 = { workspace = true, optional = true }
oxiarc-deflate = { workspace = true, optional = true }
oxiarc-snappy = { workspace = true, optional = true }
crc32fast = { workspace = true, optional = true }
sha2 = { workspace = true }
rmp-serde = { workspace = true, optional = true }
hdf5 = { workspace = true, optional = true }
symphonia = { workspace = true, optional = true }
rubato = { workspace = true, optional = true }
reqwest = { workspace = true, optional = true }
wgpu = { workspace = true, optional = true }
futures = { workspace = true, optional = true }
pollster = { workspace = true, optional = true }
tokio = { workspace = true, optional = true }
libc = { workspace = true, optional = true }
uuid = { workspace = true }
num_cpus = { workspace = true }
toml = { workspace = true }
serde_yaml = { workspace = true, optional = true }

# Additional dependencies for distributed loading
async-trait = { workspace = true, optional = true }

# For PandRS integration (when available)
# pandrs = "0.3.1"

[dev-dependencies]
criterion = { workspace = true }
tempfile = { workspace = true }

[[bench]]
name = "throughput"
harness = false

[features]
default = ["parallel", "images", "serialize", "mmap", "parquet", "csv_format", "regex", "tfrecord", "msgpack", "audio", "download", "webdataset", "numa"]
# HDF5 support available as optional feature (requires native HDF5 C library)
# Enable with: cargo build --features hdf5
# Alternative: Use Zarr or Parquet formats (Pure Rust)
parallel = ["dep:rayon"]
serialize = ["dep:serde", "dep:serde_json", "dep:oxicode", "dep:serde_yaml"]
images = ["dep:image"]
mmap = ["dep:memmap2"]
parquet = ["dep:arrow", "dep:parquet"]
csv_format = ["dep:csv"]
regex = ["dep:regex"]
tfrecord = ["dep:oxiarc-archive", "dep:crc32fast"]
msgpack = ["dep:rmp-serde", "serialize"]
hdf5 = ["dep:hdf5"]
audio = ["dep:symphonia", "dep:rubato"]
download = ["dep:reqwest", "dep:oxiarc-archive", "serialize"]
webdataset = ["dep:oxiarc-archive", "serialize"]
gpu = ["tenflowers-core/gpu", "dep:wgpu", "dep:futures", "dep:pollster", "dep:tokio"]
numa = ["dep:libc"]
distributed = ["dep:tokio", "dep:futures", "dep:async-trait", "serialize"]
cloud = ["dep:reqwest", "dep:tokio", "dep:futures", "serialize"]
compression = ["dep:oxiarc-archive", "dep:oxiarc-lz4", "dep:oxiarc-deflate", "dep:oxiarc-snappy"]

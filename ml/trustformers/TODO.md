# TrustformeRS TODO

## Status as of 2026-08-26 (Wave 6d closed; release-prep pass for 0.2.1)

- **Version**: 0.2.1 (branch `0.2.1`). Workspace has 10 members — `trustformers-core`, `trustformers-tokenizers`, `trustformers-models`, `trustformers-optim`, `trustformers-training`, `trustformers` (umbrella), `trustformers-wasm`, `trustformers-mobile`, `trustformers-debug`, `trustformers-serve`. `trustformers-py`, `trustformers-c`, and `trustformers-serve/clients/rust` are deliberately excluded from the Cargo workspace (each has its own standalone manifest); `trustformers-js` is also excluded (WASM/JS bindings, not a Cargo package).
- **Size** (`tokei` 12.1.2, 2026-08-25, repo root, `target`/`node_modules`/`.claude` excluded): 3,117 Rust files, 1,568,946 total lines, 1,310,407 lines of code. (2026-08-24's count was 3,046 / 1,527,609 / 1,275,615 — continued growth from a full day of Wave 5/6/6b/6c work across the workspace.)
- **`cargo deny check advisories` / `bans` / `licenses`**: all three **PASS**, re-run today directly from the repo root (`advisories ok` / `bans ok` / `licenses ok`, exit 0 for each). This resolves what the previous version of this file carried as P0 #1 (7 advisory findings): during Wave 5 (`advisories` package), the `opentelemetry`/`opentelemetry-jaeger` stack, `lambda-web`, and the dev-only `paste` dependency were removed outright; every `aws-sdk-*` dependency in `trustformers-serve/Cargo.toml` now builds with `default-features = false`; `jsonwebtoken` moved to 11.0.0. One advisory remains a deliberate, dated ignore in `deny.toml`: RUSTSEC-2023-0071 (RSA "Marvin Attack" timing side-channel), reached transitively through `jsonwebtoken`, with no upstream fix available at pin time.
- **File-size policy (files >2000 lines)**: **zero**, verified today with a full scan of the real source trees (`target/`, `node_modules/`, and the stale pre-split file copies under `.claude/worktrees/` excluded — the latter would otherwise pollute a naive repo-wide `find`). The three files the previous version of this file listed as the last offenders are all resolved: `trustformers/src/hub_ui.rs` and `trustformers/src/hub.rs` are now directory modules (`hub_ui/`, `hub/`); `trustformers/src/pipeline/conversational/summarization.rs` is now the directory module `trustformers/src/pipeline/conversational/summarization/` (`engine.rs` 1,347 lines + `types.rs`/`factory.rs`/`tests.rs`/`mod.rs`) — corrected this pass: the previous version of this line named `trustformers/src/pipeline/summarization.rs` as the destination, but that is a distinct, pre-existing, unrelated top-level `pipeline::summarization` module (mounted separately via `pipeline/mod.rs`, dating to the 0.1.1 era) — not the conversational one's split target. A fourth file flagged mid-Wave-6d (`trustformers-mobile/src/network_optimization.rs`, reported at 2,013 lines) is also already split, confirmed today: `network_optimization.rs` (1,186 lines) + `network_optimization_types.rs` (574) + `network_optimization_tests.rs` (402). The largest file in the workspace today is `trustformers-wasm/src/angular_components.rs` at 1,986 lines.
- **Build**: workspace-wide build is confirmed GREEN as of this pass (2026-08-26) — a full `cargo nextest run --workspace --all-features` cannot pass without every member compiling, and it passed cleanly (see Tests below), which supersedes the previous "last confirmed at Wave 4 close" caveat now that Wave 6d has closed. `trustformers-py`'s own `cargo check --all-targets` and `cargo clippy --all-targets -- -D warnings` were both re-run today from its own directory and are GREEN (exit 0); building it also compiles `trustformers-core`, `trustformers-models`, `trustformers-tokenizers`, `trustformers-optim`, and `trustformers-training` as path dependencies.
- **Tests — Wave 6d closed, full re-run (2026-08-26)**: `cargo nextest run --workspace` (default features) — **21,370 passed / 0 failed / 41 skipped** (skip count matches the Wave 6d entry baseline's 41 exactly). `--all-features` — **25,883 passed (1 leaky) / 0 failed / 113 skipped**. `cargo clippy --workspace --all-features --all-targets -- -D warnings`, `cargo test --doc --workspace --all-features`, and `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps` are all clean (0 warnings/errors/failures). This closes out the Wave 6d entry baseline below (21,277 run / 6 failed) — all three P0 items are confirmed resolved: `test_least_loaded_strategy` and `test_streaming_monitoring_interaction` (`trustformers-serve`) and `trustformers-wasm`'s `multi_model_manager`/`performance_profiler` suites (P0 #3) each individually verified PASS by name in this run's own log, with no native abort. This pass did not audit which specific commit(s) fixed each of the three, or re-derive a fresh per-crate breakdown (the Wave 6d entry baseline's own per-crate numbers, superseded by the totals above, are not repeated here — see git history for that snapshot if needed) — only the full workspace-wide totals above were verified this pass.
- **`trustformers-py`** (own standalone workspace, in-dir): `cargo test --lib --no-default-features --features python-gc` — **163 passed / 0 failed**, re-run today (152 pre-existing + 11 new, added this pass for `PyConfig::from_file`'s real JSON parsing — see P1 below). Supersedes the 97/128 figures earlier waves recorded under different harness invocations of the same suite.
- **`trustformers-tokenizers`**'s `workspace_hygiene` suite: **9/9 passed**, re-run today (`cargo nextest run -p trustformers-tokenizers --test workspace_hygiene`).

## How to read this file

This is this file's fourth generation. The version before 2026-08-18 had accumulated an 18-item stale-claim list and was rewritten from scratch against the tree as it stood then. The 2026-08-18 version in turn described a "Wave 4" whose packages had mostly self-reported FAILED. The 2026-08-24 version was a full rewrite after Wave 4 actually closed (nine packages, closing a large fraction of what 2026-08-18 still listed as open), with every claim re-checked by reading the code rather than trusting self-reports.

This 2026-08-25 revision folds in everything Waves 5 through 6c landed and closed — each independently re-verified by that wave's own adversarial verify lenses, not merely self-reported, the same discipline the 2026-08-24 version established. It also recorded Wave 6d's mission list (three failing-test regressions plus a P1/P2 backlog) **at the state it stood when Wave 6d's fix packages began**, since those packages were still active while that revision was being written.

**2026-08-26 addendum** (0.2.1 release-prep pass): the re-measurement this file's 2026-08-25 revision called for has now happened — a full `cargo nextest run --workspace` (default and `--all-features`) confirms all three Wave 6d P0 items resolved (see Status and the P0 section below). This addendum only re-ran the workspace-wide test/clippy/doctest/rustdoc gates; it did not re-audit the P1/P2 backlog items below (fabricated-value/marker sweeps, per-crate `TODO.md` detail), so those retain whatever staleness they already carried into this revision.

## P0 — resolved this pass (Wave 6d entry, 2026-08-25; closed 2026-08-26)

All three items below were open, currently-failing gates as of the Wave 6d entry snapshot (2026-08-25). A full `cargo nextest run --workspace` (both default and `--all-features`) on 2026-08-26 shows each passing individually by name, with zero failures workspace-wide — see the Tests bullet under Status above. This pass verified the *outcome* (green test, no native abort) for each, not the specific code change that fixed it; check git history around 2026-08-25/26 for the actual fix commit(s) if that detail is needed.

1. **RESOLVED** — `trustformers-serve`: `functions::tests::test_least_loaded_strategy` now passes. (Was: `select_least_loaded` mapped an unmonitored device's score to `+INFINITY`, and when *every* candidate device was unmonitored, every score tied at `+INFINITY` with an arbitrary `partial_cmp` tie-break instead of a predictable fallback.) (`server/functions.rs`)
2. **RESOLVED** — `trustformers-serve`: `integration_tests::test_streaming_monitoring_interaction` now passes within budget. (Was: `/admin/stats` awaited `measure_host_async()`, which blocked the async executor via `std::thread::sleep` during `sysinfo`'s two-sample CPU read, causing a 5s timeout.)
3. **RESOLVED** — `trustformers-wasm`'s `multi_model_manager` (3 tests) and `performance_profiler` (1 test) no longer abort natively; all four pass under a plain workspace-wide `cargo nextest run --all-features`. (Was: a `wasm-bindgen`-generated "not implemented on non-wasm32 targets" panic occurred inside a `nounwind` context, aborting the whole test process via SIGABRT instead of failing just that test.)

## P1 — verified open work

### trustformers-py
- **`src/utils.rs`'s `PyConfig::from_file` now really parses JSON** (this pass, 2026-08-25), replacing a stub that recorded only the file's path (`config.set("loaded_from", path)`) and nothing else, whatever the file contained. It reads the file, parses it as a JSON object, and populates one config entry per top-level key (string values verbatim; every other JSON value as its own JSON-serialized text, since the config store only ever holds strings); a missing file, an unreadable file, invalid JSON syntax, and a non-object top level each raise the crate's own (previously-unused) `ConfigError` naming the exact problem. 11 new tests, `std::env::temp_dir()` fixtures (valid/invalid/missing). There is deliberately no YAML path: the crate has no YAML dependency (`Cargo.toml` carries neither `serde_yaml` nor `serde_yaml_ng`), so adding YAML support was out of this change's scope.
- Real inference now backs all four registered pipelines — text-generation, text-classification, and (since Wave 5's `py-followups`, superseding the previous version of this file) token-classification and question-answering too. The `span_pipeline_unavailable` refusal these last two used to return is gone. Byte-vs-character offset semantics were also corrected crate-wide this stretch: this crate's tokenizers produce **byte** offsets, not "character offsets" as the docs previously (incorrectly) claimed; `Entity.start`/`end`, `QaAnswer.start`/`end`, and the module docs now say so, with a non-ASCII regression test locking the byte semantics.
- **Stale claim removed**: the previous version of this file said `Cargo.toml:77` still depends on the `trustformers` umbrella crate. It does not — the manifest explicitly does *not* list it (with a comment recording that removing it dropped 54 transitive packages and added none, verified by a before/after `cargo tree -e features` diff). Do not re-add it to "get" a feature; enable that feature explicitly on the direct dependency instead.
- `pyo3` is still pinned to `0.28` (`scirs2-numpy`/`scirs2-core` pinned to `0.5.1`), unchanged — the `0.28`→`0.29` migration remains explicitly deferred.
- **`PyTrainer::train()` now honestly refuses** instead of the previous version of this file's fabrication (`train_loss: 0.5`, `total_steps: 1000` unconditionally, for every model/dataset/configuration). The refusal names the real reason: forward passes and loss functions are real, but no backward/gradient path from a loss back to a model's own parameters exists anywhere in `trustformers-core`/`trustformers-models`/`trustformers-training` (`Variable`'s `ComputationGraph` is never constructed by any model's `forward()`, and `trustformers_training::trainer::ParameterAccess` has zero implementors workspace-wide) — so `train()` refuses rather than silently skipping backprop and reporting a loss that never changes.
- Still open, per Wave 5's own tracking (not re-verified this pass): a `batch_encode_plus` text-pair form for the QA pipeline as a batched list-of-dicts; crate-wide `rustfmt` (~112 pre-existing sites); a `maturin`-built, live-import verification of `__init__.py`.

### trustformers-serve
The Wave-4-era backlog this section used to itemize in detail (14+14+31+~17 marker sites across `performance_optimizer/**`, ~30 files under a blanket `#![allow(dead_code)]`) is resolved: Wave 5 verified **zero** remaining blanket `#![allow(dead_code)]` in the crate, with the previously-hidden dead code underneath either wired to something real or deleted rather than re-allowed, plus a real conflict/pattern-detection algorithm, deleted shadow-types (one — `ConcurrencyRequirementsDetector` — was intercepting the live code path entirely), a real `ChannelHealthMonitor`, a real KS-test, and a real ARIMA fit. Wave 6's `serve-stats2`/`serve-resource2` packages then landed: `custom_metrics.rs`'s CPU/memory/GPU usage now read real `sysinfo` data instead of publishing `Ok(0.5/0.7/0.8)` to Prometheus every 10s by default; the concurrency detector now computes real order statistics; `resource_management/statistics.rs`'s `PerformancePredictor` now does real OLS-based prediction instead of returning `75.0`/`(70,80)`/`0.85` regardless of input, with `AnomalyDetector`/`BottleneckAnalyzer` no longer unconditional `Ok(vec![])`; `gpu_manager`'s `select_hybrid` LeastLoaded branch had its own copy of the "0.0 utilization always wins" bug fixed (separately from P0 #1 above, which is a newer, different edge case in the same area — all-monitored-devices tie-breaking, not the zero-wins bug); `health_monitor` no longer fabricates a fully-healthy status record at a device's birth.

**Still open** (Wave 6's own C1/C2 follow-up list; not independently re-verified this pass beyond what's credited above, so treat this as "named, not yet confirmed resolved" rather than "confirmed still broken"): `predictive_analytics.rs`'s `predict()` bodies still emit invented latency/confidence/interval values in places even where the underlying fit metrics are now real; a second, independent Jaeger/Zipkin/OTLP exporter in `distributed_tracing/types.rs` (POSTing via `reqwest`) carries the same defect class Wave 5's `advisories` package fixed in the first exporter, and has never been audited; `optimization_history/statistics.rs` was flagged as having a second fake Shapiro-Wilk implementation, and `holdoutvalidation_traits.rs`/`adaptive_learning.rs`/`model_implementations.rs` were flagged with constant p-values/gradients/confidences — none of these three files were named in any later wave's landed-work summary, so their status needs a fresh check rather than being assumed either way. See `trustformers-serve/TODO.md` for the crate's own authoritative, file-line-level list (not rewritten this pass).

### trustformers-core
- The `MetalBufferHandle` RAII migration is now **complete** in both crates it spans. The previous version of this file (and, until this pass, `trustformers-core/TODO.md`) said `trustformers-models`'s `gpt2/model/{model_blocks,model_core,model_ops}.rs` had not been converted and that `cargo check -p trustformers-models --features metal,gpt2` failed with 15 compiler errors as a result. That migration finished during Wave 5 (`models-hard`); Wave 6c's own verification gate compiled that exact feature combination with zero `error[E...]` hits and ran 1,897 tests (1,895 passed, the 2 environmental flakes noted under Status). See `trustformers-core/TODO.md`, corrected in this same pass.
- **Resolved, superseding the previous version of this file's "small doc-polish item" note**: `kernels/xla_impl.rs` and `kernels/oneapi_impl.rs`'s module-level doc comments now state up front that no real XLA/oneAPI runtime is linked (verified today by reading both files' top-of-file doc comments directly) — they no longer read like a real integration at a glance the way the previous version of this file described. `kernels/riscv_impl.rs`'s module doc was already honest about this. All three features remain `= []` (zero dependencies).
- A large amount of additional Metal-path correctness work landed across Waves 5–6c — see "Recently resolved" below. One item from the previous version of this file was **not** independently re-checked this pass and is carried forward unchanged: the CPU-input/GPU-weight `LayerNorm` fallback allegedly still uploads an `input_buffer_id` as a `Pinned` buffer and never releases it (`core-polish`'s own account, originally located around `trustformers-core/src/gpu_ops/metal/*.rs:487-492`). The file that region of code lived in has been heavily restructured since (the current largest candidate, `metalbackend_initialize_mps_group.rs`, is now 1,941 lines), so the original line numbers no longer resolve to the same content — re-locate before fixing.
- `kernels/riscv_impl.rs`/`xla_impl.rs`/`oneapi_impl.rs`: the `riscv`/`xla`/`oneapi` Cargo features remain `= []` (zero dependencies) — unchanged, this is a scope decision (real scalar-CPU simulation for RISC-V; structured "no runtime linked" errors for XLA/oneAPI), not an open defect.

### Wave 6d in-flight P1/P2 backlog
Assigned to active Wave 6d packages; listed here at entry-state (2026-08-25) rather than audited by this pass, per this file's own "How to read this file" caution about documenting code other agents are mid-editing. See each crate's own `TODO.md` for full file:line detail once these packages close: `trustformers-debug` (a CPU-utilization reader that can only ever report `Some(0.0)`; a `clear_alert` that deletes every alert instead of the one named, from an irrefutable-binding bug worth a repo-wide grep for the same pattern; batch-metrics accumulation that is a live no-op; a stale module-header doc describing already-deleted functions; a P2 sweep covering Welch's-t unit consistency, a statistical p-value underflow at moderate degrees of freedom, and several more marker sites), `trustformers-optim` (an optimizer memory-delta estimate that is shape-only and therefore always exactly zero; a `TrendAnalyzer::predict` that fabricates `0.75` below 10 samples and ignores its own `horizon` argument), `trustformers-wasm` (a model-structure analyzer that invents a transformer layout without reading the model's bytes), `trustformers-tokenizers` (JAX data-iterator `map`/`filter` are no-op self-returns while their sibling `shuffle`/`repeat` are real), `trustformers-models` (the default `Random` sparse cross-attention pattern is a fixed diagonal stripe, not seeded randomness), `trustformers-mobile` (a thermal-sentinel value-range bug), and a cross-crate repo grep for the literal comment `// Simplified` to triage remaining survivors. A read-only check during this pass also found `trustformers-core/src/compiler/{graph_optimizer,analysis,jit_compiler,kernel_fusion}.rs` — separately flagged in Wave 6d's mission list as carrying flat-constant "estimates" — already computing real graph-derived heuristics rather than the named constants; this was not audited end-to-end and the package owning that code may still be mid-edit, so it is not claimed resolved here.

## P2 — smaller open items

- `trustformers-optim`: no Shampoo optimizer exists (unchanged). The rest of this section's previous claims (`genie_stub`/`sofo_stub`/`lora_rite_stub`, `StatefulOptimizer` save/load, `AdamW4bit`) were already corrected in the 2026-08-24 version of this file as either resolved or never true — not re-checked again this pass.
- `trustformers-training`: still no `benches/` directory (not re-checked this pass).
- `trustformers-core/benches/tensor_operations.rs`: as of the last check, three registered groups measured only `tensor.clone()`, with two (`complex_placeholder`, `quantization_placeholder`) explicitly labelled placeholders — not re-checked this pass; nothing benchmarks attention, KV-cache, the decode loop, or serve-side batching.
- `trustformers-serve/Cargo.toml`: still carries more literal version pins than any other workspace member — not audited pin-by-pin.

## Recently resolved — verified through Wave 6c (2026-08-25)

This section only lists items independently confirmed by reading the code (this pass) or credited to a wave whose own adversarial verify lenses confirmed it (Waves 5 through 6c) — not self-reports. Entries the previous (2026-08-24) version of this file already carried are not repeated here; see that history via git if needed.

- **`trustformers-models`**: the bert/xlstm "all-zero output whenever the `metal` feature is merely *compiled in*, even for CPU-only instances" regression (discovered right after Wave 4) was root-caused to `MetalBackend::layernorm_f32` reading `output_buffer.contents()` immediately after `commit_async` — a read-after-async-commit race against freshly-zeroed GPU memory — and fixed. DeepSeek-V2 gained real MLA (`q_a`/`kv_a` layernorms, real causal attention — previously K/V were discarded) and real per-token top-k MoE routing (previously routed every token in a sequence using only token 0's logits). S4 gained real discretization (all four methods, including ZOH via an augmented matrix exponential) and a real per-channel recurrence (the previous `S4Block` was an identity function). WordPiece and BPE tokenization gained real end-to-end offset mapping (a 725-line alignment engine, 31 new tests) — previously `None` on these two paths (24 other tokenizer families still return `None`; see "Still open" below). The GPU-resident GPT-2 forward pass, which diverged 69-81% from the CPU reference with identical weights, is now within 5e-7 relative error after the LayerNorm/device-transfer fixes below; a separate decode-time bug (the positional offset was read from the KV-cache's head-count axis instead of its sequence-length axis, causing repeated-token degeneration on every decode step after the first) is fixed; five near-duplicate last-token-logits decoders were deduplicated into one path with a working Metal arm; 7 leaked GPU scratch buffers per attention forward are now released; a causal-masked chunked-generation Metal kernel was added (the existing kernel was silently non-causal for partial-cache continuation, proven by a falsifying probe matching the non-causal reference to 1e-6) and gpt2's admission control relaxed to use it once it was correct.
- **`trustformers-core`**: the RoPE and causal-softmax Metal kernels had the same read-after-`commit_async` race as the LayerNorm bug above (silent all-zero output) — fixed, with regression tests that revert the fix to confirm it actually reproduces the old failure. `LayerNorm`'s variance estimator was inconsistent across code paths (the SIMD path, used for ≥64 elements, computed the unbiased `n-1` variance; the CPU fallback and Metal kernels used the biased `n` convention every ML framework expects) — unified to the biased convention everywhere, with the SIMD kernel now both correct and 1.4-2.9x faster than before. `Tensor::to_device_enum`'s Metal-to-CPU path read GPU buffer contents with no flush, null check, or bounds check — fixed. A real `trustformers_core::statistics` module now backs Student-t p-value computation that previously used an approximation which silently underflowed to `0.0` at moderate degrees of freedom (verified worst-case relative error 3e-12 against the new implementation).
- **`trustformers-debug`**: real Welch's-t regression detection and model comparison; real liveness-based peak-memory, critical-path, and clustering-coefficient graph analysis; real backtrace flame graphs; real FGSM/PGD adversarial-example generation; real PCA; error-recovery's success rate now starts honest instead of being pre-seeded at `1.0` before any evidence exists. A marker census across the crate went from 211 hits (65 files) to 141, with every remaining hit individually triaged as either a legitimate audit-trail comment describing an already-fixed prior fabrication, or a genuine domain constant.
- **`trustformers-mobile`**: real Android `/sys/class/thermal` zone scanning, real desktop `sysinfo::Components` temperatures, and real battery reads replace three sets of platform-specific invented constants — `ThermalState::Unknown` is wired through for genuinely unavailable data instead of a fabricated number. Download constraints (Wi-Fi-only, charging-only) now actually fail closed instead of being unenforceable placeholders. Model-size estimates now come from observed shapes instead of hardcoded per-model-family numbers.
- **`trustformers-optim` / `trustformers-training`**: `WorkerProvisioner`/`NodeProvider` traits with a structured `NoProvisioner` error replace an auto-scaler that previously mutated a worker fleet that did not exist. Secure aggregation now generates real pairwise-cancelling (Bonawitz-style) masks sized to the caller's actual tensor shapes, replacing hardcoded shapes (`[100,50]`/`[50]`/...) that did not in fact cancel out despite the code's own comment claiming they did. Temporal-coverage utilization is now a real calculation.
- **`trustformers-wasm`**: `performance_profiler.rs`'s fabricated CPU/GPU/battery/cache-hit/thermal telemetry (`Date::now() % 100`-style values) is deleted in favor of a real warmup forward pass, with regression tests — those tests were the ones hitting P0 #3 above (native SIGABRT, not a wasm32-only problem), now resolved (see P0 section).
- **`trustformers-py`**: was fully broken at Wave 4's start (`py-pipelines`'s first task was making it compile at all). See the P1 section above for what's landed since — real inference across all four pipelines, corrected byte-offset docs, an honestly-refusing `Trainer.train()`, and (this pass) `PyConfig::from_file`'s real JSON parsing.
- **Cross-cutting**: `cargo deny check advisories/bans/licenses` all green (Wave 5, re-confirmed today); zero files over the 2000-line policy limit workspace-wide (re-confirmed today); `examples/server/Cargo.toml`'s latent "workspace-inherited keys in an excluded package" defect (the same class `examples/grpc-server/Cargo.toml` had before its Wave-4 fix) is resolved, verified today by reading the manifest directly: it now has its own `[workspace]` table plus literal `version = "0.2.1"`/`edition = "2021"` instead of inheriting either; the crate builds and tests standalone (29 tests as of the last count).

## Still open — deferred, not scheduled this wave

- **24 tokenizer families** (everything except WordPiece and BPE) still return `offset_mapping: None` on their live encode paths; only WordPiece and BPE got real offset tracking this stretch. See `trustformers-tokenizers/TODO.md`.
- **`trustformers-py`'s ~18k-line legacy pure-Python subsystem** (`pipelines.py` plus ~35 other files) remains unaudited and is deliberately left unimported by the real, compiled bindings — a future wave/decision, not a defect in the live code path.
- **`trustformers-wasm`'s CoreML export** (`export/coreml.rs`) emits a JSON payload with a `.mlmodel` extension rather than a real protobuf-encoded CoreML model file.
- **`trustformers-mobile`'s lifecycle cluster** (already tracked in `trustformers-mobile/TODO.md` around its own lines 490-492) — not duplicated here.
- **`trustformers-debug` marker follow-ups** — see the Wave 6d in-flight backlog under P1 above.
- **`trustformers-serve` honest-but-inert sinks** — see the "Still open" list under the `trustformers-serve` P1 section above (`predictive_analytics.rs`, the second tracing exporter, and the three unconfirmed statistics files).

## Roadmap / not scheduled

Items below name no specific file, symbol, or acceptance criterion — they are directions, not actionable work items, and are kept separate so the P0-P2 lists above stay honest about what's actually actionable. Several per-crate `TODO.md` files carry their own longer version of this list.

- More efficient architectures / "latest research architectures as they emerge" (no specific architecture or efficiency axis named)
- Enhanced memory pooling; better mobile quantization strategies (no target metric named)
- Interactive model architecture explorer
- Broader hardware backend coverage — moving ROCm/Vulkan/OpenCL from experimental/feature-gated to verified-on-real-hardware, and eventually giving RISC-V RVV / XLA / oneAPI real implementations instead of the empty feature flags tracked above
- `trustformers-tokenizers/docs/migration/`: five of six migration guides (huggingface, sentencepiece, nltk, spacy, fairseq — tiktoken was rewritten already) plus the migration `README.md` carry fabricated method/type names describing features this crate does not have; each now carries a dated accuracy banner rather than a full rewrite, and a full pass through each is not scheduled.

## Per-crate detail

See each crate's own `TODO.md` for crate-specific open items: [trustformers-core](trustformers-core/TODO.md) · [trustformers-tokenizers](trustformers-tokenizers/TODO.md) · [trustformers-models](trustformers-models/TODO.md) · [trustformers-optim](trustformers-optim/TODO.md) · [trustformers-training](trustformers-training/TODO.md) · [trustformers](trustformers/TODO.md) (umbrella) · [trustformers-wasm](trustformers-wasm/TODO.md) · [trustformers-mobile](trustformers-mobile/TODO.md) · [trustformers-debug](trustformers-debug/TODO.md) · [trustformers-serve](trustformers-serve/TODO.md) · [trustformers-py](trustformers-py/TODO.md) · [trustformers-js](trustformers-js/TODO.md) (WASM/JS bindings; excluded, not a Cargo package). See also [TODO-CUDA.md](TODO-CUDA.md) for CUDA-specific tracking (cross-referenced from `trustformers-core/TODO.md`; keep the CUDA-6 item in exactly one of the two files, not both).

`trustformers-c` is **deprecated as of the 0.2.0 release** — zero maintenance effort, does not need to compile, and its own `TODO.md` states no new items will be tracked there going forward. Do not carry its open items into this file.

## Build commands

```bash
# Full workspace check (default features)
cargo check --workspace --all-targets

# Run tests for a specific crate (safer than a full workspace run while
# other agents may be mid-edit in a multi-agent session)
cargo nextest run -p <crate-name>

# Format / lint
cargo fmt --all
cargo clippy --workspace --all-targets -- -D warnings

# cargo-deny gates (see Status above for current pass/fail)
cargo deny check bans
cargo deny check licenses
cargo deny check advisories
```

## Development guidelines

- **Dependencies**: prefer `workspace = true` for every member dependency; `trustformers-serve` currently carries the largest count of literal version pins of any member (not audited pin-by-pin — see P2). New dependencies must be pure Rust in default features; FFI dependencies (e.g. `mecab`, used optionally for Japanese/Korean tokenization) are feature-gated and never in `default`.
- **Code quality**: no `.unwrap()`/`.expect()` outside tests; snake_case naming; files under 2000 lines (see Status above for the current, currently-empty, exceptions list); zero-warning `clippy -D warnings` in the area you touch.
- **Testing**: every behavioral fix needs a regression test that would fail against the old code (well-established across the codebase — see "Recently resolved" above for examples). Tests use `std::env::temp_dir()`, never a hardcoded path. No network access in tests; local mock servers are fine.
- **Banned crates**: openblas→oxiblas, bincode→oxicode, rustfft→oxifft, rusqlite→oxisql-sqlite-compat, zip/flate2/zstd/bzip2/lz4/tar/snap/brotli/miniz_oxide→oxiarc-\*, most `-sys` FFI crates→the matching cool-japan/oxi\* crate. Enforced by `deny.toml`'s `[bans]` table; run `cargo deny check bans` before finishing any dependency change.

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@drawable/suggestion_item_background"
    android:padding="8dp"
    android:layout_marginEnd="4dp"
    android:gravity="center_vertical">

    <!-- Type Icon -->
    <ImageView
        android:id="@+id/suggestion_type_icon"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginEnd="4dp"
        android:src="@drawable/ic_keyword"
        android:contentDescription="Suggestion type" />

    <!-- Suggestion Text -->
    <TextView
        android:id="@+id/suggestion_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/suggestion_text_color"
        android:textSize="14sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="suggestion" />

    <!-- Confidence Bar -->
    <ProgressBar
        android:id="@+id/confidence_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="20dp"
        android:layout_height="4dp"
        android:layout_marginStart="8dp"
        android:max="100"
        android:progress="75"
        android:progressTint="@color/confidence_bar_color" />

</LinearLayout>
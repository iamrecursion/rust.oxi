//! Test suite for `corpus_curation`, one file per pillar (plus shared
//! fixtures and cross-cutting engine wiring), matching the module's own
//! four-pillar structure.

mod ablation;
mod classifier;
mod contamination;
mod engine_wiring;
mod fixtures;
mod heuristics;
mod near_dup;

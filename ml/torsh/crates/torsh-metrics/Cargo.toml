[package]
name = "torsh-metrics"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
description = "Comprehensive evaluation metrics for ToRSh - powered by SciRS2"
readme = "README.md"
documentation = "https://docs.rs/torsh-metrics"
keywords = ["deep-learning", "metrics", "evaluation", "machine-learning", "accuracy"]
categories = ["science", "mathematics"]

[lints]
workspace = true

[dependencies]
torsh-core = { workspace = true }
torsh-tensor = { workspace = true }

# SciRS2 metrics capabilities
scirs2-metrics = { workspace = true }
scirs2-core = { workspace = true }

# Additional dependencies
thiserror = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[dev-dependencies]
approx = { workspace = true }
scirs2-autograd = { workspace = true }  # For array! macro in tests
criterion = { workspace = true }

[features]
default = []
# GPU metrics compute is CPU-backed today; no scirs2 GPU dependency after the
# oxicuda migration. The flag still gates the (CPU) compute_gpu code path.
gpu = []
parallel = ["scirs2-core/parallel"]
full = ["gpu", "parallel"]
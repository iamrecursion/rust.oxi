[package]
name = "torsh-models"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
description = "Pre-trained models and model zoo for ToRSh deep learning framework"
readme = "README.md"
documentation = "https://docs.rs/torsh-models"
keywords = ["tensor", "deep-learning", "models", "pretrained", "machine-learning"]
categories = ["science", "mathematics"]

[lints]
workspace = true

[features]
default = ["vision", "nlp", "audio", "multimodal"]
vision = []
nlp = []
audio = []
multimodal = []
gnn = []
vision_3d = []
video = []
generative = []
rl = []
domain = []
download = ["reqwest", "tokio", "rustls", "webpki-roots", "oxitls-rustcrypto-provider"]
diffusion_extended = []

[dependencies]
# Core dependencies
torsh-core = { workspace = true }
torsh-tensor = { workspace = true }
torsh-nn = { workspace = true }
torsh-optim = { workspace = true }
torsh-linalg = { workspace = true }

# Serialization and I/O
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
safetensors = { workspace = true }

# Error handling
thiserror.workspace = true
anyhow.workspace = true

# Model downloading (optional)
reqwest = { workspace = true, features = ["json", "stream"], optional = true }
tokio = { workspace = true, features = ["fs", "io-util"], optional = true }
# Pure-Rust TLS stack for the download client (no aws-lc-rs / ring / BoringSSL)
rustls = { workspace = true, optional = true }
webpki-roots = { workspace = true, optional = true }
oxitls-rustcrypto-provider = { workspace = true, optional = true }

# File system and utilities
sha2 = { workspace = true }
hex = { workspace = true }
chrono = { workspace = true }

# Progress reporting
indicatif = { workspace = true }

# Async traits
async-trait.workspace = true

# Logging
tracing.workspace = true

# Utilities
dirs = { workspace = true }
lazy_static = { workspace = true }
futures-util = { workspace = true }
parking_lot = { workspace = true }

# SciRS2 compliance - replaces direct rand usage
scirs2-core.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["test-util", "macros", "rt"] }
tempfile = { workspace = true }

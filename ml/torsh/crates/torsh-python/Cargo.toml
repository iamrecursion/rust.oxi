[package]
name = "torsh-python"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
description = "Python bindings for ToRSh - PyTorch-compatible deep learning in Rust"
readme = "README.md"
documentation = "https://docs.rs/torsh-python"
keywords = ["deep-learning", "python", "pyo3", "machine-learning", "bindings"]
categories = ["science", "api-bindings"]

[lints]
workspace = true

[lib]
name = "rstorch_python"
# cdylib: Python extension module (.so loaded by Python)
# rlib: Rust library linked by integration tests (propagates build script link flags)
crate-type = ["cdylib", "rlib"]
test = false

[dependencies]
# PyO3 for Python bindings
pyo3 = { workspace = true }
numpy = { workspace = true }

# ToRSh crates
torsh-core = { workspace = true }
torsh-tensor = { workspace = true }
torsh-optim = { workspace = true }
torsh-data = { workspace = true }
torsh-autograd = { workspace = true }
torsh-functional = { workspace = true }

# ✅ SciRS2 POLICY: Use scirs2-core for all scientific computing needs
scirs2-core = { workspace = true }

# Error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

# Utilities
parking_lot = { workspace = true }

[features]
default = []
extension-module = ["pyo3/extension-module", "pyo3/abi3-py38"]

[build-dependencies]
pyo3-build-config = { workspace = true }

# NOTE: The single canonical maturin manifest is the workspace-root
# `pyproject.toml` (project `torsh`), which sets `manifest-path` to this crate
# and `module-name = "rstorch._C"`. The values below only matter if this crate
# is built directly with maturin; keep them consistent with the root config.
[package.metadata.maturin]
python-source = "../../python"
module-name = "rstorch._C"
bindings = "pyo3"

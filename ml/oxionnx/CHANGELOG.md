# Changelog

All notable changes to OxiONNX will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [0.1.8] - Unreleased

## [0.1.7] - 2026-08-14

**`oxionnx-cuda` matures from a working-but-partial 25-op backend into a 40-op one** — real `Conv` support, session-lifetime device-memory management (buffer pooling, weight residency, compiled-PTX-module caching, batched dispatch), run-scoped activation residency shared with the wgpu backend, and an honestly-scoped opt-in CUDA Graph capture path. Alongside it, `oxionnx-gpu` gained device- and shape-aware dispatch tuning replacing seven flat thresholds, a crash fix for running more than one GPU-backed session in a process, and activation-buffer recycling. Highlights below; the full per-task writeup lives in `TODO.md` §21-24.

**A note on the performance numbers below**: every CUDA and wgpu figure is a within-this-cycle before/after (this session's own dispatch path measured before and after the specific change described, on one machine) — not a comparison against 0.1.6, which had no batched dispatch, no residency, and flat dispatch thresholds to compare against. Each figure states its hardware and harness inline.

### Added

- feat: **`oxionnx-cuda`: `Conv` is now CUDA-accelerated — from an unconditional CPU decline to direct dispatch across three `oxicuda-dnn` engines, the crate's 26th accelerated op (up from 25).** `conv::cuda_conv` (`oxionnx-cuda/src/conv.rs`, 110 → 1168 lines) selects `ImplicitGemmConv` for shapes its CTA-tiled implicit-GEMM kernel claims, `Conv1x1` for unit-stride/dilation/no-pad 1×1 filters, and `DepthwiseConv` when `groups == in_channels == out_channels` — deliberately bypassing `oxicuda_dnn::conv::api::conv_forward`'s auto-selector, which can route into a separately-gated Winograd path. Asymmetric `pads` (ONNX allows a different value per side; the dispatch's `ConvProblem` carries only one per spatial dimension), non-4-D shapes, and group/channel mismatches decline to the CPU rather than compute a wrong answer. `is_supported_op`'s `Conv` arm — hardcoded `false` since the op's CPU-only stub was written — is flipped to `true`, and `oxionnx::execution_providers`' `Auto` placement now actually routes a convolution to CUDA once both the op-level predicate and the dispatch-time shape/attribute checks accept it (previously unreachable from production inference even after a working kernel existed, since placement gated on the predicate alone). Fused `Relu`/`Clip` epilogues are honored where the previous attribute reader silently ignored anything past `strides`/`pads`/`dilations`/`group` — dropping SCRFD det_10g's fused activation from 24 convolutions and collapsing every detection to a degenerate corner box; `conv_params_from_attrs` is now a whitelist that declines on any attribute combination it does not recognize instead of ignoring it. Validated against two independently-written CPU oracles — a from-scratch `f64` NCHW cross-correlation in the new `conv_tests.rs` (1114 lines) and the crate's existing `reference::ref_conv` — so a bug shared between the kernel and one oracle cannot hide from the other. The underlying engines' own benchmark (`oxicuda-dnn/benches/conv_engine_gflops_regression`, an OxiCUDA-side engine-vs-engine number, not measured in this repository) reports a 256→512 pointwise convolution over 64×64 at 480 GFLOPS on `Conv1x1` versus 7345 GFLOPS tiled (15.3x), and the three face-pipeline models' dominant 3×3 layers at 875-1006 GFLOPS untiled versus 6.1-8.3 TFLOPS tiled.
- feat: **`oxionnx-cuda`: a session-lifetime device-buffer pool, weight-residency cache, and compiled-PTX-module cache replace a per-dispatch `cuMemAlloc`/`cuMemFree`/JIT-compile pattern, and `MatMul`/`Gemm` gain true batched dispatch.** `residency.rs` (new, 1537 lines) adds a size-classed device-buffer free list (`DevicePool`, 512 MiB budget, freed on check-in past its class cap), an initializer-keyed weight cache (`ResidentWeights` — identity is checked against the cached entry's recorded host address and length on every lookup, not trusted, so a genuine weight-map swap demotes to a per-dispatch upload instead of silently serving stale bytes), and pinned-memory staging (measured on this machine at ~25.8 GB/s versus pageable's ~9.7 GB/s). `CudaContext::module` adds a per-context compiled-PTX cache for the elementwise/softmax kernels this crate JIT-generates itself, previously recompiled on every one of an InSwapper frame's 57 elementwise dispatches. `matmul.rs` (70 → 1002 lines) adds real batched dispatch (`cuda_gemm_batched`, `GemmPlan::{Collapsed, StridedBatch}`) in place of a per-batch-element loop where every iteration was its own complete round trip (allocate, upload, fence, launch, fence, read back, free) — a batch of 16 previously cost 48 allocations and 32 fences to run kernels that individually take microseconds. New telemetry — `CudaContext::cache_counters()`/`cached_device_bytes()`/`release_device_caches()`/`is_weight_resident()`, and `Session::cuda_cache_counters()`/`cuda_streams_unified()` behind the `cuda` feature — reports what the caches did; `weight_bytes_uploaded` must read zero across a steady-state frame, asserted directly by `tests/batched_matmul_gpu.rs`. Measured on an RTX A4000 (sm_86, driver 550.144.03, CUDA 12.4) comparing this session's own dispatch path before this change against after it, both within this cycle, via `examples/dispatch_bench.rs` (steady state, median of three runs): batched MatMul `[4,64,128]x[4,128,64]` 1.66 ms → 0.10 ms and `[16,64,128]x[16,128,64]` 5.89 ms → 0.34 ms (both 17x — attributed to batched dispatch specifically), `[4,256,256]x[4,256,256]` 8.32 ms → 0.68 ms (12x); ArcFace's Gemm head `[1,25088]x[25088,512]` 43.65 ms → 5.56 ms (7.9x); a channel-broadcast batch case 2.93 ms → 0.58 ms (5.0x); InSwapper's AdaIN Gemm 1.04 ms → 0.31 ms (3.4x); a bias'd 3×3 Conv 1.08 ms → 0.65 ms (1.7x); Softmax `[1024,512]` 1.27 ms → 0.70 ms (1.8x); ReduceSum `[1024,512]` axis 1 0.54 ms → 0.22 ms (2.5x) — this last group of six attributed to pooling, residency and stream-ordering rather than batching.
- feat: **`oxionnx-cuda`: a device-resident value one CUDA-claimed node produces can now stay on the device for the next CUDA-claimed node to consume, instead of a host round trip at every node boundary.** This extends the run-scoped activation residency wgpu gained in 0.1.6 onto a single shared, backend-generic implementation: `src/session/gpu_activations.rs`'s `RunActivations` is now generic over a new `DeviceActivation` trait instead of hardcoding a wgpu buffer type, with `GpuActivations`/`CudaActivations` as the two instantiations, and `oxionnx_cuda::ResidentActivations`/`CudaDeviceTensor`/`try_cuda_dispatch_resident` as the trait CUDA's dispatcher consults (`try_cuda_dispatch` itself is now a thin wrapper passing `NoActivations` + host placement, byte-for-byte compatible with prior behavior). CUDA uses a more permissive keep policy than wgpu's — a value stays resident if *any* capable consumer can bind it in place, not only when *every* consumer can — because a CUDA value that must still be read back for one CPU-only consumer costs one read-back either way, while wgpu's async fence has no equivalently cheap fallback; this is safe only because it also requires the context's BLAS and DNN handles to share one CUDA stream (`CudaContext::streams_unified()`), which is what lets a recycled buffer be reused without an extra fence. Wired into the synchronous `Session::run` path only — wgpu's equivalent runs through `run_gpu_async`, so the async and rayon-parallel run paths do not yet get CUDA activation residency. The problem this replaces: measured across the three production face-pipeline models, a CUDA-claimed chain used to move 1327 MB/frame of PCIe traffic and block the host with 237 `stream.synchronize()` calls, one per claimed node regardless of whether its result ever left the device. `tests/cuda_activation_residency.rs` (new, 480 lines) proves the replacement with exact asserted integers rather than timing thresholds — a five-node all-CUDA chain now costs exactly one upload, one download and one fence, versus five of each before — covering a CPU-only node breaking residency mid-chain, a runtime decline reading its resident operand back exactly once, and eight steady-state frames allocating nothing and recycling every activation. A new `OXIONNX_CUDA_RESIDENCY` kill switch (default on) and an automatic disable under `OXIONNX_CUDA_VERIFY` (whose CPU oracle needs exact host bytes a resident operand does not have) cover the two ways to turn it back off.
- feat: **`oxionnx-cuda`: six new op families close the largest remaining gap in CUDA coverage — data movement.** `pool.rs` (new, 676 lines) adds `MaxPool`/`AveragePool` via `oxicuda_dnn::pool`'s previously-uncalled kernels, computing both the floor-mode extent the kernel produces and the ONNX ceil-mode extent and declining when they disagree (also declines non-unit `dilations`, non-`NOTSET` `auto_pad`, asymmetric pads, and a requested `Indices` output). `resize.rs` (586) adds nearest/bilinear `Resize` matching `oxicuda_dnn::resize`'s exact coordinate-transform formulas. `pad.rs` (821) and `slice.rs` (568) hand-write their own PTX — no `oxicuda-dnn` kernel exists for either — and exploit ONNX's own clamp/reflect rules to avoid a runtime bounds branch entirely: `pad.rs`'s reflect-mode index is a periodic fold (`(out - begin).rem_euclid(2*(dim-1))`, mirrored past `dim`) valid unconditionally, and `slice.rs`'s `start[d] + o*step[d]` is provably in-range by construction once `slice_params_from_node` applies ONNX's exact start/end/step clamp. `concat.rs` (322, `outer * num_inputs` device-to-device copies, no compute kernel at all) and the zero-kernel `reshape.rs` (324, covering `Reshape`/`Squeeze`/`Unsqueeze`/`Flatten` as a device-resident-input-only buffer alias via the new `CudaDeviceTensor::alias`) round out the family, bringing the running total to 36 accelerated ops. Each module ships its own CPU oracle (`reference_data_ops.rs`, new, 667 lines) and a dedicated on-device parity suite (`tests/data_ops_verify_path_gpu.rs`, new, 632 lines, one test per `verify_or_fallback` call site) alongside 85 in-module unit tests. No performance numbers are claimed for this family — the design argues from bus-traffic and fence-count reduction, not measured throughput.
- feat: **`oxionnx-cuda`: `Add`/`Sub`/`Mul`/`Div` now accelerate a channel-broadcast shape, and three normalization ops join the accelerated set — bringing `is_supported_op`'s total to 40, verified directly against its `matches!` arm rather than doc prose.** `broadcast.rs` (new, 475 lines) adds the `[1,C,1,1]`-vs-`[1,C,H,W]` and scalar-vs-tensor broadcast patterns the exact-shape-only elementwise kernels declined outright; the audit that motivated it found every one of 86 CUDA-declined nodes across the three production face-pipeline models was this exact shape falling through the equality check. `prelu.rs` (204) adds `PRelu`, with its per-channel slope held in the same weight-residency cache as `Conv`/`Gemm` operands. `norm.rs` (471) adds inference-mode `BatchNormalization` and the fused `OxiInstanceNorm` op on the existing `BatchNormTemplate`, with a module-cache key that folds in `epsilon`'s bit pattern — the template's own `kernel_name()` encodes mode/precision/channels/spatial/block but not epsilon, which is baked into the generated PTX as a literal, so two same-shape nodes with different epsilon would otherwise collide in the cache. `reduce.rs` (192 → 553) adds `ReduceMean` as `ReduceSum` over a contiguous axis range followed by an in-place device-side divide, and separately moves `ReduceSum`/`ReduceMax` off the old hand-rolled single-block `ReductionTemplate` onto `oxicuda_blas::reduction::reduce_axis`, correct for any axis length rather than only up to one block's worth. 62 new unit tests across the four modules plus 5 new on-device arms in `tests/verify_path_gpu.rs`.
- feat: **`oxionnx-cuda`: opt-in CUDA Graph capture/replay for `MatMul`/`Gemm`, scoped honestly to the narrow case where it actually helps.** `graph_cache.rs` (new, 713 lines), gated behind `OXIONNX_CUDA_GRAPH` and off by default, records a CUDA graph the first time a given GEMM shape and weight identity is dispatched and replays it thereafter, closing three pointer-stability hazards a capture requires: the buffer pool is bypassed entirely for captured dispatches (cache entries own their buffers), every externally-owned resident-weight pointer is part of the cache key, and `oxicuda-blas`'s split-K scratch workspace — previously `cuMemAlloc`'d inside every GEMM call, which CUDA forbids during capture — is now a permanent per-`(stream, type, size)` allocation. A capture that fails is permanently and silently demoted to ordinary dispatch for that key (`Slot::Poisoned`) rather than retried. Measured on an RTX A4000 with a dedicated probe (`examples/graph_dispatch_bench.rs`, interleaved runs in one process to control for drift observed on a shared GPU — the same untouched shape was separately clocked at 1.49 ms in one run and 2.78 ms in the next): graphs win by up to 10% on small, launch-overhead-bound shapes (`[1,512]x[512,512]`: -10.0%/-10.4% across two runs) and lose by as much on batched dispatch (+10.9%/+9.7% for a 4-way batch) — the sign is a property of the shape, not of the run. `Conv` was measured and deliberately excluded: the probe's three InSwapper-128 3×3 shapes replayed at -0.6%, -3.7% and +1.2% against ordinary launches — noise around zero, because a 49 MiB weight read at this card's ~350 GB/s is ~140 µs on its own, matching the isolated GEMM time; a graph cannot remove a cost that *is* the transfer. `OXIONNX_CUDA_GRAPH` and `OXIONNX_CUDA_VERIFY` deliberately do not compose (the shadow-verification path leaves graphs off with a warning). 12 unit tests plus `tests/graph_cache_gpu.rs` (new, 388 lines, 6 on-device tests including a bit-identical, not just tolerance, replay check).
- feat: **`oxionnx-cuda`: a real on-device integration-test suite, gated so a plain `cargo test` never touches a GPU.** The crate had no `tests/` directory before this cycle; it now has five on-device integration-test binaries (`matmul_shape_sweep_gpu`, `verify_path_gpu`, `batched_matmul_gpu`, `graph_cache_gpu`, `data_ops_verify_path_gpu`) and three benchmark examples, all behind a new `gpu-tests` feature and `required-features = ["gpu-tests"]` on every one of those Cargo targets — confirmed on this machine that a bare `cargo test -p oxionnx-cuda` builds and runs none of them. Turning `gpu-tests` on does not itself require a device: every fixture returns `Option<CudaContext>` and each test skips with an `eprintln!` when none is present, so `cargo nextest run --workspace --all-features` stays green on a CPU-only host — the 30 device-only tests were confirmed, via `--no-capture`, to genuinely reach their skip rather than run with an orphaned body. `#[test]` count in the crate: 70 at the start of this cycle (no integration-test directory at all) to 398 now, of which roughly 344 run without any device and the rest need one.
- feat: **`oxionnx-gpu`: `GpuContext::try_new_diagnosed()`/`try_new_diagnosed_async()` distinguish "no GPU present" from "a GPU is present but unreachable."** Plain `try_new()` returning `None` collapses those two very different situations; the new entry points return a `GpuInitError` that, on adapter-acquisition failure, re-probes every other `wgpu` backend to see whether a working GPU shows up on any of them (`GpuInitDiagnostic::GpuVisibleToAnotherBackend`/`OnlySoftwareAdapters`/`NoAdaptersAnywhere`) — evidence from the other backends, not a guess from driver filenames. Motivating case, reproduced on an RTX A4000 (driver 550.144.03, Ubuntu 22.04 container): with the NVIDIA ICD manifest present and `nvidia-smi` fully functional, `wgpu` enumerated zero Vulkan adapters because the separate `libvulkan1` loader package was not installed (the NVIDIA driver package installs the ICD manifest and driver library but not the loader itself); installing that one package turned the same call into two working adapters, no reboot required. The re-probe is memoized in a `OnceLock` and only runs after the primary `request_adapter` call has already failed, so a working GPU pays no extra cost.
- feat: **`oxionnx-ops`: 9 new regression tests assert the AVX2 runtime-dispatch arm is actually entered, independent of what it computes.** Every one of the 8 `is_x86_feature_detected!("avx2")` call sites in `simd_ops` (the `dispatch_binary`/`dispatch_unary` chokepoints plus six standalone functions) now increments a `#[cfg(test)]`-only thread-local counter on the AVX2-taken path, so a broken dispatcher (a typo'd feature string, an inverted condition, a stray `cfg`) that silently fell through to scalar on every call would fail these tests — even though the elementwise ops are bit-identical AVX2-vs-scalar and the reduction ops' documented tolerance would otherwise mask a permanently-scalar path as a clean pass. Test-only instrumentation: no shipped binary or public API changed. Runs only under `--features simd` on `target_arch = "x86_64"` (zero of these tests build on this project's aarch64 authoring machine; each also self-skips at runtime without AVX2/FMA).

### Changed

- perf: **`oxionnx-gpu`: dispatch-or-decline thresholds for every kernel family are now derived per-adapter and per-shape instead of seven hardcoded flat constants.** `GpuTuning` (new, `oxionnx-gpu/src/context/tuning.rs`, 868 lines) replaces `shaders/common.rs`'s flat `EW_GPU_THRESHOLD`/`SOFTMAX_DIM_THRESHOLD`/`REDUCE_GPU_THRESHOLD`/`LAYER_NORM`/`BATCH_NORM`/`TRANSPOSE`/`BINARY_EW` constants and `compute.rs`'s `GPU_THRESHOLD`, on two grounds a flat number cannot express: a software adapter (`lavapipe`, WARP) *is* the CPU it would be racing, so `GpuPerfClass::Software` now declines every size outright; and for GEMM, raw FLOP count is the wrong gate next to arithmetic intensity (`I = 2mkn/(mk+kn+mn)`, dominated by the smallest of the three extents), which is what actually predicts whether a shape is compute- or transfer-bound. `Integrated`/`Unknown` adapter classes deliberately inherit the `Discrete` numbers unmeasured rather than a guessed value, and are documented as such. Folded in as a real bug fix: the session-layer Gemm gate compared `2*m*k*n` against the same 10M constant `compute.rs` compared `m*k*n` against — a factor-of-two inconsistency between what was meant to be one shared threshold. Calibrated on an RTX A4000 over Vulkan (best-of-180 samples per point; ±30% run-to-run variance noted at smaller sizes, on a device that idles at 210 MHz of a 2100 MHz boost with no permission to lock clocks in this environment): the smallest GEMM shape that won at every measured intensity was ~25.2M multiply-accumulates, and a cached weight changes the answer for an otherwise-identical shape — ArcFace's head runs at 0.43x the CPU kernel's time with a warm weight cache versus 1.54x (a loss) uploading it fresh, now exposed as `oxionnx::session::gpu_residency::gemm_gpu_admits(gpu, m, k, n, weight_is_cacheable)` in place of the `Gemm` dispatch arm's bare FLOP comparison. Memory-bound elementwise/normalization/transpose/reduction kernels measured 1.8x-45x slower than the CPU kernel whenever an operand must actually cross the bus, so their floors are `usize::MAX` under transfer and drop to a small worthwhile-at-any-size number (256-262K elements, depending on kernel) only once every operand is already device-resident and no transfer is happening at all. `GpuContext::tuning()`/`perf_class()`/`set_tuning()` expose the mechanism; two new example harnesses (`gemm_shape_crossover.rs`, `kernel_crossover.rs`) reproduce the calibration tables behind these numbers.
- perf: **`oxionnx-gpu`: a released device activation buffer is now recycled into the context's pool instead of destroyed.** `RunActivations::release_after` (`src/session/gpu_activations.rs`) now returns a finished activation's buffer to `GpuContext`'s pool (`recycle_device_tensor`) rather than dropping it; `RunActivations::insert` also now correctly disposes of a *displaced* value — the case where one name is simultaneously a promoted initializer and a node output, previously dropped on the floor rather than recycled. New pool telemetry (`GpuBufferPool::{max_buffers,reuses,allocations}`; `GpuContext::{pooled_buffers,pool_byte_budget,pool_reuses,pool_allocations}`; `Session::gpu_pool_*`/`gpu_live_bytes`/`gpu_pooled_bytes`) makes the effect observable. Measured with a new paired A/B example (`examples/w4_recycle_ab.rs`, 200 measured pairs across 4 processes, outputs verified byte-identical in all of them — recycling changes only which buffer is reused, never its contents) comparing this session's own recycle-enabled path against the same session with recycling disabled, both within this cycle: recycling wins the paired median in 90 of 100 InSwapper-128 pairs (2-2.4% faster) at a 96.1% pool hit rate against 4.9%, and 98 of 100 pairs on a 48-node chain of small activations (11-28% faster) at a 98.9% hit rate. The pool settles at a bounded ceiling rather than growing — InSwapper's steady state holds 64 of 64 permitted entries at 84.59 MiB against a 256 MiB budget.
- `oxionnx::execution_providers`' `Auto` placement now also routes `Reshape`/`Squeeze`/`Unsqueeze`/`Flatten`/`Slice`/`Concat`/`Pad` and `PRelu`/`BatchNormalization`/`OxiInstanceNorm`/`ReduceMean` to CUDA once its own predicate claims them, following the same per-backend-predicate rule as `Conv` above. Internal placement tests that had hardcoded a single exemplar op no longer do — first `Conv`, then `ReduceMean`, then `Reshape` each stopped being a valid "CUDA declines this" example as the crate's own coverage grew past them mid-cycle; the tests now derive their exemplar by scanning the dispatch-op table so the next kernel CUDA gains cannot silently invalidate the test again.
- `oxicuda-driver`/`oxicuda-memory`/`oxicuda-blas`/`oxicuda-dnn`/`oxicuda-ptx`/`oxicuda-launch` updated from 0.5.3 to 0.5.5.
- `oxionnx-ops`'s package description now states 189 operators, up from 188 (a documentation correction; no operator implementation changed in this release).

### Fixed

- fix: **`oxionnx-cuda`: `MatMul` (and `ReduceSum`/`ReduceMax`) could silently return wrong numbers — a two-stream race, not a kernel bug.** `oxicuda_dnn::DnnHandle` gives its internal BLAS sub-handle its own CUDA stream so BLAS and DNN launches can overlap; `matmul.rs`/`reduce.rs` dispatched through that BLAS stream but synchronized `ctx.dnn.stream()` — the *other*, idle stream — before reading results back. Every `oxicuda-driver` stream is non-blocking, so the host could read `C` back before the GEMM had actually finished, observing the destination buffer's leftover/zeroed contents rather than a computed value. Reproduced on an RTX A4000 (sm_86) before fixing: a `64x64x64` all-ones case returned 3456 of 4096 elements wrong, an ArcFace-shaped `M=1,K=25088,N=512` case 439 of 512 wrong, and an InSwapper-shaped `M=8,K=25088,N=512` case 3220 of 4096 wrong — every wrong element reading back `0.0`. Fixed via a new `DnnHandle::synchronize_all()` (waits for both streams) called in place of the single-stream wait; `reduce.rs` had carried a comment asserting its dispatch already used the stream `ctx.dnn` owns, which was false and is now corrected. New coverage: `tests/matmul_shape_sweep_gpu.rs` (new, 205 lines) reproduces the exact shapes above element-for-element against the CPU oracle, plus an M-sweep across `m ∈ {1,2,7,8,16,17,32,64,100,128,1024}`.
- fix: **`oxionnx-cuda`: `OXIONNX_CUDA_STRICT=1` could exit `0` on a run whose kernels it had just caught disagreeing with the CPU oracle.** A shadow-verification mismatch and an ordinary CUDA dispatch failure were both collapsed into the same CPU-fallback path, so a caller checking only the process exit code saw no difference between "this GPU produced a wrong answer and was caught" and "nothing went wrong." `error.rs` (66 → 172 lines) adds a `VERIFY_MISMATCH_MARKER` string interpolated directly into the error's `Display` output (so the marker and the message cannot drift apart) plus `CudaDispatchError::is_verify_mismatch()`/`oxionnx_cuda::is_verify_mismatch()` (re-exported from the `oxionnx` crate root too) to recover that distinction even after the error has been converted to the workspace-wide `OnnxError::Internal(String)`, which is otherwise lossy.
- fix: **`oxionnx-gpu`: opening a second `GpuContext` in one process could panic with `BindGroupLayout[Id(9,1)] does not exist` on its first convolution.** `wgpu::Device`'s `PartialEq` compares an internal `(index, epoch)` id from a per-`Instance` allocator, not object identity — and `GpuContext::try_new` builds a fresh `wgpu::Instance` per context, so a second context's device routinely compares equal to the first's. Every thread-local shader-pipeline cache (`kernel_support::PIPELINES`, `conv2d`'s pipeline cache and its f16 negative cache, `gemm`'s two f16 verdict caches) was keyed partly on that device, so the second context was served pipelines the first had compiled against a bind-group layout the second context's device had never created. Affects any process building more than one GPU-backed session; reported as reliably hitting the second session's first convolution. Fixed by moving the cache from thread-locals to a field owned by `GpuContext` itself (`oxionnx-gpu/src/context/pipeline_cache.rs`, new, 367 lines; `ctx.pipelines()`), keyed on `(label, entry_point, source)` — entry point is load-bearing, since one WGSL source can compile four entry points under one label and keying on label alone would make one op's kernel silently serve another's entry point. New `oxionnx-gpu/tests/w5_multi_context.rs` (5 tests) exercises two and three live contexts concurrently and across a drop/recreate cycle.
- fix: **`tests/w2_async_run.rs`: a flaky re-poll assertion, not an implementation bug.** `Shared::complete` publishes its result under the state lock but calls `wake()` after releasing it — deliberately, since waking under the lock would deadlock an executor that polls synchronously from inside `wake()` — so a second poll landing in that window can legally observe `Ready` with a wake count of zero. The test's `polls > 1 => wakes >= 1` assertion was never a sound property of the implementation it was checking, and failed roughly half the runs on this machine; it now waits for the wake itself (30 s deadline) instead of re-polling and inferring.

## [0.1.6] - 2026-08-11

### Added

- feat: **`oxionnx-coreml`: `MlPackageModel::ensure_compiled(path) -> Result<PathBuf>`** — compile a `.mlpackage` into the crate's persistent on-disk cache and return the resulting `.mlmodelc` directory *without* loading the model, so an installer or first-run setup step can pay CoreML's compile cost up front instead of inside the first request. Present on the non-Apple stub too (returning `CoreMLError::UnsupportedPlatform`) for API parity.
- feat: **`oxionnx`: `Session::run_gpu_async` — a genuinely asynchronous GPU execution path, awaited rather than blocked on, and a distinct thing from the existing thread-per-inference `run_async`/`spawn_run`/`block_on`.** That pre-existing trio (`session::async_run`) spawns the ordinary blocking `Session::run` onto a background OS thread; it does not, and cannot, help a target with no OS threads at all. `run_gpu_async` (new, `src/session/run/sequential_async.rs`, ~932 lines) is a second, smaller node-execution loop that runs the same nodes in the same order and differs in exactly one place: a node the wgpu backend accepts is `.await`ed instead of blocked on, so the calling task yields to an event loop while the GPU works instead of blocking a thread that may never block on a GPU fence — every thread a browser page has. Underneath, `try_gpu_dispatch_async` (`src/session/gpu_dispatch.rs`) is now the real dispatcher; the synchronous `try_gpu_dispatch` is reduced to a `pollster::block_on` wrapper around it, and every kernel in `oxionnx-gpu` follows the same shape — an `async fn` ending in `read_back_and_recycle_async`, with a thin blocking wrapper — so native and browser targets run identical code instead of two independently drifting copies of the same shape/attribute validation. `block_on_gpu` (`oxionnx-gpu/src/device_guard.rs`) is the seam for that crate's own kernels: `pollster::block_on` natively, and on `wasm32` it drops the future **unpolled** — nothing submitted, allocated or uploaded — so an unconverted kernel fails safe instead of silently no-opping. `Session::enable_gpu_async()` is the paired async device-acquisition step (`session.enable_gpu_async().await` then `session.run_gpu_async(&inputs).await`), needed because acquiring a GPU adapter is itself a promise a session's synchronous constructor cannot block on. The dispatch table gained real arms for `Sub`, `Div`, `PRelu`, `Pad`, `Resize`, `Gemm` and the new fused `OxiInstanceNorm` op in the same wave, tracked in the single `GPU_DISPATCH_OPS` array both dispatchers' capability checks consult. Per-run `GpuRunStats` (GPU/CPU node counts, read-back count and bytes, per-op CPU fallback timing, weight- and activation-residency counters) accumulate on a thread-local, retrieved via `take_run_stats()`/`run_stats()`; `Session::gpu_device_error()` and `gpu_uploaded_bytes()` round out the new accessors. `examples/r3a_cost_breakdown.rs` and `examples/r3a_inswapper_residency.rs` are the measurement harnesses this and the size-gate entry below were sized from. This is proven on native targets only so far — see the "Fixed" entry below for the exact native/browser coverage split.
- feat: **`oxionnx-gpu`: session-lifetime GPU weight residency — convolution and `Gemm` weights now cross the host↔device bus once per session instead of once per dispatch.** A graph's initializers (`Conv`'s `W`/`B`, `Gemm`'s `B`/`C`) never change between frames, but every prior dispatch rebuilt their device buffers from the host tensor anyway; InSwapper-128 alone re-uploaded 502.7 MB of invariant convolution weights on every forward pass, and ArcFace's embedding head is a 51.4 MB `Gemm` `B` by itself. `oxionnx_gpu::context::resident::ResidentBuffers` (new, `oxionnx-gpu/src/context/resident.rs`) now caches a `TrackedBuffer` per caller-supplied identity for the life of the `GpuContext`, checked rather than trusted: each entry records the kernel-slot label and byte length it was uploaded for, and a lookup that disagrees with either (`Lookup::Conflict`) uploads for that one dispatch instead of silently serving the wrong bytes under a reused key. The session layer supplies the identity: `initializer_key` (`src/session/gpu_dispatch.rs`) resolves a node input to a stable, unambiguous graph-initializer name (never a value a node produced this run), and `keyed_operand_slot` restricts caching to the two `(kernel, slot)` pairs that actually bind it — `Conv`'s weight/bias, `Gemm`'s `B`/`C`. `gpu_conv2d_fused_placed_async` and `gpu_gemm_nt_placed_async` are the entry points the dispatcher calls with a `WeightKeys` built from those identities. The cache is additionally keyed per numeric format (`WeightFormat::F32`/`F16`), so a weight resident as `f32` and later requested as `f16` is a miss that uploads the second copy rather than the wrong bytes — what makes flipping `Session::set_f16_compute` mid-session safe. It lives on `GpuContext` itself, not beside the session, because `session::gpu_owner` already creates and destroys a `GpuContext` on one dedicated thread (a cross-thread destroy reproduced a driver `SIGSEGV`), and a residency map held anywhere else would destroy its buffers on whatever thread happened to drop the session's last `Arc`. Observable via `Session::gpu_resident_bytes()` and per-run `GpuRunStats::{weight_cache_hits, weight_cache_misses, weight_upload_bytes}`, and confirmed by 5 device-backed tests in `oxionnx-gpu/tests/r3b_weight_residency.rs` asserting that a second run against the same context uploads exactly zero bytes of a previously-seen weight.
- feat: **`oxionnx`/`oxionnx-gpu`: run-scoped GPU activation residency — a value one GPU node produces can now stay in its device buffer for the next GPU node to consume in place, instead of a read-back and re-upload at every node boundary.** This is the other half of the transfer problem weight residency didn't touch. `src/session/gpu_activations.rs`'s `RunActivations` (new) computes, before a run's first node executes, which node outputs may be produced straight onto the device: a name qualifies only if it is not a graph output (those must still be read back, or `take_outputs` cannot find them), some node actually consumes it (a dead output held resident pins bytes for nobody), and **every** consumer can bind it in place in the exact slot it reads it from — via `op_accepts_resident_slot` in `gpu_dispatch.rs` — with one incapable consumer disqualifying the name entirely, since otherwise the round trip is only moved to a later node, not removed. Values an `If`/`Loop` subgraph captures are always excluded, since a subgraph body runs on the CPU and never appears in `node.inputs`. Lifetime is deterministic, not policy-driven: `RunActivations::new` precomputes each name's last-consumer index across the node order, and `release_after` destroys (returning its budget bytes) every activation whose last use was the node that just ran, so a run always ends with live GPU bytes back at the resident-weight baseline. The new `oxionnx_gpu::context::activation` module supplies the primitives this rests on — `DeviceTensor` (a budget-accounted device buffer plus shape), `TensorSource` (an operand that is either a host slice or a borrowed device buffer), and `OutputPlacement`/`GpuOutput` — which let a kernel's tail, `device_guard::finish_output_async`, either read back to a host `Tensor` as before or hand back a `DeviceTensor` with no staging buffer, no copy and no fence wait. `gpu_conv2d_fused_placed_async`/`gpu_gemm_nt_placed_async` take this alongside `WeightKeys`, so one dispatch call serves both residency mechanisms at once. A consumer that turns out to need a resident value on the host after all (a runtime decline, an unsupported shape) triggers exactly one `read_device_tensor_async` read-back, memoized into the run state. Toggle with `Session::activation_residency_enabled()`/`set_activation_residency()` — off reproduces pre-residency behavior exactly, which is what lets the mechanism be A/B-measured within one session. Confirmed by 5 end-to-end tests in `src/session/tests/gpu_activation.rs`: bit-identical output with residency on vs. off on a `Conv → Relu → Conv → Add` chain, strictly less bus traffic with it on, and the live-byte total returning exactly to baseline once the run ends.
- feat: **`oxionnx`: a new optimizer fusion pass recognizes decomposed AdaIN-style spatial normalization and collapses it into a single `OxiInstanceNorm` op.** PyTorch cannot export `InstanceNormalization` for AdaIN generators — that op requires initializer-shaped `scale`/`B` operands, but AdaIN's per-channel scale and shift come from a runtime Gemm head — so exporters instead emit eight bare-arithmetic nodes: `ReduceMean(axes=[2,3]) → Sub → Mul(diff,diff)|Pow(diff,2) → ReduceMean(axes=[2,3]) → Add(eps) → Sqrt → Reciprocal|Div(1,·) → Mul(diff,·)|Div(diff,·)`. `src/optimizer/fusion/instance_norm.rs` (`fuse_instance_norm`) matches every spelling of that chain — both square forms, both reciprocal/direct-divide forms, either operand order — and rewrites it to one `OxiInstanceNorm` node, eliminating seven intermediate tensors per chain, including `diff` (materialized in full and read three times; `[1,1024,32,32]` in the widest block of its motivating case, the inswapper-128 face-swap decoder, where 12 such chains collapse out of 238 nodes). It runs deliberately after `fuse_layer_norm`: on a 4-D tensor `axes=[2,3]` is also the trailing-axis run `[-2,-1]`, so the two patterns are shape-indistinguishable at the `ReduceMean`, and LayerNorm — which can fold the trailing affine term this pass cannot — gets first refusal; the pass itself only fires once the active registry resolves `OxiInstanceNorm` (true by default, since `OxiInstanceNormOp` is registered in `default_registry()`) and runs within the `PassLevel::Extended`+ fusion block. The fused op (`oxionnx-ops/src/registry/oxi_instance_norm.rs`) computes `(x - mean) / sqrt(var + eps)` per `(n, c)` plane with no affine term, using the same two-pass variance the deleted chain used rather than the one-pass `mean(x²) - mean(x)²` form that loses catastrophically at large magnitude; it is rayon-parallel above 8192 elements and implements `execute`/`execute_inplace`/`execute_into_slots`, all three cross-checked to agree. A matching GPU kernel (`oxionnx-gpu/src/shaders/instance_norm.rs`) reduces one `(n, c)` plane per 256-thread workgroup in shared memory, structured like the existing LayerNorm shader. A new end-to-end test resolves the real inswapper-128 model and asserts exactly 12 chains fuse; a new CPU↔GPU parity test compares the two shipped kernels directly, rather than each one's own unit tests against a separately maintained reference.
- perf: **`oxionnx-gpu`: a direct implicit-GEMM Conv2D kernel replaces a CPU/GPU hybrid that was measurably slower than the CPU operator it was meant to accelerate.** The old path (`compute.rs`'s hybrid) ran im2col on the CPU and only the GEMM on the GPU, with bias and activation applied back on the CPU — measured at 0.33-0.58x the rayon CPU operator's speed on Metal, because the column matrix it re-uploaded per call is `kH*kW` times the input (InSwapper's twelve `Conv[1024,1024,3,3]` layers: a 4 MB input tile blows up into a 37.7 MB column matrix, ~595 MB of im2col crossing the bus per frame against ~50 MB for the input alone). The new kernel (`oxionnx-gpu/src/shaders/conv2d.rs`, `gpu_conv2d_implicit`) never materializes a column matrix — the im2col gather happens in-register inside the shader as the input tile is staged into workgroup memory — and fuses bias and activation into the epilogue. It walks the K axis (`C_in * kH * kW`) as an explicit `(ky, kx, ic)` loop nest rather than a flat index requiring four integer divisions per staged element, sharing its 64×64 macro-tile / 4×4 register-tile / `vec4`-blocked shared-memory scheme with the existing tiled-GEMM kernel, tuned for InSwapper's large conv-as-GEMM shapes (`M=1024, N=1024, K=9216` for its dominant layer). Measured on M3 via the new `examples/c3_conv2d_gflops.rs` benchmark: ~692 GFLOP/s at the 128×128 decoder layer. `group > 1` convolutions and any shape the device can't bind still decline to the old hybrid path. Correctness is locked in by new `oxionnx-gpu/tests/c3_conv2d_parity.rs` (870 lines) against the actual Conv inventory of three production models, read directly out of their `.onnx` files rather than assumed (InSwapper 20 nodes, ArcFace 53, SCRFD 57 — correcting a task sheet's stated 58), plus ragged and degenerate shapes those models never produce.
- feat: **`oxionnx-gpu`: four new standalone WGSL kernels — general-broadcast binary ops, a transposed-B Gemm, PRelu, and Resize — each validated against an independently structured CPU reference.** `broadcast_binary.rs` adds NumPy-style broadcasting (up to rank 4) for Add/Sub/Mul/Div, covering the `[1,C,H,W] op [1,C,1,1]` per-channel case that the existing flat elementwise kernels decline outright (49 of InSwapper's 67 Add/Mul nodes broadcast this way). `gemm.rs`'s `gpu_gemm_nt` implements `Gemm` with `transB=1`, tuned for InSwapper's AdaIN heads (`[1..64,512] × [2048,512]ᵀ`, with real `alpha`/`beta` uniforms rather than hardcoded 1.0, and `C` optional, row-broadcast, or a full matrix). `prelu.rs` adds per-channel PRelu, and `resize.rs` adds `pytorch_half_pixel` bilinear and `asymmetric` nearest resize, matching `oxionnx-ops::resize`'s reference coordinate-transform formulas exactly (InSwapper's 2 upsamples plus SCRFD's 2 FPN upsamples). Each is checked in a new test file (`k2_broadcast_binary.rs`, `k2_gemm.rs`, `k2_prelu.rs`, `k2_resize.rs`) against a CPU reference deliberately structured differently from both the kernel's own WGSL and its inline Rust reference, so a formula bug shared between those two would not also hide from the test. Each of the four is built on new shared pipeline infrastructure (`kernel_support.rs`), which memoizes every compiled `wgpu::ComputePipeline` into a thread-local keyed by `(device, label, entry_point)` instead of rebuilding it on every dispatch call; per its doc comment, an uncached rebuild cost 0.18-0.61 ms more per dispatch than an equivalent already-cached kernel moving the same bytes (M3), and across one InSwapper frame this infrastructure's kernels are collectively dispatched roughly 89 times, so caching removes tens of milliseconds of pure shader recompilation per frame.
- feat: **`oxionnx-gpu`: half-precision kernel variants are now derived, not hand-written, from a single `f32` source.** `f16_variant.rs` applies a short, anchored list of textual substitutions to the `conv2d` and `gemm_nt` kernel bodies — weights narrowed to `f16` once in storage, activations narrowed at the point they're read, products evaluated in `f16` and widened to `f32` before accumulation — rather than maintaining a hand-written second copy that could silently drift from the `f32` source it must match. If an edit moves an expected anchor, substitution returns `None` and the kernel simply reports no `f16` variant (falling back to `f32`) instead of risking a silent miscompile; the `f32` source itself stays untouched byte-for-byte. New `w2_f16_compute.rs` (562 lines) validates five properties end to end: `f16` conv2d and `gemm_nt` stay within 55 dB PSNR of their own `f32` result on realistic-magnitude tensors; resident weight bytes with the toggle on are exactly half those with it off; flipping the toggle between two runs on one context never serves one kernel the other's cached bytes; a device without `SHADER_F16` reports the mode off and stays on `f32`; and an `f16` convolution over a device-resident input/output still produces a correct result.
- feat: **`oxionnx-gpu`: new GFLOP/s benchmark and boundary-shape correctness tests for the tiled GEMM kernel.** `examples/h2_tiled_gemm_gflops.rs` measures the existing tiled MatMul kernel's end-to-end throughput (buffer upload, dispatch, blocking read-back — the same cost production code pays) through the public `gpu_matmul_tiled` entry point at `1024³` and `2048³`. Because that public wrapper declines below a 10M-FLOP threshold and falls back to a different kernel below `M/K/N=32`, new `oxionnx-gpu/tests/h2_tiled_matmul_gemm.rs` (352 lines) drives the tiled pipeline directly to reach the small and ragged shapes the wrapper would otherwise route around or skip: degenerate (`1×1×1`), fully ragged (`127×129×63`, off every tile boundary), macro-tile-aligned (`512³`/`1024³`), the smallest case forcing a second ragged tile (`65³`), and a tall-skinny im2col-shaped case (`12800×9×64`), at a mixed tolerance of `|gpu - cpu| ≤ 1e-4 × max(1, |cpu|)`.

### Changed

- perf: **`oxionnx-coreml`: `.mlpackage` compilation is now cached on disk instead of being redone — and leaked — on every load.** `MLModel::compileModelAtURL_error:` compiles into a fresh UUID-named directory under `$TMPDIR` on *every* call and neither reuses nor deletes it; `MlPackageModel::load` called it unconditionally, so every process start paid a full recompile and left the output behind permanently (7,408 orphaned `.mlmodelc` trees totalling 857 GB were measured in one developer's `$TMPDIR`). The compiled bundle is now moved into a stable, content-keyed entry — `$OXIONNX_COREML_CACHE_DIR`, else `$HOME/Library/Caches/oxionnx-coreml`, else `<temp>/oxionnx-coreml`, as `<bundle stem>-<fingerprint>.mlmodelc` — and reused from then on. The key folds the relative path, byte length and mtime of every file in the source bundle (metadata only: content-hashing hundreds of megabytes of weights would cost more than the compile it saves), so replacing a `.mlpackage` produces a new entry rather than reusing a stale compile. Installation is an atomic `fs::rename`, with a stage-then-rename fallback when `$TMPDIR` and the cache root are on different volumes, so concurrent processes racing on the same key resolve to a single complete entry and never observe a partial one; the loser of a race discards its own copy. The framework's temporary directory is renamed away or deleted on every path, including every failure path — that is the leak fix. An unloadable cache entry (a crash mid-install, a reaped directory, an OS upgrade) is evicted and recompiled exactly once rather than bricking the model forever, and a cache that cannot be used at all degrades to the previous behavior rather than failing a load that would otherwise succeed. Beyond skipping the compile, the *stable path* is what lets Apple's own ANE program cache hit across process launches: on an M3, resolving and loading SCRFD + ArcFace + InSwapper costs ~4.34 s from a never-seen path and ~0.14 s warm (30×), measured as compile 57/47/44 ms → 0.15/0.14/0.17 ms and `MLModel` load 493/1193/2503 ms → 39/62/41 ms.
- perf: **`oxionnx-coreml`: output extraction fused into a single pass.** `predict`/`predict_features` used to gather each output's elements into a `Vec<u8>` and then walk that whole buffer again to produce `Vec<f32>` (`chunks_exact(4).map(from_ne_bytes)`, or a per-element `f16::to_f32`), costing one full-size intermediate allocation and one extra traversal per output per prediction. The stride walk and the dtype conversion now happen together inside one `getBytesWithHandler:` block, writing final `f32` values straight into the returned buffer. The walk itself was rewritten around a layout planner (`CopyPlan`) that fuses every trailing dimension it legally can into one contiguous run and carries the source offset incrementally, replacing the old per-element `Σ idx[d]·strides[d]` recomputation; a fully C-contiguous array degenerates to a single bulk copy, and SCRFD's cache-line-padded outputs become one `memcpy` per row. `predict_raw`'s `read_raw_bytes` shares the same planner. Measured on the exact SCRFD `det_10g` output set (nine outputs, 252 k elements, rows padded to 32 elements — all on the strided path): 491 µs → 183 µs per frame for the copy algorithm alone (2.7×); a contiguous 1×3×128×128 InSwapper-shaped output goes 42.8 µs → 8.1 µs. Stride semantics are unchanged and are now pinned by a regression suite that checks both readers against a deliberately naive reference gather over padded fixtures (SCRFD's `[N,1]`/`[N,4]`/`[N,10]` layouts, middle-dimension padding, a non-unit innermost stride, extent-1 dimensions, and an `f16` padded case) — none of which any previous test reached, since every existing fixture was C-contiguous.
- `oxionnx-coreml`: `src/package/macos_impl.rs` gained two sibling modules — `array_read.rs` (output extraction: the layout planner, `read_raw_bytes`, `tensor_from_multi_array`) and `compile_cache.rs` (the on-disk compile cache) — keeping every file well inside the project's per-file line-count policy.
- `oxionnx-coreml`: the model-backed `#[ignore]`d tests in `src/package/tests.rs` now resolve their bundle from `OXIONNX_COREML_TEST_MODEL` (falling back to the historical `/tmp/w600k_r50.mlpackage`) and skip gracefully when it is absent, instead of hardcoding that absolute path and `.expect()`-panicking. One exported variable now drives every model-gated test in the crate.
- perf: **`oxionnx`: a measured two-tier size gate now decides whether a node is worth dispatching to the GPU at all, closing regressions of up to 36x that weight residency alone would have reopened.** `examples/r3a_inswapper_residency.rs` ran all 154 of InSwapper's nodes twice on an M3 — CPU-only, then `Auto` with every op wired and no floor beyond each kernel's own — and found only 3 of 10 dispatched op types actually win on the GPU (`Conv` at 0.44x of CPU time, `Pad` 0.26x, `Resize` 0.28x) while the rest lose, several by more than an order of magnitude (`Mul` 1.13x, `Add` 1.65x, `Gemm` 3.07x, `OxiInstanceNorm` 3.30x, `LeakyRelu` 18.82x, `Div` 28.61x, `Relu` 36.60x). The cause is structural: a memory-bound elementwise op moves the same bytes across the bus twice (upload, then read back) at a measured ~5 GiB/s effective round-trip bandwidth against a rayon-parallel CPU kernel touching them once at ~92 GB/s, plus a fixed ~1.4 ms per-dispatch cost — both sides scale linearly in element count, so no size threshold fixes it while operands are transferring. `gpu_min_transfer_elements` (new, `src/session/gpu_residency.rs`) therefore gates each op by which `ResidencyTier` its dispatch is actually in: under `ResidencyTier::Transferred` the structural losers get `MEMORY_BOUND_TRANSFER_FLOOR = Some(usize::MAX)` — never dispatch while operands cross the bus, a measured claim rather than a tunable — while `Conv`/`Pad`/`Resize`/`MatMul`/`Gemm` keep no session-level floor, since they measured as winners (or, for `Gemm`, have a distinct cause, below). Once every one of a node's operands is already device-resident (`node_residency_tier` requires *all* of them — a `Conv` whose weight is cached but whose activation still uploads does not qualify), the two-times-transfer cost disappears and the same elementwise kernels become worthwhile at any size that fills one workgroup: `RESIDENT_DISPATCH_FLOOR = 256` elements, replacing a previously uncalibrated `4096` chosen by analogy with the wrong cost model. Separately, `GEMM_GPU_MIN_FLOPS` (10 MFLOP) now gates `Gemm` ahead of `gpu_gemm_nt`, which has no minimum-size threshold of its own: InSwapper's 12 AdaIN heads (`[1,512] x [2048,512]ᵀ`, 2.1 MFLOP each, measured 3.07x slower on the GPU) now decline, while ArcFace's 25.7 MFLOP head still dispatches. The FLOP arithmetic behind these gates was also widened from `usize` to `u64` in `oxionnx-gpu/src/compute.rs`'s `GPU_THRESHOLD`/`gemm_flops`: on a 32-bit `usize` target the product for a 2048³ GEMM (8.6 GFLOP) overflowed and silently declined exactly the large multiplies the threshold exists to admit — measured in Chrome as a 2048³ multiply completing in 0.0 ms with no device error, versus a 1024³ one dispatching normally in 7.7 ms.
- **`oxionnx-gpu`: the five pre-existing kernel families — elementwise, normalization, reduction, softmax, transpose — were converted to the same async-first, budget-checked buffer path the new kernel batch uses.** Every dispatch function's real implementation is now `gpu_x_async`, with `gpu_x` reduced to a `block_on_gpu` wrapper; ad hoc `device.create_buffer_init` calls and direct pool locking were replaced with shared `ctx.upload_buffer`/`ctx.pooled_buffer`/`ctx.staging_buffer` helpers; and a new `budget_admits` pre-flight check declines a dispatch outright when it would exceed the context's byte budget, rather than attempting the allocation and finding out. `GpuBufferPool::get_buffer` changed accordingly, from returning `wgpu::Buffer` to a fallible `Option<TrackedBuffer>`.

### Fixed

- fix: **wasm32-unknown-unknown CPU inference actually works now — previous releases only compiled.** Every prior "WebAssembly support" changelog entry (0.1.0, 0.1.4) described `cargo check`/`build --target wasm32-unknown-unknown` succeeding, which is not the same claim as a model running in a browser without panicking; it never did, for two independent reasons that both had to be fixed:
  - **`oxifft`'s default `threading` feature (rayon) is a `compile_error!` on wasm32.** `oxionnx-ops` depended on it via a plain `{ workspace = true }`, which always pulled the default feature set. Fixed by target-splitting the dependency — `[target.'cfg(not(target_arch = "wasm32"))'.dependencies] oxifft = { features = ["std", "threading"] }` (native parallel DFT unchanged) vs. `[target.'cfg(target_arch = "wasm32")'.dependencies] oxifft = { features = ["std"] }` — which additionally required the workspace-level `oxifft` entry to declare `default-features = false` itself: Cargo silently ignores a member's `default-features = false` override on a `workspace = true` dependency whose workspace-level entry leaves `default-features` unspecified (verified empirically; it only warns, threading stays on).
  - **`std::time::Instant::now()`/`SystemTime::now()` compile on wasm32-unknown-unknown but panic the instant either is called** — there is no OS clock without a JS shim, and no `cargo check`/`build` can catch a runtime-only panic. Both were called unconditionally from the model-load path (`session/loading.rs`'s parse/build/optimize/sort/plan stage timers), the per-node dispatch path (`session/run/{dispatch,sequential,parallel}.rs`), and the unseeded-PRNG fallback for `Random*`/`Bernoulli` ops (`oxionnx-ops/src/registry/random_ops/rng.rs`, `dsp/bernoulli.rs`) — all reachable from an ordinary `WasmSession::run_one` call with no GPU/threading feature involved. Fixed with a `time_compat` module in each crate (`oxionnx`, `oxionnx-ops`) that re-exports `std::time::{Instant, SystemTime, UNIX_EPOCH}` unchanged on every other target and swaps in `web-time`'s `Performance.now()`/`Date.now()`-backed drop-ins (identical API, including `Add<Duration>` and ordering) on wasm32-unknown-unknown; every call site now routes through it.
  - **`Session::run_async`/`spawn_run`/`block_on` start inference on a plain `std::thread`**, which compiles on wasm32-unknown-unknown (that target has no OS threads) but panics identically to the above the instant it is called. Nothing on the wasm-facing API (`WasmSession::run_one`/`run_multi`, which call `Session::run` synchronously) ever referenced it, so `session::async_run` — and its `block_on`/`RunFuture`/`RunHandle` re-exports — is now `#[cfg(not(target_arch = "wasm32"))]` in full: a caller reaching for it on that target gets a compile-time "not found" instead of the previous runtime panic. `gpu_owner`'s `atexit`-hook quiescence check (native-only regardless, see below) no longer references the now wasm32-absent worker-count query on that target.
  - Two long-dormant wasm32-only warnings surfaced once the above unblocked the build far enough to reach them (both pre-existing, unrelated to this fix, simply never reachable before): an unused `total_jobs` in `oxionnx-ops`'s N-D convolution (computed only for the rayon path, which does not exist on wasm32) and a never-called `SdpaJob::macs_per_slice` in the attention kernel (same rayon-threshold shape) — both now `#[cfg(not(target_arch = "wasm32"))]`-gated alongside the code that used them.
  - `pollster` (the `gpu` feature's native blocking executor for `wgpu` adapter/device requests) moved from an unconditional optional dependency to `[target.'cfg(not(target_arch = "wasm32"))'.dependencies]`, matching `oxionnx-gpu`'s own `Cargo.toml`, so a future wasm32+`gpu` build does not carry a dependency that cannot do anything useful there.
  - `scripts/check_wasm.sh` added: builds `oxionnx` for wasm32 with the `wasm` feature and `cargo check`s `oxionnx-ops`, the two crates this fix touches.
  - **`--target wasm32-unknown-unknown --features wasm,gpu` now builds.** It did not as of v0.1.5: wgpu's WebGPU backend holds internal `Rc`/`RefCell`/`Cell` state (`WebDevice`, `WebBuffer`, …), which is not `Send`/`Sync`, while this crate's `Session: Send + Sync` compile-time assertion was deliberately unconditional across every feature combination, including wasm32. Fixed by adding a real async WebGPU execution path and exempting the assertion where it can no longer hold: `GpuContext::try_new_async()` now acquires a real `wgpu::Backends::BROWSER_WEBGPU` adapter (previously it returned `None` unconditionally on wasm32, since every kernel ended in a blocking read-back the browser can never complete); the GPU kernels' read-back is now an `async fn` awaiting a genuine `map_async` promise (`device_guard::read_back_web`, `#[cfg(target_arch = "wasm32")]`-gated); and `Session::enable_gpu_async()` / `Session::run_gpu_async()` are the entry points that drive them, with the `Session: Send + Sync` assertion (`src/session/mod.rs`) now `#[cfg(not(target_arch = "wasm32"))]`-gated to allow it. This is proven on native targets only so far: `pollster::block_on`-driven tests (`src/session/run/sequential_async.rs`'s own tests, `src/session/gpu_dispatch_tests.rs`, `src/session/tests/gpu_activation.rs`, `src/session/tests/gpu_f16.rs`) exercise the async entry points and run loop, but on native `enable_gpu_async` takes the blocking `try_new` branch and read-back goes through the synchronous `read_back_blocking`, never `BROWSER_WEBGPU` acquisition or `read_back_web` — those two browser-specific pieces have zero runtime coverage anywhere. The wasm-bindgen-exported `WasmSession` (`src/wasm.rs`) has no GPU/async-GPU references at all and its `run_one`/`run_multi` still call the synchronous `Session::run`, which continues to correctly decline every GPU node on wasm32; there is also no `wasm-bindgen-test` anywhere in this repo, and `scripts/check_wasm.sh` still only builds/checks the CPU-only `wasm` feature. Wiring `enable_gpu_async`/`run_gpu_async` into `WasmSession` and validating against a real browser remains open work (tracked in `TODO.md` §8). `gpu_owner`'s `atexit`-based quiescence hook (POSIX `atexit(3)`, declared via a raw `extern "C"` block) is separately native-only and was already unreachable there before this fix.
- fix: **`oxionnx-gpu`: the reusable GPU buffer pool no longer leaks device memory without bound, and every buffer this crate allocates is now checked against a live-byte budget before creation.** Dropping a `wgpu::Buffer` frees nothing on the WebGPU backend — `WebBuffer`'s `Drop` impl is empty (`wgpu` 29.0.4) — so `GpuBufferPool` (`oxionnx-gpu/src/context/tracker_pool.rs`) evicting its LRU entries by dropping bare `wgpu::Buffer`s was bookkeeping and nothing more there: memory kept accumulating until `createBuffer` started failing, which is unrecoverable inside wgpu's web backend. `TrackedBuffer` and `GpuMemoryBudget` (new, `oxionnx-gpu/src/context/budget.rs`) fix this at the allocation boundary: every buffer this crate creates is now a `TrackedBuffer`, which calls `wgpu::Buffer::destroy` and releases its reservation on drop (safe on both backends once `queue.submit` has run), and every allocation reserves its bytes against a shared `GpuMemoryBudget` first, declining rather than allocating once the ceiling (`DEFAULT_LIVE_BYTE_BUDGET`, 1.5 GiB, sized for a browser tab sharing one GPU process with every other tab) would be exceeded — introspectable and adjustable via the new `GpuContext::{live_gpu_bytes, gpu_byte_budget, set_gpu_byte_budget}`. `GpuBufferPool::get_buffer` now returns `Option<TrackedBuffer>` instead of an unconditional `wgpu::Buffer`, and a new `reclaim_for` evicts idle pooled entries — budget-accounted exactly like in-use buffers, since both cost the same driver memory — before declining an allocation outright. Every allocation site in `oxionnx-gpu/src/compute.rs` (matmul, conv2d, im2col) moved from raw `device.create_buffer(_init)` calls onto budget-checked `GpuContext` helpers (`upload_buffer`/`alloc_buffer`/`staging_buffer`/`pooled_buffer`), closing the same hole at every call site rather than only inside the pool. Separately, binding a pooled buffer via `as_entire_binding()` could bind up to 2x the requested size (`GpuBufferPool::get_buffer` accepts anything within 2x to avoid churn), which could exceed `max_storage_buffer_binding_size` even though the requested size had already passed validation; `elementwise.rs`, `normalization.rs`, `reduction.rs`, `softmax.rs`, `transpose.rs`, and conv2d's GEMM output binding now all bind an exact-range `wgpu::BufferBinding` instead of the pooled buffer's full extent, matching the pattern the new `conv2d.rs` kernel used from the start; a new regression test, `a_reused_oversized_pooled_buffer_binds_at_its_requested_size`, locks it in.
- fix: **`oxionnx`: the GPU `Reduce*` dispatch path no longer wraps a negative axis into a huge `usize` or under-reports a fully-reduced tensor's rank.** `normalize_reduce_axes`/`normalize_single_reduce_axis` (`src/session/gpu_dispatch.rs`) used to compute `axis as usize` directly on a negative `i64`, so an axis of `-1` became `18446744073709551615` instead of `rank - 1`; an out-of-range axis now declines the whole dispatch back to the CPU kernel, which reports the error properly, instead of indexing off the end of the shape. Separately, `single_axis_reduce_shape` used to return `[1]` for a full reduction with `keepdims=0` on a rank-1 input, where ONNX (and NumPy: `np.sum(x, axis=0, keepdims=False).shape == ()`) specifies a genuine rank-0 scalar — so the same graph could report a different output *rank* depending only on whether the GPU arm accepted the node or the CPU kernel ran instead, with anything downstream driven by a `Shape` node then seeing one dimension too many; element count was identical either way, only the reported shape was wrong. Both are exercised by the shape/attribute gating unit tests in `gpu_dispatch_tests.rs`'s `gating_tests` module, which run without a live GPU adapter.
- fix: **`oxionnx-ops`: CPU Conv2D's im2col workspace is now capped instead of scaling unbounded with kernel volume × output area.** The column matrix is `[C/group * kH * kW, OH * OW]` `f32`, and for InSwapper's largest layers alone that is 302-411 MB per call, on top of resident weights — on `wasm32`, an unsatisfiable `WebAssembly.Memory.grow` aborts the module outright, and wasm linear memory never shrinks, so a single such request permanently raises the tab's memory ceiling. `conv2d.rs` now caps any single im2col workspace at 64 MiB (`IM2COL_WORKSPACE_MAX_BYTES`); above that, output columns are processed in blocks (`im2col_block`/`im2col_block_maybe_parallel`), each GEMM'd into its own slice of the destination via a new leading-dimension-aware `sgemm_*_ldc` path, with block boundaries rounded to whole output rows wherever possible (the native multi-threaded path still keeps one capped buffer per rayon worker, so aggregate peak is `workers × 64 MiB`, still far below `workers × the uncapped size`). The result is bit-identical to the unblocked path by construction — `matrixmultiply`'s GEMM accumulates with `N` as its outermost loop, so splitting `N` externally cannot change any element's accumulation order — and is asserted as such by new tests (`sgemm_column_blocks_are_bitwise_identical_to_one_call`, `blocked_im2col_matches_the_monolithic_path_bitwise`, `blocked_im2col_matches_on_the_inswapper_worst_shapes`), while convolutions already under the cap keep their exact prior code path and cost. The same cycle also extended the existing `wasm-threads`-gated rayon parallelism convention to `conv_nd.rs`'s N-D im2col path, which had previously hard-disabled rayon on every `wasm32` build regardless of that feature.
- fix: **`oxionnx-gpu`: the WGSL `reflect`-mode `Pad` kernel produced wrong values in the leading padding region on Vulkan/NVIDIA.** `reflect_coord`'s original formula computed a period-`2*(dim-1)` modulo in `i32` and corrected a negative result by adding the period back — `var c = (out_coord - pad_before) % period; if (c < 0) { c = c + period; }` — which assumes WGSL's `%` on a negative dividend returns a negative-or-zero remainder. On Vulkan with the NVIDIA driver (naga 29, driver 550) it does not: `-1 % 6` evaluated to `3` and `-2 % 6` to `2`, the unsigned results for the two's-complement bit patterns rather than a sign-of-dividend remainder, so the `c < 0` correction branch never fired and the leading pad region silently wrapped instead of reflecting (`out_coord=1`, `pad_before=2`, `dim=4` read source index 3 instead of the correct 1; a coincidental match at `out_coord=0` masked the bug there). Fixed by moving the whole reduction into `u32`, on an operand that is non-negative by construction (`raw >= 0` uses `u32(raw) % period` directly; `raw < 0` computes `u32(-raw) % period` and subtracts from `period`, safe because `-raw`'s magnitude is bounded by the tensor extent), so no branch depends on the sign behavior of `%` on a negative operand. `reflect_coord_host` (`k2_pad.rs`) states the same formula in plain Rust as the test oracle, cross-checked against literal NumPy `reflect`-mode output. `w3_gpu_kernel_parity.rs`'s adapter-availability panic message was also expanded to name a second failure mode particular to Linux: `GpuContext` requests `Backends::VULKAN` only there with no GL fallback, so a host with a working NVIDIA driver but a missing loader package (`libvulkan.so.1`, Debian/Ubuntu's separate `libvulkan1`) enumerates zero adapters and fails all 17 tests in the file, which reads as broken kernels rather than a missing system package.

## [0.1.5] - 2026-08-06

### Added

**Production-grade hardening program** — a 12-lens audit (spec conformance ×3, engine, proto
robustness, panics, GPU, CUDA/CoreML, stubs, API/release, performance, test gaps) covering the
whole workspace produced 232 findings, fixed as 3 waves of parallel implementation under strict
file-ownership partitioning. Highlights below; the full 16-domain breakdown lives in `TODO.md` §17.

- feat: **Async execution** — `Arc<Session>::run_async(inputs) -> RunFuture`, executor-agnostic (`.await` under tokio/async-std/smol, or drive it with the crate's own dependency-free `block_on`); `spawn_run()` returns a blocking `RunHandle` for callers with no async executor at all. The receiver is `Arc<Self>` because the background thread outlives the call.
- feat: **Cancellation tokens** — `SessionBuilder::with_session_cancellation(token)` makes every operator check a `CancellationToken` before it runs, unwinding with `OnnxError::Cancelled` at the first node boundary after `token.cancel()` (sequential path, rayon parallel path, and inside `If`/`Loop`/`Scan` bodies). Session-scoped by default; `GenerationConfig::with_cancellation` additionally gives independent per-generation cancellation on a shared session.
- feat: **Streaming token generation** — `session.generate(prompt, GenerationConfig)` returns a `TokenStream` that runs one forward pass per `next()`, feeding the model's `present.*` key/value outputs back in as the next step's `past.*` inputs; stops on EOS, a token cap, or cancellation. Greedy (argmax) selection only, by design — no sampler/RNG/tokenizer; `emit_logits` hands out the raw logits row for sampling outside the crate.
- feat: **Session serialization** — `session.save_optimized(path)` / `Session::load_optimized(path)` / `SessionBuilder::load_optimized(path)` persist the *post-optimization* graph (nodes, rewritten weight table, value-info, metadata, nested subgraphs) as a version-tagged, length-prefixed binary and reload it at `OptLevel::None`, so constant folding/CSE/fusion/dead-code-elimination never re-run (proven by counting operator executions during load: zero). Deterministic encoding — a cache file can be content-hashed; a truncated, foreign, or wrong-version file always fails as a typed `OnnxError::Parse`.
- feat: **ONNX local functions (`FunctionProto`) are inlined into the graph at load time** in both the eager parser and, closing a gap found in this program, the streaming parser — `load_streaming`/`stream_model_*` previously built their `GraphProto` without ever calling the inliner, so a model loaded through the streaming path failed with `UnsupportedOp` on any function call even though the same model loaded eagerly.
- feat: **Opset-aware execution plumbing** — the registry now carries the model's declared `ai.onnx` opset (parsed from `opset_import`), and `Softmax`/`LogSoftmax`/`Hardmax` branch on it instead of hardcoding opset-13+ semantics: a pre-13 model gets the spec's default-axis-1-plus-flatten-to-2D contract, opset 13+ gets the direct per-axis reduction it had all along. Previously every model ran the opset-13+ contract regardless of its declared opset, which is silently wrong on rank>2 tensors.
- feat: **Rank-generic `Conv`/`ConvTranspose`** — new N-D entry points (`conv::conv`, `conv::conv_transpose`) take `pads` in the ONNX `[begin_0..begin_{r-1}, end_0..end_{r-1}]` layout, so 1D and 3D convolutions now run through a real kernel instead of being hardcoded to 2D. The dedicated 2D fast path (`conv2d`, `conv_transpose2d`, im2col/Winograd) is untouched and keeps its exact public signature.
- feat: **Rank-0 (scalar) tensor support** — `Tensor`/`TensorView` now represent a true ONNX rank-0 result (`shape: vec![]`, one element) instead of every rank-reducing op silently promoting it to shape `[1]`; the core layer's `transpose`/`slice`/`select`/`to_tensor` handling of an empty shape (previously panicking or violating the data/shape invariant) is fixed accordingly. `Det` on a 2-D input and the two new loss ops (`reduction=mean|sum`) are the first operators to emit rank 0 end-to-end, confirmed to survive shape resolution and output-slot allocation, not just the raw kernel call. Most other rank-reducing ops (e.g. `Squeeze` to a scalar, an all-axes `Reduce*`) still promote to `[1]` — a known, tracked gap, not a regression.
- feat: **23 new / newly-registered operators**, taking the registry from 165 to 188 canonical `op_type`s (203 including aliases): `RNN` (implemented but never wired into the registry); the QOperator quantization family `QLinearConv`, `QLinearMatMul`, `MatMulInteger`, `ConvInteger`, `DynamicQuantizeLinear`; classic-CNN/vision ops `LRN`, `LpPool`, `GlobalLpPool`, `MaxUnpool`, `MaxRoiPool`, `Upsample`, `CastLike`; linear-algebra / reconstruction / cropping ops `Det`, `Col2Im`, `CenterCropPad`; generators `RandomNormal`, `RandomUniform`, `RandomNormalLike`, `RandomUniformLike`, `Multinomial`; and loss ops `NegativeLogLikelihoodLoss`, `SoftmaxCrossEntropyLoss`.
- feat: **`oxionnx-ops::einsum`: ellipsis (`...`) support and cross-operand label broadcasting** — the equation parser now handles numpy-compatible `...` tokens (e.g. `...ij,...jk->...ik` for broadcast batched matmul), right-aligning and broadcasting each operand's ellipsis axes exactly like numpy's leading-dimension rule, including when operands bind a different number of ellipsis axes. A named label shared across operands now broadcasts when one side's extent is `1` (matching `numpy.einsum`) instead of unconditionally erroring on any extent mismatch, which the previous implementation did even for legitimately broadcastable shapes. Large contractions now lower to `matrixmultiply::sgemm` through a greedy pairwise decomposition (`execute_pairwise`, batch/M/K/N bucketing per contraction step) instead of always running the scalar loop nest; contractions at or below `GENERAL_PATH_FLOP_LIMIT` (4096 multiply-accumulates) still use the direct scalar interpreter (`execute_general`), which stays allocation-free and doubles as the GEMM path's test oracle. The 315-line `einsum.rs` grew into an `einsum/` module directory (`mod.rs`, `parse.rs`, `operand.rs`, `contract.rs`, `tests.rs`; 35 tests) to hold this new logic. The matching `infer_einsum_shape` shape-inference pass (`src/optimizer/shape_inference_ext/advanced.rs`) had two bugs this made newly reachable: label extents were resolved first-writer-wins instead of broadcast (publishing an under-sized output slot once two different extents became legal for the same label), and `.` bytes were stripped from every subscript before parsing, so no ellipsis equation ever inferred a shape at all. Both are fixed and cross-checked against `numpy.einsum` and a real `Session::run` of the same node in a new dual-mode integration test, `tests/w3_einsum_shape_inference.rs` (20 cases).
- feat: `#[non_exhaustive]` on the workspace's public error/config enums (`OnnxError`, `CoreMLError`, `CudaError`, the DirectML error type, `oxionnx-proto`'s reader error, and the execution-provider enum), so new failure modes can be added without a semver break — downstream exhaustive `match`es on these types now need a wildcard arm.
- feat: **`oxionnx-coreml`: `predict_raw()` — dtype-preserving inference** — `MlPackageModel::predict_raw()` returns each output's contents verbatim (no `Float16` → `f32` up-conversion), via new portable `RawArray { shape, dtype, data }` / `MlArrayDtype` (`F32`/`F16`/`F64`/`I32`/`I8`) types, for CoreML→CoreML pipelines that need the exact bytes an upstream model produced. Dtype coverage matches `predict()`'s today (`F32`/`F16`; other source dtypes raise `UnsupportedOutputDtype`).
- feat: **`oxionnx-coreml`: `predict_features()` — non-`MLMultiArray` outputs** — `MlPackageModel::predict_features()` surfaces *every* declared output through a new `FeatureOutput` enum (`MultiArray`/`Image`/`Sequence`/`Dictionary`/`String`/`Int64`/`Double`), where `predict()` only ever accepted `MLMultiArray`-shaped outputs and rejected everything else with `MissingOutput`. `Image` outputs decode from their backing `CVPixelBuffer` for four standard pixel formats (`OneComponent8`, `32BGRA`, `OneComponent16Half`, `OneComponent32Float`); `Sequence` outputs decode via new `SequenceValue::{Int64, String}`. Two new error variants — `CoreMLError::UnsupportedFeatureType` and `CoreMLError::UnsupportedPixelFormat` — cover unrepresentable feature types and unrecognized pixel formats respectively.
- feat: **`oxionnx-coreml`: `compute_plan_breakdown()` — per-operator device-placement histogram** — `MlPackageModel::compute_plan_breakdown() -> HashMap<String, ComputePlanSummary>` reports ANE/GPU/CPU/unknown op counts keyed by `operatorName` (e.g. distinguishing several `gather` occurrences scattered across the graph), sharing its traversal with `compute_plan_summary()` so per-operator sums always reconcile with the flat total. New public `ComputePlanSummary::merge()` accumulation primitive backs both.
- feat: **`oxionnx-coreml`: `model_metadata()`** — `MlPackageModel::model_metadata() -> HashMap<String, String>` walks `MLModelDescription::metadata()` for `author`/`license`/`version`/`description`, plus `creator.`-prefixed custom creator-defined keys. Every key is optional; a model with no metadata returns an empty map rather than an error.
- feat: **`oxionnx-coreml`: iOS / tvOS / visionOS platform support** — the CoreML runtime (previously macOS-only) now builds for every Apple platform CoreML itself ships on, behind `cfg(any(target_os = "macos", target_os = "ios", target_os = "tvos", target_os = "visionos"))`. macOS and `aarch64-apple-ios` are compile-validated directly by this crate; tvOS/visionOS are Rust tier-3 targets sharing the identical CoreML API surface.
- feat: concurrency stress test — `oxionnx-coreml/tests/concurrent_predict.rs` (the crate's first `tests/` integration-test file) drives 8 threads × 20 `predict()` calls each against one shared `Arc<MlPackageModel>`, exercising the documented `Send + Sync` contract end-to-end for the first time; reads the model bundle path from `OXIONNX_COREML_TEST_MODEL` and skips gracefully when unset instead of hardcoding an absolute path.
- feat: **`oxionnx-directml`: real D3D12/DirectML backend, replacing the Windows skeleton** — the crate grew from a 4-module scaffold (`context`/`dispatch`/`error`/`kernels`, claiming only `MatMul`/`Add`/`Mul`/`Relu`/`Sigmoid`) into a full execution provider: Windows-only FFI glue for genuine D3D12 device/buffer/PSO/shader management (`backend/d3d12/{device,buffer,pso,shader,hlsl_backend}.rs`) and the DirectML metacommand API itself (`backend/dml/{device,binding,tensor,op,dml_backend}.rs`), selected per-context as `BackendKind::DirectMl` (genuine `IDMLDevice` operators, resolved via `LoadLibraryW`/`GetProcAddress` rather than statically linked, so a missing `DirectML.dll` can't stop the host process from launching) or `BackendKind::Hlsl` (D3D12 compute shaders `D3DCompile`'d at run time when DirectML itself isn't present). New dedicated kernels (`kernels::{conv,softmax,reduce}`) join a rewritten `matmul`/`elementwise`, all built on a platform-neutral planner (`plan.rs` + `plan/nn.rs`: shape validation, dispatch-grid math, buffer sizing) and DirectML tensor-descriptor layout math (`layout.rs`, including the non-obvious `TotalTensorSizeInBytes` computation for broadcast/0-stride tensors) that compiles and unit-tests on every target, not only Windows. The router now claims `MatMul`, `Gemm`, `Add`/`Sub`/`Mul`/`Div`, `Relu`/`Sigmoid`/`Tanh`, `Softmax`, `ReduceSum`/`ReduceMean`/`ReduceMax`/`ReduceMin` and `Conv` — each restricted to exactly the shapes the backend can express correctly rather than guessed (`MatMul` is 2-D×2-D only, the binary ops require identical operand shapes, `Conv` only handles explicit `NOTSET` `auto_pad`, `Reduce*` only a single resolved axis); anything wider declines to `Ok(None)` and the tuned CPU kernel runs instead. The `windows` FFI dependency stays target-gated behind `cfg(target_os = "windows")`; `stub_backend.rs` keeps every non-Windows target on the same permanently-`None`-returning, `Send + Sync` `Backend` type it always used, so CPU fallback on macOS/Linux/WASM is unchanged.
- feat: **`oxionnx-directml`: CPU reference oracle and hardware self-check — but the GPU path itself remains unverified in this repository** — `reference.rs`/`reference/nn.rs` reimplement every claimed op as a scalar loop with a documented per-op tolerance (bit-exact for `Add`/`Sub`/`Mul`/`Relu`, ~1 ULP for `Div`, ~1e-6 for `Sigmoid`/`Tanh`, `sqrt(K)`-scaled for `MatMul`/`Gemm`) and are themselves cross-validated against `oxionnx-ops`'s tuned CPU kernels in the new `tests/reference_vs_ops.rs`, which pulls `oxionnx-ops` in only as a `[dev-dependencies]` entry — never a runtime dependency of the execution provider it exists to bypass. The new `examples/directml_self_check.rs`, driving `DirectMLContext::self_check()`, runs the real GPU path against this oracle element-by-element and reports a typed exit code (`0` pass, `1` a claimed op disagreed with the oracle, `2` no context was available so nothing was tested, `3` the GPU path itself failed before producing a result to compare) — this is the only mechanism able to validate the Windows-only FFI under `backend/d3d12`/`backend/dml`, and this crate's own README states plainly that path has never been executed on real hardware in this project: the platform-neutral modules (`plan`, `layout`, `reference`, `hlsl`) are unit- and property-tested on non-Windows hosts, while the Windows FFI glue is compile- and lint-checked only, via a cross-compiled `cargo clippy --target x86_64-pc-windows-gnu`, and has never been linked or run. Every activation is opt-in and off by default even on Windows: `OXIONNX_DIRECTML=1` to acquire a device at all, `OXIONNX_DIRECTML_VERIFY=1` to shadow-compare every dispatched op against the oracle at runtime (the same pattern as `oxionnx-cuda`'s `OXIONNX_CUDA_VERIFY`), `OXIONNX_DIRECTML_STRICT=1` to turn a kernel failure into a hard error instead of a silent CPU fallback, and `OXIONNX_DIRECTML_ALLOW_WARP=1` to permit the software adapter so `self_check` can run on a GPU-less Windows VM.

### Changed

- perf: **CPU performance package** — small-M `MatMul` routed through `sgemm` instead of a scalar loop; attention/flash-attention parallelized and also routed through `sgemm`; KV-cache append is now in-place instead of reallocating; broadcast operands avoid materializing the expanded tensor; convolution gained threading, cache-blocked im2col retains a Winograd filter-transform cache across calls, transpose/reduce walk strides instead of copying into a temporary, hot maps switched to `hashbrown`, and the run loop dropped several per-node clones. Reproducible wall-clock deltas are deliberately not published here: this workspace was built under roughly a dozen concurrent agents with 2-3x timing noise between identical runs, and the throwaway A/B harnesses used to validate each change against noise (not just eyeballing an end-to-end number) were deleted after measuring — only the resulting parity/regression tests remain in the suite.
- perf: **GPU performance** — the device is now asked for its own reported limits instead of accepting wgpu's conservative defaults (which forced ordinary-sized tensors onto the CPU path unnecessarily); `Softmax` uses a single workgroup-level-reduction pipeline instead of two passes (see the pipeline merge below); the buffer pool gained a 256 MiB byte budget with LRU eviction (previously bounded only by buffer *count* while evicting the *smallest* buffer first, so a segmentation model's largest activations could pin several GB of untouched VRAM with no eviction over time); `Conv` reuses im2col upload buffers across a chunked dispatch within a byte/iteration budget instead of holding all of them live at once (an earlier "encode everything into one command buffer" attempt measured as a *regression* under real memory pressure and was not shipped); wasm32 GPU readback behavior changed — see Fixed.
- **`oxionnx-gpu` public API**: `GpuTensorTracker` and `GpuContext::tracker` removed — the type's docs promised "data can remain on the GPU between operations without being read back," but nothing in the workspace ever called `store`/`take`/`is_on_gpu` outside its own unit test, so it was dead code documenting a capability the engine did not actually have. `GpuContext::softmax_pass1_pipeline` + `softmax_pass2_pipeline` merged into a single `softmax_pipeline`. `GpuBufferPool::return_buffer` no longer takes a `size` parameter. New `DEFAULT_POOL_BYTE_BUDGET` constant (256 MiB). All re-exported through the root crate via `pub use oxionnx_gpu::*`; acceptable at 0.1.x.
- perf: `oxionnx-coreml`: `predict()`/`predict_raw()` input construction (`multi_array_from_f32`) now builds each `MLMultiArray` by aliasing the caller's `Tensor::data` slice directly (`initWithDataPointer_shape_dataType_strides_deallocator_error` with `deallocator: None`) instead of allocating CoreML-owned storage and `copy_nonoverlapping`-ing into it — removes one allocation and one copy per input, per `predict`/`predict_raw` call.
- `oxionnx-coreml/src/package.rs` (886 lines) split into a `package/` module directory (`mod.rs`, `macos_impl.rs`, `stub_impl.rs`, `tests.rs`, `stub_tests.rs`) to stay under the project's per-file line-count policy; pure relocation, no behavior change.
- Workspace: `oxionnx-coreml` and `oxionnx-directml` now inherit their Apple FFI (`objc2`, `objc2-foundation`, `objc2-core-ml`, `objc2-core-video`, `block2`) and Windows (`windows`) dependency versions from new `[workspace.dependencies]` entries (`workspace = true`) instead of hardcoding per-crate versions.
- Updated CUDA / OxiCUDA dependencies from 0.1.8 to 0.5.3 (`oxicuda-driver`, `oxicuda-memory`, `oxicuda-blas`, `oxicuda-dnn`, `oxicuda-ptx`, `oxicuda-launch`).
- `oxifft` updated from 0.3.2 to 0.4.2 — see Fixed for the correctness fix this brings to `DFT`/`STFT`.
- `aes-gcm` updated from 0.10 to 0.11; `encrypt_bytes`/`decrypt_bytes` (`oxionnx/src/encryption.rs`) updated for the new `Nonce` construction API (`<&Nonce<_>>::try_from(...)`), now surfacing a clean error instead of relying on `Nonce::from_slice`'s panic-on-mismatched-length behavior.

### Fixed

**Parser hardening** (`oxionnx-proto`):
- fix: length-delimited field reads now check `pos + len` against the buffer everywhere — both the eager and streaming parsers had independently reimplemented this unchecked (the streaming reader also had one fully unguarded slice) — instead of panicking on truncated or malformed `.onnx` bytes.
- fix: nested subgraph attributes are now depth-limited instead of allowing unbounded recursion, which could stack-overflow the process on a crafted model.
- fix: lengths read from untrusted model bytes are clamped before sizing an allocation (`vec![0u8; len]` in the streaming reader could previously OOM-abort on a hostile length).
- fix: `TensorProto` typed-data fields now use the correct field numbers and element encodings; non-packed (unpacked) repeated numeric fields, previously silently dropped, are now read; group wire types (3/4) are skipped instead of aborting the whole parse; the varint decoder no longer silently truncates bits above 64 on a 10-byte varint; `TensorProto.segment` is read instead of ignored.
- fix: `GraphProto.sparse_initializer`/`value_info` and `AttributeProto.tensors`/`graphs`/`sparse_tensor`/`type_proto`/`ref_attr_name` are now parsed instead of being unhandled.
- fix: weight decode for uint8/int8/bool/double/bfloat16 initializers no longer silently zero-fills; `STRINGS`-list attributes are wired through graph conversion instead of dropped; external-data `location` is sandboxed against path traversal / absolute-path reads, and its offset/length arithmetic is checked instead of panicking on a reversed or overflowing range; negative or overflowing `dims` are rejected with a typed error instead of being cast to a huge `usize`.

**Spec-conformance fixes** (`oxionnx-ops`):
- fix: `Slice` honors negative `start`/`end`/`step` values instead of ignoring them.
- fix: `Pad` accepts the opset-18 `axes` input (previously panicked), applies negative pads as a real crop (previously clamped to 0), and implements `wrap` mode instead of silently falling back to `constant`.
- fix: `Resize` implements real cubic interpolation (previously silently degraded to nearest-neighbor), honors all `nearest_mode` variants including `round_prefer_floor` (previously always floored, shifting the image by half a pixel), and handles `coordinate_transformation_mode` values it previously ignored (`tf_crop_and_resize`, `tf_half_pixel_for_nn`).
- fix: `GRU` with `linear_before_reset=0` — the ONNX **default** — applied the reset gate at the wrong index; opset-conformant models relying on the default now compute the correct gate.
- fix: `ScatterElements`/`ScatterND` implement the `reduction` attribute (opset 16 `add`/`mul`/`max`/`min` — previously always overwrote regardless of the attribute) and bounds-check indices, including negative indices counted from the end (previously written to row 0).
- fix: `QuantizeLinear`/`DequantizeLinear` honor the `axis` attribute for per-channel scale/zero-point broadcast (previously mathematically wrong for any axis but the last) and derive the saturation range from the target dtype instead of hardcoding an int8 `[-128, 127]` clamp, which corrupted the more common default uint8 case.
- fix: `TreeEnsembleClassifier`/`Regressor` guard against cyclic node tables instead of looping forever on a malformed or adversarial real tree model, route NaN inputs per `nodes_missing_value_tracks_true`, and implement `MIN`/`MAX` aggregate functions (previously silently treated as `SUM`).
- fix: opset-aware `Softmax`/`LogSoftmax`/`Hardmax` semantics — see Added.
- fix: **`DFT`/`STFT` (`oxionnx-ops/src/dsp/dft.rs` + `stft.rs`, both built on `oxifft::fft`/`ifft`) inherit an upstream x86_64 correctness fix** — bumping `oxifft` 0.3.2 → 0.4.2 pulls in a fix for two bugs in the SSE3 SIMD butterfly kernel that every x86_64 CPU without AVX2 dispatched to: a wrong complex-multiply lane order, and twiddle-recurrence drift that broke Bluestein's chirp round-trip for non-power-of-two lengths. On such hosts, `DFT`/`STFT` outputs were wrong by orders of magnitude — a wrong-answer bug, not a precision one. aarch64 (NEON) and AVX2+ x86_64 went through a different, unaffected kernel throughout and were never wrong. This is an upstream fix, not independently re-verified against this repository's own test suite — see `oxifft`'s `CHANGELOG.md` for the root-cause writeup, including its stated caveat that the AVX2/AVX-512 tiers remain hand-audited but have never been executed on real hardware there.
- fix: dozens of additional panics-on-malformed-input closed across shape/reduce axis normalization, `Concat`/`Split`/`Transpose`/`Unsqueeze`/`Expand`/`Reshape` bounds checks, `GridSample`/`RoiAlign` string-attribute parsing (previously read as ints, silently keeping the default), SDPA mask broadcast, `Attention`'s `is_causal`, `LSTM`/`GRU`/`RNN` `clip`/`layout`, `Gelu`/`Clip`/`Cast`/`Mod`/`Bitwise*`/`Dropout`/`GroupNorm` attribute handling, `TfIdfVectorizer` `ngram_counts` + batched input, SVM Platt scaling, and 0-size-dim (empty tensor) handling across the engine — full 16-domain list in `TODO.md` §17 wave 1.

**GPU / CUDA / DirectML hardening**:
- fix: `oxionnx-gpu` dispatch now declines to the CPU path instead of silently producing wrong results on shapes its kernels don't actually support — MatMul dropping batch dimensions, Softmax ignoring `axis`, reduction ignoring `keepdims`, elementwise ops gating on element count instead of shape equality, and fused Conv discarding the optimizer's fused activation are all closed; three previously-disagreeing op-support lists are unified so implemented GPU kernels are actually reachable from the `Auto` placement path.
- fix: `oxionnx-gpu` backend dispatches are clamped to the 65535 `max_compute_workgroups_per_dimension` limit and checked against `max_buffer_size` instead of panicking on large tensors/weights; every wgpu call now runs inside an error scope with CPU fallback instead of panicking on a validation/OOM/device-lost error; blocking readback has a timeout instead of being able to hang forever; `LeakyRelu`'s WGSL kernel reads the node's `alpha` attribute instead of hardcoding `0.01`.
- fix: **wasm32 GPU context creation now honestly declines** (`GpuContext::try_new`/`try_new_async` return `None`) instead of uploading inputs, allocating buffers, encoding a dispatch, and calling `queue.submit` for every node and then discarding the result — blocking `map_async` readback is impossible in the browser, so the previous path was pure overhead that could never produce a value while the CPU silently recomputed the same op. The CPU path now runs directly with no wasted GPU work.
- fix: `oxionnx-cuda` — batch-broadcast `MatMul` operands (e.g. 3-D activations × 2-D weight), `ReduceSum`/`ReduceMax` silently truncating to the first 256 elements of the axis, `Softmax` ignoring the ONNX `axis` attribute, `LeakyRelu`/`HardSigmoid` ignoring the node's `alpha`/`beta`, and `Gemm`'s bias epilogue dropping bias for shapes other than `[N]`/`[M,N]` are all fixed. An `OXIONNX_CUDA_VERIFY` shadow-comparison gate was added, since unlike `oxionnx-directml` this crate previously had no opt-in verification gate and these bugs shipped silently to every CUDA user by default.
- fix: **`decide_placement` is now the single source of truth for CPU/accelerator routing** — its `OpPlacement::Auto` arm previously only ever weighed the wgpu-flavored `is_gpu_capable` against `gpu_threshold_bytes` and could only return `ProviderKind::Gpu` or `Cpu`; `Cuda`/`DirectMl` were reachable only through an explicit `OpPlacement::Manual` pin, never through `Auto`'s size-based heuristic. `Auto` now calls a new `select_accelerator(op)`, which consults each backend's own op-support predicate in priority order `Cuda > DirectMl > Gpu` (`oxionnx_cuda::is_supported_op` / `oxionnx_directml::is_supported_op` / `is_gpu_capable`) and applies `gpu_threshold_bytes` uniformly to whichever it picks — so, for example, `Conv` is never routed to CUDA (which has no CUDA convolution kernel; see below), even though wgpu's `is_gpu_capable` claims `Conv`. `OpPlacement::Manual` pins now also pass through a new hard floor, `MIN_GPU_DISPATCH_BYTES` (4096 bytes = one page = 1024 `f32` elements): an accelerator pin below the floor is overridden back to `Cpu`, since the fixed cost of a discrete-GPU round trip (~20 µs of upload/dispatch/fence/readback) outweighs the entire cost of a CPU elementwise op that small by more than an order of magnitude. New `provider_supports_op(provider, op)` exposes the per-backend predicate directly.
- fix: **`oxionnx-cuda`: dead, silently-wrong `Conv` GPU implementation removed** — `cuda_conv` previously contained a real `oxicuda_dnn::conv::api::conv_forward` NCHW code path (device upload, descriptor construction, bias epilogue) that was already permanently unreachable behind a bare `if true { return Ok(None); }`, because `oxicuda-dnn`'s convolution engines have stubbed GEMM phases and produced numerically wrong results; the dead code is deleted and `cuda_conv` is now an honest, documented, unconditional decline. New `oxionnx_cuda::is_supported_op(op)` reports exactly the 25 ops `try_cuda_dispatch` can claim (`MatMul`/`Gemm`, 16 unary activations, `Add`/`Sub`/`Mul`/`Div`, `ReduceSum`/`ReduceMax`, `Softmax`) and `false` for `Conv` and everything else, so the placement logic above can skip CUDA before paying for an upload/dispatch/readback instead of discovering the decline only after a round trip.

**Core hardening**:
- fix: `oxionnx-core`'s advertised `no_std` support previously failed to build; it is repaired and now compile-verified (`cargo check -p oxionnx-core --no-default-features`).
- fix: `encrypt_bytes` derived AES-GCM nonces from wall-clock time instead of a CSPRNG; nonces are now CSPRNG-sourced.
- fix: topological sort could underflow `in_degree` (a `usize`) when a node's output name collided with a known input/initializer name, or where `in_degree`/`dependents` were built under inconsistent conditions.
- fix: `Tensor::new` only validated `data.len() == shape.iter().product()` in debug builds; the new fallible `Tensor::try_new` validates unconditionally, including release builds, and returns a typed `OnnxError::ShapeMismatch` instead of risking a panic or silent corruption from untrusted model bytes. `Tensor::new`'s signature and infallible behavior are unchanged for callers who can guarantee the invariant statically.
- `deny.toml` added at the workspace root, enforcing the COOLJAPAN banned-crate list via `cargo deny check bans`.
- fix: `oxionnx-proto`: `AttributeProto.strings` (`repeated bytes strings = 9`) was re-parsed as a nested protobuf message instead of being read as a raw string, corrupting ONNX-ML `TreeEnsemble` models whose `nodes_modes` values (e.g. `"BRANCH_GTE"`) happen to start with bytes that decode as a plausible tag + length prefix — raised a spurious `length-delimited: EOF (need 82, have 8)` error. Field 9 entries are now pushed verbatim, with a regression test covering the reported case (#3).
- fix: `oxionnx-coreml`: `compute_plan_summary()` only walked the `MLProgram`'s `"main"` function, silently under-counting models that declare additional functions (e.g. stateful submodels). Both `compute_plan_summary()` and the new `compute_plan_breakdown()` now iterate every declared function.

---

## [0.1.4] - 2026-06-08

### Added
- feat: **Subgraph parsing + end-to-end execution for `If`, `Loop`, `Scan` operators** — ONNX models containing control-flow nodes now parse and execute correctly from real `.onnx` files. Previously the `If`/`Loop`/`Scan` kernels existed and passed in-memory unit tests but were never reachable from a loaded model due to two disconnects:
  1. `oxionnx-proto`: `AttributeValue` now carries `g: Option<Box<GraphProto>>` for graph-valued attributes (wire field 6); `parse_attribute` parses nested subgraphs via the existing `parse_graph` recursively (both eager and streaming parser paths fixed simultaneously). `build_subgraph` in `model.rs` converts a nested `GraphProto` into a runtime `Graph`, synthesising `Constant` nodes for subgraph-local initializers without touching the 48-literal `Graph` struct.
  2. `oxionnx-core`: `OpContext` gains a `weights: Option<&'a HashMap<String, Tensor>>` field so model weights can be accessed by subgraph nodes without cloning. `Session::dispatch_node` now populates `outer_scope`, `weights`, and `registry` in all three dispatch paths (slot-write, inplace, default-execute) and the parallel rayon path.
  3. `oxionnx-ops` control-flow kernels (`IfOp`, `LoopOp`, `ScanOp`) pass `ctx.weights` through to `execute_subgraph`, giving subgraph nodes access to outer-model initializers at zero allocation cost.
- feat: 9 new end-to-end integration tests (`tests/control_flow_e2e.rs`): `If` true/false branch, `Loop` accumulation, `Loop` zero-iteration, `Loop` with outer-scope weight, `Scan` element-wise map, `Scan` with running-state, `If` with sequential subgraph ops, `If` with subgraph-local initializer.
- feat: 2 new `oxionnx-proto` unit tests: `test_subgraph_attribute_parsed` (parser round-trip), `test_subgraph_attribute_wired_into_graph` (build_subgraph populates Attributes.graphs).
- perf: **Phase F.13 complete** — zero-copy `execute_into_slots` for `AttentionOp` and `MultiHeadAttentionOp`, the last two ops in the Phase F sweep. Backed by `sdpa_into` / `sdpa_output_shape` / `reshape_from_heads_into` / `multi_head_attention_into` in `attention/core.rs` (allocation-free kernels; existing public APIs unchanged). The `no-outproj` MHA path now skips the `concat` allocation entirely via `reshape_from_heads_into`; the `with-outproj` path writes the projected output directly into the caller's pooled buffer. Phase F slot-write sweep closed at 58 of 121 ops.
- fix: SIMD path in `scaled_dot_product_attention` did not zero `row_out` between query rows — `weighted_sum_v` accumulates, so for `seq_q > 1` with the `simd` feature enabled the result was incorrect. The extraction into `sdpa_into` naturally fixed this by writing directly into per-row slices of the caller buffer (no shared `row_out` temp at all).
- feat: 21 new tests in `oxionnx-ops/tests/output_slots_attention_test.rs`: AttentionOp and MultiHeadAttentionOp slot-write correctness vs `execute`, broadcast mask, custom scale, `seq_k ≠ seq_q`, 4D Q, second-call resize (grow and shrink), qkv-projection, out-projection with/without bias, error parity, batch > 1.

- perf: **Phase F.14 complete** — zero-copy `execute_into_slots` for 47 additional operators, bringing the total to 105 of 121 ops with hand-coded slot-write bodies. True zero-copy (no output Vec allocation): LayerNorm/GroupNorm/BatchNorm/RmsNorm/InstanceNorm/Softmax/LogSoftmax via new `_into` kernel variants in `nn/normalization.rs`; Not/IsInf/IsNaN/BitwiseNot/Shape/Size/Constant/ConstantOfShape inline into slot; CumSum inline (same-size as input). Reduced-allocation: ReduceSum/Mean/Max/Min/Prod/L1/L2/LogSum/LogSumExp/SumSquare via new `reduce_with_into`+`reduce_output_shape` in `math/reduce.rs`; ArgMax/ArgMin via `arg_reduce_into`; TopK (2 outputs) via `top_k_into`; variadic ops and comparison/bitwise binary ops via macro update (call+move avoiding Vec<Tensor> wrapper). 41 new tests in `output_slots_f14_test.rs`.
- feat: `NodeInfo` and `Session::nodes()`: public per-node graph introspection — enumerate a model's compute nodes (name, op type, inputs, outputs, and a deterministic attribute summary) in topological order; `NodeInfo` re-exported as `oxionnx::NodeInfo` (#2).
- `ReshapeOp` now reads the ONNX `allowzero` attribute: when `allowzero=0` (default) a `0` in the target shape copies the corresponding input dimension; when `allowzero=1` a `0` is a literal zero-size dimension. A new `resolve_reshape(input_dims, numel, shape, allowzero)` helper is re-exported from `oxionnx-ops::shape`. 7 new unit tests cover all `allowzero` code paths (`test_reshape_allowzero_*` in `oxionnx-ops/src/shape/tests.rs`).
- `console_error_panic_hook = "0.1.7"` added as an optional workspace dependency wired into the `wasm` feature for improved panic messages in WebAssembly environments. (The panic hook itself always worked; it just had more to catch than intended until 0.1.6 -- see below.)

### Changed
- Updated CUDA / OxiCUDA dependencies to 0.1.8.
- `oxifft` updated from 0.3 to 0.3.2.
- wasm32 compatibility: thread-pool initialization in `src/session/loading.rs` is now skipped under `#[cfg(target_arch = "wasm32")]`; all rayon imports and parallel-execution functions in `src/session/run/parallel.rs` are likewise gated with `#[cfg(not(target_arch = "wasm32"))]`. WASM sessions now build cleanly without a thread pool. (Compiling cleanly was as far as this went: `oxifft`'s `threading` default feature still hit a `compile_error!` on this target via `oxionnx-ops`, and `std::time::Instant::now()` -- called unconditionally from the load path this note describes -- panics at runtime on wasm32-unknown-unknown regardless of what compiles. Both were fixed in 0.1.6; see below.)

### Fixed
- `SplitOp` now honors the opset-1..12 `split` attribute for chunk sizes when no `split` input tensor is present, fixing YOLO11 (opset 11) models that failed with `reshape: element count mismatch (33600 vs [1, 128, 20, 20])` in the C2PSA attention block (#1).

---

## [0.1.3] - 2026-05-16

### Added

- feat: `ProviderKind::DirectMl` variant (behind `directml` feature) — `ProviderKind` enum now covers CPU, GPU (wgpu), CUDA, and DirectML
- feat: `SessionBuilder::with_provider_kinds()` — non-trivial ordered execution provider list that routes dispatch at runtime; CPU is always the implicit terminal fallback
- feat: `SessionBuilder::provider_kinds()` accessor — returns the configured provider list for introspection
- feat: `Session::provider_kinds()` accessor — returns the provider list stored in the session
- feat: provider-list dispatch loop in `run_sequential_inner` — when `providers` is non-empty, each node is attempted through the list in order; falls back to legacy heuristic path when list is empty (backward compatible)
- feat: `try_provider_list_dispatch` helper — encapsulates CUDA / DirectML / GPU / CPU provider dispatch for the provider-list path; all providers return `None`-graceful results with CPU fallback guaranteed
- `ProviderKind` re-exported from `oxionnx` crate root

## [0.1.2] - 2026-04-19

### Added
- Phase D — Operator-Native TypedTensor Dispatch: `native_dtypes()` + `execute_typed()` opt-in hooks on the `Operator` trait; 40 pilot operators (23 math, 14 NN, Identity, Cast, Reshape) execute natively without f32 round-trips; all other operators fall back transparently via a correct default implementation
- Phase E — DirectML Execution Provider (Windows): new `oxionnx-directml` subcrate with `DirectMLContext::try_new()` cross-platform shim; Windows D3D12 context skeleton; HLSL compute shader scaffolds for MatMul, Add, Mul, Relu, Sigmoid; feature-gated behind `directml`; CPU fallback on non-Windows and unsupported ops
- Phase F — Operator-Level IOBinding Reuse: `supports_output_slots()` + `execute_into_slots()` opt-in hooks on the `Operator` trait; `SizeClassPool::acquire()` integrated on the slot path; pilot implementations for 40 operators; `IoBinding::take_output_buffer` / `put_output_buffer` helpers for caller-owned buffer management
- `OnnxError::DTypeMismatch` — new error variant for dtype validation in the typed dispatch path
- `TypedOpContext<'a>` — parallel context struct for typed operator dispatch
- Phase F complete — F.10 remainder sweep: all 121 operators opt into `supports_output_slots = true`; full slot-write path with pointer-identity pool reuse across the entire operator set
- Phase F.12 — zero-copy `execute_into_slots` hand-coded bodies for 30 operators: 22 pilot ops (Phase D/F pilot), plus 9 shape ops (Squeeze/Unsqueeze/Flatten/Expand/Split/Tile/DepthToSpace/SpaceToDepth/ReverseSequence), 12 elementwise NN ops (Clip/LeakyRelu/PRelu/HardSigmoid/Celu/Elu/Selu/ThresholdedRelu/LpNorm/MeanVarianceNorm/Hardmax/Shrink), 6 conv/pool ops (MaxPool/AveragePool/GlobalAveragePool/GlobalMaxPool/Pad/Resize), and Gather/ScatterND/ScatterElements
- D.3 scoped slice — `MatMulOp::execute_typed` with native dispatch for F32, F16, BF16, I8→I32, I32 dtypes; `native_dtypes()` declared; INT8×INT8→I32 triple-loop GEMM kernel; F16/BF16 kernels with f32 accumulator — no round-trip through f32 for quantized or half-precision matmul
- `oxionnx-ops/src/conv.rs` refactored into six focused submodules (`conv2d`, `im2col`, `winograd`, `pooling`, `tests`) to clear headroom for D.3 Conv native typed kernels (v0.1.9+)
- Integration test `full_slot_coverage_smoke.rs`: pointer-identity across 100 iterations + pool reuse assertions for the slot-write infrastructure

### Improved
- 100 new tests across typed dispatch, IOBinding, DirectML EP, and session slot paths (1074 total)
- Operator trait is fully backward-compatible: all four new methods carry correct default implementations

## [0.1.1] - 2026-04-14

### Added
- RNN operators: GRU, LSTM, RNN, and sequence processing ops (`oxionnx-ops/src/rnn.rs`)
- Extended ML operators: SVM, tree ensemble, decision tree classifiers/regressors (`oxionnx-ops/src/ml_svm.rs`, `ml_tree.rs`)
- Attention operators: expanded multi-head attention and transformer support (`oxionnx-ops/src/attention.rs`)
- Indexing operators: additional gather/scatter/slice variants (`oxionnx-ops/src/indexing.rs`)
- Quantized inference ops: extended quantized operator coverage (`oxionnx-ops/src/quantized.rs`)
- Spatial operators: additional convolution and pooling variants (`oxionnx-ops/src/spatial.rs`)

### Improved
- Fusion optimizer: major expansion of graph fusion rules and pattern matching (`src/optimizer/fusion.rs`)
- Session API: improved execution flow and error handling (`src/session.rs`)
- GPU compute context: refined WebGPU context management and kernel dispatch (`oxionnx-gpu/src/context.rs`)
- GPU shaders: additional WGSL shader kernels (`oxionnx-gpu/src/shaders.rs`)
- CUDA backend: conv, elementwise, and quantized op refinements (`oxionnx-cuda/src/conv.rs`, `elementwise.rs`)
- Proto streaming parser and mmap loader improvements (`oxionnx-proto/src/streaming_parser.rs`, `mmap_loader.rs`)
- SIMD ops: extended SIMD-accelerated operator paths (`oxionnx-ops/src/simd_ops.rs`)
- Math and NN ops: expanded coverage (`oxionnx-ops/src/math.rs`, `nn.rs`)

## [0.1.0] - 2026-03-26

### Added
- Initial release of OxiONNX pure Rust ONNX inference engine
- 147 ONNX operator implementations
- GPU acceleration via wgpu (MatMul, Softmax, ReLU, Sigmoid, GELU, Reduce)
- SIMD optimization (NEON aarch64, AVX2 x86_64)
- Graph optimization: constant folding, operator fusion, CSE, dead code elimination
- Multi-dtype support (DType enum, TypedTensor, TensorStorage)
- INT8 quantized MatMul with per-channel scale/zero-point
- Mixed-precision inference (f16 activations, f32 accumulation)
- Control flow operators: If, Loop, Scan with nested subgraphs
- Streaming token generation for autoregressive models
- Async inference API (run_async)
- Session serialization (save/load pre-optimized graphs)
- Memory-mapped weight loading (mmap feature)
- Tensor arena allocator with buffer pooling
- Strided tensor views (zero-copy transpose, slice, squeeze)
- Broadcasting iterator (zero-allocation)
- AES-GCM model encryption (encryption feature)
- WebAssembly support via wasm-bindgen (bindings and `cargo check --target wasm32-unknown-unknown` only; CPU inference did not actually run without panicking until 0.1.6 -- see that entry)
- no_std support for oxionnx-core
- Execution provider abstraction with CPU/GPU fallback chain
- ONNX-ML operators: Linear, TreeEnsemble, SVM, Normalizer, Scaler, LabelEncoder, TfIdf
- Model metadata extraction API
- Model validation and schema checking
- Graph diff utility for debugging optimizations
- Model pruning (strip unused weights)
- Symbolic shape propagation
- Opset coverage report generator
- Benchmark-based CPU/GPU path selection
- Numerical tolerance validation
- CI pipeline (GitHub Actions)
- Property-based testing with proptest
- Fuzz testing for protobuf parser
- 595 tests, 0 clippy warnings

[0.1.7]: https://github.com/cool-japan/oxionnx/releases/tag/v0.1.7
[0.1.6]: https://github.com/cool-japan/oxionnx/releases/tag/v0.1.6
[0.1.5]: https://github.com/cool-japan/oxionnx/releases/tag/v0.1.5
[0.1.4]: https://github.com/cool-japan/oxionnx/releases/tag/v0.1.4
[0.1.3]: https://github.com/cool-japan/oxionnx/releases/tag/v0.1.3
[0.1.2]: https://github.com/cool-japan/oxionnx/releases/tag/v0.1.2
[0.1.1]: https://github.com/cool-japan/oxionnx/releases/tag/v0.1.1
[0.1.0]: https://github.com/cool-japan/oxionnx/releases/tag/v0.1.0

# npm Publishing Workflow for OxiMedia WASM bindings
# Builds and publishes @cooljapan/oximedia to npm using wasm-pack.
#
# Only the bundler-target build (pkg-bundler) is renamed to and published as
# @cooljapan/oximedia. pkg-web and pkg-node are built and uploaded as CI
# artifacts for inspection/local testing only -- they are NOT published and
# keep wasm-pack's default scoped name (@cooljapan/oximedia-wasm), so their
# package.json never claims to be an installable package under a name
# nobody publishes. @cooljapan/oximedia-web is a separate, independently
# published package (see /web in this monorepo); it is not produced here.

name: npm Publish

on:
  # Manual trigger for publishing
  workflow_dispatch:
    inputs:
      publish_target:
        description: 'Publish target'
        required: true
        default: 'dry-run'
        type: choice
        options:
          - dry-run
          - npm
      npm_tag:
        description: 'npm dist-tag (e.g., latest, beta, next)'
        required: false
        default: 'latest'
        type: string

  # Trigger on release tags
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always
  WASM_PACK_VERSION: "0.13.1"
  SCOPE: "cooljapan"
  NPM_PACKAGE: "@cooljapan/oximedia"

jobs:
  build:
    name: Build WASM packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: wasm-build-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: wasm-build-

      - name: Install wasm-pack
        run: cargo install wasm-pack --version ${{ env.WASM_PACK_VERSION }} --locked || true

      - name: Install protoc (for prost-build)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq protobuf-compiler

      - name: Build for bundler target
        working-directory: oximedia-wasm
        run: wasm-pack build --scope ${{ env.SCOPE }} --target bundler --out-dir pkg-bundler --release

      - name: Build for web target
        working-directory: oximedia-wasm
        run: wasm-pack build --scope ${{ env.SCOPE }} --target web --out-dir pkg-web --release

      - name: Build for Node.js target
        working-directory: oximedia-wasm
        run: wasm-pack build --scope ${{ env.SCOPE }} --target nodejs --out-dir pkg-node --release

      # Only the bundler build is published, so only it is renamed to
      # @cooljapan/oximedia. pkg-web/pkg-node keep wasm-pack's default
      # scoped name (@cooljapan/oximedia-wasm) -- they are uploaded below as
      # CI artifacts for inspection only, never published, and must not be
      # confused with the separately-published @cooljapan/oximedia-web
      # package (see /web in this monorepo).
      - name: Rename bundler package to @cooljapan/oximedia
        working-directory: oximedia-wasm
        run: |
          if [ -f "pkg-bundler/package.json" ]; then
            sed -i "s|\"@${SCOPE}/oximedia-wasm\"|\"${NPM_PACKAGE}\"|g" "pkg-bundler/package.json"
            echo "Updated pkg-bundler/package.json → ${NPM_PACKAGE}"
          fi

      - name: Show package info
        working-directory: oximedia-wasm
        run: |
          echo "=== bundler (published as ${NPM_PACKAGE}) ==="
          cat pkg-bundler/package.json
          echo ""
          echo "=== web (unpublished build artifact) ==="
          cat pkg-web/package.json
          echo ""
          echo "=== node (unpublished build artifact) ==="
          cat pkg-node/package.json
          echo ""
          echo "=== WASM size ==="
          ls -lh pkg-bundler/*.wasm

      - name: Upload bundler package (published)
        uses: actions/upload-artifact@v4
        with:
          name: npm-pkg-bundler
          path: oximedia-wasm/pkg-bundler/

      - name: Upload web package (unpublished build artifact)
        uses: actions/upload-artifact@v4
        with:
          name: npm-pkg-web
          path: oximedia-wasm/pkg-web/

      - name: Upload node package (unpublished build artifact)
        uses: actions/upload-artifact@v4
        with:
          name: npm-pkg-node
          path: oximedia-wasm/pkg-node/

  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    needs: [build]
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Download bundler package
        uses: actions/download-artifact@v4
        with:
          name: npm-pkg-bundler
          path: pkg-bundler

      - name: Show package contents
        run: |
          echo "=== package.json ==="
          cat pkg-bundler/package.json
          echo ""
          echo "=== Files ==="
          ls -la pkg-bundler/

      # Dry run (default for workflow_dispatch, or RC tags)
      - name: Publish (dry-run)
        if: >-
          github.event.inputs.publish_target == 'dry-run' ||
          (github.event_name == 'push' && contains(github.ref, 'rc'))
        working-directory: pkg-bundler
        run: npm publish --access public --dry-run
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # Publish to npm
      - name: Publish to npm
        if: >-
          github.event.inputs.publish_target == 'npm' ||
          (github.event_name == 'push' && !contains(github.ref, 'rc'))
        working-directory: pkg-bundler
        run: |
          NPM_TAG="${{ github.event.inputs.npm_tag || 'latest' }}"
          echo "Publishing ${NPM_PACKAGE} with tag: ${NPM_TAG}"
          npm publish --access public --tag "${NPM_TAG}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Verify published package
        if: github.event.inputs.publish_target == 'npm' || (github.event_name == 'push' && !contains(github.ref, 'rc'))
        run: |
          sleep 10
          npm view ${{ env.NPM_PACKAGE }} version

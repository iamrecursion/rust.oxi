[package]
name = "oximedia-multicam"
description = "Multi-camera editing and synchronization tools"
keywords = ["multi-camera", "synchronization", "video", "editing", "multimedia"]
categories = ["multimedia::video"]
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
homepage.workspace = true
repository.workspace = true
readme = "README.md"

[lints]
workspace = true

[dependencies]
oximedia-core = { workspace = true }
oximedia-audio = { workspace = true }
oximedia-timecode = { workspace = true }
oximedia-align = { workspace = true }
oximedia-analysis = { workspace = true }
oximedia-codec = { workspace = true, features = ["mjpeg", "image-io"] }
oximedia-cv = { workspace = true }
oximedia-image = { workspace = true }
oxifft = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true, features = ["derive"] }
rayon = { workspace = true }

[dev-dependencies]

# oxifft's `threading` (rayon) backend is a hard `compile_error!` on wasm32, so
# the workspace baseline is `std` only; restore it for native targets.
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
oxifft = { workspace = true, features = ["threading"] }

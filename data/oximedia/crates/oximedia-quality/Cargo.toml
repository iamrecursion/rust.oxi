[package]
name = "oximedia-quality"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
homepage.workspace = true
repository.workspace = true
description = "Video quality assessment and objective metrics for OxiMedia"
keywords = ["quality", "video", "metrics", "multimedia", "broadcast"]
categories = ["multimedia::video"]
readme = "README.md"

[lints]
workspace = true

[features]
default = []

[dependencies]
oximedia-core = { workspace = true }
oxifft = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
rayon = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "metrics_bench"
harness = false

# oxifft's `threading` (rayon) backend is a hard `compile_error!` on wasm32, so
# the workspace baseline is `std` only; restore it for native targets.
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
oxifft = { workspace = true, features = ["threading"] }

# Cargo configuration for WASM builds

[target.wasm32-unknown-unknown]
# Use lld for faster linking
rustflags = ['-C', 'link-arg=-s', '--cfg', 'getrandom_backend="wasm_js"']

[build]
# Set default target for this crate
target = "wasm32-unknown-unknown"

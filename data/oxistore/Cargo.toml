[workspace]
resolver = "2"
members = [
    "crates/oxistore-core",
    "crates/oxistore-kv-redb",
    "crates/oxistore-kv-sled",
    "crates/oxistore-kv-fjall",
    "crates/oxistore",
    "crates/oxistore-columnar",
    "crates/oxistore-cache",
    "crates/oxistore-blob",
    "crates/oxistore-blob-azure",
    "crates/oxistore-blob-s3",
    "crates/oxistore-blob-gcs",
    "crates/oxistore-encrypt",
    "crates/oxistore-compress",
    "crates/oxistore-interop-tests",
]

[workspace.package]
version = "0.3.1"
edition = "2021"
rust-version = "1.89"
authors = ["COOLJAPAN OU (Team Kitasan)"]
license = "Apache-2.0"
repository = "https://github.com/cool-japan/oxistore"
description = "OxiStore is the COOLJAPAN-blessed Pure Rust low-level storage layer: embedded key-value, columnar in-memory & on-disk, and blob storage abstraction (local fs + cloud)."

[workspace.dependencies]
oxistore = { path = "crates/oxistore", version = "0.3" }
oxistore-core = { path = "crates/oxistore-core", version = "0.3" }
oxistore-encrypt = { path = "crates/oxistore-encrypt", version = "0.3" }
oxistore-compress = { path = "crates/oxistore-compress", version = "0.3" }
oxiarc-deflate = { version = "0.4.2" }
oxiarc-core = { version = "0.4.2" }
oxicrypto = { version = "0.3.0" }
oxicrypto-sig = { version = "0.3.0" }
# PKCS#11 HSM adapter — pulled from crates.io (NOT a path dep) to avoid
# re-introducing an upward coupling from oxicrypto into oxistore. The C-FFI
# `cryptoki` dependency is only built when the adapter's `pkcs11` feature is
# enabled via oxistore-encrypt's opt-in `oxicrypto-pkcs11` feature.
oxicrypto-adapter-pkcs11 = { version = "0.3.0", default-features = false }
oxistore-kv-redb = { path = "crates/oxistore-kv-redb", version = "0.3" }
oxistore-kv-sled = { path = "crates/oxistore-kv-sled", version = "0.3" }
oxistore-kv-fjall = { path = "crates/oxistore-kv-fjall", version = "0.3" }
oxistore-columnar = { path = "crates/oxistore-columnar", version = "0.3" }
oxistore-cache = { path = "crates/oxistore-cache", version = "0.3" }
oxistore-blob = { path = "crates/oxistore-blob", version = "0.3" }
oxistore-blob-azure = { path = "crates/oxistore-blob-azure", version = "0.3" }
oxistore-blob-s3 = { path = "crates/oxistore-blob-s3", version = "0.3" }
oxistore-blob-gcs = { path = "crates/oxistore-blob-gcs", version = "0.3" }
redb = { version = "4.1.0" }
sled = { version = "0.34", default-features = false }
fjall = { version = "3.1.5" }
criterion = { version = "0.8.2", features = ["html_reports"] }
arrow = { version = "59.0.0" }
parquet = { version = "59.0.0", default-features = false, features = ["arrow", "experimental"] }
hashlink = { version = "0.12" }
bytes = { version = "1" }
tokio = { version = "1", features = ["fs", "io-util", "sync", "rt", "rt-multi-thread", "macros", "time"] }
sha2 = { version = "0.11.0", default-features = false }
digest = { version = "0.11.3", default-features = false }
hmac = { version = "0.13.0" }
base64 = { version = "0.23.0" }
async-trait = { version = "0.1" }
aws-sigv4 = { version = "1.4.5", default-features = false, features = ["sign-http", "http1"] }
aws-credential-types = { version = "1.2.14" }
quick-xml = { package = "oxixml-quickxml-compat", version = "0.1.2" }
url = { version = "2.5.8" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0" }
oxihttp-client = { version = "0.2.1", features = ["tls"] }
hex = { version = "0.4" }
proptest = { version = "1" }
ahash = { version = "0.8" }
# The three entries below exist solely for `oxistore-interop-tests`, a
# `publish = false` workspace member that hosts cross-workspace `oxisql`
# integration tests. No publishable oxistore crate depends on `oxisql-*` —
# this keeps the crates.io dependency graph acyclic (oxisql depends on
# oxistore, so the reverse edge must not exist in any published crate).
oxisql-core = "0.4.1"
# Enables the storage-engine feature matching each moved test's backend
# (e.g. `redb-storage`); the base entry here carries no features.
oxisql-embedded = { version = "0.4.1" }
# Used by the `EncryptedPooledStore` integration test.
oxisql-pool = { version = "0.4.1" }
keyring-core = { version = "1.0.0" }

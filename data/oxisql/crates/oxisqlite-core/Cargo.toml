# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition.workspace = true
name = "oxisqlite-core"
version.workspace = true
authors = ["the Limbo authors"]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "oxisqlite-core — core engine of the Pure-Rust SQLite-compatible oxisqlite fork (C-free)"
readme = "README.md"
categories = ["database", "embedded"]
keywords = ["sqlite", "database", "sql", "embedded", "btree"]
license.workspace = true
repository.workspace = true

[lib]
name = "limbo_core"
path = "lib.rs"

[[bench]]
name = "benchmark"
path = "benches/benchmark.rs"
harness = false

[[bench]]
name = "json_benchmark"
path = "benches/json_benchmark.rs"
harness = false

[[bench]]
name = "mvcc_benchmark"
path = "benches/mvcc_benchmark.rs"
harness = false

[[bench]]
name = "tpc_h_benchmark"
path = "benches/tpc_h_benchmark.rs"
harness = false

[[test]]
name = "durability"
path = "tests/durability.rs"

[[test]]
name = "correlated"
path = "tests/correlated.rs"

[[test]]
name = "conflict"
path = "tests/conflict.rs"

[[test]]
name = "limit_params"
path = "tests/limit_params.rs"

[[test]]
name = "in_null"
path = "tests/in_null.rs"

[[test]]
name = "upsert"
path = "tests/upsert.rs"

[[test]]
name = "schema_cookie"
path = "tests/schema_cookie.rs"

[[test]]
name = "analyze"
path = "tests/analyze.rs"

[[test]]
name = "without_rowid"
path = "tests/without_rowid.rs"

[[test]]
name = "group_by_having"
path = "tests/group_by_having.rs"

[[test]]
name = "delete_update_index_safety"
path = "tests/delete_update_index_safety.rs"

[[test]]
name = "ctas"
path = "tests/ctas.rs"

[[test]]
name = "auto_vacuum"
path = "tests/auto_vacuum.rs"

[[test]]
name = "update_returning"
path = "tests/update_returning.rs"

[[test]]
name = "alter_rename"
path = "tests/alter_rename.rs"

[[test]]
name = "translate_expr_value_fixes"
path = "tests/translate_expr_value_fixes.rs"

[[test]]
name = "cte_and_joins"
path = "tests/cte_and_joins.rs"

[[test]]
name = "vtab_order_by_pushdown"
path = "tests/vtab_order_by_pushdown.rs"

[[test]]
name = "vtab_schema_columns"
path = "tests/vtab_schema_columns.rs"

[[test]]
name = "compound_select_set_ops"
path = "tests/compound_select_set_ops.rs"

[[test]]
name = "regexp_operator"
path = "tests/regexp_operator.rs"

[[test]]
name = "autoindex_balance"
path = "tests/autoindex_balance.rs"

[[test]]
name = "views"
path = "tests/views.rs"

[[test]]
name = "pragma_panic_hardening"
path = "tests/pragma_panic_hardening.rs"

[[test]]
name = "triggers"
path = "tests/triggers.rs"

[[test]]
name = "json_malformed_blob"
path = "tests/json_malformed_blob.rs"

[[test]]
name = "pragma_index_introspection"
path = "tests/pragma_index_introspection.rs"

[[test]]
name = "multi_database"
path = "tests/multi_database.rs"

[dependencies.bitflags]
workspace = true

[dependencies.chrono]
workspace = true
# "clock" (not just "now") is genuinely required here: functions/datetime/mod.rs's
# `Modifier::Localtime`/`Modifier::Utc` handling implements SQLite's
# `datetime('now','localtime')`/`'utc'` modifiers, which perform a real
# host-OS-timezone conversion via `chrono::Local` — that needs the full
# "clock" feature (`iana-time-zone`, and on macOS `core-foundation-sys`), not
# just "now". All *other* call sites in this crate that previously used
# `chrono::Local::now()` merely to read the current instant (io/*.rs's
# `Clock::now()`, and 'now' time-value parsing) have been switched to
# `chrono::Utc::now()`, since SQLite's own 'now' time-value is UTC and
# `Instant::{secs,micros}` are timezone-independent — but the crate as a
# whole cannot drop "clock" while the localtime/utc modifiers still need it.
# Note: even if this crate could drop "clock", the workspace's default build
# would still pull `iana-time-zone`/`core-foundation-sys` in transitively via
# `arrow-arith`/`arrow-array`/`arrow-cast`/`arrow-csv`/`arrow-json` (through
# `oxisql-datafusion` → `datafusion` → `arrow`), which each independently
# request chrono's "clock" feature in their own manifests — confirmed via
# `cargo tree -i core-foundation-sys`.
features = ["clock"]

# ── Crate-local third-party version pins (deliberate, not an oversight) ────
# The pins below (crossbeam-skiplist, hex, libc, libm, parking_lot, rand,
# regex, regex-syntax, ryu, tracing; plus quickcheck/quickcheck_macros and
# the criterion feature set under [dev-dependencies] further down) are kept
# crate-local rather than swept to `workspace = true`. `oxisqlite-core` is
# the C-free fork of limbo 0.0.22's `core` crate, and matching upstream's own
# per-crate version pins keeps this manifest diffable line-for-line against
# limbo for future upstream merges — none of these are shared with another
# workspace member at a *different* version (which would be the actual bug
# worth fixing; see `log`/`env_logger` in oxisqlite-sqlite3-parser and
# `tokio` in oxisqlite, which WERE swept to `workspace = true` because they
# had drifted from an already-shared workspace-level requirement).
[dependencies.crossbeam-skiplist]
version = "0.1.3"

[dependencies.fallible-iterator]
workspace = true

[dependencies.getrandom]
workspace = true

[dependencies.hex]
version = "0.4.3"

[dependencies.libc]
version = "0.2.186"
optional = true

[dependencies.libm]
version = "0.2"

[dependencies.limbo_ext]
workspace = true
features = ["core_only"]

[dependencies.limbo_macros]
workspace = true

[dependencies.limbo_sqlite3_parser]
package = "oxisqlite-sqlite3-parser"
version = "0.4.2"
path = "../oxisqlite-sqlite3-parser"

[dependencies.limbo_time]
package = "oxisqlite-time"
version = "0.4.2"
path = "../oxisqlite-time"
features = ["static"]
optional = true

[dependencies.limbo_uuid]
package = "oxisqlite-uuid"
version = "0.4.2"
path = "../oxisqlite-uuid"
features = ["static"]
optional = true

[dependencies.miette]
workspace = true

[dependencies.parking_lot]
version = "0.12.5"

[dependencies.rand]
version = "0.10.1"

[dependencies.regex]
version = "1.12.4"

[dependencies.regex-syntax]
version = "0.8.11"
features = ["unicode"]
default-features = false

[dependencies.ryu]
version = "1.0.23"

[dependencies.serde]
workspace = true
optional = true

[dependencies.strum]
workspace = true
features = ["derive"]

[dependencies.strum_macros]
workspace = true

[dependencies.thiserror]
workspace = true

[dependencies.tracing]
version = "0.1.44"

[dependencies.uncased]
workspace = true

[dev-dependencies.criterion]
version = "0.8"
features = [
    "html_reports",
    "async",
    "async_futures",
]

[dev-dependencies.env_logger]
workspace = true

[dev-dependencies.lru]
version = "0.18.0"

[dev-dependencies.quickcheck]
version = "1.1"
default-features = false

[dev-dependencies.quickcheck_macros]
version = "1.2"
default-features = false

[dev-dependencies.rand]
version = "0.10.1"

[dev-dependencies.rand_chacha]
version = "0.10.0"

[dev-dependencies.rstest]
version = "0.26.1"

[dev-dependencies.sorted-vec]
version = "0.8.10"

[dev-dependencies.tempfile]
workspace = true

[dev-dependencies.test-log]
version = "0.2.21"
features = ["trace"]


[features]
default = [
    "fs",
    "uuid",
    "time",
    "json",
    "static",
]
fs = ["limbo_ext/vfs"]
fuzz = []
index_experimental = []
io_uring = [
    "native-io",
    "dep:io-uring",
    "rustix/io_uring",
    "dep:libc",
]
json = []
# Off-by-default: dynamic (dlopen) SQL extension loading via `libloading`.
# Quarantined so the default dependency closure carries no dlopen/FFI loader.
load-extension = ["dep:libloading"]
# Off-by-default: native epoll/kqueue event-loop I/O backend (`unix`, via
# polling + rustix -> linux-raw-sys on Linux). The default build uses the pure
# std-blocking `generic` backend instead; this is the perf vs. purity trade-off.
native-io = [
    "dep:polling",
    "dep:rustix",
]
omit_autovacuum = []
serde = ["dep:serde"]
simulator = [
    "fuzz",
    "serde",
]
static = ["limbo_ext/static"]
time = ["limbo_time/static"]
uuid = ["limbo_uuid/static"]

[target.'cfg(not(target_family = "wasm"))'.dependencies.libloading]
version = "0.9.0"
optional = true


[target.'cfg(not(target_family = "windows"))'.dev-dependencies.pprof]
version = "0.15.0"
features = [
    "flamegraph",
]

[target.'cfg(target_family = "unix")'.dependencies.polling]
version = "3.11.0"
optional = true

[target.'cfg(target_family = "unix")'.dependencies.rustix]
version = "1.1.4"
features = ["fs"]
optional = true

[target.'cfg(target_os = "linux")'.dependencies.io-uring]
version = "0.7.13"
optional = true

# Windows file-locking backend (io/windows.rs): LockFileEx/UnlockFileEx via
# windows-sys. `WindowsIO`/`WindowsFile` are compiled unconditionally on
# `target_os = "windows"` (see io/mod.rs), so this is a mandatory (non
# `optional`) dependency for that target only; it never enters the
# dependency graph for any other target.
[target.'cfg(target_os = "windows")'.dependencies.windows-sys]
workspace = true



[package]
name = "zstd"
version = "0.13.3"
edition = "2021"
publish = false
license = "Apache-2.0"
description = "Pure-Rust zstd-API shim backed by oxiarc-zstd (COOLJAPAN Pure Rust Policy v2). Local [patch.crates-io] override for oxisql — NOT the upstream zstd crate; implements only the bulk surface arrow-ipc uses."

[dependencies]
oxiarc-zstd = "0.4.2"

[workspace]

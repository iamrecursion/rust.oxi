[package]
name = "oxisql-sqlite-compat"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Pure-Rust SQLite-compatible backend for OxiSQL via the oxisqlite Pure-Rust engine (C-free fork of limbo)"
readme = "README.md"
keywords = ["sqlite", "sql", "database", "oxisqlite", "pure-rust"]
categories = ["database"]

[dependencies]
oxisql-core = { workspace = true }
limbo       = { package = "oxisqlite", path = "../oxisqlite", version = "0.4.2", features = ["index_experimental"] }
async-trait = { workspace = true }
thiserror   = { workspace = true }
tokio       = { workspace = true }
log         = { workspace = true }
lru         = { workspace = true }
chrono      = { workspace = true }

[features]
## Enable synchronous (blocking) wrappers around [`SqliteConnection`].
##
## Each blocking method drives the underlying async API to completion via a
## freshly-built `current_thread` Tokio runtime.  Useful for integrating
## OxiSQL into non-async code paths.
blocking = []

[dev-dependencies]
tokio.workspace = true

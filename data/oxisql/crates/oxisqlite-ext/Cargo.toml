# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition.workspace = true
name = "oxisqlite-ext"
version.workspace = true
authors = ["the Limbo authors"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "oxisqlite-ext — extension API for the Pure-Rust oxisqlite engine (C-free fork of limbo)"
readme = "README.md"
categories = ["database"]
keywords = ["sqlite", "extension", "database", "vtab", "udf"]
license.workspace = true
repository.workspace = true

[lib]
name = "limbo_ext"
path = "src/lib.rs"

[dependencies.limbo_macros]
workspace = true

[features]
core_only = []
static = []
vfs = []

[target.'cfg(not(target_family = "wasm"))'.dependencies.chrono]
workspace = true
# "now" (not "clock") — `get_current_time()` below only needs `Utc::now()`, not
# the host OS timezone database that "clock" (`iana-time-zone` / macOS
# `core-foundation-sys`) provides.
features = ["now", "oldtime", "wasmbind"]

[target.'cfg(not(target_family = "wasm"))'.dependencies.getrandom]
workspace = true

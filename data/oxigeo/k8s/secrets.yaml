# OxiGeo Kubernetes Secrets Template
# IMPORTANT: Do not commit actual secrets to git!
# Use sealed-secrets, external-secrets, or vault for production
# Author: COOLJAPAN OU (Team Kitasan)

apiVersion: v1
kind: Secret
metadata:
  name: oxigeo-secrets
  namespace: oxigeo
  labels:
    app.kubernetes.io/name: oxigeo
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # PostgreSQL credentials
  postgres-db: oxigeo
  postgres-user: oxigeo
  postgres-password: CHANGEME  # Replace with secure password

  # Redis password (if authentication is enabled)
  redis-password: ""  # Empty for no auth

  # S3 credentials (if using S3 storage)
  aws-access-key-id: ""
  aws-secret-access-key: ""
  aws-region: us-east-1

  # Azure credentials (if using Azure storage)
  azure-storage-account: ""
  azure-storage-key: ""

  # GCS credentials (if using GCS storage)
  gcs-project-id: ""
  gcs-service-account-key: ""

  # API keys for external services
  api-key: ""
  jwt-secret: ""

---
# Example: SealedSecret (if using sealed-secrets operator)
# apiVersion: bitnami.com/v1alpha1
# kind: SealedSecret
# metadata:
#   name: oxigeo-secrets
#   namespace: oxigeo
# spec:
#   encryptedData:
#     postgres-password: AgBi...  # Encrypted value
#     postgres-user: AgCd...      # Encrypted value
#   template:
#     metadata:
#       labels:
#         app.kubernetes.io/name: oxigeo
#     type: Opaque

---
# Example: ExternalSecret (if using external-secrets operator)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: oxigeo-secrets
#   namespace: oxigeo
# spec:
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: oxigeo-secrets
#     creationPolicy: Owner
#   data:
#   - secretKey: postgres-password
#     remoteRef:
#       key: oxigeo/postgres
#       property: password
#   - secretKey: postgres-user
#     remoteRef:
#       key: oxigeo/postgres
#       property: username

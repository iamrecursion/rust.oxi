[package]
name = "oxigeo-jupyter"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/oxigeo-jupyter"
readme = "README.md"
description = "Jupyter integration for OxiGeo with interactive widgets and visualization"
keywords = ["gis", "science", "jupyter", "notebook", "visualization"]
categories = ["science::geo", "visualization", "development-tools"]

[dependencies]
# Internal dependencies
oxigeo-core = { workspace = true, default-features = true }
oxigeo-algorithms = { workspace = true, default-features = true }
oxigeo-geotiff.workspace = true

# Jupyter kernel (optional: evcxr pulls in pathfinder_simd which requires
# nightly ARM intrinsics; gate behind feature so default publish builds pass)
evcxr = { workspace = true, optional = true }

# Visualization
plotters.workspace = true

# Serialization
serde.workspace = true
serde_json.workspace = true
base64.workspace = true

# Error handling
thiserror.workspace = true

# Display and formatting
comfy-table.workspace = true

# Async support
tokio = { workspace = true, optional = true }

[dev-dependencies]
tempfile.workspace = true

[features]
default = []
async = ["tokio"]
jupyter-kernel = ["evcxr"]

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

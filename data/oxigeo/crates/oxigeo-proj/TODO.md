# TODO: oxigeo-proj

> **Purpose:** Pure-Rust coordinate transformation and projection support for OxiGeo — EPSG database, WKT2/WKT1/ESRI WKT, Helmert/Molodensky/NTv2/NADCON datum transforms, ~140 projections.
> **Status (2026-07-28):** 18,513 Rust LoC · 783 tests all-features (770 default-features) · 0 known stubs
> **Roadmap:** v0.2.0 → v0.2.1 (current) → v1.0.0

## High Priority (next slice — verified gaps)

- [x] Fix dead `eta` placeholder in ellipsoidal Transverse Mercator forward + AreaOfUse wiring (completed 2026-05-17)
  - **Done (A):** Removed dead `let eta = ... * 0.0` local and corrected the comment in `cylindrical.rs` to document that `c = e_prime2 * cos_lat * cos_lat` is already η² per Snyder 1987 eq. (8-3). TM outputs are bit-for-bit identical.
  - **Done (B):** Wired `AreaOfUse::contains` into `Transformer::transform`. Added `strict: bool` field (default `true`) + `with_strict(bool)` builder. Added `ProjError::OutOfAreaOfUse { lon, lat, crs }` variant. Added `Crs::area_of_use() -> Option<AreaOfUse>` accessor. Geographic-CRS-only gating prevents false positives on projected-CRS inputs.
  - **Tests added (5):** test_tm_eta_removed_does_not_change_projected_coords, test_transformer_default_is_strict, test_transformer_strict_rejects_point_outside_aou, test_transformer_non_strict_passes_through, test_transformer_aou_check_skipped_when_crs_has_no_aou.

- [x] Implement NTv2 grid shift file parser for high-accuracy datum transforms (completed 2026-05-17)
  - **Done:** Converted `src/grid_shift.rs` → `src/grid_shift/mod.rs` (directory module, public API unchanged). New `src/grid_shift/ntv2.rs` (~700 LoC) implementing NTv2 per NRCan IOGP Geomatics Guidance Note 7-2 §4.4.4. Endianness sniff via NUM_OREC bytes 8-11. Parses 11 OREC overview records (GS_TYPE, VERSION, SYSTEM_F/T, ellipsoid axes), then per sub-grid 11 SREC (bounds in arcseconds, cell size, GS_COUNT). Data records: `(lat_shift, lon_shift, lat_accuracy, lon_accuracy)` as f32. `transform(lon, lat)` converts to arcseconds, finds deepest child sub-grid via DFS, bilinear interpolates over 4 surrounding cells, returns corrected (lon, lat). Added `ProjError::Ntv2ParseError(String)` and `ProjError::Ntv2OutOfGrid { lon, lat }`. `byteorder = { workspace = true }` added to Cargo.toml. `NtV2Grid` re-exported from `lib.rs`.
  - **Tests added (9):** test_ntv2_parse_header_endianness_detection_le, test_ntv2_parse_header_endianness_detection_be, test_ntv2_bilinear_interpolation_at_node_returns_shift_exactly, test_ntv2_bilinear_interpolation_at_center_averages_four_neighbours, test_ntv2_outside_grid_extent_errors, test_ntv2_nested_grid_child_preferred_when_parent_set, plus 3 additional geometry/field tests. Total: 466 tests pass.

- [x] Implement compound CRS support (horizontal + vertical) (completed 2026-05-17)
  - **Done:** `CrsSource::Compound { horizontal: Box<Crs>, vertical: Box<Crs> }` added to `src/crs.rs`. `Crs::compound(horizontal, vertical) -> Result<Crs>` constructor validates horizontal is Geographic/Projected and vertical is Vertical. `Crs::from_compound_wkt()` routes `COMPOUNDCRS`/`COMPD_CS` WKT keywords to recursive parse of child geographic/projected + vertical CRS blocks; `from_wkt()` dispatches to it. `to_wkt()` emits `COMPOUNDCRS["name",h_wkt,v_wkt]`; `is_equivalent()` checks both component pairs. `transform_3d` in `src/transform/mod.rs` gains compound branch: horizontal xy pair transformed via nested `Transformer`, z passed through when source/target vertical CRS are equivalent (ellipsoidal height passthrough); mismatched vertical CRS returns `Err(ProjError::GeoidNotAvailable { vertical_crs })`. `ProjError::InvalidCompoundCrs { reason: String }` and `ProjError::GeoidNotAvailable { vertical_crs: String }` added to `src/error.rs`.
  - **Tests added (7):** test_transform_3d_compound_to_compound_same_vertical, test_transform_3d_compound_to_horizontal_drops_z, test_transform_3d_horizontal_to_compound_passes_z, test_from_wkt_compound_wkt2_parses_components, test_transform_3d_compound_geoid_unavailable_errors, test_crs_compound_rejects_invalid_arguments, test_transform_3d_simple_crs_backward_compat. Total: 473 tests pass.

- [x] Add batch coordinate transformation with SIMD acceleration (completed 2026-05-17)
  - **Done:** New `src/transform/simd.rs` (~776 LoC) — four batch kernels with AVX2 runtime-dispatch (`is_x86_feature_detected!("avx2")`): `tmerc_forward_batch` (Snyder §8 ellipsoidal series with `f64::mul_add` FMA, 4-lane unrolled), `merc_forward_batch` (ellipsoidal Mercator + spherical/EPSG:3857 path), `merc_inverse_batch` (Newton-Raphson 5 iterations), `lcc_forward_batch` (sphere-based LCC) + `lcc_cone_params`. `parse_ellipsoid` free function extracts `(a, e2)` from PROJ string recognising `+ellps`, `+datum`, explicit `+a/+b/+f/+rf`. `src/transform/mod.rs`: private `try_simd_batch` detects `Geographic→Projected` transform, parses PROJ string, dispatches to TM/UTM/Merc/LCC simd kernel, returns `None` for other projections (stereographic etc.) — falls through to proj4rs scalar loop.
  - **Tests added (12):** test_transform_batch_matches_scalar_within_1ulp_tm, test_transform_batch_matches_scalar_within_1ulp_mercator, test_transform_batch_matches_scalar_lcc_1e9, test_transform_batch_partial_lane_at_tail, test_transform_batch_empty_input, test_transform_batch_falls_back_when_no_simd_path_available, plus 6 unit tests in simd.rs. Total: 485 tests pass.

- [x] Implement coordinate epoch support for time-dependent transformations (ITRF) (completed 2026-05-17).
  - **Done:** `src/epsg/extended/additional_geographic.rs` gains 4 ITRF EPSG registrations (7900=ITRF97, 7901=ITRF2000, 7902=ITRF2005, 7930=ITRF2008). `src/datum_transform.rs` extended: `ItrfTransformParams::inverse()` (negates all 7 params); new `ItrfPreset { source, target, params, ref_epoch_yr }` struct; `itrf_preset_table() -> [ItrfPreset; 3]` with IERS-cited ITRF97→ITRF2000, ITRF2000→ITRF2005, ITRF2005→ITRF2008 rate parameters; `find_itrf_params(source_crs, target_crs) -> Option<(ItrfTransformParams, f64)>` chain-walks the table both forward and backward. `src/crs.rs` gains `Crs::itrf_name() -> Option<String>` — returns string if EPSG code is in 7900/7901/7902/7930 set or if `authority_name` contains "ITRF". `src/transform/mod.rs`: `Transformer` gains `source_epoch: Option<f64>`, `target_epoch: Option<f64>`, `itrf_params: Option<(ItrfTransformParams, f64)>` fields; `with_epoch(t0, t1) -> Self` builder resolves params via `find_itrf_params`; `transform_3d` gains ITRF branch that computes `delta_t = t0 - ref_epoch_yr`, applies `ItrfTransformParams::apply_bursa_wolf` to the geocentric XYZ intermediate before and after the standard pipeline. Identity short-circuit: `with_epoch(t, t)` → `delta_t = 0` → zero correction.
  - **Tests added (9):** test_itrf_epoch_identity_passthrough, test_itrf_params_inverse_negates_all, test_find_itrf_params_forward_lookup, test_find_itrf_params_reverse_lookup, test_find_itrf_params_unknown_returns_none, test_itrf_epoch_transform_applies_linear_rate, test_crs_itrf_name_detection, test_itrf_epoch_chain_across_two_frames, test_itrf_preset_table_has_three_entries. Total: 494 tests pass.

- [x] Implement area-of-use validation to warn when coordinates are outside CRS bounds (completed 2026-05-18)
  - **Done:** `src/area_of_use.rs` extended with `AreaOfUse::contains(lon, lat) -> bool` handling antimeridian crossing (when `west > east`, the region wraps as `[west, 180] ∪ [-180, east]` — covers NZGD2000 EPSG:4167 style regions). `src/transform/mod.rs` gains `pub enum AreaOfUseCheck { Off (default), Warn, Strict }`, `pub struct AreaOfUseWarning { lon, lat, epsg, west, south, east, north }` (Copy), `Transformer.area_of_use_check` + `Transformer.last_warning: Cell<Option<AreaOfUseWarning>>`. Builder `with_area_of_use_check(mode)` (resets warning), accessor `last_warning()`, `clear_warning()`, getter `area_of_use_check()`. Private `check_area_of_use(lon, lat)` looks up `area_of_use_for_epsg(self.source_crs.epsg_code())` and dispatches per mode (Off → silent, Warn → records + continues, Strict → returns `Error::OutsideAreaOfUse {...}`). Unknown EPSG always passes silently. Wired into both `transform()` (first step before no-op return) and `transform_batch()` (SIMD fast-path). Legacy `with_strict`/`OutOfAreaOfUse` system preserved orthogonally. New `Error::OutsideAreaOfUse { lon, lat, epsg, west, south, east, north }` variant. Re-exports `AreaOfUseCheck`, `AreaOfUseWarning` from `lib.rs`.
  - **Tests added (10):** test_aou_contains_point_inside_bbox, test_aou_contains_antimeridian_crossing, test_aou_contains_outside_returns_false, test_transformer_aou_off_passes_outside_silently, test_transformer_aou_warn_records_warning, test_transformer_aou_strict_returns_error, test_transformer_aou_unknown_epsg_passes_through, test_transformer_aou_global_crs_always_passes, test_transformer_aou_builder_resets_warning, test_transformer_aou_batch_strict_rejects_first_offender. Total: 573 tests pass.

- [x] Add EPSG registry auto-update mechanism from EPSG database exports (completed 2026-05-19)
  - **Done:** New `build.rs` (~86 LoC) reads `epsg-snapshot/minimal.json` at compile time via serde_json build-dep, generates `$OUT_DIR/generated_epsg.rs` containing `pub(crate) fn register_generated_epsg(db: &mut EpsgDatabase)` with `or_insert_with` semantics (built-ins always win). `src/epsg/types.rs` gains `include!(concat!(env!("OUT_DIR"), "/generated_epsg.rs"))` at module level (std feature only) and calls `register_generated_epsg(self)` at end of `initialize_builtin_codes`. `epsg-snapshot/minimal.json` ships 58 records: ETRS89, NAD27, NAD83, WGS72, GDA94, JGD2000, NAD83(CSRS), UTM zones (north/south), NSIDC EASE-Grid, WGS84 geocentric, regional CRS codes. `cargo:rerun-if-changed` keeps incremental builds precise.
  - **Tests added (13 in `tests/epsg_autoupdate_test.rs`):** snapshot-sourced codes present, built-in codes preserved (4326, 3857), unknown code returns Err, count > baseline, field assertions.

## Medium Priority (planned — design sketched)

- [x] Implement vertical CRS and geoid height transformations (completed 2026-05-18)
  - **Done:** New `src/geoid.rs` (~498 LoC). `GeoidModel` enum (Egm96, Egm2008) + `display_name()`, `default_grid_spacing_deg()`. `GeoidGrid { model, lat_step_deg, lon_step_deg, lat_min_deg, lon_min_deg, n_lat, n_lon, heights_m: Vec<f32> }` — row-major storage. `synthetic_grid(model)` + `synthetic_height_m(lat, lon)` analytic test helpers (sin(lat)*cos(lon)*30 m). `load_egm_grid(path, model, lat_min, lon_min, lat_step, lon_step, n_lat, n_lon) -> Result<GeoidGrid>` — loads NGA-format LE-f32 grid with size validation. `GeoidGrid::geoid_height_m(lat, lon)` — bilinear interpolation with longitude wrap (mod 360°) and latitude clamp. `orthometric_to_ellipsoidal(lat, lon, h_ortho)` = h_ortho + N; `ellipsoidal_to_orthometric(...)` = h_ellip - N. `VerticalDatumKind` + `classify_vertical_datum(name)` classifies known datum strings. New `Error::GeoidFileFormat(String)` + `Error::NoGeoidAvailable` variants. `Transformer` gains `geoid: Option<Arc<GeoidGrid>>` field + `with_geoid(grid)` builder + `geoid()` accessor; `transform_3d` Compound-CRS branch applies undulation when both src and dst differ in vertical-datum kind; silent passthrough when no geoid set (back-compat). Re-exported from `lib.rs` under `#[cfg(feature = "std")]`.
  - **Tests added (14):** test_geoid_synthetic_grid_has_expected_dims, test_geoid_height_at_grid_node_exact, test_geoid_height_bilinear_interpolates_midpoint, test_geoid_longitude_wraps_at_antimeridian, test_geoid_latitude_clamps_at_pole, test_orthometric_to_ellipsoidal_round_trip, test_load_egm_grid_size_mismatch_errors, test_load_egm_grid_round_trip_via_tempfile, test_transformer_compound_with_geoid_applies_undulation, test_transformer_no_geoid_falls_back_silently, test_classify_vertical_datum_recognises_known_strings, plus 3 additional unit tests. Total: 590 tests pass.

- [x] Add coordinate operation pipeline (chain multiple transforms) (completed 2026-05-17)
  - **Done:** New `src/pipeline.rs` (~700 LoC). `Unit` enum (M, Km, Ft, USFt, Rad, Deg) with `to_meters_per_unit()`/`to_radians_per_unit()`. `StepKind` enum (AxisSwap { order: [u8;4] }, UnitConvert { from, to }, Project { proj_string }, Passthrough). `PipelineStep { kind, inverse }`. `Pipeline { steps, inverse }` with `new()`, `step()`, `step_inv()`, `with_inverse()`, `transform()`, `transform_many()`, `from_proj_string()` builder. `parse_pipeline(s) -> Result<Vec<ParsedStep>>` public helper splits on `+step` tokens, parses each sub-string. Re-exported from `lib.rs` as `ParsedStep, Pipeline, PipelineStep, StepKind, Unit, parse_pipeline`.
  - **Tests added (9):** test_pipeline_empty_returns_passthrough, test_pipeline_single_step_axisswap_order_2_1_swaps_xy, test_pipeline_unit_convert_m_to_km, test_pipeline_two_step_axisswap_then_unit_convert, test_pipeline_from_proj_string_recognises_step_keyword, test_pipeline_inv_global_reverses_all_steps, test_pipeline_inv_per_step_only_reverses_that_step, test_pipeline_project_step_delegates_to_proj4rs, test_parse_pipeline_splits_multiple_steps. Total: 538 tests pass.

- [x] Implement CRS detection from WKT/PROJ string (auto-identify EPSG code) (completed 2026-05-17)
  - **Done:** New `src/crs_identify.rs` (~400 LoC). `CrsFingerprint { datum: Option<String>, projection: Option<String>, params: BTreeMap<String, f64> }` — canonical normalised shape. `normalise_datum()` lowercases, replaces separators with `_`, maps known aliases (`WGS84`/`WGS1984` → `wgs_84`, `NAD83` → `nad_83`, etc.), strips ESRI `D_` prefix. `fingerprint_from_wkt(wkt)` — calls `parse_wkt`, walks `WktNode` tree to extract `DATUM`, `PROJECTION`, and `PARAMETER` nodes; geographic CRS roots with no `PROJECTION` child fingerprinted as `longlat`. `fingerprint_from_proj(proj)` — tokenises `+key=value` pairs; `+datum` wins over `+ellps`. `fingerprints_match(query, candidate)` — projection synonyms (`longlat`↔`latlong`), datum equality, per-parameter tolerance (`EPS_ABS=1e-9 + EPS_REL=1e-7 * |v|`). `identify_epsg_from_wkt(wkt) -> Option<u32>` and `identify_epsg_from_proj(proj) -> Option<u32>` iterate `available_epsg_codes()` in sorted order. Re-exported from `lib.rs` under `#[cfg(feature = "std")]`.
  - **Tests added (25):** 16 inline unit tests in `crs_identify.rs` + 9 integration tests in `tests/crs_identify_test.rs` covering WGS84/UTM/Web Mercator/BNG identification, PROJ string identification, unknown-datum None return, malformed WKT None return, datum normalisation case-insensitivity, param tolerance. Total: 563 tests pass.

- [x] Add geodetic distance and azimuth calculations (Vincenty inverse) (completed 2026-05-17)
  - **Done:** New `src/geodesic.rs` (~700 LoC). `GeodesicParams { a, b, max_iter, tol }` groups ellipsoid + convergence params (avoids >7-arg clippy limit). `VincentyResult { distance_m, azimuth_fwd_deg, azimuth_rev_deg, iterations }`, `VincentyDirectResult { lat2_deg, lon2_deg, azimuth_rev_deg }`. `vincenty_inverse(lat1,lon1,lat2,lon2,params)` — full iterative Vincenty 1975 inverse: reduced latitudes U1/U2, lambda iteration with C damping factor, u²/A_v/B_v/Δσ correction, azimuth normalization to [0,360). `vincenty_direct(lat1,lon1,azimuth,dist,params)` — direct formula with σ iteration. `haversine_distance_m(lat1,lon1,lat2,lon2,radius)`. WGS84 wrappers: `wgs84_inverse`, `wgs84_direct`, `wgs84_haversine_m`. `GeodesicError { AntipodalPoints, InvalidInput }`. Re-exported from `lib.rs`.
  - **Tests added (18):** test_wgs84_inverse_london_paris_within_10m, test_wgs84_inverse_coincident_points_zero, test_wgs84_inverse_equatorial_segment, test_wgs84_inverse_polar_north_south, test_wgs84_inverse_azimuth_due_east, test_wgs84_direct_vs_inverse_round_trip, test_haversine_vs_vincenty_close_points_agree_within_50m, test_wgs84_inverse_near_antipodal_returns_result, plus 10 additional unit/edge tests. Total: 519 tests pass.

- [x] Implement dynamic datum transformation using Helmert parameters with rates (completed 2026-05-19).
  - **Done:** `src/pipeline.rs` extended with `HelmertRateParams { dtx, dty, dtz, drx, dry, drz, ds, ref_epoch }` and `StepKind::HelmertTemporal { params: HelmertParams, rates: HelmertRateParams, epoch: f64 }`. `parse_helmert_temporal_step` parses `+dtx=`, `+dty=`, `+dtz=`, `+drx=`, `+dry=`, `+drz=`, `+ds=`, `+ref_epoch=`, `+t_epoch=`. `apply_helmert_temporal` computes `dt = epoch - ref_epoch`, converts mm/yr → m (÷1000), mas/yr → rad (×π/648_000_000), ppb/yr → ppm (÷1000), delegates to `apply_helmert`. `HelmertRateParams` re-exported from `lib.rs`.
  - **Tests added (6):** test_parse_helmert_temporal_step_parses_all_rate_fields, test_apply_helmert_temporal_at_ref_epoch_equals_static_helmert, test_apply_helmert_temporal_accumulates_rates_proportionally, test_helmert_temporal_inverse_round_trip, test_pipeline_helmert_temporal_step_parses_and_applies, test_helmert_rate_units_mm_per_year_converted_correctly. Total: 626 tests pass.

- [x] Add support for engineering/local CRS definitions (completed 2026-05-19).
  - **Done:** `src/crs.rs` — `Crs::is_engineering() -> bool` added (returns `true` for `CrsType::Engineering`). `src/transform/mod.rs` — identity transformer path: when `crs.is_engineering()` skip `to_proj_string()` and return a no-op `Transformer` that passes all coordinates through unchanged. Engineering coordinates are pre-defined locally so datum transformation is meaningless.
  - **Tests added (3 in `tests/engcrs_test.rs`):** test_engineering_crs_is_detected_by_is_engineering, test_engineering_crs_transform_is_identity_passthrough, test_non_engineering_crs_is_not_detected. Total: 626 tests pass.

## Low Priority / Future (speculative — one-liners only)

- [x] Extend PROJ pipeline grammar with Helmert, Cart, Vgridshift, Hgridshift step kinds (completed 2026-05-19).
  - **Done:** `src/pipeline.rs` extended (~519 LoC added, 780→1299). New public types: `HelmertParams { tx,ty,tz,rx,ry,rz,s,convention }`, `HelmertConvention { PositionVector, CoordinateFrame }`, `EllipsoidParams { a, f }`, `ShiftDirection { Forward, Inverse }`. `StepKind` gained 4 variants: `Helmert`, `Cart` (geographic↔ECEF Bowring 1985 inverse), `Vgridshift` (stub — deferred grid loading), `Hgridshift` (stub). `parse_pipeline_step` extended with 4 new keyword arms (`helmert`, `cart`, `vgridshift`, `hgridshift`). `ellipsoid_from_name` lookup table for WGS84/GRS80/Bessel. Helmert position_vector/coordinate_frame convention distinction implemented. Re-exported `EllipsoidParams, HelmertConvention, HelmertParams, ShiftDirection` from `lib.rs`.
  - **Tests added (12):** test_parse_helmert_step_position_vector, test_parse_helmert_step_coordinate_frame, test_apply_helmert_identity_zero_params, test_apply_helmert_inverse_round_trip, test_parse_cart_step_wgs84, test_apply_cart_forward_origin_yields_x_a, test_apply_cart_round_trip_bowring, test_parse_vgridshift_step_unknown_grid_yields_error, test_parse_hgridshift_step_unknown_grid_yields_error, test_pipeline_with_helmert_then_unitconvert_chains, test_pipeline_with_cart_inverse_returns_geographic, test_parse_ellipsoid_grs80_lookup. Total: 617 tests pass.
- [x] Add thread-safe CRS cache with LRU eviction for transformer reuse (completed 2026-05-18).
  - **Done:** New `src/cache.rs` (~347 LoC). `TransformerCache { inner: Arc<Mutex<LruInner>>, capacity: usize }` thread-safe via mutex; `TransformerKey { src_epsg, dst_epsg }` `Hash+Eq+Copy`. `LruInner { map: HashMap, order: VecDeque }`. API: `new(capacity)` (clamped ≥1), `capacity()`, `len()`, `is_empty()`, `contains(src, dst)`, `clear()`, `cached_keys() -> Vec<TransformerKey>` (MRU-first), `get_or_build(src, dst) -> Result<Arc<Transformer>, Error>` (atomic check-and-insert with race-loss recovery; oldest evicted from `order.pop_back()` when at capacity). Custom `Debug` impl + `Clone`. Poison-recovering `lock_inner` helper via `PoisonError::into_inner` (panic-free). `Transformer.last_warning` field migrated from `Cell` to `Mutex` so `Transformer: Send + Sync` (required for `Arc<Transformer>` thread-safety). Re-exported `TransformerCache`, `TransformerKey` from `lib.rs` under `#[cfg(feature = "std")]`.
  - **Tests added (15):** test_cache_empty_starts_with_zero_len, test_cache_get_or_build_inserts_on_miss, test_cache_get_or_build_returns_same_arc_on_hit (Arc::ptr_eq), test_cache_evicts_oldest_when_full, test_cache_hit_moves_key_to_mru, test_cache_capacity_clamped_to_at_least_one, test_cache_clear_empties, test_cache_contains_after_insert, test_cache_thread_safe_concurrent_inserts (8 threads), test_cache_cached_keys_in_mru_order, plus 5 additional (clone-shares-state, error propagation, unit tests). Total: 605 tests pass.
- [x] Implement geoid model loading (EGM96, EGM2008) for orthometric heights (completed 2026-05-20).
  - **Done:** New `src/geoid_formats.rs` (~594 LoC, `#[cfg(feature = "std")]`). Parses the real distribution formats the existing raw-grid `load_egm_grid` does not. **EGM96 ASCII** (`WW15MGH.GRD`): `Egm96AsciiHeader` + `parse_egm96_ascii_header`/`parse_egm96_ascii_str`/`parse_egm96_ascii_file` — 6-number header (`lat_min lat_max lon_min lon_max dlat dlon`), row-major undulation values, derives `n_lat`/`n_lon`, tolerant of extra whitespace. **EGM2008 2.5′ binary**: `Egm2008BinaryHeader { n_lat, n_lon }` + `parse_egm2008_binary_25_header`/`parse_egm2008_binary_25`/`parse_egm2008_binary_25_file` — 8-byte LE-u32 header + row-major f32 LE body, canonical 4321×8640 geometry. `load_geoid_auto(path)` sniffs ASCII-digit/sign vs binary header, erring conservatively on ambiguity. Reuses `GeoidGrid`/`GeoidModel`/`Error::GeoidFileFormat`; `load_egm_grid` signature untouched. Re-exported from `lib.rs` under `#[cfg(feature = "std")]`.
  - **Tests added (18):** 13 in `tests/geoid_formats_test.rs` (header canonical/truncated, ASCII 3×3 round-trip, count-mismatch error, extra-whitespace, file-via-tempfile, EGM2008 header canonical/zero-dim, 3×3 synthetic grid, size-mismatch error, round-trip height lookup, auto-detect ASCII/binary) + 5 inline. Total: 720 tests pass.
- [x] Add South Pole / North Pole specific projection handling (UPS). (completed 2026-05-19)
  - **Done:** New `src/ups_projection.rs` (~230 LoC). `UpsHemisphere { North, South }`. `UpsCoordinate { easting, northing, hemisphere }`. `PolarStereographicParams { semimajor_a, eccentricity_e, scale_factor_k0, false_easting, false_northing, hemisphere }` with `wgs84_ups_north()` / `wgs84_ups_south()` constructors. `polar_stereo_w(e) -> f64` = sqrt((1+e)^(1+e)·(1-e)^(1-e)) ≈ 1.00324 for WGS84. `polar_stereographic_forward` (Snyder Ch.21 ellipsoidal t-formula, ρ = 2ak₀t/W). `polar_stereographic_inverse` (fixed-point 10-iteration convergence on conformal latitude). `ups_from_geographic` (auto-selects hemisphere; returns Error::InvalidCoordinate for |lat| < 80°). `ups_to_geographic`. `ups_zone_letter` (Y/Z north, A/B south, None inside UTM band). Re-exported from `lib.rs`.
  - **Tests added (16):** 16 integration tests covering pole forward (1 m tolerance), round-trip at 85°N/S and 88°N (1 mm tolerance), known-value validation, inverse at origin, zone letter partitioning, low-latitude rejection, W constant. Total: passes.
- [x] Implement `GeocentricCRS` for 3D ECEF transformations with full accuracy (completed 2026-05-19).
  - **Done:** New `src/geocentric.rs` (~480 LoC). `GeocentricEllipsoid { semimajor_a, flattening_f }` with `eccentricity_squared()`, `semiminor_b()`, and named constructors `wgs84()`, `grs80()`, `airy1830()`, `bessel1841()`. `EcefCoordinate { x, y, z }` with `new()`, `magnitude()`, `subtract()`. `GeocentricCrs { name, epsg_code, ellipsoid }` with `wgs84()` (EPSG:4978), `etrs89()` (EPSG:4936), `nad83()`, `custom()`. `geographic_to_ecef` via prime-vertical-radius N-formula. `ecef_to_geographic` via Bowring 1985 closed-form. `ecef_to_geographic_iterative` via Heiskanen-Moritz (≤10 iters, 1e-12 tol). `EcefTransformer` with optional `Helmert7Params`, `transform()` / `transform_batch()`. `geographic_to_geographic_via_ecef` three-step datum conversion. Re-exported from `lib.rs`.
  - **Tests added (14):** round-trip, Bowring vs iterative, pole, equator, ellipsoid constants, EPSG:4978 tag, Helmert application, transformer identity. Total: passes.
- [x] Add PROJ.db SQLite database reader for complete EPSG lookup. (completed 2026-05-19)
  - **Done:** New `src/epsg/proj_db.rs` (~330 LoC) gated `#[cfg(feature = "proj-db")]`. `ProjDb { conn, path }` with `open()` (read-only), `open_first_available()` (searches standard paths: `$PROJ_DATA/proj.db`, `/usr/share/proj/proj.db`, etc.). Schema detection queries `sqlite_master` to select between PROJ ≥8 `crs_view` and legacy `geodetic_crs`/`projected_crs` union. `ProjDbEntry { code, name, proj_string, kind, area_of_use, deprecated }`. `lookup_epsg()`, `lookup_authority()`, `list_epsg_codes()`, `count_epsg_codes()`. `populate_from_proj_db(db, proj_db)` bulk-fills via `entry.or_insert_with(...)` (built-ins always win). `EpsgDatabase::populate_from_system_proj_db()` convenience method. `ProjError::ProjDbError(String)` variant. Re-exported from `lib.rs`.
  - **Tests added (13):** default paths, env-var override, nonexistent-path error, fixture lookup, authority lookup, count/list, populate priority. All use in-memory SQLite fixtures — no real proj.db required.
- [x] Implement coordinate operation selection by accuracy (pick best transform) (completed 2026-05-19).
  - **Done:** New `src/operation_selection.rs` (~290 LoC). `CandidateOperation { name, accuracy_m, area_of_use: Option<AreaOfUse>, source_id }`. `OperationRanking { candidate_idx, effective_accuracy_m, coverage_fraction, score }`. `select_best_operation(candidates, query_bbox) -> Option<usize>` — filters by `area_coverage_fraction < 0.01` when query_bbox provided; identity transforms (accuracy_m == 0.0) score `f64::INFINITY`. `rank_operations(candidates, query_bbox) -> Vec<OperationRanking>` — full sorted list descending by score. `area_coverage_fraction(aou, bbox) -> f64` — rectilinear intersection / query area; handles zero-extent point queries. `operation_score(accuracy_m, coverage) -> f64` — `ln_1p(coverage³ / accuracy_m.max(1e-9))`. Re-exported from `lib.rs`.
  - **Tests added (13 in `tests/operation_selection_test.rs`):** test_operation_score_higher_for_better_accuracy, test_operation_score_higher_for_better_coverage, test_operation_score_identity_is_infinite, test_select_best_single_candidate, test_select_best_empty_returns_none, test_select_best_chooses_higher_accuracy_when_coverage_equal, test_select_best_chooses_better_coverage_when_accuracy_equal, test_select_best_filters_low_coverage_candidates, test_select_best_no_query_bbox_uses_accuracy_only, test_rank_operations_returns_sorted_descending, test_area_coverage_fraction_full_overlap_returns_one, test_area_coverage_fraction_no_overlap_returns_zero, test_area_coverage_fraction_partial_overlap. Total: 652 tests pass.
- [ ] Add benchmarks against proj4rs and PROJ C library for accuracy regression testing.

## Cross-crate dependencies

- **Blocks:** `oxigeo-qc` (CRS validator), `oxigeo-algorithms` (CrsTransformer integration), `oxigeo-services` (reproject pipeline), `oxigeo-geotiff`/`oxigeo-geoparquet` (CRS metadata)
- **Blocked by:** None directly (proj4rs optional via the `proj4rs-compat` feature; OxiProj is the default pure-Rust engine)

## Recently completed (verbatim)

- [x] Expand built-in EPSG registry to 500+ codes (added extended.rs with 300+ definitions covering NAD83 State Planes, European, Asian-Pacific, South American, African, Polar grids, geographic CRS, and vertical CRS)
- [x] Add WKT2:2019 (ISO 19162) parsing and generation (WKT2 keywords GEOGCRS/PROJCRS/GEODCRS/VERTCRS/ENGCRS/COMPOUNDCRS/BOUNDCRS, axis extraction, ellipsoid alias support, 26 tests)
- [x] Add Oblique Mercator and Hotine Oblique Mercator projections (Hotine variant A, spherical, 3D rotation-matrix approach, 8 tests)
- [x] Implement Albers Equal-Area Conic projection (forward + inverse, Snyder p.98-103, 7 tests)
- [x] Add Polyconic and Equirectangular projections (American Polyconic with Newton-Raphson inverse + Plate Carrée, 12 tests)

---
*Last audited: 2026-07-28*

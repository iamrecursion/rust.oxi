# TODO: oxigeo-pmtiles

> **Purpose:** Pure Rust PMTiles v3 reader/writer for OxiGeo — single-file tile archive with varint directory + Hilbert curve tile IDs
> **Status (2026-07-28):** ~6,050 Rust LoC · 508 tests all-features / 397 default-features · 0 real-code stubs (2 pending items are roadmap features: Hilbert curve visualization utilities, oxigeo-services HTTP-serving integration)
> **Roadmap:** v0.2.1 (current) → v1.0.0

## High Priority
- [x] Implement tile data retrieval by (z, x, y) (completed 2026-04-19, part of P1)
- [x] Add gzip/brotli/zstd decompression via OxiARC (completed 2026-04-19, part of P1; `compression` feature, no C deps)
- [x] Implement leaf directory support (root + leaf, per PMTiles v3 spec; reader follows `run_length == 0` entries)
- [x] Add HTTP range-request reader: fetch only the header + directory + requested tiles over the network without downloading the entire file (completed 2026-05-19).
  - **Done:** New `http-range` feature (dep: `oxigeo-streaming`). New `src/http_reader.rs` (~280 LoC) gated `#[cfg(feature = "http-range")]`. `HttpPmTilesReader` wraps `HttpObjectStore + ObjectUrl`. `open(url_str)` eagerly fetches header (127 bytes) + root directory via two HTTP Range requests. `get_tile(z,x,y)` two-level directory walk; leaf directories cached by `(offset, length)` key in `HashMap`. `block_on_async` bridges async `HttpObjectStore::get_range` to sync callers via `tokio::task::block_in_place` / fallback runtime. `PmTilesError::IoError(String)` and `PmTilesError::HttpError(String)` variants added. Re-exported `HttpPmTilesReader` from `lib.rs`.
  - **Tests added (10):** `TcpListener`-based mock server (127.0.0.1:0, keep-alive, Range/206/200/404 support) — test_http_reader_open_fetches_header, test_open_invalid_magic_returns_error, test_get_tile_returns_some, test_get_tile_returns_none, test_get_tile_walks_leaf_directory, test_leaf_cache_grows, test_leaf_cache_stable, test_404_returns_io_error, test_header_accessor, test_root_directory_accessible. Total: 339 tests pass with feature.
- [x] Implement tile content deduplication in PmTilesBuilder (completed 2026-04-19, part of P1; FNV-1a content hash, shared offsets)
- [x] Add PMTiles v3 archive validation (completed 2026-05-17)
  - **Done:** New `src/validate.rs` (~620 LoC) — `ValidationIssue` enum (9 variants: HeaderMagicMismatch, UnsupportedVersion, RootDirOutOfBounds, LeafDirOutOfBounds, TileDataOutOfBounds, OverlappingTiles, NonMonotonicTileIds, DirectoryDecodeError, VarintTruncated) + `ValidationReport { passed, issues }`. `validate_archive(bytes) -> ValidationReport` performs 8-pass validation: magic+version, 4 section bounds checks, root-dir BFS + leaf decode + per-tile data bounds, monotonic tile-ID check per leaf, overlapping-range detection (sort-and-scan, dedup-safe). `validate_archive_strict(bytes) -> Result<(), Vec<ValidationIssue>>`. Re-exported from `src/lib.rs`.
  - **Tests added (23):** 10 integration tests in `tests/validate_test.rs` + 13 inline unit tests in `validate.rs` covering all issue variants. Total: 206 tests pass.

## Medium Priority
- [x] Implement streaming/async tile reader (read tiles from `AsyncRead` / `Read` source without loading entire file) (completed 2026-05-19).
  - **Done:** New `src/async_reader.rs` (~300 LoC) gated `#[cfg(feature = "async")]`. `AsyncPmTilesReader<R: AsyncRead + AsyncSeek + Unpin>`. `open(reader) -> Result<Self>` (async) eagerly reads header + root directory via `read_range`. `get_tile(z,x,y)` / `get_tile_by_id(tile_id)` two-level directory walk with leaf cache; returns decompressed tile bytes. `enumerate_tiles()` BFS walk of directory tree returning `Vec<(tile_id, z, x, y)>`. `tile_compression()`, `decompress_tile(data)` — matches `Compression` to zlib/gzip/zstd/brotli decode. `async = ["dep:tokio"]` feature added to `Cargo.toml`. Re-exported from `lib.rs`.
  - **Tests added (10 in `tests/async_reader_test.rs`):** test_async_reader_open_reads_header_and_root_dir, test_async_reader_get_tile_present_returns_some, test_async_reader_get_tile_missing_returns_none, test_async_reader_get_tile_by_id_matches_get_tile, test_async_reader_enumerate_tiles_finds_all, test_async_reader_leaf_cache_grows_on_new_leaves, test_async_reader_leaf_cache_stable_on_repeat, test_async_reader_decompress_tile_none_compression_passthrough, test_async_reader_invalid_header_returns_error, test_async_reader_round_trip_data_integrity. Total: 339 tests pass.
- [x] Add metadata JSON parsing and structured access (completed 2026-05-17).
  - **Done:** New `src/metadata.rs` — `PmTilesMetadata` struct with typed well-known fields (`name`, `description`, `format`, `bounds: Option<[f64;4]>`, `center: Option<[f64;3]>`, `minzoom`, `maxzoom`, `attribution`) and flattened `extra: serde_json::Map` for unknown keys. `from_bytes(data, compression) -> Result<Self>` decompresses then JSON-parses; empty slice returns all-None. `to_json()` round-trip serializer. `PmTilesReader::metadata()` added to `src/pmtiles.rs`. Added `serde`+`serde_json` workspace deps to Cargo.toml.
- [x] Implement PmTilesReader tile enumeration (completed 2026-05-17).
  - **Done:** `TileInfo { tile_id, z, x, y, data_offset, data_length }` struct. `PmTilesReader::enumerate_tiles() -> Result<Vec<TileInfo>>` — two-level BFS walk (root → leaf pages per `run_length==0` detection), expands run-length entries (each run of N consecutive tiles sharing same data yields N `TileInfo` records with identical offsets). `hilbert::tile_id_to_zxy` converts tile IDs to (z,x,y). `PmtilesError::InvalidArchive`, `UnsupportedCompression`, `JsonParse` variants added. `TileInfo` and `PmTilesMetadata` re-exported from `lib.rs`.
  - **Tests added (19):** 11 integration tests in `tests/enumerate_metadata_test.rs` + 7 inline unit tests. Total: 225 tests pass.
- [x] Add tile type auto-detection from tile data content (completed 2026-05-17)
  - **Done:** New `src/tile_detect.rs` (~280 LoC). `DetectedTileFormat` enum (8 variants: Png, Jpeg, WebP, Avif, Mvt, GZip, Zstd, Unknown). `detect_tile_format(data: &[u8]) -> DetectedTileFormat` — magic byte sniffing: PNG (8-byte signature), JPEG (SOI marker 0xFF 0xD8 0xFF), WebP (RIFF+WEBP 12-byte), AVIF (ftyp box at offset 4), GZip (2-byte), Zstd (4-byte magic 0xFD2FB528), MVT heuristic (protobuf wire type 2/field 3 = `layers` key). `FormatCounts` accumulator for tallying across a set of tiles. `PmTilesReader::detect_tile_format(z,x,y)` and `PmTilesReader::detect_dominant_format(sample_size)` added to `src/pmtiles.rs`. Re-exported from `lib.rs`.
  - **Tests added (27):** 10 integration tests in `tests/tile_detect_test.rs` + 17 inline unit tests covering all format signatures, FormatCounts accumulation, Unknown fallback. Total: 262 tests pass.
- [x] Implement run-length encoding optimization in PmTilesBuilder for consecutive tiles with identical data (completed 2026-04-18)
- [x] Add PMTiles-to-MBTiles conversion (oxigeo-mbtiles integration) (completed 2026-05-19)
  - **Done:** New `mbtiles` feature + optional `rusqlite` dep. New `src/mbtiles_export.rs` (~250 LoC) gated `#[cfg(feature = "mbtiles")]`. `MbTilesExporter<'a> { reader }` with `export_to_path(P) -> Result<MbTilesExportStats>` and `export_to_connection(&Connection) -> Result<MbTilesExportStats>`. `MbTilesExportStats { tiles_written, min_zoom, max_zoom, bytes_written, metadata_keys }`. Schema: `metadata` (unique name index) + `tiles WITHOUT ROWID`. TMS row conversion: `tms_row = (1 << z) - 1 - xyz_y`. Performance pragmas: `PRAGMA journal_mode=OFF; PRAGMA synchronous=OFF`. `format_from_tile_type`: PNG→"png", JPEG→"jpg", WebP→"webp", MVT→"pbf". Re-exported from `lib.rs`.
  - **Tests added (13):** schema creation, empty archive, 3-tile export, required metadata fields, format strings for PNG/MVT, TMS row arithmetic (z=0/2/10), file export + re-open, byte-exact round-trip, overwrite behaviour, zoom range in stats. Total: 13 pass.
- [x] PMTiles v3 archive writer with root + leaf directory hierarchy (verified 2026-06-06)
  - **Goal:** Writer produces valid PMTiles v3 archives from (tile_id, tile_data) pairs. Root-only for < ~16k tiles, root+leaves for larger archives.
  - **Design:** Two-pass: collect (tile_id, content_hash, data) → sort by tile_id → run-length compress directory → decide root/leaf split → write header with seek-and-patch for offsets.
  - **Files:** Enhanced writer.rs (run-length + leaf support), writer/leaves.rs (leaf split logic)
  - **Tests:** 499 tests pass (0 failed, 5 skipped); 0 clippy warnings
- [x] Implement clustered vs. non-clustered directory layout selection based on tile ordering (completed 2026-05-20)
  - **Done:** New `src/layout.rs` (~290 LoC). `LayoutStrategy { Auto (default), Clustered, LeafOptimized, Compact }`. `analyze_tile_ordering(&[(tile_id, data_offset, data_length)]) -> LayoutAnalysis { tile_count, unique_data_count, dedup_ratio, is_clustered, max_gap, mean_gap }` checks monotonic non-decreasing offsets in tile-id order + dedup/gap stats. `choose_strategy(&analysis, strategy)` resolves `Auto`: `dedup_ratio > 0.5` → Compact, else `tile_count >= 16384` → LeafOptimized, else Clustered (explicit strategies pass through). `apply_strategy_to_writer(&mut PmTilesBuilder, strategy)` sets the header `clustered` flag. `PmTilesBuilder` gains `layout_strategy`/`clustered_override` fields + `set_layout_strategy`/`layout_strategy` accessors; `build()` resolves `Auto` from the pre-run-length manifest before serialising the header (explicit override wins, else dedup-derived default). `PmTilesReader::is_clustered()` + `detected_layout()` re-derive the analysis from the stored root directory. Re-exported from `lib.rs`.
  - **Tests added (21):** 14 in `tests/layout_test.rs` (empty/monotonic/unsorted analysis, dedup ratio, Auto small→Clustered / large(16384)→LeafOptimized / dedup-heavy→Compact, explicit pass-through, default-is-Auto, override, writer header byte 96 == 1/0 for Clustered/Compact, reader is_clustered, round-trip detected_layout) + 7 inline. Total: 388 tests pass.
- [x] Add bounding box and center point auto-calculation from tile extents (completed 2026-05-17)
  - **Done:** New `src/webmerc.rs` (~160 LoC) — `tile_to_lonlat(z,x,y) -> (f64,f64)` top-left corner; `tile_bounds_lonlat(z,x,y) -> (min_lon,min_lat,max_lon,max_lat)` with ±85.051129° latitude clamp; `lonlat_to_tile(z,lon,lat) -> (u32,u32)` inverse. `PmTilesBuilder` extended with `auto_bounds()` (iterates tile set, unions `tile_bounds_lonlat` extents, writes e7-scaled i32 fields), `auto_zoom_range()` (derives min/max from Hilbert tile_id_to_zxy), `auto_center()` (bbox midpoint at min_zoom), `auto_all()` (chains zoom→bounds→center, idempotent). `add_tile_by_id(tile_id, data)` bypass method added for compact.rs. Re-exported `tile_to_lonlat`, `tile_bounds_lonlat`, `lonlat_to_tile` from `lib.rs`.
  - **Tests added (10):** test_tile_to_lonlat_z0_origin, test_tile_to_lonlat_z1_quadrants, test_tile_bounds_lonlat_z0_full_world, test_tile_bounds_lonlat_z1_ne_quadrant, test_lonlat_to_tile_round_trip_at_z10, test_auto_bounds_single_tile_z0, test_auto_bounds_multi_tile_aggregates, test_auto_center_is_bbox_midpoint, test_auto_zoom_range_from_tiles, test_auto_all_chaining. Total: 292 tests pass.
- [x] Implement archive compaction (rewrite archive to remove gaps from deleted/replaced tiles) (completed 2026-05-17)
  - **Done:** New `src/compact.rs` (~220 LoC). `CompactOptions { deduplicate, sort_by_tile_id, preserve_metadata }`. `CompactStats { tiles_read, tiles_written, tiles_deduplicated, bytes_before, bytes_after, reduction_pct }`. `compact_archive(bytes, opts) -> Result<Vec<u8>>` — reads all tiles via `enumerate_tiles()`, re-feeds into fresh `PmTilesBuilder`, deduplicates via `HashMap<Vec<u8>,u64>`, propagates header bounds/center when `preserve_metadata=true`. `compact_archive_default(bytes)` convenience wrapper. `compact_archive_with_stats(bytes, opts) -> Result<(Vec<u8>, CompactStats)>`. Added `PmTilesBuilder::add_tile_by_id` for direct tile_id insertion. Re-exported from `lib.rs`.
  - **Tests added (14):** test_compact_empty_archive_returns_valid_archive, test_compact_single_tile_round_trip, test_compact_multi_tile_preserves_all_tiles, test_compact_deduplication_reduces_unique_contents, test_compact_no_deduplicate_keeps_all, test_compact_stats_bytes_before_after, test_compact_stats_tiles_read_equals_enumerate, test_compact_default_options_returns_valid_archive, test_compact_tile_data_preserved_exactly, plus 5 additional. Total: 276 tests pass.

## Low Priority / Future
- [x] Add S3/GCS/Azure Blob Storage range-request adapter for cloud-hosted PMTiles (completed 2026-05-19)
  - **Done:** New `src/cloud_reader.rs` (~800 LoC) gated `#[cfg(feature = "cloud-storage")]`. `CloudProvider { S3 { region }, Gcs, AzureBlob { account } }`. `CloudCredentials { access_key, secret_key, session_token, bearer_token }` (default anonymous). `CloudObjectUri { provider, bucket, key }` with `parse()` (handles `s3://`, `gs://`, `az://`), `to_https_url()` (virtual-host S3, GCS path-style, Azure FQDN). `CloudPmTilesReader` with lazy header+directory init, tile read via `reqwest` range requests, bearer-token auth. S3 sigv4/Azure shared-key explicitly deferred (documented). Re-exported from `lib.rs`.
  - **Tests added (12):** 7 pure URI-parsing tests pass; 5 network tests marked `#[ignore]`.
- [x] Implement ETag-based caching for HTTP range-request reader (completed 2026-05-19).
  - **Done:** New `src/etag_cache.rs` (~450 LoC) gated `#[cfg(feature = "http-range")]`. `EtagCache { entries: HashMap<RangeKey, CachedEntry>, lru_order: VecDeque<RangeKey>, max_entries, seq }`. `get(offset, length) -> Option<(Vec<u8>, String)>` — returns data + ETag and bumps LRU. `peek` — non-mutating. `insert` — evicts LRU front when at capacity. Zero-capacity no-ops. `http_reader.rs` extended with `etag_cache: Option<EtagCache>`, `with_etag_cache(max_entries)` builder, `cached_byte_range_count()`, and cached `fetch_range` path (cache hit skips network; miss inserts with synthetic ETag `"<offset>-<length>"` for immutable-archive semantics). ETag stored for future `If-None-Match` revalidation (documented as out-of-scope pending `HttpObjectStore` header support). Re-exported `EtagCache` from `lib.rs`.
  - **Tests added (10 in `tests/etag_cache_test.rs`):** test_etag_cache_new_empty, test_etag_cache_insert_get_round_trip, test_etag_cache_get_miss_returns_none, test_etag_cache_eviction_when_at_capacity, test_etag_cache_lru_order_updated_on_get, test_etag_cache_clear_resets_size, test_etag_cache_zero_capacity_never_stores, test_http_reader_with_etag_cache_uses_cache_on_second_fetch, test_http_reader_etag_cache_zero_capacity_disables_caching, test_http_reader_etag_cache_count_increases_with_distinct_ranges. Total: 349 tests pass.
- [x] Add tile set diffing between two PMTiles archives (completed 2026-05-18)
  - **Done:** New `src/diff.rs` (~466 LoC). `TileChange` enum (Added/Removed/Changed) with `tile_id()`, `zoom()`, `xy()` accessors. `DiffReport { added, removed, changed, unchanged_count }` with `total_changes()`, `total_added_bytes()`, `total_removed_bytes()`, `changed_byte_delta()`, `is_empty()` helpers. `DiffSummary { added, removed, changed, unchanged }` Copy struct with `total_changes()` / `total_tiles()`. `diff_archives(old, new) -> Result<DiffReport>` — opens both readers, enumerates tiles into `HashMap<tile_id, (z, x, y, fnv1a_hash, byte_size)>`, computes set difference; tiles in both compared by FNV-1a content hash (offset 0xcbf2_9ce4_8422_2325, prime 0x0100_0000_01b3 — matches writer dedup). Output sorted by tile_id for determinism. `diff_archives_summary(old, new)` convenience wrapper. Re-exported from `lib.rs`.
  - **Tests added (19):** 7 inline unit tests + 12 integration tests covering identical archives, empty-vs-empty, added-only, removed-only, changed-only content, byte tallies, disjoint zoom levels, total_changes, unchanged_count, sorted output, summary matches report, invalid archive. Total: 329 tests pass.
- [ ] Implement Hilbert curve visualization/debugging utilities
- [ ] Add integration with oxigeo-services for HTTP tile serving directly from PMTiles
- [x] Implement parallel tile encoding in PmTilesBuilder using rayon (feature-gated) (completed 2026-05-19)
  - **Done:** New `src/parallel_encode.rs` (~300 LoC), feature `parallel = ["dep:rayon", "compression"]`. `RawTile { tile_id, raw_data }`. `ParallelEncodeConfig { chunk_size=64, threads: Option<usize> }`. `ParallelBuildStats { tiles_encoded, chunks_processed, total_bytes_raw, total_bytes_compressed, compression_ratio }`. `compress_tile`, `compress_tiles_parallel` (rayon par_iter), `build_pmtiles_parallel` (full pipeline). Re-exported from lib.rs.
  - **Tests added (12 in `tests/parallel_encode_test.rs`):** round-trip, order preservation, empty/single/chunk-size variants, stats validation.
- [x] Add PMTiles v2 backward compatibility reader (completed 2026-05-19).
  - **Done:** New `src/header_v2.rs` + `src/v2_reader.rs`. `PmTilesV2Entry { z, x, y, offset, length, is_dir }`, `PmTilesV2Header { version, metadata, root_entries }`. `parse_v2_header()` two-pass boundary detection: pass 1 reads candidates, pass 2 retains entries whose offsets don't overlap tile data. `read_v2_entry()` decodes 17-byte entries (u8 z, u24 x/y LE, u48 offset LE, u32 length LE). `PmTilesV2Reader` with `from_bytes()`, `from_file()`, `get_tile()`, `enumerate_tiles()`, `upgrade_to_v3()`. `detect_pmtiles_version()` peeks magic. `InvalidV2Header` error variant added. Re-exported from `lib.rs`.
  - **Tests added (24):** synthetic archive construction, magic detection, header parse, entry decode, Hilbert parity, tile retrieval, upgrade to v3. Total: passes.
- [x] Implement directory compression (gzip/brotli/zstd) in writer for smaller archive headers (completed 2026-05-18)
  - **Done:** `src/writer.rs` extended (~150 LoC). New private `compress_internal_section(data, compression)` dispatcher routes to `oxiarc_archive::{gzip, brotli, zstd}` for non-None codecs; `None`/`Unknown` returns verbatim. `split_into_leaves(entries, compression)` updated to compress each leaf chunk individually (reader can decompress one leaf at a time via existing `read_leaf_directory(offset, length)` path). `build()` updated to compress root directory as a single frame and metadata JSON as a single frame. Header byte 97 now reflects actual compression. Gated `#[cfg(feature = "compression")]`; without feature, non-None compression returns `PmTilesError::UnsupportedCompression`. Reader (`pmtiles.rs`) required no changes — Slices 12 & 13 already wired `decompress_data(raw, &self.header.internal_compression)` into root dir load, leaf dir load, and metadata parse paths; those calls now operate on actual compressed bytes instead of always-uncompressed ones.
  - **Tests added (10):** test_writer_emits_compressed_root_dir_when_gzip_selected (gzip magic 0x1f 0x8b), test_writer_emits_compressed_root_dir_when_brotli_selected, test_writer_emits_compressed_root_dir_when_zstd_selected (zstd magic 0x28 B5 2F FD), test_writer_emits_uncompressed_root_dir_when_none_selected, test_writer_emits_compressed_metadata_when_internal_compression_set, test_reader_decompresses_root_dir_when_header_says_gzip, test_reader_decompresses_root_dir_when_header_says_zstd, test_round_trip_archive_with_gzip_internal_compression_preserves_all_tiles (100 tiles), test_round_trip_archive_with_compressed_leaves_finds_tiles_at_high_zoom (16384 tiles to force leaf creation), test_compressed_archive_smaller_than_uncompressed_for_repetitive_tile_layout. Total: 361 tests pass with features.
- [x] Add tile re-compression (transcode between gzip/brotli/zstd) during archive construction (completed 2026-05-18)
  - **Done:** New `src/transcode.rs` (~429 LoC). `TranscodeOptions { from: Compression, to: Compression, level: Option<i32> }` with `Default { from: Unknown, to: Gzip, level: None }`. `TranscodeStats { tiles_transcoded, tiles_skipped_identity, bytes_before, bytes_after }` with `ratio()`. `transcode_tile(data, from, to, level) -> Result<Vec<u8>>` — decompress(from) → recompress(to); identity short-circuit when `from == to`. `transcode_archive(bytes, opts)` opens `PmTilesReader`, auto-detects source compression when `opts.from == Compression::Unknown`, enumerates tiles, transcodes each via `transcode_tile`, mirrors source header (tile_type, zoom range, bounds, center, metadata JSON) onto output `PmTilesBuilder`. `transcode_archive_with_stats` exposes byte tallies. Internal `decompress_with`/`compress_with` dispatch to `oxiarc_archive::{gzip, brotli, zstd}`. `PmTilesBuilder` extended with `internal_compression`/`tile_compression` fields + `set_tile_compression()`/`set_internal_compression()` setters. Gzip level clamped to [0,9], brotli quality to [0,11], default 6. Re-exported behind `#[cfg(feature = "compression")]` from `lib.rs`.
  - **Tests added (19):** 8 inline unit tests + 11 integration tests in `tests/transcode_test.rs` covering identity passthrough, gzip↔zstd round-trip, brotli↔gzip round-trip, empty archive, single-tile compression change, header updates, byte counts, metadata preservation, unsupported-format errors, default-options round-trip, ratio boundary. Total: 333 tests pass.
- [x] Implement extract sub-region: create a new PMTiles archive containing only tiles within a bbox (completed 2026-05-17)
  - **Done:** New `src/extract.rs` (~350 LoC). `ExtractOptions { min_zoom: Option<u8>, max_zoom: Option<u8>, preserve_metadata: bool }` (Default: None/None/true). `bbox_to_tile_range(z, min_lon, min_lat, max_lon, max_lat) -> Result<(u32,u32,u32,u32)>` — converts WGS-84 bbox to inclusive tile grid range; correctly handles tile-y north-up inversion (tile for max_lat has lower y-index than tile for min_lat); rejects antimeridian-crossing bboxes with `PmTilesError::InvalidBounds`. `extract_subregion(archive, bbox, opts) -> Result<Vec<u8>>` — opens `PmTilesReader`, derives effective zoom range, iterates each zoom level via `bbox_to_tile_range`, fetches tiles via `reader.get_tile(z,x,y)`, builds fresh `PmTilesBuilder`. If `preserve_metadata`, copies header bounds intersected with requested bbox. `PmTilesError::InvalidBounds(String)` added to `src/error.rs`. Re-exported from `lib.rs`.
  - **Tests added (17):** 7 integration tests in `tests/extract_test.rs` + 10 inline unit tests covering full-world z=0, single-tile z=2, empty result, single-tile round-trip, multi-zoom preservation, outside-bbox exclusion, zoom-range metadata copying, y-inversion correctness, antimeridian rejection. Total: 309 tests pass.

## Cross-crate dependencies
- **Blocks:** `oxigeo` (re-exported via `pmtiles` feature), `oxigeo-services` (HTTP tile serving from PMTiles), `oxigeo-cli` (potential `tile` subcommand consumer)
- **Blocked by:** `oxiarc-*` (gzip/brotli/zstd compression — already wired via `compression` feature)

---
*Last audited: 2026-07-28*

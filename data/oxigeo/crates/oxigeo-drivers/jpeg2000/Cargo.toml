[package]
name = "oxigeo-jpeg2000"
description = "Pure Rust JPEG2000 (JP2/J2K) driver for OxiGeo - JP2 box parsing and JPEG2000 codestream decoding"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/oxigeo-jpeg2000"
readme = "README.md"
keywords = ["jpeg2000", "jp2", "j2k", "jpx", "codec"]
categories = ["science::geo", "parser-implementations", "multimedia::images"]

[features]
default = ["std"]
std = ["oxigeo-core/std", "thiserror/std"]
async = ["oxigeo-core/async", "dep:tokio"]

[dependencies]
oxigeo-core = { workspace = true, default-features = true }
thiserror = { workspace = true }
byteorder = { workspace = true }
tracing = { workspace = true }
num-traits = { workspace = true }

# Async support
tokio = { workspace = true, optional = true, features = ["io-util"] }

[dev-dependencies]
proptest.workspace = true
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
criterion.workspace = true
tempfile = { workspace = true }

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

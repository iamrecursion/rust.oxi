[package]
name = "oxigeo-geojson"
description = "GeoJSON (RFC 7946) driver for OxiGeo - Pure Rust vector data processing with streaming support"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/oxigeo-geojson"
readme = "README.md"
keywords = ["geojson", "gis", "vector", "json", "gdal"]
categories = ["science::geo", "parser-implementations", "encoding"]

[features]
default = ["std"]
std = ["oxigeo-core/std", "thiserror/std", "serde/std"]
async = ["oxigeo-core/async", "dep:tokio"]
arrow = ["oxigeo-core/arrow"]

[dependencies]
oxigeo-core = { workspace = true, default-features = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# Async
tokio = { workspace = true, optional = true, features = ["io-util", "fs"] }

[dev-dependencies]
proptest = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

# TODO: oxigeo-gpkg

> **Purpose:** Pure Rust GeoPackage (GPKG) reader/writer for OxiGeo — SQLite format parser without C dependencies.
> **Status (2026-07-28):** ~3,992 Rust LoC (as of last LoC count; substantially larger now given the modules below) · 553 tests (all-features) · 0 in-source `TODO:` markers · every item tracked in this file (High/Medium/Low) is now `[x]` — re-verified 2026-07-28, no open gaps found.
> **Roadmap:** v0.1.7 → v0.2.1 (current slice) → v1.0.0

## High Priority (next slice — verified gaps)

- [x] GeoPackage writer (OGC 12-128r19 compliant `.gpkg` creation) (completed 2026-05-17)
  - **Done:** Two-layer pure-Rust SQLite B-tree writer with no `libsqlite3-sys` dependency. Layer 1 — `src/sqlite_writer/`: `mod.rs` (PAGE_SIZE=4096, SQLITE_MAGIC, APP_ID_GPKG=0x4750_4B47, USER_VERSION_130=10_300), `page_allocator.rs` (1-indexed page arena, `alloc()`/`write(page,bytes)`/`finalize() -> Vec<u8>`), `record.rs` (`RecordValue` enum, `encode_record` with serial-type selection: NULL=0, i8..i64=1..6, f64=7, BLOB=2N+12, TEXT=2N+13), `btree_writer.rs` (`LeafPageBuilder` with `try_add(rowid, payload) -> bool`/`emit(page_num, header_offset) -> Vec<u8>` mirroring `build_leaf_page` from `btree.rs:910`; `GpkgError::RowOverflowsPage { size, max }`), `file_header.rs` (`write_file_header` writing all 100 bytes per `sqlite_reader.rs` offset table). Layer 2 — `src/writer/`: `schema_emitter.rs` (verbatim OGC DDL for gpkg_spatial_ref_sys/gpkg_contents/gpkg_geometry_columns; default SRS rows srs_id -1/0/4326), `feature_writer.rs` (`encode_gpkg_point(x,y,srs_id) -> Vec<u8>` producing 29-byte GPB blob: GP magic + flags=0x20 + LE i32 srs_id + WKB Point LE f64 x/y), `builder.rs` (`GeoPackageBuilder::new(srs_id).add_feature_table(name, geom_type, points).build() -> Result<Vec<u8>>`). Scope limited to single-leaf-page tables (record < ~4061 B); interior-page chains deferred to v0.2.0. Round-trip verified via existing reader.
  - **Tests added (8):** test_writer_minimal_empty_gpkg_validates_header, test_writer_sqlite_master_contains_three_system_tables, test_writer_default_srs_rows_present, test_writer_roundtrip_point_feature_table, test_writer_roundtrip_mixed_attribute_types, test_writer_contents_row_bbox_matches_features, test_writer_rejects_oversized_record, test_writer_application_id_gpkg_magic. Total: 248 tests pass.

- [x] Tile pyramid reader for raster `gpkg_contents.data_type == 'tiles'` (completed 2026-05-17)
  - **Done:** New `src/tile_pyramid.rs` — `TilePyramidReader<'a>` with `open(gpkg, table_name)`, `get_tile(zoom, col, row) -> Result<Option<Vec<u8>>>`, `tile_matrix(zoom)`, `zoom_levels()`. Backed by `GeoPackage::scan_table_by_name` + `CellValue::Blob`. Added `GpkgError::TableNotFound` and `TileSetNotFound` variants. Re-exported from `lib.rs`.
  - **Tests added (5):** test_tile_pyramid_open_returns_error_for_unknown_table, test_tile_pyramid_zoom_levels_sorted, test_tile_pyramid_tile_matrix_accessor, test_tile_pyramid_get_tile_returns_none_when_missing, test_tile_pyramid_get_tile_returns_blob. Total: 117 tests pass.

- [x] `gpkg_extensions` registry parser (completed 2026-05-17)
  - **Done:** New `src/extensions.rs` — `pub struct GpkgExtension { table_name: Option<String>, column_name: Option<String>, extension_name: String, definition: String, scope: ExtensionScope }` + `pub enum ExtensionScope { ReadWrite, WriteOnly }` with `FromStr` (defaults unknown to `ReadWrite`). `GeoPackage::load_extensions(&mut self) -> Result<Vec<GpkgExtension>>` added to `src/gpkg.rs` following the exact `load_contents` pattern: `scan_table_by_name("gpkg_extensions")` tolerates absence (`Ok(vec![])` if no such table). Column layout per OGC §F.4: 0=table_name, 1=column_name, 2=extension_name, 3=definition, 4=scope. Re-exported from `src/lib.rs`.
  - **Tests added (9):** test_extensions_loads_known_rtree_entry, test_extensions_empty_when_table_absent, test_extensions_classifies_scope_correctly, test_extensions_nullable_table_name_and_column_name, test_extensions_multiple_rows, test_extensions_unknown_scope_defaults_to_readwrite, test_extensions_definition_field_preserved, test_extensions_column_name_present, test_extensions_extension_name_preserved. Total: 228 tests pass.

- [x] SQLite WAL (Write-Ahead Logging) mode read support (completed 2026-05-17)
  - **Done:** New `src/wal.rs` (~247 LoC) — `WalReader { page_size: u32, committed_pages: HashMap<u32, Vec<u8>> }`. `from_bytes(data: &[u8]) -> Result<Self, GpkgError>` validates 32-byte WAL header (magic `0x377f0682`/`0x377f0683` BE), parses per-frame 24-byte header (page_number, commit_flag, salt-1/2, checksum-1/2). Frame checksums validated via cumulative s0/s1 walChecksumBytes algorithm (pairs of u32 words with wrapping add); frame validation halts at first checksum mismatch. Only committed frames (`commit_flag != 0`) contribute; last valid frame per page wins. `apply_to_main(main: &[u8]) -> Result<Vec<u8>>` materializes overlay as a new `Vec<u8>` (Option B — non-intrusive, avoids touching `btree.rs:633` direct page access). `overlay_wal(main, wal)` convenience wrapper. `GeoPackage::from_files(main: Vec<u8>, wal: Option<Vec<u8>>)` added to `gpkg.rs`. Error variants added: `InvalidWalMagic(u32)`, `WalChecksumMismatch { page: u32 }`, `WalSizeMismatch { expected: u32, actual: u32 }`. `WalReader` and `overlay_wal` re-exported from `lib.rs`.
  - **Tests added (12):** test_wal_parses_valid_magic_le, test_wal_parses_valid_magic_be, test_wal_invalid_magic_returns_error, test_wal_overlay_replaces_main_page, test_wal_overlay_preserves_unaffected_pages, test_wal_invalid_checksum_skips_frame, test_wal_truncated_returns_only_valid_prefix_frames, test_wal_empty_wal_returns_original_main, plus 4 additional frame/commit validation tests. Total: 240 tests pass.

## Medium Priority (planned — design sketched)

- [x] RTree shadow-table reader (`rtree_<table>_<geom>` virtual tables) (completed 2026-05-17).
  - **Done:** New `src/rtree.rs` (~580 LoC) — `GpkgRTreeReader { nodes: HashMap<i64, Vec<u8>>, max_node_id: i64 }` with `open(gpkg, table_name, geom_column) -> Result<Self>` (reads `rtree_{table}_{col}` via `scan_table_by_name`; determines `max_node_id` from node ids), `search(min_x, min_y, max_x, max_y) -> Vec<i64>` depth-first BFS from node 1, `search_node(node_id, ...)` dispatches interior vs. leaf by `cell_id > 0 && cell_id <= max_node_id`. `parse_node_blob` reads big-endian 4-byte magic + 4-byte cell-count + 24-byte cells (i64 node_id, f64 min_x, f64 min_y, f64 max_x, f64 max_y per the SQLite R*-tree shadow table format). `for_testing()` constructor + `build_leaf_node_blob`/`build_interior_node_blob` helpers for unit tests. `GpkgRTreeReader` and `RTreeEntry` re-exported from `lib.rs`.
  - **Tests added (7):** test_rtree_empty_reader_search_returns_empty, test_rtree_leaf_node_returns_matching_feature_ids, test_rtree_leaf_node_skips_non_overlapping, test_rtree_interior_node_traverses_to_leaf, test_rtree_open_missing_table_returns_error, test_rtree_large_point_cloud_100_entries_all_found, test_rtree_multi_level_tree_depth_3_correctness. Total: 272 tests pass.

- [x] Related Tables Extension (GPKG-RTE) — many-to-many relationships (completed 2026-05-17)
  - **Done:** New `src/related_tables.rs` (~260 LoC). `RelationType` enum (6 variants: Media, SimpleAttributes, RelatedFeatures, Tiles, Attributes, Other(String)). `GpkgRelation { id, base_table_name, base_primary_column, related_table_name, related_primary_column, relation_name, mapping_table_name, relation_type }`. `MappingRow { base_id, related_id }`. `GeoPackage::load_relations()` scans `gpkg_relations`; absent table → `Ok(vec![])`. `GeoPackage::load_mapping_table(table_name)` scans the named mapping table for (base_id, related_id) row pairs. Re-exported from `lib.rs`.
  - **Tests added (12):** test_related_tables_empty_when_absent, test_related_type_from_name_media, test_related_type_from_name_simple_attributes, test_related_type_from_name_related_features, test_related_type_from_name_tiles, test_related_type_from_name_attributes, test_related_type_from_name_other, test_mapping_table_empty_when_absent, test_gpkg_relation_fields, test_mapping_row_fields, test_load_relations_and_mapping_integrated, test_relation_type_display. Total: 305 tests pass.

- [x] M and ZM coordinate variants in WKB encoding (PointM, PointZM, …) (completed 2026-05-17)
  - **Done:** `src/vector.rs` (2226 LoC) split into `src/vector/` directory module: `types.rs` (Point2D/Point3D/Point4D, GpkgGeometry enum), `wkb.rs` (WKB constants + parser), `feature.rs` (FeatureRow/FeatureTable). New `CoordDim { XY, XYZ, XYM, XYZM }` enum + `dim_from_wkb_type(u32) -> (CoordDim, u32)` stripping ISO offsets (base+2000=XYM, +3000=XYZM) and EWKB high bits (0x40000000=M, 0xC0000000=ZM). `Point4D { x, y, z: Option<f64>, m: Option<f64> }` with `to_xy()` / `to_xyz()`. 12 new GpkgGeometry variants (PointM, PointZM, LineStringM, LineStringZM, PolygonM, PolygonZM, Multi* variants). `parse_wkb_geometry` extended for M/ZM paths; `to_gpb` encodes new variants with correct GeoPackage Binary flags byte high-nibble. Backward compatibility preserved.
  - **Tests added (17):** test_wkb_point_m_round_trip, test_wkb_point_zm_round_trip, test_wkb_linestring_m_parses_three_coords_per_vertex, test_wkb_polygon_zm_round_trip_with_hole, test_wkb_ewkb_m_highbit_flag_parses_as_m, test_wkb_ewkb_zm_highbit_flag_parses_as_zm, test_wkb_iso_3000_offset_for_zm_parses_correctly, plus 10 additional unit tests. Total: 265 tests pass.

- [x] Attribute filter with SQL-like `WHERE` parsing (completed 2026-05-17)
  - **Done:** New `src/filter.rs` (~550 LoC). `Comparator` enum (Eq, Ne, Lt, Lte, Gt, Gte, Like, NotLike, IsNull, IsNotNull). `FilterOperand` enum (Column(usize), Literal(CellValue)). `Predicate { left, op, right: Option<FilterOperand> }`. `FilterExpr { Pred, And, Or, Not }` with builder helpers `col_eq`, `col_lt`, `col_like`, `and`, `or`, `not`. `evaluate(expr, row) -> bool` — SQL three-valued NULL semantics (null comparisons return false); LIKE glob via `like_matches_bytes` (handles `%`, `_`, `\` escape). `scan_table_filtered` + `scan_table_filtered_paginated` added to `btree.rs`. `GeoPackage::scan_table_filtered` + `scan_table_filtered_paginated` added to `gpkg.rs`. Re-exported `Comparator, FilterExpr, FilterOperand, Predicate, evaluate as evaluate_filter` from `lib.rs`.
  - **Tests added (14):** test_filter_eq_integer_matches, test_filter_ne_text_excludes, test_filter_lt_gte_numeric_range, test_filter_like_glob_percent, test_filter_like_glob_underscore, test_filter_is_null, test_filter_and, test_filter_or, test_filter_not, test_filter_not_like, test_filter_is_not_null, test_filter_null_ne_null_returns_false, test_scan_table_filtered_paginated, test_filter_combined_and_or. Total: 346 tests pass.

- [x] GeoPackage schema-constraints validation (`gpkg_data_column_constraints`) (completed 2026-05-18)
  - **Done:** New `src/schema_constraints.rs` (~805 LoC). `ConstraintType { Range, Enum, Glob }` enum with `FromStr` (case-insensitive). `DataColumnConstraint { constraint_name, constraint_type, value: Option<String>, min: Option<f64>, min_is_inclusive: Option<bool>, max, max_is_inclusive, description }` (OGC §F.5 column layout). `load_data_column_constraints(gpkg) -> Result<Vec<DataColumnConstraint>>` follows defensive `scan_table_by_name → Ok(vec![])` pattern; rows with <8 cols or unknown constraint_type silently skipped. `ConstraintValidator { constraints: HashMap<String, Vec<DataColumnConstraint>> }` groups rules by name. `validate(name, &CellValue) -> Result<(), ConstraintViolation>` dispatches: Range (numeric, honours inclusivity, NULL passes), Enum (text or stringified numeric, NULL passes), Glob (SQLite-style pattern via pure-Rust `glob_matches(pat, text)` — handles `*`, `?`, `[abc]`, `[^abc]`/`[!abc]`, `[a-z]` ranges; case-sensitive). `ConstraintViolation { constraint_name, constraint_type, actual_value, reason }`. New `GpkgError::ParseError(String)` and `GpkgError::ConstraintViolation { constraint_name, reason }`. Re-exported from `lib.rs`.
  - **Tests added (26):** 17 inline unit tests + 9 integration tests covering empty-when-absent, range inclusive/exclusive bounds, enum accept/reject, glob `*`/`?` wildcards, validator dispatch by name, violation reason carries diagnostic. Total: 389 tests pass.

- [x] Feature-table pagination (`scan_table_paginated(root, offset, limit)`) (completed 2026-05-17)
  - **Done:** `src/btree.rs` (+146 LoC, now 1516 total). `scan_table_paginated(file_data, root_page, page_size, offset, limit) -> Result<Vec<(i64, Vec<CellValue>)>>` — DFS B-tree traversal with `skipped`/`collected` counters; early-terminates the moment `collected == limit` (no further page reads). `count_table_rows(file_data, root_page, page_size) -> Result<u64>` — reads only leaf page header's 2-byte cell count, no payload decoding. `GeoPackage::scan_table_paginated(&self, table_name, offset, limit)` and `GeoPackage::count_table_rows(&self, table_name)` in `gpkg.rs`. Added `scan_table_paginated` and `count_table_rows` to `pub use btree::` in `lib.rs`.
  - **Tests added (13):** test_paginate_offset_zero_limit_all, test_paginate_offset_zero_limit_exact, test_paginate_offset_beyond_end, test_paginate_offset_partial, test_paginate_limit_zero, test_paginate_full_pages, test_paginate_last_page, test_count_table_rows_matches_scan, test_count_table_rows_empty_table, test_paginate_rows_in_ascending_rowid_order, test_paginate_gpkg_method_matches_btree_direct, plus 2 additional. Total: 318 tests pass.

- [x] `gpkg_metadata` / `gpkg_metadata_reference` parser (completed 2026-05-17).
  - **Done:** New `src/metadata.rs` (~240 LoC). `MetadataScope` enum (19 variants per OGC MD_ScopeCode Table 16): `FromStr` (unknown→Undefined) + `as_str()` canonical strings. `GpkgMetadata { id, md_scope, md_standard_uri, mime_type, metadata }`. `ReferenceScope` enum (5 variants: geopackage/table/column/row/row-col). `GpkgMetadataReference { reference_scope, table_name, column_name, row_id_value, timestamp, md_file_id, md_parent_id }`. `GeoPackage::load_metadata()` + `load_metadata_references()` in `src/gpkg.rs` following `load_contents` scan pattern; absent tables → `Ok(vec![])`.
  - **Tests added (15):** test_metadata_empty_when_table_absent, test_metadata_scope_from_str_all_variants, test_reference_scope_from_str_variants, scope roundtrips, end-to-end parse of synthesized GPKG bytes, struct equality. Total: 287 tests pass.

- [x] Bidirectional conversion to `oxigeo-geojson::FeatureCollection` (completed 2026-05-17)
  - **Done:** New crate dep `oxigeo-geojson-stream = { workspace = true }` added to `Cargo.toml`. New `src/vector/geojson_convert.rs` (~370 LoC). `gpkg_geom_to_geojson(g: &GpkgGeometry) -> Result<GeoJsonGeometry, GpkgError>` — handles all 28 `GpkgGeometry` variants; M/ZM project down (M dropped, Z kept); `Empty` → `Null`. `geojson_geom_to_gpkg(g: &GeoJsonGeometry) -> Result<GpkgGeometry, GpkgError>` — inverse; `Null` → `Empty`. `feature_table_to_geojson(table) -> Result<FeatureCollection, GpkgError>` — each `FeatureRow` → `GeoJsonFeature` with fid as `FeatureId::Number`; SRS metadata omitted (RFC 7946). `feature_table_from_geojson(fc, name, geometry_column) -> Result<FeatureTable, GpkgError>` — schema inferred from first non-empty properties; numeric feature IDs used as FIDs, else sequential 1-based; absent properties → `FieldValue::Null`. `GpkgError::UnsupportedGeometry { kind: String }` added to `src/error.rs`. Re-exported from `src/vector/mod.rs` and `src/lib.rs`.
  - **Tests added (13):** test_round_trip_point_feature_table, test_round_trip_linestring_feature_table, test_round_trip_polygon_with_holes, test_round_trip_attributes_int_real_text_null, test_from_geojson_assigns_sequential_fids_when_missing, test_from_geojson_uses_numeric_feature_id_when_present, test_to_geojson_null_geometry_maps_to_geojson_null, test_to_geojson_drops_srs_metadata, plus 5 additional. Total: 363 tests pass.

## Low Priority / Future (speculative — one-liners only)

- [x] Tiled Gridded Coverage Data extension for elevation/DEM tiles (completed 2026-05-19).
  - **Done:** New `src/coverage.rs` (643 LoC). `CoverageDatatype { Integer, Float }` with `FromStr` (returns `GpkgError::InvalidCoverageDatatype` for unknowns). `GridCellEncoding { Grid, PixelIsArea, PixelIsPoint }` with `FromStr` (lenient, defaults to Grid). `GriddedCoverage { table_name, datatype, scale, offset, precision, data_null, grid_cell_encoding, uom, field_name, quantity_definition }`. `TileGriddedAncillary { id, tpudt_id, scale, offset, min, max, mean, std_dev }`. `load_gridded_coverages(reader)` reads `gpkg_2d_gridded_coverage_ancillary` — returns `Ok(vec![])` if table absent. `load_gridded_tile_ancillary(reader, table_name)` reads `gpkg_2d_gridded_tile_ancillary` filtered by table. `unscale_value(raw, coverage, tile_ancillary)` applies tile-ancillary scale/offset if present; `data_null` → `f64::NAN`. `unscale_tile_buffer_u16` / `unscale_tile_buffer_i16` bulk versions. `GpkgError::CoverageNotFound` + `InvalidCoverageDatatype` added. Re-exported from `lib.rs`.
  - **Tests added (21 targeted / 29 total):** 14 inline + 7 integration (5 DB roundtrip). Total: 438 tests pass.
- [x] data-columns constraints extension (column descriptions, enumerations) (completed 2026-05-19)
  - **Done:** New `src/data_columns.rs` (421 LoC). `DataColumn { table_name, column_name, name, title, description, mime_type, constraint_name }` (7 OGC-spec fields). `DataColumnsCatalog` with `by_table`/`by_constraint` secondary indices; `load()` reads `gpkg_data_columns` via B-tree walk (mirrors `schema_constraints.rs` pattern); absent table returns empty catalog (no error). `for_table()`, `for_column()`, `columns_using_constraint()`, `iter()`, `len()`, `is_empty()`. `read_data_columns_rows()` low-level reader. Re-exported `DataColumn`, `DataColumnsCatalog`, `read_data_columns_rows` from `lib.rs`.
  - **Tests added (12 in `tests/data_columns_test.rs`):** absent-table fast-path, row loading, filtering, NULL optional fields, MIME type, index consistency.
- [x] Multiple geometry columns per feature table (completed 2026-05-19).
  - **Done:** New `src/multi_geom.rs` (312 LoC). `GeometryColumnDef { table_name, column_name, geometry_type_name, srs_id, has_z, has_m }` — per-column metadata from `gpkg_geometry_columns`. `MultiGeomColumnSet { table_name, columns: Vec<GeometryColumnDef> }` with `primary()` (first), `find_by_name(name)`, `column_count()`, `has_multiple()`. `load_geometry_columns_for_table(reader, table_name) -> Result<Option<MultiGeomColumnSet>>` — queries `gpkg_geometry_columns` filtered by table name. `load_all_geometry_columns(reader) -> Result<Vec<MultiGeomColumnSet>>` — groups all rows by table name. `has_multiple_geometry_columns(reader, table_name) -> Result<bool>`. `GeoPackageBuilder` gains `add_geometry_column_def(&mut self, def: GeometryColumnDef)` and `build_from_ref(&mut self) -> Result<Vec<u8>>`. Re-exported from `lib.rs`.
  - **Tests added (12 in `tests/multi_geom_test.rs`):** test_geometry_column_def_fields, test_multi_geom_column_set_primary_is_first, test_multi_geom_column_set_find_by_name, test_has_multiple_true_when_two_columns, test_has_multiple_false_when_one_column, test_load_geometry_columns_for_table_empty_result, test_load_geometry_columns_for_table_single_row, test_load_all_geometry_columns_groups_by_table, test_load_geometry_columns_missing_table_returns_none, test_multi_geom_column_set_column_count, test_builder_add_geometry_column_def_registers, test_builder_build_from_ref_produces_valid_gpkg. Total: 450 tests pass.
- [x] FlatGeoBuf export of GeoPackage feature tables (completed 2026-05-19).
  - **Done:** New `flatgeobuf-export` feature in `Cargo.toml` gating optional `oxigeo-flatgeobuf` + `oxigeo-core` deps. New `src/flatgeobuf_export.rs` (~550 LoC) gated `#[cfg(feature = "flatgeobuf-export")]`. `FlatGeoBufExporter<'a> { gpkg }` with `export_table<W: Write+Seek>(table_name, writer) -> Result<usize>` (returns feature count) and `export_tables_to_dir(dir) -> Result<HashMap<String,usize>>`. Helpers: `gpkg_geometry_to_core` dispatches all 14 geometry variants (Point/LineString/Polygon/Multi*/Z/M/ZM/Empty/GeometryCollection); `gpkg_value_to_field` coerces all 6 GpkgValue variants; `build_fgb_header` infers geometry type from first non-empty feature (Unknown for mixed), sets `has_z`/`has_m` flags. `GpkgError::FlatGeoBufExportError(String)` added. Re-exported `FlatGeoBufExporter` from `lib.rs`.
  - **Tests added (10 in `tests/flatgeobuf_export_test.rs`):** test_export_empty_feature_table_writes_nonzero_bytes, test_export_point_table_feature_count_matches, test_export_linestring_table_returns_ok, test_export_polygon_with_hole_returns_ok, test_export_multipolygon_returns_ok, test_export_table_with_string_and_int_columns_returns_ok, test_export_nonexistent_table_returns_error, test_export_all_tables_creates_one_file_per_table, test_export_z_coordinate_table_returns_ok, test_export_table_with_null_field_value_returns_ok. Total: 460 tests pass.
- [x] CRS reprojection on read via `oxigeo-proj` (completed 2026-05-18)
  - **Done:** New `reproject` feature added to `Cargo.toml` with optional `oxigeo-proj = { workspace = true, optional = true }` dep. New `src/reproject.rs` (~407 LoC) gated `#[cfg(feature = "reproject")]`. `CrsReprojector { transformer, src_epsg, dst_epsg }` wraps `oxigeo_proj::Transformer::from_epsg`. `new(src, dst)` maps `oxigeo_proj::Error` → `GpkgError::ReprojectionError(String)`. `reproject_geometry(&GpkgGeometry) -> Result<GpkgGeometry>` exhaustively dispatches over every variant — 2D, Z, M, ZM, all `GeometryCollection*` (recursively), `Empty`. Z and M ordinates pass through verbatim; only planar `(x,y)` transformed. `reproject_feature_row(&FeatureRow) -> Result<FeatureRow>` preserves FID + fields. `reproject_feature_table(&FeatureTable) -> Result<FeatureTable>` updates `srs_id = Some(dst_epsg as i32)`, preserves name/geometry_column/schema. Manual `Debug` impl (Transformer is not Debug). New `GpkgError::ReprojectionError(String)` variant. Re-exported from `lib.rs` behind cfg gate.
  - **Tests added (9):** test_reprojector_construct_4326_to_3857, test_reprojector_point_4326_to_3857_matches_known_value, test_reprojector_round_trip_4326_to_3857_to_4326_within_tolerance, test_reprojector_linestring_preserves_vertex_count, test_reprojector_polygon_with_hole_reprojects_both_rings, test_reprojector_multipolygon_each_part_reprojected, test_reprojector_z_coords_preserved, test_reprojector_unknown_epsg_returns_error, test_reproject_feature_table_updates_srs_metadata. All `#![cfg(feature = "reproject")]`. Total: passes with feature enabled.
- [x] Incremental INSERT/UPDATE/DELETE without full rewrite (completed 2026-05-19)
  - **Done:** New `src/incremental_mutation.rs` (~380 LoC). `GeoPackageEditor { source_path, feature_table, pending, snapshot, next_fid, srs_id }` using buffered-rewrite strategy (snapshot at open, buffer mutations, atomic commit via `GeoPackageBuilder`). `open(path, table)` reads bytes + scans `gpkg_contents`/`gpkg_geometry_columns` for SRS/geom-type. `insert_feature(row) -> i64`, `update_feature(fid, row)`, `delete_feature(fid)` with `GpkgError::FeatureNotFound(i64)` for unknown FIDs. `commit_to_path(dst)`, `commit_in_place()` (writes to .tmp then `fs::rename` for atomicity), `rollback()`. `MutationStats { inserts_applied, updates_applied, deletes_applied, features_after, features_before }`. New error variants: `FeatureNotFound(i64)`, `MutationError(String)`. Re-exported from `lib.rs`.
  - **Tests added (12):** open reads existing, insert assigns FID, consecutive inserts increment, update buffers, nonexistent update/delete errors, pending counters, commit writes inserts/updates/omits deletes, combined-ops count, rollback drops pending. Total: 12 pass.
- [x] File-integrity check (system tables, foreign keys, SRS refs) (completed 2026-05-18)
  - **Done:** New `src/integrity.rs` (~623 LoC). `IntegrityIssue` enum (8 variants: MissingRequiredTable, MissingRequiredSrs, ContentsRefsMissingTable, GeometryColumnsRefsMissingTable, GeometryColumnsRefsMissingSrs, OrphanedExtensionRow, AppIdMismatch, UserVersionTooOld) each with `description()`. `IntegrityReport { passed, issues }` with `issue_count()`, `has_issue_of(pred)`. Constants `GPKG_APP_ID = 0x4750_4B47`, `MIN_USER_VERSION = 10300` (1.3.0), `REQUIRED_SRS = &[-1, 0, 4326]`. `check_integrity(gpkg) -> IntegrityReport` runs 6 checks in sequence: app_id match, user_version ≥ 1.3.0, required SRS rows present, contents → table refs valid, geometry_columns → table+SRS refs valid (only flag presence-but-with-broken-reference; attribute-only GPKGs are valid), extensions → table refs valid. `check_integrity_strict(gpkg) -> Result<(), Vec<IntegrityIssue>>`. Each check is a separate fn for unit-testability. Reuses existing `gpkg.reader.header.application_id`/`user_version` public fields — no new accessors needed. Re-exported from `lib.rs`.
  - **Tests added (20):** 12 integration tests + 8 inline unit tests covering minimal-valid passes, feature-table passes, missing spatial_ref_sys, dangling contents row, geom_cols srs_id=99999, app_id corruption, user_version=10200, orphaned extension row, passed-flag correctness, strict variant Ok/Err paths, missing required SRS rows. Total: 409 tests pass.
- [x] GeoPackage-to-MBTiles tile-table extraction (completed 2026-05-19).
  - **Done:** New `src/mbtiles_export.rs` gated on `mbtiles-export` feature + optional rusqlite dep. `GpkgMbTilesExporter<'a>` with `new()`, `export_to_path()`, `export_to_connection()`. `GpkgMbTilesStats { tiles_written, bytes_written, min_zoom, max_zoom, format, metadata_keys }`. `create_schema()` — tiles WITHOUT ROWID + bulk-write PRAGMAs. `detect_format_from_blob()` PNG/JPEG/WebP magic. `xyz_to_tms_row()` XYZ→TMS axis flip. `MbTilesExportError` variant added to error.rs. Re-exported from `lib.rs`.
  - **Tests added (15):** schema creation, format detection, TMS conversion, empty/non-empty pyramid export, byte-exact round-trip, metadata fields, export_to_path. Total: passes.
- [x] Trigger-based change-tracking extension (completed 2026-05-19)
  - **Done:** New `src/change_tracking.rs` (~280 LoC), feature `change-tracking = ["dep:rusqlite"]`. `ChangeOperation { Insert=1, Update=2, Delete=3 }`. `ChangeLogEntry { id, table_name, operation, feature_id, committed_at }`. `ChangeTracker` wrapping `rusqlite::Connection` with `open`, `open_in_memory`, `create_changes_table`, `enable_tracking` (3 AFTER triggers), `disable_tracking`, `is_tracking`, `get_all_changes`, `get_changes_since`, `clear_changes`, `clear_all_changes`, `tracked_tables`. SQL injection prevention via `^[A-Za-z_][A-Za-z0-9_]*$` identifier validation. `GpkgError::ChangeTrackingError` added.
  - **Tests added (12 in `tests/change_tracking_test.rs`):** table creation, trigger install/drop, is_tracking, INSERT/UPDATE/DELETE audit, get_changes_since, clear methods, tracked_tables listing.
- [x] Concurrent read access with file locking (completed 2026-05-20)
  - **Done:** New `src/locking.rs` (~324 LoC, `#[cfg(all(feature = "file-locking", unix))]`). POSIX advisory locks via `fcntl(F_SETLK/F_SETLKW)`: `LockMode { Shared (F_RDLCK), Exclusive (F_WRLCK) }`, `GpkgFileLock { fd, path, mode, locked }` with `acquire` (blocking `F_SETLKW`), `try_acquire` (non-blocking `F_SETLK`; `EAGAIN`/`EACCES` → `Ok(None)`), `release` (idempotent `F_UNLCK`), `mode`/`path`/`is_locked` accessors, and `Drop` (best-effort release + close). `lock_for_read`/`lock_for_write` wrappers. Owns the fd via `into_raw_fd`, closes exactly once, and closes on every error / `None` path to avoid leaks and respect per-PROCESS lock semantics (documented in the module doc — this is not BSD `flock`). New `LockingError(String)` variant on `GpkgError` (always compiled); `file-locking = ["dep:libc"]` feature; `compile_error!` on non-unix targets with the feature. Re-exported from `lib.rs` under the unix+feature gate.
  - **Tests added (12):** in `tests/locking_test.rs` — acquire shared/exclusive, explicit release, drop auto-release, four cross-process contention cases via `libc::fork()` (shared+shared compatible; exclusive-vs-shared, exclusive-vs-exclusive, shared-vs-exclusive all `Ok(None)`; child reaped via `waitpid`), mode/path/is_locked accessors. Total: 494 tests pass.

## Cross-crate dependencies
- **Blocks:** `oxigeo-oxigeo` (umbrella streaming for `.gpkg`), `oxigeo-services` (vector tile endpoints)
- **Blocked by:** `rusqlite 0.37` / `libsqlite3-sys 0.35` constraint (per project memory) — only relevant for dev-dependencies; pure-Rust path is unblocked

## Recently completed (kept verbatim)
- [x] Add 3D geometry support (PointZ, LineStringZ, PolygonZ) (completed 2026-04-19: full Z-variant enum, WKB parse/encode for ISO `1000 + base` and `0x8000_0000 | base` conventions, GeoJSON `[x,y,z]` output)
- [x] Add full SQLite B-tree page traversal (completed 2026-04-19: `btree` module with leaf/interior page parsing, varint encode/decode, record serial-type dispatch, `scan_sqlite_master`, `scan_table`, `scan_table_by_name`, `load_contents` on `GeoPackage`)

---
*Last audited: 2026-07-28*

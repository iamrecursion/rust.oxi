[package]
name = "oxigeo-gpkg"
description = "Pure Rust GeoPackage (GPKG) reader for OxiGeo - SQLite format parser without C dependencies"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/oxigeo-gpkg"
readme = "README.md"
keywords = ["geopackage", "gpkg", "sqlite", "gis", "geospatial"]
categories = ["science::geo", "parser-implementations", "encoding"]

[features]
default = ["std", "geojson-convert"]
std = ["thiserror/std"]
reproject = ["dep:oxigeo-proj"]
flatgeobuf-export = ["dep:oxigeo-flatgeobuf", "dep:oxigeo-core"]
# mbtiles-export uses the Pure-Rust OxiSQL engine (no C/FFI).
mbtiles-export = ["dep:oxisql-core", "dep:oxisql-sqlite-compat", "dep:tokio"]
change-tracking = ["dep:oxisql-sqlite-compat", "dep:oxisql-core", "dep:tokio"]
file-locking = ["dep:libc"]
# GeoPackage <-> GeoJSON conversion (vector::geojson_convert). Pulls in
# oxigeo-geojson-stream (and transitively the regex family) plus serde_json;
# enabled by default, disable via `default-features = false` for wasm /
# size-constrained builds that don't need GeoJSON conversion.
geojson-convert = ["dep:oxigeo-geojson-stream", "dep:serde_json"]

[dependencies]
thiserror = { workspace = true }
tracing = { workspace = true }
oxigeo-geojson-stream = { workspace = true, optional = true }
serde_json = { workspace = true, optional = true }
oxigeo-proj = { workspace = true, optional = true }
oxigeo-flatgeobuf = { workspace = true, optional = true }
oxigeo-core = { workspace = true, optional = true, default-features = true }
# Pure-Rust SQLite engine (mbtiles-export feature only)
oxisql-core = { workspace = true, optional = true }
oxisql-sqlite-compat = { workspace = true, optional = true }
tokio = { workspace = true, optional = true }
libc = { workspace = true, optional = true }

[dev-dependencies]
libc = { workspace = true }
tempfile = { workspace = true }
oxisql-core = { workspace = true }
oxisql-sqlite-compat = { workspace = true, features = ["blocking"] }
tokio = { workspace = true, features = ["rt"] }

[[test]]
name = "geojson_convert_test"
required-features = ["geojson-convert"]

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

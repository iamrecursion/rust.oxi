[package]
name = "oxigeo-drivers-advanced"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/oxigeo-drivers-advanced"
readme = "README.md"
description = "Advanced geospatial format drivers for OxiGeo (JPEG2000, GeoPackage, KML/KMZ, GML)"
keywords = ["geospatial", "gdal", "gis", "geopackage", "drivers"]
categories = ["science::geo", "encoding", "parser-implementations"]

[dependencies]
# Core
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
byteorder = { workspace = true }
tracing = { workspace = true }

# XML parsing (for KML/GML)
quick-xml = { workspace = true }

# ZIP support (for KMZ) / Compression
oxiarc-archive = { workspace = true }
oxiarc-core = { workspace = true }

# SQLite (for GeoPackage) — optional: only included when the `geopackage` feature is enabled
oxisql-sqlite-compat = { workspace = true, optional = true, features = ["blocking"] }
oxisql-core = { workspace = true, optional = true }

# Full pure-Rust JPEG2000 codec (for the `jpeg2000` feature)
oxigeo-jpeg2000 = { workspace = true, optional = true }

# Base64 encoding
base64 = { workspace = true }

# Spatial indexing
rstar = { workspace = true }

# Async support (optional)
tokio = { workspace = true, optional = true }

[dev-dependencies]
tempfile = { workspace = true }
approx = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
criterion = { workspace = true }
proptest = { workspace = true }


[features]
default = ["jpeg2000", "kml", "gml"]
jpeg2000 = ["dep:oxigeo-jpeg2000"]
geopackage = ["dep:oxisql-sqlite-compat", "dep:oxisql-core"]
kml = []
gml = []
async = ["tokio"]

[[bench]]
name = "format_bench"
harness = false

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

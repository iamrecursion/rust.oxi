# TODO: oxigeo-geojson

> **Purpose:** Pure-Rust streaming GeoJSON reader and writer for OxiGeo (RFC 7946).
> **Status (2026-07-28):** ~3,803 Rust LoC · 451 tests (all-features) · 0 in-source `TODO:` markers.
> **Roadmap:** v0.1.7 → v0.2.1 (current slice) → v1.0.0

## High Priority (next slice — verified gaps)

- [x] True incremental streaming parser — `IncrementalFeatureReader<R: Read>` (completed 2026-05-16)
  - **Done:** New `src/incremental.rs` module — `IncrementalFeatureReader<R>` using a byte-level prelude scanner + `serde_json::Deserializer` per-feature. O(1) memory regardless of feature count. Re-exported from `lib.rs`. Added `IoError`/`InvalidStructure` error variants to `GeoJsonError`.
  - **Tests added:** `test_incremental_reads_100_features_one_at_a_time`, `test_incremental_preserves_id_and_properties`, `test_incremental_empty_features_array`, `test_incremental_errors_on_malformed_feature`, `test_incremental_skips_unknown_top_level_fields`
  - **Files:** `src/incremental.rs` (new, ~553 LoC), `src/lib.rs`, `src/error.rs`, `tests/incremental_test.rs`.

- [x] Douglas-Peucker geometry simplification as a writer transform (completed 2026-05-17)
  - **Done:** New `src/simplify.rs` — iterative (stack-based, no heap recursion) Ramer-Douglas-Peucker `simplify_dp(points, epsilon)`. Perpendicular distance via scalar-projection clamped to [0,1]. Ring-closure invariant preserved. Degenerate guard: if simplification produces <4 points for a closed ring (<2 for open line), returns original. `GeoJsonWriter::with_simplify(tolerance)` builder hooks into `serialize_geometry` for `LineString`, `Polygon`, `MultiLineString`, `MultiPolygon`. `simplify_dp` re-exported from `lib.rs`.
  - **Tests added (8):** test_dp_straight_line_collapses_to_two_endpoints, test_dp_zero_tolerance_is_identity, test_dp_preserves_ring_closure, test_dp_degenerate_polygon_falls_back_to_original, test_dp_multipolygon_each_ring_simplified, test_writer_with_simplify_reduces_output_byte_count, test_writer_without_simplify_preserves_all_vertices, test_writer_simplify_multilinestring. Total: 142 tests pass.

- [x] TopoJSON encoder for shared-arc output (completed 2026-05-17)
  - **Done:** Three-phase TopoJSON 3.0 encoder at `src/topojson/`. Phase 1 — `quantize.rs`: `QuantBbox`, `QuantTransform { scale_x, scale_y, translate_x, translate_y }`, `compute_fc_bbox()`, `collect_all_rings()` with `RingSource` metadata; epsilon guard for degenerate zero-extent bboxes; default grid q=10_000. Phase 2 — `arcs.rs`: `normalize_ring()`, `detect_junctions()` (vertex-membership + neighbour-pair comparison across all rings), `cut_ring_into_arcs()`, `commit_arc()` (canonical dedup via `min(arc, reversed_arc)` as Vec<QuantPoint>; reversed reference stored as `!(idx as i32)` per TopoJSON §2.1.4 — bitwise NOT, not negation), `delta_encode()` (first point absolute, subsequent delta). Phase 3 — `writer.rs`: strongly-typed `Topology` serde struct; `feature_collection_to_topojson(fc, options) -> Result<String, GeoJsonError>`; `TopoOptions { quantization: u32, object_name: String, pretty: bool, coordinate_precision: usize, include_bbox: bool }` with Default (10_000, "data", false, 6, true) + builder methods. `GeoJsonError::TopologyError(String)` added to `src/error.rs`. Re-exported from `src/lib.rs`.
  - **Tests added (18):** test_two_adjacent_polygons_share_one_arc, test_quantisation_round_trip_within_tolerance, test_disjoint_polygons_no_shared_arcs, test_multipolygon_with_holes_arc_structure, test_point_geometries_have_no_arcs, test_polygon_ring_closure_preserved, test_negative_arc_index_decodes_to_reversed_arc, plus 11 additional unit + integration tests. Total: 229 tests pass.

## Medium Priority (planned — design sketched)

- [x] WKT geometry serialization / deserialization (completed 2026-05-17)
  - **Done:** New `src/wkt.rs` (~935 LoC). `pub fn geometry_to_wkt(g: &GeoJsonGeometry) -> String` covers all 14 variants: `POINT(x y)`, `POINT Z (x y z)`, `LINESTRING(...)`, `POLYGON((outer),(hole))`, `MULTI*`, `GEOMETRYCOLLECTION(...)`, and `GEOMETRYCOLLECTION EMPTY` for `Null`. `pub fn geometry_from_wkt(s: &str) -> Result<GeoJsonGeometry, GeoJsonError>` — hand-written `Tokenizer` (emits `Word | Number | LParen | RParen | Comma`, skips whitespace) + recursive descent parser with per-keyword dispatch; handles optional `Z` modifier, `EMPTY` keyword, both modern `MULTIPOINT((1 2),(3 4))` and legacy `MULTIPOINT(1 2, 3 4)` forms; rejects NaN/Inf. `GeoJsonError::WktParseError(String)` added to `src/error.rs`. `geometry_to_wkt` and `geometry_from_wkt` re-exported from `src/lib.rs`.
  - **Tests added (20):** test_wkt_point_roundtrip, test_wkt_point_z_roundtrip, test_wkt_linestring_roundtrip, test_wkt_polygon_with_holes_roundtrip, test_wkt_multipoint_modern_form_parses, test_wkt_multipoint_legacy_form_parses, test_wkt_multipolygon_z_roundtrip, test_wkt_geometrycollection_mixed_types_roundtrip, test_wkt_point_empty_maps_to_null, test_wkt_invalid_tokens_returns_parse_error, test_wkt_unmatched_parens_returns_parse_error, plus 9 additional coverage tests. Total: 211 tests pass.

- [x] CRS reprojection on read/write via `oxigeo-proj` (completed 2026-05-19)
  - **Done:** New `src/reproject.rs` (459 LoC) gated `#[cfg(feature = "reproject")]`. `ReprojectOptions { source_crs, target_crs, densify_segments, error_on_nan }`. `Reprojector` with `new()` (parses `EPSG:XXXX` / OGC URN), `reproject_position()` (handles 2D/3D/4D), `reproject_geometry()` (all geometry variants), `reproject_feature()`, `reproject_feature_collection()` (clears deprecated `crs` member when target=WGS84). `extract_crs_from_geojson_value()` parses legacy named-CRS form. Analytical transforms: WGS84↔Web Mercator (EPSG:4326↔3857); unsupported pairs return `ReprojectError`. `parse_feature_collection_with_reprojection()` and `write_feature_collection_with_reprojection()` added to parser/writer. `GeoJsonError::ReprojectError(String)` variant. Feature: `reproject = ["dep:oxigeo-proj"]`. Re-exported from `lib.rs`.
  - **Tests added (13 in `tests/reproject_test.rs`):** identity, WGS84↔WebMercator, Z-coord preservation, polygon rings, geometry collection, feature properties, CRS member clearing, named-CRS parsing, round-trip.
  - **Why deferred:** Depends on the `oxigeo-proj` 0.2.x API stabilisation.

- [ ] Spatial-index integration with `oxigeo-index` RTree on `FeatureCollection`
  - **Re-verified 2026-07-28:** Still open — no `oxigeo-index` dependency in `Cargo.toml`, no `src/spatial_index.rs`.
  - **Goal:** O(log n) bbox queries on parsed collections.
  - **Files:** `(new) src/spatial_index.rs`.
  - **Why deferred:** Pulls in a heavy dependency; gate behind a feature.

- [x] Geometry area / length / centroid helpers on `GeoJsonGeometry` (completed 2026-05-17).
  - **Done:** `src/types.rs` extended with three methods on `GeoJsonGeometry`. `area() -> f64` — shoelace via `ring_signed_area` + `polygon_area`; exterior minus holes; MultiPolygon sums parts; GeometryCollection recurses; Z variants project to XY. `length() -> f64` — Euclidean segment-sum via `line_length_2d`/`line_length_3d` (3-D includes Z); polygon length = perimeter; MultiLineString sums lines; GeometryCollection recurses. `centroid() -> Option<[f64; 2]>` — Point/MultiPoint: arithmetic mean; LineString: length-weighted midpoint traversal; Polygon: Bourke signed-area centroid formula on exterior ring; MultiPolygon: area-weighted mean; MultiLineString: length-weighted mean; GeometryCollection: area-weighted if any areal members else length-weighted else point mean.
  - **Tests added (12):** test_area_unit_square_polygon, test_area_polygon_with_hole, test_area_multipolygon_sums_parts, test_area_point_and_linestring_zero, test_length_unit_horizontal_line, test_length_3d_diagonal, test_length_polygon_perimeter, test_length_point_zero, test_centroid_point, test_centroid_unit_square_polygon, test_centroid_linestring_midpoint, test_centroid_null_returns_none. Total: 241 tests pass.

- [x] Bbox clipping (Cohen-Sutherland for lines, Sutherland-Hodgman for polygons) (completed 2026-05-17).
  - **Done:** New `src/clip.rs` (~560 LoC). `ClipBox { min_x, min_y, max_x, max_y }` with `new()`, `contains_point()`, `from_bbox()`. `clip_linestring(coords, clip) -> Vec<Vec<[f64;2]>>` — Cohen-Sutherland: outcode loop per segment, stitches adjacent clipped segments (shared endpoint within 1e-10) into continuous strings. `clip_polygon_ring(ring, clip) -> Vec<[f64;2]>` — Sutherland-Hodgman with 4 successive half-plane clippings via `sh_clip_one_edge`; degenerate rings (< 3 vertices) return empty. `clip_polygon(rings, clip)` — clips exterior + holes, drops degenerate holes. `clip_geometry(geom, clip) -> Option<GeoJsonGeometry>` — dispatches all 14 variants including 3-D (Z interpolated at intersection via parametric `z_at_point_on_ring`). Re-exported from `src/lib.rs`.
  - **Tests added (16):** test_clip_point_inside/outside, test_clip_linestring_fully_inside, test_clip_linestring_crosses_boundary, test_clip_linestring_two_segments, test_clip_polygon_square_fully_inside, test_clip_polygon_larger_than_clip_box, test_clip_geometry_dispatch, test_clip_multilinestring_filters, plus 7 additional variants. Total: 270 tests pass.

- [x] Regex filter operator for string property matching (completed 2026-05-17)
  - **Done:** `src/filter.rs` extended with `FilterOp::MatchesRegex(String)` and `FilterOp::NotMatchesRegex(String)`. `CompiledRegexFilter { field, pattern: regex::Regex, negate }` pre-compiles for reuse. `FeatureFilter::with_regex_filter(field, pattern)` and `with_not_regex_filter` builder methods. `FilterExpr::CompiledRegex` dispatches to `CompiledRegexFilter::matches`. `regex` crate added to workspace and `Cargo.toml`.
  - **Tests added (11):** test_regex_filter_basic_match, test_regex_filter_no_match, test_regex_filter_negated, test_regex_filter_missing_property_no_match, test_regex_filter_non_string_property_no_match, test_regex_filter_case_insensitive, test_regex_filter_anchored_pattern, test_regex_filter_integration_with_feature_filter, test_compiled_regex_filter_matches_multiple_features, test_not_regex_filter_excludes_matches, test_regex_filter_special_characters_escaped. Total: 281 tests pass.

- [x] Feature dissolve / merge by property key (completed 2026-05-19).
  - **Done:** New `src/dissolve.rs` (~450 LoC). `DissolveStrategy { Multipart, Coalesce }`. `PropertyAggregator { First, Last, Sum, Mean, Min, Max, Count, Concat, KeepIfSame, Drop }`. `DissolveOptions { group_by, strategy, property_aggregators, default_aggregator }`. `DissolveStats { input_features, output_features, groups_formed, features_with_null_group }`. `dissolve_features()` groups by `value.to_string()` key; `dissolve_feature_collection()` wrapper. `merge_geometries_multipart()` collapses homogeneous types into Multi* or GeometryCollection. `aggregate_property()` implements all 10 aggregator variants. `DissolveError` variant added to error.rs. Re-exported from `lib.rs`.
  - **Tests added (13):** points→MultiPoint, polygons→MultiPolygon, null group, three groups, aggregator Sum/Mean/Min/Max/Concat/KeepIfSame/Drop, feature collection wrapper. Total: passes.

- [x] Coordinate-precision truncation on parse (completed 2026-05-17)
  - **Done:** `src/parser.rs` extended (+80 LoC). `GeoJsonParser` gains `coordinate_precision: Option<u8>` field (default `None`). Builder method `with_coordinate_precision(decimals: u8) -> Self` clamps to `[0, 15]`. Private `#[inline] fn maybe_truncate(&self, v: f64) -> f64` — passes NaN/±Inf unchanged, otherwise `(v * 10^p).round() / 10^p`. Free coordinate helper functions (`parse_coord_2d`, `parse_coord_3d`, etc.) converted to `GeoJsonParser` methods so they have access to `self.maybe_truncate`. Applied at every leaf coordinate site: Point, PointZ, LineString, Polygon, MultiPoint, MultiLineString, MultiPolygon and Z variants. GeometryCollection is handled automatically via recursion.
  - **Tests added (8):** test_parse_default_no_truncation_preserves_15_digits, test_parse_precision_6_truncates_to_micro_degrees, test_parse_precision_0_truncates_to_integer, test_parse_precision_applies_to_polygon_holes, test_parse_precision_applies_to_multipolygon, test_parse_precision_preserves_nan_and_inf_passthrough, test_parse_precision_z_coordinates_truncated_too, test_parse_precision_geometrycollection_recurses_into_members. Total: 312 tests pass.

## Low Priority / Future (speculative — one-liners only)

- [ ] GeoJSON-LD context output for semantic-web tooling
- [x] Property-schema inference (detect field types across features) (completed 2026-05-17)
  - **Done:** New `src/schema.rs` (~270 LoC). `InferredType` enum (8 variants: String, Number, Integer, Boolean, Null, Array, Object, Mixed) with `merge()` implementing Integer→Number subtype coercion and Null→typed promotion. `FieldSchema { inferred_type, nullable, non_null_count, total_count, min/max_string_len, min/max_numeric }` with `fill_rate()`. `FeatureSchema { fields: HashMap<String,FieldSchema>, feature_count, null_properties_count }` with `field_names()` (sorted), `is_fully_populated()`, `sparse_fields(threshold)`. `infer_schema(iter)` (collects first), `infer_schema_slice(&[&GeoJsonFeature])` (single-pass), `infer_schema_from_collection(fc)`. Re-exported from `lib.rs`.
  - **Tests added (18+):** test_infer_empty_collection, test_infer_single_string_field, test_infer_string_with_nullable, test_infer_integer_field, test_infer_number_field_mixed_int_float, test_infer_boolean_field, test_infer_null_only_field, test_infer_mixed_type_field, test_infer_fill_rate_partial, test_infer_is_fully_populated, test_infer_sparse_fields, test_infer_string_len_stats, test_infer_numeric_stats, test_infer_from_collection, plus 4 additional. Total: 304 tests pass.
- [ ] Polygon offset/buffering on GeoJSON geometries
- [x] Feature sorting by property value or Hilbert/geohash spatial key (completed 2026-05-19)
  - **Done:** New `src/sort.rs` (~250 LoC). `FeatureSortKey { Property(String), Hilbert { precision: u8 }, Geohash { precision: u8 } }`. `SortOrder { Ascending, Descending }`. `sort_features`, `sort_features_owned`, `sort_feature_collection`. `hilbert_key(lon, lat, precision) -> u64` — maps [-180,180]×[-90,90] to 2^p×2^p grid via standard Hilbert rotation algorithm. `geohash_key(lon, lat, precision) -> String` — Niemeyer base-32 ("0123456789bcdefghjkmnpqrstuvwxyz") bit-interleave. `feature_centroid` extractor. `compare_json_values` with total order (Null<Bool<Number<String<Array<Object). Added `GeoJsonGeometry::centroid() -> Option<[f64;2]>` to `types.rs` (polygon: signed-area-weighted, linestring: mean, multipolygon: area-weighted). Re-exported from `lib.rs`.
  - **Tests added (8+):** hilbert key properties, geohash (origin, SF prefix), property sort (ascending string, descending numeric, missing-to-end), spatial clustering (Hilbert/geohash), collection metadata preservation, sort_features_owned. Total: passes.
- [x] Parallel parsing for large `features` arrays via rayon (feature-gated) (completed 2026-05-20).
  - **Done:** New `src/parallel_parse.rs` (~297 LoC, `#[cfg(feature = "parallel")]`). `ParallelParseOptions { chunk_size: 256, threads: None, preserve_order: true, coordinate_precision: None }` + builders. `parse_features_parallel(s, &opts)` decodes the FeatureCollection once, then parses the `features` array in parallel via `par_chunks`, delegating each item to the shared `parser::parse_feature_value` (extracted as `pub(crate)`, also called by the sequential loop so output is byte-identical). `threads: Some(n)` runs inside a **local** `ThreadPoolBuilder::build()?` pool via `pool.install` (never `build_global`, so repeated calls are safe); `None` uses the global pool. `preserve_order` collects `Result<Vec<Vec<_>>>` and flattens in input order (short-circuits on first error). Reconstructs the collection (bbox / 3-D bbox / crs / name) to match the sequential path. `parse_features_parallel_default` wrapper. New `parallel = ["dep:rayon"]` feature. Re-exported from `lib.rs`.
  - **Tests added (13):** in `tests/parallel_parse_test.rs` — empty / single / 100-feature matches-sequential, thread-count override (local pool), order preservation, unordered-as-set, coordinate precision, malformed-feature error propagation, default chunk_size 256, builder methods, chunk_size 1 / larger-than-array, id+properties preserved. Total: 421 tests pass.
- [x] GeoJSON diff (added / removed / changed features) (completed 2026-05-19).
  - **Done:** New `src/diff.rs` (685 LoC). `PropertyChange { key, before: Option<Value>, after: Option<Value> }`. `FeatureChangeDetail { id: Option<String>, geometry_changed, property_changes }`. `FeatureDiff { Added(GeoJsonFeature), Removed(GeoJsonFeature), Changed { before, after, detail } }`. `GeoJsonDiff { added, removed, changed, unchanged_count }` with `total_changes()`, `is_empty()`, `total_features_seen()`. `diff_feature_collections(old, new) -> GeoJsonDiff` — ID-keyed matching (String/Number FeatureId → String key), positional fallback for unkeyed features. `geometries_equal_within_eps(a, b, eps) -> bool` — recursive coordinate comparison. `diff_properties(old, new) -> Vec<PropertyChange>`. `feature_id_to_string(id) -> String`. Re-exported from `lib.rs`.
  - **Tests added (14 in `tests/diff_test.rs`):** test_diff_empty_collections_is_empty, test_diff_added_features, test_diff_removed_features, test_diff_unchanged_features_counted, test_diff_changed_geometry, test_diff_changed_properties_detected, test_diff_property_added_key, test_diff_property_removed_key, test_diff_id_keyed_matching_by_number_id, test_diff_id_keyed_matching_by_string_id, test_diff_positional_fallback_no_ids, test_geometries_equal_within_eps_points, test_geometries_equal_different_types_returns_false, test_diff_total_changes_sum_is_correct. Total: 340 tests pass.
- [x] Self-intersection / ring-orientation validity check (completed 2026-05-19)
  - **Done:** New `src/validity.rs` (~430 LoC). `WindingOrder { Clockwise, CounterClockwise, Degenerate }`. `GeometryValidityIssue { SelfIntersectingRing, IncorrectWinding, DuplicateVertex, FewerThanMinimumVertices, UnclosedRing }`. `GeometryValidityReport { issues, is_valid }`. `ring_signed_area`, `ring_winding_order` (shoelace), `check_ring_self_intersection` (O(n²) pairwise strict-interior t,u∈(0,1)), `validate_polygon_rings` (exterior CCW / holes CW), `validate_geometry` (all variants), `fix_ring_winding`, `fix_geometry_winding`. Re-exported from `lib.rs`.
  - **Tests added (20+ in `tests/validity_test.rs`):** winding CCW/CW/degenerate, signed area, self-intersection detection (bowtie), polygon ring validation, unclosed ring, too few vertices, fix winding helpers.
- [ ] Integration tests against Natural Earth and OSM dump corpora

## Cross-crate dependencies
- **Blocks:** `oxigeo-oxigeo` (umbrella streaming reader uses our types), `oxigeo-streaming`, `oxigeo-services`
- **Blocked by:** None for High Priority; `oxigeo-proj` for the CRS-reprojection item

## Recently completed (kept verbatim)
- [x] Add MultiPointZ, MultiLineStringZ, MultiPolygonZ geometry variants for full 3D multi-geometry support
- [x] Implement GeoJSON-seq / newline-delimited GeoJSON (RFC 8142) reader and writer for line-delimited streaming (SeqWriter RS/NDJSON + SeqReader bulk/lazy, 8 tests)
- [x] Add OR-logic and NOT-logic to FeatureFilter (FilterExpr with AND/OR/NOT composition + property matching, 7 tests)
- [x] Implement 6-dimensional bbox support `[minx, miny, minz, maxx, maxy, maxz]` for 3D collections (bbox_3d() on GeoJsonGeometry/GeoJsonFeature, union_bboxes_3d(), parse_bbox_3d(), compute_bbox_3d(), write_features_iter_3d() in oxigeo-geojson-stream, 8 tests)

---
*Last audited: 2026-07-28*

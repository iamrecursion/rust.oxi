# TODO: oxigeo-index

> **Purpose:** Pure-Rust spatial index (R-tree) for OxiGeo vector data.
> **Status (2026-07-28):** 8,358 Rust LoC (tokei, `src/`) · 460 tests all-features / 446 default-features, 0 failed · 0 real stubs · `no_std` regression across ~20 modules (see Low Priority item below)
> **Roadmap:** v0.1.7 → v0.2.0 → v0.2.1 (current) → v1.0.0

## High Priority (next slice — verified gaps)

- [x] Upgrade R-tree from linear split to R*-tree with forced reinsert (completed 2026-05-17)
  - **Done:** Full R*-tree split heuristic (Beckmann et al. SIGMOD 1990) in `src/rtree/node.rs::rstar_choose_split_axis_and_index`. Axis selection by minimum margin-sum; distribution selection by minimum overlap (area tie-break). Forced reinsertion in `src/rtree/mod.rs` with `reinsert_levels: HashSet<usize>` guard to prevent infinite recursion. p = ⌈0.3×M⌉ farthest entries removed (by centroid distance) and reinserted from root. `check_min_fill_invariant` public validator.
  - **Tests added (5):** test_rstar_split_produces_two_groups_above_min_entries, test_rstar_forced_reinsertion_happens_once_per_level_per_insert, test_rstar_axis_selection_picks_minimum_margin_axis, test_rstar_split_invariant_min_entries_per_node, test_rstar_query_correctness_after_1000_inserts. Total: 120 tests pass.

- [x] Add 3D R-tree variant (`RTree3D<T>` + `Bbox3D` queries) for point cloud and volumetric data (completed 2026-05-17)
  - **Done:** New `src/bbox3d.rs` — `Bbox3D { min_x, min_y, min_z, max_x, max_y, max_z }` with full method set including `volume()`, `surface_area()` (2*(wh+wd+hd)), `min_distance_sq_to_point(x,y,z)`. New `src/rtree3d/node.rs` — R*-tree split for 3D using surface-area margin-sum across 3 axes; volume-overlap tie-break; forced reinsertion (p=15 farthest by 3D centroid distance). New `src/rtree3d/knn.rs` — min-heap priority-queue with 3D MINDIST² pruning. New `src/rtree3d/bulk.rs` — Sort-Tile-Recursive bulk load tiling X→Y→Z. New `src/rtree3d/mod.rs` — `RTree3D<T>` with `insert`, `search`, `nearest_k`, `bulk_load`, `remove`, `len`, `is_empty`. Re-exported from `src/lib.rs`.
  - **Tests added (7):** test_bbox3d_volume_and_surface_area_basic, test_bbox3d_intersects_disjoint_and_overlapping, test_rtree3d_insert_and_search_simple, test_rtree3d_knn_3d_distance_orders_correctly, test_rtree3d_point_cloud_1000_uniform_query_correctness, test_rtree3d_z_axis_discriminator_query_only_returns_z_overlap, test_rtree3d_bulk_load_returns_all_for_world_query. Total: 208 tests pass.

- [x] Add `search_dedup` to `GridIndex` (completed 2026-05-17)
  - **Done:** `src/grid_index.rs` gains two purely additive methods: `search_dedup_by<K, F>(&self, query, key_fn) -> Vec<&T>` (caller-supplied `Fn(&T) -> K` where `K: Hash + Eq`; uses `HashSet<K>` to track seen keys, returns only first occurrence per key) and `search_dedup_by_ptr(&self, query) -> Vec<&T>` (pointer-identity dedup via `*const T as usize`). `search` is unchanged. `GridIndex` now stores values in a `Vec<T>` to permit stable pointer identity across the arena; `search` returns refs into that arena.
  - **Tests added (4):** test_search_dedup_no_duplicates_single_cell, test_search_dedup_removes_duplicates_multi_cell, test_search_dedup_by_ptr_removes_same_reference, test_search_dedup_custom_key_fn. Total: 195 tests pass.

- [x] Add Hilbert R-tree variant for better spatial locality on disk-backed indices (completed 2026-05-17)
  - **Done:** New `src/rtree/hilbert.rs` (469 LoC) — `xy_to_hilbert` ported inline from `oxigeo-pmtiles` (no new dep). `compute_hilbert_value(bbox, world, order)` normalises bbox centre to 2^order integer grid. `HilbertRTree<T>` stores Hilbert-sorted flat entry array plus one level of internal `HilbertInternal` nodes (MBR + max_hilbert, fan-out 16) for search pruning. API: `new()`, `bulk_load(items, order)`, `search(query) -> Vec<&T>`, `len()`, `is_empty()`. Re-exported from `src/rtree/mod.rs` and `src/lib.rs`. Insertion after bulk_load deferred to v0.2.0.
  - **Tests added (17):** test_hilbert_value_consistent_for_same_point, test_hilbert_bulk_load_orders_entries_along_curve, test_hilbert_search_returns_all_overlapping_bboxes, test_hilbert_search_skips_disjoint_subtrees, test_hilbert_compared_to_rstar_returns_same_set, test_hilbert_empty_search_returns_empty, test_hilbert_single_item_found, plus 10 additional unit tests. Total: 225 tests pass.

## Medium Priority (planned — design sketched)

- [x] Implement line-segment intersection query (completed 2026-05-17).
  - **Done:** `src/rtree/mod.rs` extended (+189 LoC, now 975 total). `RTree::search_line(line: &[(f64,f64)], buffer: f64) -> Vec<&T>` — computes corridor bbox per segment via `line_corridor_bbox`, calls `search_node` recursively, deduplicates by raw pointer address (`search_line_node` collects into `HashSet<*const T>`). `segment_intersects_bbox_buffered(ax, ay, bx, by, min_x, min_y, max_x, max_y, buffer)` implements Liang-Barsky parametric clip with `|denom| < f64::EPSILON` guard for axis-parallel segments. Re-exported from `src/lib.rs`. Dedup by pointer identity (same value inserted in overlapping cells counted once).
  - **Tests added (7):** test_search_line_single_segment_finds_overlapping_bbox, test_search_line_buffer_expands_corridor, test_search_line_empty_line_returns_empty, test_search_line_no_overlap_returns_empty, test_search_line_multipoint_path_finds_all_nearby, test_search_line_dedup_prevents_double_counting, test_search_line_exact_endpoint_touch. Total: 239 tests pass.

- [ ] Add polygon-polygon intersection test (beyond bbox overlap, actual geometry intersection)
  - **Goal:** Filter R-tree hits by exact geometry predicate.
  - **Files:** `src/operations.rs` (extend)
  - **Why deferred:** Pushes geometry logic into index crate; better to expose a callback or let callers use `oxigeo-algorithms::vector::intersects`.
  - **Re-verified 2026-07-28:** the underlying primitive now exists — `src/polygon_boolean.rs::polygon_intersection(a, b) -> BooleanResult` (real Sutherland-Hodgman clip) and `polygons_intersect_bbox_test(a, b) -> bool` were added since this item was written (see the "Implement polygon union, intersection, and difference" item below, completed 2026-05-19). Callers can already compose `RTree::search` + `polygon_intersection`/`polygons_intersect_bbox_test` by hand; what's still missing is the dedicated "filter R-tree hits by exact geometry predicate" convenience wrapper this item describes, so it remains open.

- [x] Implement Visvalingam-Whyatt simplification as alternative to Douglas-Peucker (completed 2026-05-17)
  - **Done:** `src/operations.rs` extended (+381 LoC, now 855 total). `simplify_visvalingam(coords, min_effective_area) -> Vec<(f64,f64)>` and `simplify_visvalingam_to_count(coords, target_count) -> Vec<(f64,f64)>`. Algorithm: min-heap with lazy invalidation via version counters (`VwState` doubly-linked list, `VwHeapEntry` using `f64::to_bits()` as `u64` for Ord without external dep). Monotonicity rule: neighbour areas clamped to `max(new_area, removed_area)` ensuring non-decreasing removal sequence for correct `_to_count` semantics. Closed ring detection (first==last within epsilon); seam preserved. Re-exported from `src/lib.rs`.
  - **Tests added (7):** test_vw_straight_line_collapses_to_two_endpoints, test_vw_preserves_first_and_last_vertex, test_vw_removes_smallest_area_vertex_first, test_vw_threshold_zero_returns_input_unchanged, test_vw_to_count_exact_target, test_vw_closed_ring_preserves_closure, test_vw_lazy_heap_invalidation_with_neighbour_area_decrease. Total: 232 tests pass.

- [x] Add multi-polygon support to validation module (`validate_multipolygon` with shared-edge checks) (completed 2026-05-18)
  - **Done:** `src/validation.rs` extended (540 → 859 LoC). Two new `ValidationIssue` variants: `PartsOverlapInterior { part_a, part_b }`, `SharedEdgeUsesOppositeOrientation { part_a, part_b, edge_start, edge_end }`. `MultiPolygon { parts: Vec<Polygon> }` with `new`, `parts()`, `len()`, `is_empty()`. `validate_multipolygon(&MultiPolygon) -> ValidationResult` runs per-part `validate_polygon` then cross-part checks: `polygon_interiors_overlap` (segment-cross excluding shared/touching + centroid-inside-other via existing `point_in_ring`), `shared_edges_same_direction` (a→b == b→a flagged — same-direction shared edges indicate both parts lying on same side of seam = overlap). Helpers `approx_coord_eq` (1e-9 epsilon), `rings_share_edge` (pub(crate), `#[cfg_attr(not(test), allow(dead_code))]`), `ring_segments`, `segments_share_endpoint`, `ring_centroid`, `ring_contains_point` (thin wrapper reusing private `point_in_ring`). Re-exported `MultiPolygon`, `validate_multipolygon` from `lib.rs`.
  - **Tests added (12):** 2 inline unit tests + 10 integration tests covering two-disjoint-squares-valid, edge-sharing-valid, overlapping-interiors-invalid, vertex-touching-valid, single-part-forwarding, empty-valid, invalid-ring-forwarding, three-disjoint-valid, containment-detection, accessor round-trip. Total: 363 tests pass.

- [x] Implement spatial hash grid as a third index option (completed 2026-05-17).
  - **Done:** New `src/spatial_hash.rs` (~350 LoC). `SpatialHashGrid<T>` — dynamic spatial hash grid requiring no pre-declared extent. Arena `Vec<(Bbox2D, T, bool)>` with tombstone removal (O(1)); `HashMap<(i64,i64), Vec<usize>>` cell map (cells created on demand). `cells_for_bbox` lazy iterator over `[col_min..=col_max] × [row_min..=row_max]`; handles negative coordinates via IEEE-754 floor. API: `new(cell_size)`, `insert(bbox, value) -> usize` (stable arena index), `search(query) -> Vec<&T>` (dedup via `HashSet<usize>` seen-set), `remove(arena_index) -> bool`, `compact()` (rebuilds arena + cell map), `len()`, `is_empty()`, `iter()`, `cell_size()`, `cell_entry_count()`, `search_with_index`. Re-exported from `lib.rs`.
  - **Tests added (15):** test_spatial_hash_insert_and_search_simple, dedup, remove, len, negative_coords, compact, 1000-item world query, point bbox, search_with_index stability, iter skips dead, post-compact search, extent, out-of-range remove. Total: 263 tests pass.

- [x] Add window query with result count limit (top-k within bbox, sorted by distance to centre) (completed 2026-05-17)
  - **Done:** `src/rtree/node.rs` gains `search_with_bbox_node`. `src/rtree/mod.rs` gains `search_with_bbox(query) -> Vec<&T>` (all items whose bbox overlaps query) + `search_top_k(query, k, cx, cy) -> Vec<&T>` (sorts results by `min_distance_to_point(cx,cy)`, truncates to k). `SpatialQuery::top_k_in_window(tree, bbox, k, cx, cy)` static method in `src/operations.rs` as ergonomic wrapper. Re-exported from `lib.rs`.
  - **Tests added (8):** test_search_top_k_returns_k_closest, test_search_top_k_sorted_ascending_distance, test_search_top_k_fewer_than_k_in_window, test_search_top_k_window_filters_outside, test_search_top_k_zero_k_returns_empty, test_search_top_k_centre_item_first, test_search_with_bbox_all_overlapping, test_spatial_query_top_k_in_window_static. Total: 271 tests pass.

- [x] Implement polygon union, intersection, and difference (boolean operations) (completed 2026-05-19).
  - **Done:** New `src/polygon_boolean.rs` (~700 LoC). `BooleanOp { Union, Intersection, Difference, SymmetricDifference }`. `BooleanResult { Single, Multiple, Empty }`. Public entry points: `polygon_boolean()`, `polygon_union()`, `polygon_intersection()`, `polygon_difference()`, `polygon_symmetric_difference()`, `polygons_intersect_bbox_test()`. Fast paths: degenerate → identical → bbox-disjoint → containment (point-in-polygon). Intersection/Difference via Sutherland-Hodgman half-plane clipping; Union via Weiler-Atherton augmented vertex-list tracing with fallback to `Multiple([a,b])`. Symmetric difference as union of both one-sided differences. Hole support deferred (documented). Re-exported from `lib.rs`.
  - **Tests added (13):** disjoint/identical/overlapping/containment for each operation, bbox-test helpers. Total: passes.

## Low Priority / Future (speculative — one-liners only)

- [x] Add adaptive grid index that automatically subdivides hot cells (quadtree-like refinement) (completed 2026-05-18).
  - **Done:** New `src/adaptive_grid.rs` (~440 LoC) — loose-quadtree implementation. `AdaptiveGrid<T> { root, max_items_per_cell, max_depth, len }` with `AdaptiveNode<T>` 2-variant enum: `Leaf { bbox, items: Vec<(Bbox2D, T)> }`, `Internal { bbox, items: Vec<(Bbox2D, T)>, children: Box<[AdaptiveNode<T>; 4]> }`. `new(world_bbox, max_items_per_cell, max_depth)` (max_items clamped to ≥1). `insert(bbox, value)` descends only when item's bbox fits entirely inside exactly one child (NE/NW/SE/SW); else stays at parent (loose-quadtree). `subdivide` uses `std::mem::replace` with placeholder leaf to safely redistribute; items that fit a child recurse so they may cascade further. `split_bbox` defends against degenerate midpoint splits. `search(query)` DFS with bbox-overlap pruning + `HashSet<*const T>` defensive dedup. Public `len`, `is_empty`, `depth`, `leaf_count`, `cell_count`. Re-exported from `lib.rs`.
  - **Tests added (13):** 3 inline unit + 10 integration tests covering empty search, single insert/search, splits when max_items exceeded, no split beyond max_depth, overlap correctness, disjoint exclusion, large item stays at internal, 1000-random correctness vs brute force, leaf_count growth, depth report. Total: 351 tests pass.
- [x] Implement geographic distance queries (haversine/vincenty) alongside Euclidean distance (completed 2026-05-17).
  - **Done:** New `src/geo_distance.rs` (~588 LoC). WGS84 constants (`WGS84_A`, `WGS84_B`, `WGS84_INV_F`, `WGS84_MEAN_RADIUS_M`). `haversine_m(lat1,lon1,lat2,lon2) -> f64` + `haversine_m_with_radius(...)`. `vincenty_inverse_wgs84(lat1,lon1,lat2,lon2) -> Option<VincentyGeoResult>` — iterative Vincenty 1975 inverse with coincident fast-path and equatorial branch; returns None for near-antipodal non-convergence. `GeoPoint { lat_deg, lon_deg }`, `GeoNearestResult<T> { value, distance_m, lat_deg, lon_deg }`. `geo_nearest_k<T>(points, query_lat, query_lon, k) -> Vec<GeoNearestResult<T>>` — O(n) linear-scan k-NN sorted ascending. `geo_within_radius<T>`, `geo_bbox_extent_m(bbox) -> (width_m, height_m)`. `SpatialQuery::geo_nearest_k` and `SpatialQuery::geo_within_radius` delegation wrappers in `operations.rs`. Re-exported from `lib.rs`.
  - **Tests added (22):** test_haversine_london_paris, test_haversine_coincident_zero, test_haversine_equatorial_one_degree, test_haversine_polar, test_vincenty_london_paris_closer_than_haversine, test_vincenty_coincident_zero, test_geo_nearest_k_sorted_ascending, test_geo_nearest_k_returns_at_most_k, test_geo_nearest_k_empty_input, test_geo_within_radius_filters_correctly, test_geo_bbox_extent_m_plausible, plus 11 additional tests. Total: 300 tests pass.
- [x] Add Voronoi diagram construction from point set (completed 2026-05-19).
  - **Done:** New `src/voronoi.rs` (501 LoC). `VoronoiPoint { x, y }`, `VoronoiCell { seed, vertices: Vec<(f64,f64)>, neighbors: Vec<usize> }`, `VoronoiDiagram { cells, bbox }`. `circumcenter(a,b,c) -> Option<(f64,f64)>` — standard formula, None for collinear. `build_voronoi(points, bbox) -> Result<VoronoiDiagram, IndexError>` — uses half-plane intersection algorithm (O(N²·k), k≈6 Delaunay degree): for each seed, clips the full bbox polygon against the perpendicular bisector half-plane of each Delaunay neighbor (via `delaunator::triangulate`); handles edge cases: empty (→ empty diagram), 1-point (→ full bbox cell), 2-point (→ perpendicular bisector split), all-collinear (→ slab partition). Sutherland-Hodgman clipping (4 half-planes) for bbox boundary. `find_cell_containing(diagram, query)` winding-number PIP scan. `cell_areas(diagram)` shoelace per cell. Added `delaunator = { workspace = true }` dep. Re-exported from `lib.rs`.
  - **Tests added (7 integration + inline):** test_circumcenter_equilateral, test_circumcenter_collinear_returns_none, test_build_voronoi_empty, test_single_point, test_two_points_perpendicular_bisector, test_four_corners, test_grid_3x3_yields_9_cells, test_neighbors_symmetric, test_cells_clipped_to_bbox, cell_areas_sum_equals_bbox_area (0.000% error). Total: 375 tests pass.
- [x] Delaunay triangulation (Bowyer-Watson, public API) (planned 2026-06-06)
  - **Goal:** Public `triangulate(points: &[Coord]) -> Result<Triangulation, IndexError>` in new `src/delaunay.rs`. `Triangulation` struct: `points: Vec<Coord>`, `triangles: Vec<[usize;3]>`, `adjacency: Vec<[Option<usize>;3]>`, plus `convex_hull() -> Vec<usize>` method.
  - **Design:** Hand-rolled Bowyer-Watson incremental insertion: (1) compute super-triangle enclosing all points; (2) for each input point, find all triangles whose circumcircle contains it (cavity); (3) delete cavity triangles, re-triangulate the boundary polygon by connecting it to the new point; (4) strip super-triangle vertices and all triangles touching them. Robust circumcircle predicate (reuse math from `voronoi::circumcenter` if present, else implement). Handle degeneracies: collinear points (return error or empty), duplicate points (skip silently). Pure-Rust, no_std + alloc capable. Use crate-root `Coord { x: f64, y: f64 }` type. Export via explicit `pub mod delaunay;` + `pub use delaunay::{Triangulation, triangulate};` in `src/lib.rs`.
  - **Files:** new `src/delaunay.rs`; update `src/lib.rs` (add pub mod + pub use). No new dependencies needed.
  - **Tests:** 3 non-collinear points → exactly 1 triangle; square 4 points → exactly 2 triangles; empty circumcircle property (no input point lies inside any triangle's circumcircle); collinear input → Ok with empty triangulation; duplicate points handled gracefully. (~8 tests)
  - **Risk:** Medium — Bowyer-Watson correctness requires careful cavity detection; circumcircle predicate floating-point robustness; super-triangle sizing must reliably enclose all inputs.
- [x] Add spatial clustering (DBSCAN) operating on the RTree index (completed 2026-05-17)
  - **Done:** New `src/clustering/mod.rs` + `src/clustering/dbscan.rs` (~360 LoC). `ClusterLabel = i32` type alias; `NOISE = -1`, `UNVISITED = -2` constants. `DbscanOptions { eps: f64, min_points: usize }` (Default: 1.0 / 4). `DbscanResult { labels, num_clusters, noise_count }`. `dbscan_rtree(points, options) -> DbscanResult` — builds `RTree<usize>` internally (O(N log N) average). `dbscan_with_rtree(rtree, points, options)` — caller-supplied tree. `range_query_eps(rtree, points, cx, cy, eps) -> Vec<usize>` — bbox prefilter `[cx±eps, cy±eps]` + exact Euclidean `dx²+dy² <= eps²` post-filter. BFS expand-cluster with border-point relabeling. Re-exported from `lib.rs`.
  - **Tests added (12):** test_dbscan_empty_input, test_dbscan_single_dense_cluster, test_dbscan_two_separated_clusters, test_dbscan_noise_points_labeled_minus_one, test_dbscan_min_points_threshold_creates_noise, test_dbscan_eps_too_small_all_noise, test_dbscan_with_rtree_uses_provided_tree, test_range_query_eps_excludes_outside_radius, test_dbscan_border_point_gets_cluster_id, test_dbscan_identical_points, test_dbscan_chain_of_points, test_dbscan_results_consistent_with_brute_force. Total: 323 tests pass.
- [x] Implement feature-gated parallel spatial join using rayon (completed 2026-05-18).
  - **Done:** New `src/parallel_join.rs` (~209 LoC) gated `#[cfg(feature = "parallel")]`. `ParallelJoinOptions { chunk_size: Option<usize>, max_threads: Option<usize> }` (Default = both None). `spatial_join_parallel(left: &RTree<A>, right: &[(Bbox2D, B)]) -> Vec<(A, B)>` partitions right input via `par_chunks` (auto chunk targets ~4 chunks per thread, clamped ≥1), each thread queries the left R-tree via `RTree::search(&bbox)` (which returns `Vec<&T>`), results collected via `flat_map_iter`. `spatial_join_with_options(left, right, opts)` respects `max_threads` via private `rayon::ThreadPoolBuilder::install`; falls back to ambient pool on construction failure. Trait bounds: `A: Send + Sync + Clone`, `B: Send + Sync + Clone`. New `parallel = ["std", "dep:rayon"]` cargo feature added to `Cargo.toml` (rayon as optional workspace dep). Re-exported under cfg gate from `lib.rs`.
  - **Tests added (14):** 4 inline unit + 10 integration tests covering empty inputs (both/left/right), equivalence with sequential `SpatialQuery::spatial_join` on small + 1000x500 inputs, disjoint inputs, max_threads=1, chunk_size=1, default options, known-pattern exact pair count. Total: 365 tests pass.
- [x] Add streaming/online insertion that maintains a balanced tree without full rebuild (completed 2026-05-19).
  - **Done:** New `src/streaming_rtree.rs` (373 LoC). `StreamingInsertConfig { rebalance_threshold: usize, max_pending_fraction: f64 }` (Default: 512 / 0.3). `StreamingRTree<T> { stable: RTree<T>, pending: Vec<(Bbox2D, T)>, total_inserted, total_rebuilds, config }`. `insert(&mut self, bbox, value)` — appends to pending, triggers `rebuild()` when `pending.len() >= rebalance_threshold` OR `pending.len() / total_inserted > max_pending_fraction`. `rebuild()` drains pending into stable via individual inserts. `rebuild_from_scratch()` collects all entries + bulk STR-reloads for optimal tree quality. `search(&bbox) -> Vec<&T>` — unions stable + pending results (no duplication). Accessors: `stable_len()`, `pending_len()`, `len()`, `is_empty()`, `rebuild_count()`, `total_inserted()`. Re-exported from `lib.rs`.
  - **Tests added (12 in `tests/streaming_rtree_test.rs`):** test_streaming_rtree_empty_search_returns_empty, test_streaming_rtree_insert_one_item_appears_in_search, test_streaming_rtree_search_returns_correct_subset, test_streaming_rtree_flush_moves_pending_to_stable, test_streaming_rtree_rebuild_threshold_triggers_flush, test_streaming_rtree_pending_fraction_triggers_flush, test_streaming_rtree_rebuild_from_scratch_empties_pending, test_streaming_rtree_rebuild_count_increments, test_streaming_rtree_total_inserted_tracks_all, test_streaming_rtree_search_spans_stable_and_pending, test_streaming_rtree_large_batch_correctness, test_streaming_rtree_is_empty_after_no_inserts. Total: 387 tests pass.
- [x] Implement minimum bounding circle (smallest enclosing circle) for point sets (completed 2026-05-19).
  - **Done:** New `src/bounding_circle.rs` (~400 LoC). `BoundingCircle { center_x, center_y, radius }` with `empty()`, `from_point(p)`, `from_two(a,b)` (diameter circle), `from_three(a,b,c) -> Option<Self>` (circumscribed circle, None if |signed_area| < 1e-12). `contains_point` with epsilon `1e-10`. `contains_point_strict`. `intersects_bbox` via closest-point-on-rect clamp. `area()`, `diameter()`. `smallest_enclosing_circle(points) -> BoundingCircle` — iterative Welzl O(n expected) with Knuth MMIX LCG shuffle (seed `0x12345678`) for determinism. `smallest_enclosing_circle_from_bboxes` flattens 4 corners per bbox. Private helpers: `circle_from_boundary` (0/1/2/3 support points), `lcg_shuffle` (MMIX constants `a=6364136223846793005, c=1442695040888963407`). Re-exported from `lib.rs`.
  - **Tests added (16 in `tests/bounding_circle_test.rs` + inline):** test_bounding_circle_empty_radius_zero, test_circle_from_point_radius_zero, test_circle_from_two_midpoint_and_half_dist, test_circle_from_three_equilateral_circumcenter, test_circle_from_three_collinear_returns_none, test_circle_contains_center, test_circle_contains_boundary_within_epsilon, test_circle_does_not_contain_far_point, test_circle_intersects_bbox_overlap/no_overlap, test_smallest_enclosing_circle_empty/single/two_points/unit_square, test_smallest_enclosing_circle_contains_all_100_random_points, test_smallest_enclosing_circle_from_bboxes. Total: 413 tests pass.
- [ ] Fix the crate-wide `no_std` regression (re-verified 2026-07-28: broader than originally scoped)
  - **Verified gap:** `cargo build -p oxigeo-index --no-default-features` currently fails with 210 errors across 21 files — not just `GridIndex` as originally noted here. Root cause is unqualified `use std::collections::{HashMap, HashSet, BinaryHeap}` / `use std::cmp::Reverse` / `use std::f64::consts::PI` (needs `core::f64::consts::PI`) plus a few bare `vec!`/`Vec` uses missing `alloc::vec`/`alloc::vec::Vec` imports, in every module added since the original no_std baseline: `grid_index.rs`, `adaptive_grid.rs`, `bounding_circle.rs`, `operations.rs` (top-k `BinaryHeap`), `spatial_hash.rs`, `sweep/bentley_ottmann.rs`, `clustering/dbscan.rs`, `delaunay.rs`, `voronoi.rs`, `streaming_rtree.rs`, `polygon_boolean.rs`, `rtree/hilbert.rs`, `rtree3d/{mod,node,knn,bulk}.rs`. The originally-clean `rtree/mod.rs`/`rtree/node.rs`/`rtree/serial.rs`/`validation.rs` also now fail transitively (cascading from the above) or via their own unguarded `std::` imports.
  - **Goal:** `cargo build -p oxigeo-index --no-default-features` succeeds again, restoring the README's `no_std` (with `alloc`) claim for the whole public surface, not just the original R-tree core.
  - **Design:** Gate each `std::collections::{HashMap,HashSet,BinaryHeap}` behind `hashbrown`/`alloc::collections::BinaryHeap` (already the pattern used in the untouched no_std-clean modules); replace `std::cmp::Reverse`/`std::f64::consts::PI` with `core::` equivalents; add missing `alloc::vec::Vec`/`alloc::vec` imports. Re-run `cargo build --no-default-features` iteratively per file until clean, then add a CI check so it can't regress silently again.
  - **Files:** the 21 files listed above.
  - **Prerequisites:** None.
- [x] Implement line intersection sweep-line algorithm for efficient batch intersection detection (completed 2026-05-17).
  - **Done:** New `src/sweep/mod.rs` + `src/sweep/bentley_ottmann.rs` (~540 LoC). `Segment { idx: usize, p0: (f64,f64), p1: (f64,f64) }` — idx is caller-assigned identifier; p0 normalized to left endpoint. `IntersectionPoint { x, y, seg_a, seg_b }` — always `seg_a < seg_b`, each pair reported once. `find_all_intersections(segments: &[Segment]) -> Vec<IntersectionPoint>` — Bentley-Ottmann: events batched by x (Insert < Cross < Remove order); status = `Vec<usize>` sorted by `y_at_x` recomputed on demand with `OrdF64` total-order wrapper. Vertical segments handled by scanning entire status for active segments in y-range. Private `segment_intersect` parametric helper (determinant epsilon `1e-10`). Dedup via `HashSet<(usize,usize)>` for both reported and scheduled pairs. Collinear-overlap reporting deferred (documented). Re-exported from `lib.rs`.
  - **Tests added (15):** test_sweep_empty_input_returns_empty, test_sweep_two_parallel_lines_no_intersection, test_sweep_two_crossing_lines_finds_one_point, test_sweep_t_junction_finds_endpoint_intersection, test_sweep_vertical_segments_handled, test_sweep_horizontal_segments_handled, test_sweep_dense_grid_finds_intersections, test_sweep_reports_each_pair_exactly_once_with_a_lt_b, test_sweep_matches_brute_force_on_random_50_segments, plus 6 additional. Total: 338 tests pass.

## Cross-crate dependencies

- **Blocks:** `oxigeo-qc` (STRtree for topology overlap detection), `oxigeo-algorithms` (spatial_join), `oxigeo-services`
- **Blocked by:** None (zero workspace dependencies — pure leaf crate)

## Recently completed (verbatim)

- [x] Implement entry deletion from RTree (completed 2026-04-19, part of I1 R-tree enhancements)
- [x] Add bulk loading (Sort-Tile-Recursive / STR) for RTree (completed 2026-04-19, part of I1 R-tree enhancements)
- [x] Implement proper priority-queue k-NN search on RTree (completed 2026-04-19, part of I1 R-tree enhancements)
- [x] Add serialization/deserialization for RTree (completed 2026-04-19, part of I1 R-tree enhancements)

---
*Last audited: 2026-07-28*

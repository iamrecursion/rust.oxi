[package]
name = "wasm-uppercase"
version.workspace = true
edition.workspace = true
# Demo WASM plugin for rs3gw, not a crates.io crate.
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
# Minimal dependencies for WASM compilation

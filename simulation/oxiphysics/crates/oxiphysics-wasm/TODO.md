# oxiphysics-wasm TODO

**Version:** 0.1.3 | **Updated:** 2026-06-11 | **Status:** v0.1.x complete — production baseline; forward roadmap active
**Scale:** 30,334 SLoC | 922 tests

Legend: `[x]` shipped · `[ ]` planned · `[~]` deferred/blocked

> CI policy: publish stays on the existing `.github/workflows/npm-publish.yml`; headless browser tests run via a local script — never a new workflow yaml.

## Completed (v0.1.0 – v0.1.3)

### Phase 1: Foundation
- [x] Define error types (`error` module) and shared primitives (`types`)
- [x] Implement math helpers (`math_helpers` submodules: vectors, quaternions, transforms)
- [x] Add unit tests (826 tests passing at 0.1.0; 922 as of v0.1.3)

### Phase 2: Engine Logic
- [x] `engine` module — self-contained simulation engine (step/state/world submodules)
- [x] `physics_config` — global simulation parameters
- [x] `simulation_api` — top-level simulation entry points (`WasmPhysicsEngine`)
- [x] `sim_controls` — play/pause/reset/step controls
- [x] `body_query` — rigid body state queries
- [x] `particle_system` — particle update and management
- [x] `events` — collision/sensor event queue

### Phase 3: Bridge Modules
- [x] `analytics_bridge` — performance metrics export
- [x] `constraint_bridge` — constraint/joint configuration bridge
- [x] `debug_tools` — debug info extraction (`DebugInfo`)
- [x] `fluid_bridge` — SPH/LBM fluid state bridge
- [x] `io_bridge` — scene serialization in/out
- [x] `js_api` — JavaScript-facing API surface
- [x] `material_bridge` — material parameter bridge
- [x] `renderer` — render data extraction for WebGL

### Phase 4: Documentation
- [x] All 1,276 public items documented
- [x] Key type exports: `WasmPhysicsEngine`, `Vec3Wasm`, `QuatWasm`, `TransformWasm`, `RigidBodyConfig`, `SimulationConfig`, `ColliderConfig`, `ContactResult`, `RaycastResult`, `BodyState`, `DebugInfo`

### Phase 5: wasm-bindgen Packaging (0.1.1)
- [x] Add `wasm-bindgen` dependency to `Cargo.toml`
- [x] Annotate exported types and functions with `#[wasm_bindgen]` (engine/mod.rs — `WasmPhysicsEngine` JS class with full API)
- [x] Configure `wasm-pack` build pipeline (run `wasm-pack build --target web`)
- [x] Generate JS/TS glue code (auto-generated by wasm-pack)

> The former Phase-5 "Publish npm package (0.2.0)" item is carried forward into v0.2.0 below.

### Phase 6: Runtime Integration (0.1.1)
- [x] Web Worker / `SharedArrayBuffer` multi-threaded support (`web_worker` — `SharedStateBuffer`, `SimCommand`, `SimResult`, `WorkerBridge`, `WorkerRuntime`)
- [x] WGPU/WebGL render integration (`webgl_bridge` — `WebGlBridge`: procedural sphere/box/capsule/cylinder/plane meshes, `WebGlRenderFrame` instance arrays, `WebGlCamera` with perspective + look-at)
- [x] Performance benchmarks vs. Rapier WASM (`wasm_bench` — `WasmBenchHarness`, rigid-body/particle/SPH timing, `RapierReference` comparison table)
- [x] Examples: browser-based rigid body demo, fluid demo

### Phase 7: Umbrella Module Coverage for Web Demos (shipped 0.1.1)
> **Goal (original, 2026-05-04):** Expose the Phase 13-19 umbrella modules that matter for interactive browser demos. Focus is on gameplay / interaction / observability rather than full backend parity (Python gets the full surface in its Phase 6).
> Every module below was planned 2026-05-04 as the Wave 3 annotation pass (add `#[wasm_bindgen]` + `_js` wrappers) and shipped 2026-05-06.

#### 7.1 Gameplay & Interaction (high priority)
- [x] `character` — `CharacterController::move_and_slide/jump` — headline WASM demo: WASD + mouse-look capsule character on terrain
- [x] `trigger` — `TriggerVolume` enter/exit events — for pickup zones, goal areas, damage regions in demos
- [x] `animation` — `AnimationClip`, `AnimationPlayer` — cinematic sequences driven from JS
- [x] `query` — `Ray`, `raycast` — mouse-pick from browser, hover highlights, line-of-sight
- [x] `rope` — `Rope::step` + `iter_segments` for JS render consumption
- [x] `ik` — `IkChain`, `IkSolver` — reach / look-at demos
- [x] `navmesh` — `NavMesh::from_triangles`, `Query` — click-to-move demo with funnel-smoothed paths
- [x] `rollback` — `RollbackBuffer`, resimulate — essential for networked browser multiplayer

#### 7.2 Visualization & Observability
- [x] `debug_draw` — `DrawCommand`, `DrawList` mapped to Float32Array batches for WebGL / WebGPU instancing
- [x] `telemetry` — `PhysicsStats`, rolling averages — bind to browser performance overlay
- [x] `profiler` — `FrameReport`, `to_folded_stacks` — exportable from browser for offline flamegraph analysis
- [x] `scene` — `SceneDescription` JSON round-trip — load authored scenes from `.json` served over HTTP

#### 7.3 Scripting-friendly helpers
- [x] `interpolator` — `smooth_damp`, `SpringFollower` — critical for camera / UI motion in demos
- [x] `noise` — `ValueNoise3D`, `FractalNoise::turbulence_force` — procedural wind / terrain
- [x] `force_field` — `ForceFieldSystem::apply_to_batch` — explosion / wind / vortex effects
- [x] `snapshot` — `SnapshotManager` — undo/redo in authoring demos
- [x] `material_table` — `MaterialTable` presets — user-selectable surface types

#### 7.4 Packaging & Demos
- [x] Replaced `(0.2.0)` placeholders in Phase 5/6 with concrete tasks: `wasm-pack build --target web --release --out-dir pkg/`, npm publish pipeline, demos artifact
  - **Goal:** Four runnable browser demos (character/rope/sph-pool/navmesh) live under `crates/oxiphysics-wasm/web-demos/` and consume the wasm-pack `pkg-web` build. Placeholder fake `pkg-bundler/*` stubs are removed/`.gitignore`d. A landing `index.html` lists all four demos. The existing `npm-publish.yml` uploads the demos as an artifact.
  - **Design:**
    1. Layout: `web-demos/{index.html, README.md, shared/{canvas-util.mjs,ui-util.mjs}, character/, rope/, sph-pool/, navmesh/}`; each demo imports from `../../pkg-web/oxiphysics.js`.
    2. Placeholder cleanup via `git ls-files` + `wc -c` size-match check, then `git rm --` (never plain rm); `pkg-bundler/`, `pkg-web/`, `pkg-node/` gitignored.
    3. Landing `index.html`: grid of 4 demo tiles with SVG placeholder thumbnails.
    4. CI: "Upload demos artifact" step added to the existing `.github/workflows/npm-publish.yml` (no new workflow).
    5. JS/TS style: ESM `.mjs` only, vanilla WebGL2, no bundler, no Three.js, ≤300 lines JS each.
  - **Tests:** programmatic `#[wasm_bindgen_test]` smoke per bridge type in `tests/wasm_bridge_smoke.rs`; build verification (`wasm-pack build --target web`); manual `SMOKETEST.md`; compile guard (`cargo check --target wasm32-unknown-unknown`).
- [x] Browser demo: character controller on heightfield (headline — 64×64 heightfield, WASD + mouse-look, WebGL2)
- [x] Browser demo: rope ragdoll with mouse drag (32-segment)
- [x] Browser demo: SPH pool (5000 particles, splash impulse; GPU when the wgpu backend lands, CPU fallback today)
- [x] Browser demo: navmesh click-to-move (funnel-smoothed path)
- [x] `tests/wasm_bridge_smoke.rs` — programmatic `#[wasm_bindgen_test]` smoke coverage per bridge type
- [x] `web-demos/index.html` landing page listing all four demos

## v0.2.0 — Size, speed, types

Focus: ship a small, fast, well-typed npm artifact; multi-worker scaling; local-script test automation.

- [ ] npm bundle size optimization
  - **Goal:** measure current `oxiphysics_bg.wasm` baseline, then rigid-only split bundle ≤1.5 MB gzipped; `wasm-opt -Oz` + per-domain code splitting (feature-sliced builds: core/rigid, sph, navmesh); size-budget assertion in `build.sh`/xtask.
- [ ] SIMD128 + relaxed-simd build variants
  - **Goal:** ≥1.5x SPH step speedup vs baseline wasm in Chrome; feature-detecting JS loader picks simd/non-simd artifact.
- [ ] TS type-definitions upgrade
  - Note: flat `.d.ts` already ships via wasm-pack; remaining work is a hand-authored overlay with branded handle types, typed vectors, JSDoc.
  - **Goal:** `tsc --strict` consumer example compiles.
- [ ] Worker pool v2 over SharedArrayBuffer
  - Note: single Worker + SAB layout exists (`src/web_worker.rs`).
  - **Goal:** 4-worker island/domain partitioning with atomics job queue gives 2.5x on 10k-particle SPH vs one worker.
- [ ] Deterministic lockstep two-tab netcode demo (dep: umbrella deterministic mode)
  - **Goal:** two browser tabs (BroadcastChannel transport) stay hash-identical for 10k ticks using the existing `rollback_bridge`; desync banner on mismatch; demo added to `web-demos/`.
- [ ] OffscreenCanvas rendering path
  - **Goal:** render from the worker via OffscreenCanvas + existing `webgl_bridge`; main-thread long tasks <2 ms while simulating+rendering 5k bodies.
- [ ] Headless browser test runner script
  - **Goal:** `wasm-pack test --headless --chrome` wrapper script runs the bridge smoke suite locally (no new workflow yaml); documented in README.
- [ ] Publish npm package (carried from Phase 5)
  - Publish stays on the existing `npm-publish.yml` workflow; no new workflow yamls.

## v0.3.0 — WebGPU & DX

- [ ] WebGPU compute in browser (dep: gpu crate SPH pipeline)
  - **Goal:** 100k-particle SPH density+force at 60 fps in Chrome WebGPU by compiling the oxiphysics-gpu wgpu backend to wasm32; CPU fallback intact for Safari/Firefox.
- [ ] React + Vue component examples
  - **Goal:** `usePhysicsWorld` hook + `<PhysicsCanvas>` component examples in `web-demos/`, ≤150 LoC each, consuming the npm package.
- [ ] Public benchmark page vs rapier.js / jolt-wasm
  - **Goal:** reproducible in-browser harness (boxes pyramid, ragdoll pile, raycast storm) with committed JSON results + methodology doc; honest about wasm flags used.
- [ ] Streaming scene loading
  - **Goal:** 50 MB scene interactive <2 s on throttled 4G: fetch + incremental `SceneDescription` build, oxiarc-zstd-compressed snapshots decoded in wasm.

## v1.0 — Production & validation

- [ ] npm 1.0
  - **Goal:** API freeze across the 27+ bridges; ESM-only exports map; browser support matrix doc (incl. SAB COOP/COEP requirements); publish stays on existing `npm-publish.yml`; no breaking changes within 1.x verified by d.ts diff script.

## Deferred / research track

- [~] Umbrella modules intentionally not bound in WASM — Python Phase 6 covers the full surface; the browser rarely needs these — Ready when: a concrete web demo needs one.
  - `event_bus` (use JS `EventTarget`), `replay` (use browser devtools), `buoyancy` (heavy, niche), `contact_cache` (internal optimization), `scheduler` (internal), `spatial_grid` (internal), `lod` (internal), `xpbd` (heavy, use rigid + rope instead), `aero` (niche)
- [~] WebGPU compute on Safari/Firefox — Ready when: stable WebGPU ships in those browsers (the CPU fallback path covers them until then).

## Cross-crate dependencies

| This crate's item | Depends on |
|---|---|
| Deterministic lockstep netcode demo | umbrella `DeterminismConfig` (oxiphysics v0.2.0) |
| WebGPU compute in browser | oxiphysics-gpu SPH pipeline (gpu v0.2.0) |
| Streaming scene loading | oxiarc-zstd snapshot containers (io v0.2.0) |
| npm publish / demos artifact | existing `npm-publish.yml` only (CI policy) |

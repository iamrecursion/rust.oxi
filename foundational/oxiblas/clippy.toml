# OxiBLAS clippy configuration.
#
# `msrv` must track `[workspace.package] rust-version` in Cargo.toml so
# clippy does not suggest APIs/idioms unavailable on the crate's declared
# minimum supported Rust version.
msrv = "1.85"

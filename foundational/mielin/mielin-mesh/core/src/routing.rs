//! Routing logic with geographic awareness

pub struct RoutingTable {}

impl RoutingTable {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for RoutingTable {
    fn default() -> Self {
        Self::new()
    }
}

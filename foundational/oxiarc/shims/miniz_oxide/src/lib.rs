//! `miniz_oxide` 0.8.9 API shim for `[patch.crates-io]`, backed by
//! [`oxiarc_miniz_compat`]. See that crate's documentation for the supported
//! surface and the documented differences from upstream.

#![forbid(unsafe_code)]

pub use oxiarc_miniz_compat::*;


# OxiArc - Development Roadmap (v0.4.3, 2026-09-12)

## Version History

- **v0.4.2** (2026-09-08, Unreleased) — P2/P3 program: HTTP `Content-Encoding` decoding + three new Pure Rust image container formats, closing the two routes an ecosystem-wide dependency audit found pulling `flate2` into 39 of 93 `~/work` project lockfiles (Tier A: `ureq`'s default `gzip` feature; Tier B: the `image` crate's `png`/`tiff` codecs, ~30 projects). **Five new crates** (13 → 18 members): `oxiarc-http` (RFC 9110 `Content-Encoding`/`Accept-Encoding`: gzip/deflate/br/zstd/legacy-`compress`/RFC 9842 `dcb`/`dcz`, negotiation, bounded decode limits), `oxiarc-png` (ISO/IEC 15948, every colour type/depth, Adam7, APNG, Apple `CgBI`, `png`-0.18-shaped compat), `oxiarc-jpeg` (ITU-T T.81 baseline/progressive/lossless/arithmetic SOF9-11/OJPEG, reduced-/enlarged-scale decode, `zune_jpeg`/`jpeg-decoder`-shaped compat facades), `oxiarc-tiff` (TIFF 6.0 + BigTIFF, every baseline codec incl. CCITT G3/G4 with a new uncompressed mode, `rayon`, `tiff`-0.11-shaped compat), `oxiarc-image` (thin `image`-0.25-crate-shaped facade over the three, no image-processing operations). **The keystone**: a rewritten, genuinely resumable DEFLATE/zlib/gzip core (`InflateStream`, `WrappedInflate`, `InflateReader`/`AsyncInflateReader`) replacing every consumer's previous `read_to_end`-then-decode shape (gzip `FHCRC` now verified by default on the new type; a sticky fault latch closes a silent-truncation defect; `oxiarc-png`'s `IDAT` chain, `oxiarc-tiff`'s strips and `oxiarc-http`'s bodies all share it), plus equivalent push decoders for `oxiarc-zstd` (`ZstdStream`, real sliding-window ring) and `oxiarc-brotli` (`BrotliStream`, atomic meta-block header parsing). **Encoder/decoder rewrites**: `oxiarc-deflate`'s encoder is now a faithful zlib `deflate.c`/`trees.c` port, byte-identical to CPython `zlib.compress` at every level 1-9 (was up to 179% larger; call-size no longer changes cost or output, was up to 13x; level 0's stored blocks are cut at a different, RFC-legal boundary than zlib's pending-buffer bound, so its bytes differ — never by being larger); `oxiarc-zstd` decode throughput rebuilt around the reference decoder's data layout (2-8x on per-byte-work-bound shapes, 0.5x-4x of `zstd -b -d` on 11 of 12 measured shapes); `oxiarc-lzw`'s TIFF/GIF decoder rebuilt to libtiff's own `LZWDecode` shape (1.25x-2.6x faster, 0.73-0.80x of libtiff's throughput; GIF 5.8x-24x, now Pillow-oracle-verified for the first time). **New format capability**: Brotli shared (custom LZ77) dictionaries both directions plus RFC 9842 `Content-Encoding: dcb` framing (72/72 + 96/96 vs `brotli -D`); the legacy UNIX `compress(1)`/`.Z` container in `oxiarc-lzw` (`Content-Encoding: compress`), explicit LZW bit order and 9-16-bit codes; a reusable `xz::XzDecoder` in `oxiarc-lzma` plus multi-block `XzWriter`; CCITT Group 3/4 uncompressed mode in `oxiarc-tiff` (T.4 §4.2.1.3.2 — libtiff 4.7.1 parses but cannot decode it); JPEG two-component (`JCS_UNKNOWN`) frames closing a TIFF two-channel-chunky-JPEG gap opened and closed within this same cycle; `oxiarc-core` gained a shared `sha256` module. **Workspace integration**: `oxiarc-http/tests/cross_crate_inflate.rs` proves PNG/TIFF/HTTP all decode through the same shared inflate core; 15 new `cargo-fuzz` targets (12 named by the program plus 3 more); `oxiarc detect`/`info` recognise PNG/JPEG/TIFF by magic as a CLI-layer fallback (never added to `ArchiveFormat`); `deny.toml` gained `png`/`tiff`/`image`/`jpeg-decoder`/`jpeg-encoder`/`zune-jpeg`/`zune-png`/`zune-core`/`mozjpeg` bans. **Security**: `oxiarc-cli --memory-limit`'s SI byte-size parser overflowed `u64` on a crafted value (debug panic / silently-tiny release limit, pre-existing since v0.2.8); a Zstandard frame using offset code 31 (RFC 8878's maximum) overflowed `usize` on 32-bit targets in `oxiarc-zstd`'s sequence decoder (pre-existing since the crate's original decoder; 64-bit hosts unaffected); `oxiarc-zstd`'s legacy one-shot decode path (`decompress`/`decompress_multi_frame`/`ZstdDecoder::decode_frame`, public since well before this release) accepted several classes of malformed Zstandard frame the streaming decoder already refused, and could carry a failed frame's partial output into a reused decoder — all fixed in shared code, found by a 983,701-execution differential fuzz campaign against `ZstdStream`. **Four narrow breaking changes, all scoped**: `LzwConfig::GIF` is now genuinely LSB-first (was accidentally MSB, no published release affected); `LzwConfig` gained a `bit_order` field, so external struct-literal construction no longer compiles without setting it (`LzwConfig::new` and the named constants are unaffected — `oxiarc-lzw` is a published crate, unlike the five brand-new ones); `oxiarc-zstd`'s `decompress_multi_frame` no longer tolerates leading garbage or a truncated skippable-frame prefix (trailing-garbage tolerance unchanged); the `dcz` wire preamble gained its dictionary-binding header within this same unreleased cycle (no published consumer affected). No archive/stream wire format changed and no public read/decode-path API was removed. Final state: **5,505 tests passing, 0 failed, 0 skipped** (5,159 via `cargo nextest run --workspace --all-features` + 346 doctests via `cargo test --doc --workspace --all-features`); zero clippy warnings (`--all-features --all-targets -D warnings`, also `--no-default-features`), verified per crate by each track *and* workspace-wide: the literal `cargo fmt --all -- --check`, `cargo clippy --workspace --all-features --all-targets -- -D warnings`, `cargo clippy --workspace --no-default-features -- -D warnings` and `cargo build --workspace --no-default-features` gates were all run green on 2026-09-08 (GATES track, independently re-run by the FINALGATE gatekeeper and again by the FIX track after the gatekeeper's findings were applied), alongside `RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps --all-features`, `cargo deny check bans`/`licenses`, and a real `cargo +1.85.0 check --workspace` MSRV compile; `cargo deny check bans` clean with the new image-format bans; 227,104 Rust code lines across 716 files workspace-wide including `fuzz/` and `formal/` (tokei `Code` column). **Re-verified 2026-09-12** (`/runall`'s own full pass immediately ahead of release: fresh `cargo nextest`/doctest/tokei run, not reused) — all of the above held, plus three real issues this pass caught and fixed that the 2026-09-08 gates could not have seen (none reachable by any single-crate `cargo check`): (1) `encoding_rs` had drifted to 0.8.41, silently requiring rustc 1.88 against this workspace's real, enforced MSRV 1.85 — capped to `>=0.8.35, <0.8.40`; (2) `oxiarc-http`'s five sibling `[dev-dependencies]` used `{ workspace = true }`, which survives packaging as a version requirement against not-yet-published siblings and would have failed `cargo publish` — changed to path-only, with `deny.toml` gaining `allow-wildcard-paths = true` to match; (3) all 17 internal `oxiarc-*` `[workspace.dependencies]` requirements were the loose `"0.4"` (`^0.4`), which a published `oxiarc-core 0.4.1` genuinely satisfies despite lacking this cycle's new `sha256`/`BitCache` APIs — a real latent semver defect for any consumer resolving an older compatible version, not just a first-publication artifact — tightened to `"0.4.2"` workspace-wide. See CHANGELOG.md's `encoding_rs` entry under Changed for the published-facing half of this.

- **v0.4.1** (2026-08-06) — Security hardening + two encoder-ratio limitations closed + hygiene pass. **Security**: ZIP encryption salts and ZipCrypto headers now always come from a real OS CSPRNG (`zip::encryption::generate_salt`/`ZipCrypto::generate_header_random` previously fell back to a low-entropy seed — wall-clock nanos, PID, a counter, stack/heap addresses, thread-id hash — on non-Unix targets or if `/dev/urandom` couldn't be opened); WinZip-AES's cipher internals (`SubBytes`/`SubWord`/`MixColumns`) rewritten from a table+branch implementation to a branchless bitsliced Boyar-Peralta S-box circuit, closing a cache-timing side channel (ciphertext unchanged — checked against FIPS 197 for all 256 inputs); x86 CRC-32 PCLMULQDQ path fixed (previously silently wrong: wrong fold constants + wrong lane extraction) and dispatch enabled now that it's verified; 7z folder coder-chain decompression bounded against amplification bombs (per-folder visited-coder tracking + a ratio-capped cumulative output budget). **Encoder-ratio limitations closed** (both changes alter only the *bytes produced*, remain RFC-conformant and reference-decodable, and are strictly smaller-or-equal, never larger): Zstandard gained `FSE_Compressed_Mode` sequence-table encoding (was Predefined/RLE-only); Brotli gained literal block splitting (quality 10-11). **Breaking change**: the ZIP write-side salt/header generation API changed from infallible to fallible (CSPRNG failure is now a reported error, not a silent weak fallback) — the one intentional breaking change this release; no archive/stream wire format changed and no public API was removed on any read/decode path. **Hygiene/infrastructure**: `deny.toml` (banned-crate enforcement), `rustfmt.toml`/`clippy.toml` added, `ManuallyDrop` removal, docs-truth fixes, new LZW round-trip example, `oxiarc-szip/TODO.md` added. Final state: **2,658 tests passing, 0 failed** (2,519 via nextest, all-features, 13/13 crates green + 139 doctests); zero clippy warnings (`--all-features --all-targets -D warnings`); zero rustdoc warnings; `cargo fmt --all --check` clean.
- **v0.4.0** (2026-07-30) — DEFLATE/zlib decoder performance rewrite. Motivation: throughput — the inflate path read the bitstream one `Read::read` call at a time and wrote every decoded byte twice (once into a separate LZ77 ring buffer, once into the output). Rewritten: a buffered `BitReader` (`BitReader::buffered`/`with_buffer_capacity`) that refills via bulk 64-bit little-endian loads instead of per-bit reads, paired with a register-resident `BitCache` (`BitReader::detach`/`reattach`/`refill_cache`) a decoder's inner loop can decode many symbols against without the store/load-forwarding stall of a memory-resident accumulator (`BitReader::new`/exact mode is unchanged and still required wherever the reader must not advance past the bits actually consumed, e.g. ZIP's byte-aligned data descriptor immediately following a DEFLATE member); `HuffmanTree` now decodes through a two-level root+sub-table layout (root widened from a single-level 9-bit table to a 10-bit root, in the style of zlib's `inflate_table`/libdeflate) instead of one flat table; the LZ77 history is now the output buffer itself (`InflateWindow`, backed by `Vec::extend_from_within`) rather than a separate ring buffer that required writing every decoded byte twice; `Adler32::update` folds 32-byte groups through a closed-form reduction instead of one add-pair per byte, letting the compiler auto-vectorize it like zlib's `DO16` unrolling (output stays bit-identical). New opt-in zero-copy entry points: `oxiarc_deflate::inflate_into(src, dst)` and `zlib::zlib_decompress_into`, decompressing directly into a caller-supplied buffer with no intermediate `Vec` and no output-size guessing (a stream that would overflow `dst` is rejected with `BufferTooSmall` rather than truncated), plus `Inflater::with_output_capacity`/`MAX_OUTPUT_CAPACITY_HINT` to pre-size the decoder's output buffer from an untrusted size hint clamped to 64 MiB (GZIP decoding now seeds this automatically from the trailing ISIZE field). Non-breaking: no archive/stream wire format changed and no public API was removed — existing callers of `inflate`, `Inflater::new`, `zlib_decompress`, etc. see only a speed-up. New `oxiarc-deflate/tests/inflate_differential.rs` proves the buffered fast path, the exact-mode path, and the new `_into` APIs all agree byte-for-byte across stored/fixed/dynamic blocks, maximum-distance (32 KiB) back-references, and hostile/truncated/corrupted input (plus an optional CPython `zlib` oracle comparison behind the pre-existing `zlib-oracle` feature), and a new `fuzz_inflate_into` target cross-checks the growable-`Vec` and slice-sink decode paths byte-for-byte. Only oxiarc-core and oxiarc-deflate changed source this cycle; the other 11 crates are unchanged from 0.3.6. Final state: **2,468 tests passing, 0 failed** (2,329 via nextest across 101 binaries + 139 doctests, 13/13 crates green); zero clippy warnings (`--all-features --all-targets -D warnings`); zero rustdoc warnings; `cargo fmt --all --check` clean; release build clean; `cargo audit` clean; `cargo deny check bans` clean; ~116,354 Rust lines across 339 files (tokei).
- **v0.3.6** (2026-07-13) — reference-interop production-hardening campaign. Root problem: the test suites only exercised oxiarc→oxiarc round-trips, which **masked total interop failure** — several codecs were self-consistent *private dialects* that passed their own tests while failing ~100% against the reference implementation in both directions. A 22-auditor differential+audit investigation produced a 75-item remediation plan (P0–P3); **all 75 items were implemented**, and every codec is now validated by reference-tool differential testing, both directions, with permanent regression gates (new opt-in oracle Cargo features that self-skip when the tool is absent, so CI stays hermetic: `zstd-oracle`, `brotli-oracle`, `xz-oracle` in oxiarc-lzma and oxiarc-archive, `bzip2-oracle`, `lz4-oracle`, `snappy-oracle`, `zlib-oracle`, `tiff-oracle`, `libaec-oracle`, `zip-oracle` — joining 0.3.5's `lha-oracle`). Measured results: **zstd** (flagship; resolves Known Issue #1) — the true root cause was the FSE backward bitstream being read FIFO/LSB-first instead of RFC 8878 LIFO/MSB-first, mirrored in the writer (hence self-round-trips passed), compounded by the 4-stream Huffman jump table read as offsets instead of sizes and unvalidated FSE table/state indexing (the reported `fse.rs` index-OOB panic); baseline 0/64 real frames decoded (62 panics, 2 silent corruptions) → **64/64 corpus + 101/101 wide reference frames decode byte-identical; 85/85 oxiarc frames accepted by `zstd -d`; 9/9 dictionary frames; 60,000 fuzz cases, 0 panics**; the Huffman literals encoder is wired in (sequence FSE stays predefined/RLE — RFC-valid, a ratio limitation only, see Known Issues). **brotli** — full RFC 7932 rewrite of decoder AND encoder (window-bits tree, 704-symbol insert-and-copy table, block-type switching, code-length VLC, context maps + §7.1 LUTs, distance ring rules, metadata blocks, two-level Huffman tables) including the byte-exact 122,784-byte Appendix A static dictionary with all 121 transforms; baseline 141/172 reference-stream decode failures + 21 silently wrong and `brotli -d` rejecting 441/441 oxiarc outputs → **608/608 reference streams decode byte-identical (zero silent mismatches); 588/588 oxiarc streams accepted by `brotli -d`** (real compressed meta-blocks). **lzma/xz** — the LZMA2 decoder reset the uncompressed position per chunk, desyncing `pos_state`, so standard multi-chunk `.xz` was undecodable; now **60/60 `.xz` decode + 8/8 encode byte-identical vs `xz 5.8.3`** (multi-block OK), plus a stateful chunked encoder (cross-chunk matching) and `LzmaProperties` validation (`new(20,20,4)` previously SIGABRT'd via a multi-TiB allocation). **bzip2** — multi-stream/concatenated `.bz2` (pbzip2/lbzip2/`cat`) was silently truncated to the first stream; now full multi-stream decode, **324/324 both directions vs `bzip2 1.0.8`**, legacy randomised-block de-randomisation, a multi-Huffman-table encoder (libbz2 `sendMTFValues`) reaching **99.9% of the reference compression ratio** (was up to +50% larger), and `decompress_with_limit`. **szip** — AEC/CCSDS-121 framing put the RSI reference sample outside the block option-ID field, breaking libaec interop both ways incl. silent wrong output; rewritten per CCSDS-121.0-B-2 §5.2, validated against **live libaec 1.1.4: 2450/2450 decode + 4900/4900 encode byte-identical**. **lzw** — TIFF LZW had no Clear Code support (100% incompatible with real TIFF from libtiff/Pillow/GDAL); now **125/125 both directions vs Pillow/libtiff, with oxiarc's encoded output byte-identical to libtiff's**. **deflate** — the core codec was already correct (bit-exact vs CPython zlib/gzip + the gzip CLI), but the streaming/trait wrappers silently truncated output past 32 KiB, broke DEFLATE bit-continuity across calls, couldn't decode multi-member gzip (incl. oxiarc's own parallel output), and had an O(n²) bomb-amplifiable zlib concatenation path hanging >60s — all fixed and reference-verified. **lz4** — decompression bomb (per-sequence `max_output` bypass: 64 B → 1,020,020 B), LASTLITERALS(5) violation (reference lz4 rejected the output), and the ignored block-independence flag (`lz4 -BD` undecodable) all fixed; **11/11 encode + 44/44 decode + 3/3 linked-block vs `lz4 1.10.0`**. **snappy** — the 64 KiB per-chunk cap is now enforced (was 21x amplification) + bounded APIs. **lzhuf** — lh4–lh7 decoders returned `Ok` with silently truncated output on truncated streams (**4,442/4,442 truncation trials now return `Err`**; was 4,440 silent short-`Ok`); streaming decoder hardened; the 64-bit size extension header (0x42) is honored in the stream reader. **archive** — externally-encrypted ZIPs (`zip -e`, 7-Zip, WinRAR) were reported UNENCRYPTED and silently mis-extracted (detection used a homegrown 0xEE,0xEE marker instead of general-purpose bit 0) — fixed, plus DOS-date month=0 underflow panic, AE-2 CRC=0 spec compliance, the Info-ZIP data-descriptor check byte, and correct civil-date DOS timestamps; XZ: unbounded block-header allocation (28-byte crafted file → SIGABRT), filter-props OOB panic, and block-header CRC32 now validated; 7z: unbounded stream counts (capacity-overflow panic / TiB reservation) and the kCrc aggregate (175 bytes → 256 MB) bounded; CAB: MSZIP LZ77 window now carried across CFDATA blocks, CFDATA checksums validated, per-folder decode cache (was O(files × folder_size)), unknown methods now Err instead of silent stored; ISO: directory-record OOB panic on small LEN_DR fixed; TAR: GNU sparse ('S') and PAX 0.1 sparse entries fully supported in the streaming reader (bsdtar-verified byte-identical; previously silent mis-decode); also fixed a pre-existing flaky shared-temp-path race in the async_lzh/async_tar tests. **CLI** — `--memory-limit` (the advertised decompression-bomb defense) was silently unenforced for file-based gzip/xz/bzip2; now enforced **during** decode for all formats (gzip ISIZE, xz stream index, lz4/zstd declared sizes, bzip2/brotli/snappy bounded decoders): a brotli bomb under `--memory-limit 1M` peaks at **3.3 MB RSS vs 72.9 MB** and exits non-zero; `.br` files (no magic bytes) gained an extension fallback so file-path commands work; end-to-end matrix **303/303 pass** (13 formats × 7 subcommands, oxiarc- and reference-produced inputs, byte-identical); corrupt/truncated inputs across all formats: 0 panics, 0 exit-101; path traversal blocked. **API stability** — 18 public enums marked `#[non_exhaustive]` (formats/methods/status/errors), 106 `#[must_use]` on consuming builders, internal types no longer leaked in public signatures. Wire-format note: szip and the crate-private LZW stream framing changed from private dialects to the standard formats (old oxiarc-only streams of those two codecs are not readable — intended). Final state: **2,426 tests passing, 0 failed, 0 ignored** (2,289 via nextest across 100 binaries + 137 doctests; 112 suites); zero clippy warnings (`--all-features --all-targets`, also clean with `--no-default-features`); `cargo build --workspace --no-default-features` green (Pure Rust default preserved); `cargo fmt --all --check` clean; rustdoc clean; ~114,394 lines across 336 Rust files (tokei). Acid test: the 64 real-world zstd frames (OxiGDAL Zarr v3 chunks) that triggered this campaign decode **64/64 byte-identical through the `oxiarc` CLI, 0 panics**.
- **v0.3.6** (2026-07-08): Production-readiness hardening release. A structured 11-dimension audit (security, panic-freedom, API stability, tests/fuzz, docs, packaging, CLI UX, stubs/fabrications, cross-platform, format completeness) drove ~30 fixes across the workspace. **Security / untrusted-input**: fixed a Zip-Slip path-traversal in the 7z/CAB/ISO 9660 extract paths (they bypassed the `..`-stripping sanitizer the ZIP/TAR/LZH paths used; the sanitizer now also drops `..`/root/drive-prefix components and `resolve_output_path` adds a canonicalized-root containment check); bounded every header-driven allocation against the real remaining input length via `try_reserve`/`try_reserve_exact` (ZIP central-dir/entry counts + compressed sizes, LZH, 7z, TAR, XZ/LZMA dict up to a 1.5 GiB cap, zstd `content_size`), closing decompression-bomb / capacity-overflow-panic vectors that `--memory-limit` did not cover; fixed an integer-underflow panic in the encrypted-ZIP length computation (`checked_sub`) and a DOS-date-field underflow panic in `LocalFileHeader::modified_time`; made WinZip-AES tag / password-verification comparisons constant-time; replaced the AES-256-only cipher that silently zero-padded shorter keys with a genuine key-length-dependent AES-128/192/256; rejected spanned/multi-volume ZIP instead of misreading it. **Correctness (found by the new fuzz/example work)**: fixed a genuine LZMA2 multi-chunk decode bug that corrupted any varied (non-repeated-byte) input crossing more than one chunk boundary — this affected the DEFAULT `encode_lzma2`/`decode_lzma2` path for inputs above the 2 MiB chunk size (root cause: only the first chunk set the dictionary-reset flag, so the decoder's literal context diverged and the range coder desynced; every fresh-encoder chunk now resets the dict, spec-conformant LZMA2 reset field 3, liblzma-decodable, zero ratio loss); fixed an lh1 (LZHUF) decode DoS where a tiny malformed stream with a huge declared size looped producing zero-padding output until OOM (BitReader now signals exhaustion and decode errors promptly); corrected the CRC-32 runtime-dispatch diagnostics so `implementation_name()`/`is_simd_available()` report the path actually selected (the aarch64 PMULL constants were verified correct by exact software simulation of the clmul/fold/Barrett arithmetic and kept enabled; x86 PCLMULQDQ stays disabled pending validation). **API stability for 1.0**: marked status/mode enums (`FlushMode`, `DecompressStatus`, zstd `BlockType`/`LiteralsBlockType`, and the codec error enums) `#[non_exhaustive]`, demoted zstd advanced internal re-exports to `pub(crate)`, and made zstd encoder docs honest about its literals (Raw/RLE) + Predefined-FSE reality. **Tooling / tests**: proptest round-trip + no-panic suites across all 10 codecs, 18 cargo-fuzz harnesses for the decoder/parser entry points, runnable `examples/` across the crates, CAB/ISO/corrupt-input/CLI integration tests, regenerated shell completions + man pages (relocated under `oxiarc-cli/`), `deny.toml`, `SECURITY.md`/`CONTRIBUTING.md`, per-crate `LICENSE` symlinks + `[package.metadata.docs.rs]`, and `cargo package` excludes so oracle-corpus fixtures / `TODO.md` / stale `.bak` files no longer ship. 2,004 tests passing (all features, 1 skipped); zero clippy / check / rustdoc warnings; ~100,081 lines across 195 files. CI/CD was intentionally left out of scope for this release.
- **v0.3.5** (2026-07-07): LZH canonical bitstream rewrite — the lh4/lh5/lh6/lh7 codec previously implemented a private, self-consistent-only format; rewritten to genuine canonical LHA format (MSB-first bit order + corrected code-table/position encoding, new `oxiarc_core::msb_bitstream` module), validated against 6 real-LHA-produced `.lzh` archives and a live `lha` (Lhasa) oracle at both the codec level (`oxiarc-lzhuf`) and archive level (`oxiarc-archive`) via a new opt-in `lha-oracle` Cargo feature in both crates. Also: fixed genuine undefined behavior (Miri-detected) in oxiarc-core's CRC SIMD code (unsound pointer arithmetic, 7 call sites); fixed a resource leak in `ZipWriter`/`TarWriter`/`LzhWriter::into_inner()` in oxiarc-archive; fixed oxiarc-cli exit codes so `test`/`list` on unrecognized archive formats correctly return non-zero instead of silently exiting 0; dependency updates (`glob`, `clap_complete` bumps, `crossbeam-epoch` security fix for RUSTSEC-2026-0204, removed an unused oxiarc-core dependency from oxiarc-szip); added regression test coverage for deflate max-length-match decoding and snappy max-size-block handling (both already-correct, closing test gaps). 1,878 tests passing (all features), zero clippy/check/rustdoc warnings.
- **v0.3.4** (2026-07-06): Interoperability hardening release — spec-conformance defects found via downstream FVRS integration testing were root-caused and fixed across LZMA/LZMA2 (distance-slot probability layout, state mapping, LZMA2 control-byte reset field, embedded EOS markers), bzip2 (bit order, CRC-32 variant, MTF/Huffman pipeline, format minimums), 7z (substream sizes, 0-byte members, varint decoding, header/CRC handling), ZIP (Shift-JIS/EFS/CP437 name decoding, writer EFS flag), TAR (char-boundary panic, PAX long-name write support), XZ (block-header CRC, index unpadded size, self-describing chunk framing), and LZH (`-lh1-`/`-lhd-`/unknown-method listing, beyond-window LZSS/Huffman fixes, Shift_JIS filenames). All codecs validated bidirectionally against liblzma/libbz2/bsdtar/CPython; hermetic golden-vector test suites committed. 1,799 tests passing (all features), zero clippy/check/rustdoc warnings.
- **v0.3.3** (2026-06-06): oxiarc-brotli high-entropy/incompressible round-trip fix — incompressible data now round-trips byte-for-byte across all quality levels (1–11). Fixed two underlying bugs: (1) incomplete length-limited Huffman codes (replaced the `ceil(-log2 p)` heuristic with the package-merge algorithm, which always yields a complete, length-optimal code) and (2) insert lengths above 319 were silently truncated (unified the insert-length code table between encoder and decoder, extending categories up to ~4 MiB inserts). 13 new high-entropy regression tests. 1,679 tests passing, 2 skipped, zero warnings. No other crate changed.
- **v0.3.2** (2026-05-31): AEC/SZIP codec (oxiarc-szip) — full CCSDS-121.0-B-2 compliant encoder/decoder with `BitReader`/`BitWriter` bit manipulation primitives, `SzipParams` configuration struct, `SzipError` error enum. Round-trip tests for all sample scenarios.
- **v0.3.1** (2026-05-16): LZH custom dictionary (`LzhEncoder::with_dictionary`, `LzhDecoder::with_dictionary`, `LzssEncoder/Decoder::preload_dictionary`). LZMA custom dictionary (`LzmaEncoder/Decoder::with_dictionary`). LZMA memory pool (`LzmaPool`, `PooledBuf`, `LzmaDecoderPooled`) — amortizes large dict allocations. Archive repair/recovery (`repair_zip`, `repair_tar`, `ZipRepair`, `TarRepair`, `RepairReport`) for truncated/corrupt ZIP+TAR archives. Snappy + Brotli interop test vectors (35 new integration tests). 1446 tests (77 new), 3 skipped, zero warnings.
- **v0.3.0** (2026-05-17): DEFLATE Zopfli-style optimal parsing (`OptimalParser`, `with_optimal_parsing`). Snappy parallel frame compression (`compress_parallel`, `parallel` feature). LZ4 true bounded-memory streaming (block-level `Lz4Compressor`/`Lz4Decompressor`, `with_memory_budget`). LZH 4-byte hash + `LzssOptimalParser` + `with_optimal()` builder. LZMA BT4 binary tree match finder (`Bt4MatchFinder`, `MatchFinder` trait; level 9 now uses BT4). `MappedFile` zero-copy memory-mapped primitive in oxiarc-core (`mmap` feature). 1325 tests (44 new), 3 skipped, zero warnings.
- **v0.2.8** (2026-05-08): SIMD CRC32 via aarch64 PMULL (Apple Silicon) and x86_64 SSE 4.2 (Snappy CRC32C). Progress/cancel builders (`with_progress`, `with_cancel`) on lz4, zstd, and lzma2. Raw-preserve append in `oxiarc add` (ZIP/LZH byte-for-byte). ISO 9660 read support (list/extract/info/detect). CLI `--memory-limit` option for extract and list. 1281 tests passing (2 skipped), 1442 public API items, 58,356 lines, 12 crates, 182 Rust files.
- **v0.2.7** (2026-04-21): All workspace crates feature-complete, tested, and API-stable. All policies enforced. 1206 tests passing, 1394 public API items.
- **v0.2.6** (2026-03-21): Brotli fixes: is_single_symbol() bug fix, write_prefix_code_and_build_tree() function, Kraft inequality i32 fix, comprehensive roundtrip tests.
- **v0.2.5** (2026-03-18): New codecs: Brotli (RFC 7932) with quality levels 0-11, static dictionary, streaming; Snappy with block and framed formats, CRC32C. DEFLATE streaming (GzipStreamEncoder/Decoder, ZlibStreamEncoder/Decoder) with flush modes (sync_flush, full_flush, partial_flush). LZ4 acceleration parameter for compress_block_with_accel(). LZW streaming encoder/decoder (LzwStreamEncoder/LzwStreamDecoder, TIFF and GIF modes). Brotli/Snappy archive integration (BrotliReader/BrotliWriter, SnappyReader/SnappyWriter with format detection). EntryBuilder pattern with fluent API. Serde serialization for Entry types (optional feature). CLI: dry-run mode (--dry-run/-n), sort by ratio, Brotli/Snappy format support. Total: 1038 tests, ~47,241 lines, 150 files.
- **v0.2.4** (2026-03-16): Dependency updates (clap 4.5→4.6, clap_complete 4.5→4.6), clippy fixes (collapsible match guards, sort_by→sort_by_key, redundant .max(0)). Total: 799 tests, ~40,406 lines, 127 files.
- **v0.2.3** (2026-03-11): Async ZIP (async_zip), async deflate (async_deflate), GZip module, GIF LZW codec (gif_lzw), LSB bitstream (bitstream_lsb). Total: 799 tests, ~39,417 lines, 127 files.
- **v0.2.2**: Previous release
- **v0.2.1**: Previous release
- **v0.2.0**: LZW crate, full Zstandard encoder, Bzip2 parallel compression
- **v0.1.0**: Initial release — core codecs, archive formats, CLI

## Phase 1: Core Foundation (COMPLETE)

- [x] BitStream (LSB-first bit packing)
  - [x] BitReader with u64 buffer
  - [x] BitWriter with u64 buffer
  - [x] Generic over Read/Write traits
- [x] RingBuffer for LZ77/LZSS
  - [x] Configurable sizes (4K-64K)
  - [x] Safe modulo wrapping
  - [x] Copy-from-self for match expansion
- [x] CRC implementations
  - [x] CRC-32 (ZIP/GZIP polynomial)
  - [x] CRC-32 slicing-by-8 optimization (8x table lookup, ~3-5x faster)
  - [x] CRC-64/ECMA-182 (XZ format)
  - [x] CRC-64 slicing-by-8 optimization (8x table lookup, ~3-5x faster)
  - [x] CRC-16/ARC (LZH polynomial)
- [x] Core traits
  - [x] Compressor/Decompressor streaming traits
  - [x] ArchiveReader/ArchiveWriter traits
  - [x] Entry metadata structure
- [x] Error handling with thiserror

## Phase 2: DEFLATE Codec (COMPLETE)

- [x] Huffman trees
  - [x] Canonical Huffman code generation
  - [x] Tree building from code lengths
  - [x] Fixed Huffman tables (RFC 1951)
  - [x] Dynamic Huffman tree generation
  - [x] Package-merge algorithm for length-limited codes
- [x] LZ77 encoder
  - [x] 32KB sliding window
  - [x] Hash chain pattern matching
  - [x] Lazy matching for better compression
- [x] Inflate (decompression)
  - [x] Stored blocks (type 00)
  - [x] Fixed Huffman blocks (type 01)
  - [x] Dynamic Huffman blocks (type 10)
- [x] Deflate (compression)
  - [x] Fixed Huffman encoding
  - [x] Dynamic Huffman encoding (RLE code length encoding)
  - [x] Automatic block type selection (size estimation)
  - [x] Compression levels 0-9
  - [x] Frequency counting and optimal tree building
- [x] Zlib wrapper (RFC 1950)
  - [x] Adler-32 checksum implementation
  - [x] Zlib header with compression level indicator
  - [x] Streaming compressor/decompressor

## Phase 3: LZH Codec (Complete)

- [x] LZSS encoder/decoder
  - [x] Ring buffer implementation
  - [x] Length/distance coding
- [x] LZH Huffman trees
  - [x] CODES tree (literals + lengths)
  - [x] OFFSETS tree (distances)
- [x] Method support
  - [x] lh0 (stored)
  - [x] lh5 (8KB window, most common)
  - [x] lh4, lh6, lh7 (other window sizes)

## Phase 4: Container Formats (Partial)

### ZIP
- [x] Local file header parsing
- [x] Central directory parsing
- [x] File extraction with DEFLATE
- [x] File extraction with stored method
- [x] Archive creation (ZipWriter)
- [x] Zip64 extensions (large files)
- [x] Data descriptor support (FLAG_DATA_DESCRIPTOR, central directory based reading)
- [x] ZIP encryption (traditional) — ZipCrypto implemented + e2e test (2026-04-20)
- [x] ZIP encryption (AES) — WinZip AE-2 AES-256 implemented + e2e test (2026-04-20)

### GZIP
- [x] Header parsing (RFC 1952)
- [x] Decompression with CRC-32 verification
- [x] Compression (archive creation)
- [x] Optional fields (FNAME, FCOMMENT, etc.)

### TAR
- [x] UStar header parsing
- [x] Entry listing
- [x] File extraction
- [x] Archive creation (TarWriter)
- [x] PAX extended headers (long filenames, metadata)
- [x] GNU LongName/LongLink headers

### LZH
- [x] Level 0/1/2/3 header parsing
- [x] Extension headers (filename, directory, etc.)
- [x] More extension headers (0x40 OS attr, 0x41 Windows timestamps, 0x42/0x43 64-bit sizes, 0x44 comment, 0x46 Unix perms, 0x50 owner names, 0x51 owner IDs, 0x54 Unix mtime)
- [x] Shift_JIS filename decoding
- [x] Path sanitization
- [x] File extraction with CRC-16 verification
- [x] Archive creation (LzhWriter - stored mode)
- [x] LH5 compression encoding (roundtrip working)

## Phase 5: LZMA Codec (Complete)

- [x] Range coder
  - [x] Range encoder with cache mechanism
  - [x] Range decoder
  - [x] 11-bit probability model
- [x] LZMA model
  - [x] Literal model with context
  - [x] Length model (match/rep lengths)
  - [x] Distance model (slot + direct + align)
  - [x] State machine (12 states)
- [x] LZMA encoder
  - [x] Literal encoding
  - [x] Match encoding
  - [x] Rep match encoding
  - [x] End marker encoding
  - [x] Optimal parsing with price calculation and dynamic programming
  - [x] Compression levels 0-9 (greedy for levels 0-6, optimal for levels 7-9)
- [x] LZMA decoder
  - [x] Full LZMA stream decoding
  - [x] Known/unknown uncompressed size

## Phase 6: Advanced Features (Future)

### Additional Codecs
- [x] LZMA2 (for 7z/xz)
- [x] BZip2 (BWT + MTF + Huffman + Zero-run encoding, full roundtrip support)
- [x] LZ4 (official frame format with XXHash32 checksums)
  - [x] Official LZ4 frame format (RFC compatible)
  - [x] XXHash32 implementation for frame/block/content checksums
  - [x] Block independence flag and configurable block sizes (64KB-4MB)
  - [x] Content size in header and content checksum verification
  - [x] LZ4-HC high compression mode (levels 1-12)
  - [x] Optimal parsing for level 12 (dynamic programming)
- [x] Zstandard (RFC 8878: full decoder — FSE incl. custom/repeat tables + 1/4-stream Huffman literals; encoder — LZ77 + Huffman-compressed literals + predefined/RLE FSE sequences; XXHash64, streaming, dict modules; reference-interop verified 2026-07-13, see Known Issues #2 for the remaining sequence-table ratio limitation)
- [x] GIF LZW codec (gif_lzw module in oxiarc-lzw, LSB bitstream)
- [x] LSB bitstream (bitstream_lsb module in oxiarc-lzw)
- [x] Brotli (RFC 7932 with LZ77, context-dependent Huffman coding, static dictionary, quality levels 0-11, streaming API)
- [x] Snappy (block format + framed format with CRC32C checksums, streaming Write/Read API)

### Additional Formats
- [x] 7z archive format (read support with LZMA/LZMA2 decompression)
- [x] XZ file format (compression and decompression)
- [x] CAB (Microsoft Cabinet, read support with None/MSZIP decompression)
- [ ] RAR (read-only, legal constraints)

### Performance
- [x] CRC-32 slicing-by-8 optimization (8x table lookup, ~3-5x faster)
- [x] CRC-64 slicing-by-8 optimization (8x table lookup, ~3-5x faster)
- [x] LZ77 hash function optimization (improved avalanche properties, multiplication-based)
- [x] LZ77 match finding optimization (early rejection, loop unrolling, best_len check)
- [x] LZ77 large input handling (proper chunking and window sliding)
- [x] BWT key-based sorting optimization (4-byte prefix keys for faster sorting)
- [x] Comprehensive performance benchmarks
  - [x] CRC benchmarks (crc_bench)
  - [x] LZ77 benchmarks (lz77_bench, deflate_bench)
  - [x] BWT benchmarks (bwt_bench, bzip2_bench)
  - [x] LZ4 benchmarks (lz4_bench)
  - [x] LZH benchmarks (lzhuf_bench)
  - [x] LZMA benchmarks (lzma_bench)
  - [x] Zstandard benchmarks (zstd_bench, parallel_bench)
  - [x] LZW benchmarks (lzw_bench)
  - [x] Brotli benchmarks (brotli_bench)
  - [x] Snappy benchmarks (snappy_bench)
  - Performance numbers:
    - LZ77: 48-400 MB/s (level 1), 13-275 MB/s (level 5), 0.3-253 MB/s (level 9)
    - BWT Forward: 2-11 MB/s, Inverse: 60-320 MB/s
- [x] Parallel compression (partially complete)
  - [x] LZ4 parallel frame compression (rayon-based block-level parallelism)
  - [x] Zstandard parallel compression (rayon-based block-level parallelism)
  - [x] Bzip2 parallel compression (rayon-based block-level parallelism)
  - [x] Parallel GZIP — pigz-style multi-member parallel GZIP (`gzip_compress_parallel`, `ParallelGzipEncoder`, `parallel` feature in oxiarc-deflate)
  - [x] Parallel LZMA2 — multi-threaded LZMA2 compression (`lzma2_compress_parallel`, `ParallelLzma2Encoder`, `parallel` feature in oxiarc-lzma)
- [x] LZ4 block-layer prefix dictionary support (`Lz4DictBlockEncoder`, `Lz4DictBlockDecoder`, `compress_block_with_dict`, `decompress_block_dict`)
- [x] LZ77 heuristics tuning API (`Lz77Params`, `Lz77Preset` — nice_match + chain configuration in oxiarc-deflate)
- [x] DEFLATE thread-safe memory pool (`DeflatePool`, `PooledBuf` — amortizes buffer allocations in oxiarc-deflate)
- [x] DEFLATE streaming compression/decompression (GzipStreamEncoder/Decoder, ZlibStreamEncoder/Decoder with configurable block sizes)
- [x] LZ4 acceleration parameter (compress_block_with_accel, adaptive skip scaling)
- [x] Memory-mapped file support (`MappedFile` in oxiarc-core, `mmap` feature, done 2026-05-16)
- [x] LZMA custom dictionary support (`LzmaEncoder::with_dictionary`, `LzmaDecoder::with_dictionary`)
- [x] LZMA memory pool (`LzmaPool`, `PooledBuf`, `LzmaDecoderPooled` — amortizes large dict allocations, `parallel` feature in oxiarc-lzma)
- [x] LZH custom dictionary support (`LzhEncoder::with_dictionary`, `LzhDecoder::with_dictionary`, `LzssEncoder/Decoder::preload_dictionary`)
- [x] Archive repair/recovery (`repair_zip`, `repair_tar`, `ZipRepair`, `TarRepair`, `RepairReport` — handles truncated/corrupt ZIP+TAR archives in oxiarc-archive)
- [x] Snappy memory pool (`SnappyPool`, `PoolStats`, `compress_frame_pooled` — thread-safe buffer reuse for FrameEncoder/FrameDecoder in oxiarc-snappy)
- [x] Snappy dictionary APIs (`compress_block_with_dict`, `decompress_block_with_dict`, `compress_frame_with_dict`, `decompress_frame_with_dict` in oxiarc-snappy)
- [x] Snappy async I/O (`AsyncSnappyCompressor`, `AsyncSnappyDecompressor` — async-io feature in oxiarc-snappy)
- [x] Zstd multi-frame decompression (`decompress_multi_frame`, `decompress_multi_frame_with_dict`; streaming dict multi-frame fix in oxiarc-zstd)
- [x] CLI man pages — full set of troff `.1` man pages for all CLI subcommands in `man/` directory
- [x] Async deflate (async_deflate module in oxiarc-deflate, async-io feature)
- [x] GZip module (gzip module in oxiarc-deflate)
- [x] Async ZIP support (async_zip module in oxiarc-archive, async-io feature)
- [x] DEFLATE flush modes (sync_flush, full_flush, partial_flush for GzipStreamEncoder/ZlibStreamEncoder)
- [x] LZW streaming encoder/decoder (LzwStreamEncoder/LzwStreamDecoder with TIFF and GIF modes)
- [x] EntryBuilder pattern with fluent API (oxiarc-core)
- [x] Serde serialization for Entry types (optional serde feature in oxiarc-core)
- [x] Streaming with async I/O (completed 2026-07-07) — all per-crate async I/O done: core traits/wrappers (AsyncCompressor/AsyncDecompressor, StreamingAsyncCompressor/Decompressor, compress_concurrent/decompress_concurrent), brotli (BrotliAsyncCompressor/Decompressor), lzma (async_lzma), archive TAR/LZH/ZIP async readers, snappy async I/O, async DEFLATE (GzipStream/ZlibStream). True bounded-memory async streaming through codec internals is an explicit non-goal (each sub-crate's own TODO documents this); open a new item if that scope is wanted later.

### Quality / Testing
- [x] Snappy interop integration tests (16 tests against wire-format golden vectors covering block and framed formats in oxiarc-snappy)
- [x] Brotli interop integration tests (19 tests across quality levels 0-11 covering RFC 7932 compliance in oxiarc-brotli)
- [x] Brotli high-entropy/incompressible round-trip fix (v0.3.3) — package-merge length-limited Huffman codes + unified encoder/decoder insert-length table (inserts up to ~4 MiB); incompressible data round-trips byte-for-byte across quality 1–11; 13 new high-entropy regression tests in oxiarc-brotli

### Platform
- [ ] WASM bindings
- [ ] Python bindings (PyO3)

## Phase 7: CLI Enhancements & Documentation (Complete)

- [x] List command
  - [x] JSON output support (--json flag with pretty-printing)
- [x] Extract command (ZIP, GZIP, TAR, LZH, XZ, 7z, CAB, LZ4, Zstd, Bzip2, Brotli, Snappy)
  - [x] Timestamp preservation (--preserve-timestamps, -t)
  - [x] Permission preservation (--preserve-permissions)
  - [x] All metadata preservation (-p for timestamps + permissions)
  - [x] Overwrite modes (--overwrite, --skip-existing, --prompt)
  - [x] Stdin/stdout support for single-file formats
- [x] Info command
- [x] Detect command
- [x] Test command (archive integrity)
- [x] Create command (ZIP, TAR, GZIP, LZH, XZ, LZ4, Zstd, Bzip2, Brotli, Snappy)
  - [x] Stdin/stdout support for single-file formats
- [x] Convert command (format conversion including 7z, CAB, Brotli, Snappy input)
- [x] Dry-run mode (--dry-run, -n for create and extract commands)
- [x] Sort by ratio (Ratio variant added to SortBy enum)
- [x] Progress bars and verbose output (extract command with -v/--verbose and -P/--progress)
- [x] Recursive directory handling
- [x] Filter patterns (include/exclude)
- [x] Shell completion scripts (bash, zsh, fish, powershell)
- [x] Comprehensive README documentation
  - [x] Installation instructions (cargo install, build from source)
  - [x] Format support matrix with full feature breakdown
  - [x] Performance benchmarks with real data
  - [x] Detailed CLI examples for all commands
  - [x] API usage examples for all major codecs
  - [x] Contributing guidelines following COOLJAPAN policies

## Phase 8: HTTP Content-Coding, Incremental Inflate, and Image Codecs (P2/P3 program, started 2026-09-07)

Origin: an ecosystem-wide dependency audit found `flate2` in 39 of 93 `~/work` project lockfiles. Two routes dominate: (Tier A) `ureq`'s default `gzip` feature, and (Tier B, ~30 projects) `image` → `png`/`tiff` (which arrive via eframe/egui/arboard/plotters in any UI project). Neither route can be closed by configuration alone without losing bandwidth (HTTP) or image I/O (Tier B). This phase closes both with Pure Rust, oxiarc-based code. Design reports (7 investigators + 1 completeness critic, 2026-09-07) fixed the interface contracts below; where an implementer's reading of a report disagrees with this section, this section wins.

### Root finding that drives P2
- [x] Verified 2026-09-07: `GzipStreamDecoder::fill_buffer` and `ZlibStreamDecoder::fill_buffer` (oxiarc-deflate/src/streaming.rs) call `read_to_end` and decode the whole stream before serving the first byte; `ZlibStreamDecoder::with_max_output` is enforced only after full expansion; `Inflater as Decompressor` cannot accept partial input and, after a mid-stream EOF, a second call returns `Ok((0, n, Done))` with silently truncated output (inflate.rs:231/236/246). `BrotliDecompressor<R>` and `ZstdStreamDecoder<R>` also `read_to_end`; `oxiarc-zstd` has no output cap at all. A truly resumable inflate is therefore the keystone of everything below.

### Owner decisions (made 2026-09-07; recorded so nobody re-litigates them)
1. gzip `FHCRC` is verified by default in the new `WrappedInflate` (`verify_header_crc(true)`); legacy one-shot `gzip_decompress` keeps its behaviour unless re-based (CHANGELOG note required).
2. Legacy `GzipStreamDecoder` keeps returning `Ok(0)` on non-gzip leading bytes (`strict_first_member(false)`); every new type (`InflateReader`, `oxiarc-http`) is strict.
3. All five new crates are `#![forbid(unsafe_code)]`; `oxiarc-tiff` pays one extra pass at the typed-buffer API boundary instead of a bytecast module.
4. An `oxiarc-image` facade crate IS in scope (thin: sniff/open/load_from_memory/save + typed pixel buffers + `image`-shaped names), because 10 of the 17 actionable Tier-B projects depend on the `image` facade, not on `png`/`tiff` directly. It is not an image-processing library.
5. PackBits and CCITT G3/G4 live as private modules inside `oxiarc-tiff`.
6. `oxiarc-jpeg` `arithmetic` feature (SOF9/10/11) is implemented and default-on; hierarchical JPEG (SOF5/6/7/13/14/15) returns a named `Unsupported` error (no reference encoder exists to test against).
7. Apple `CgBI` PNGs are decoded (raw deflate IDAT, BGR(A) swap, premultiplied-alpha flag exposed) behind the lenient default; strict mode rejects them with a named error.
8. `Transfer-Encoding: chunked` is out of scope for `oxiarc-http` (the HTTP client owns transfer coding; documented). `dcz` (RFC 9842 zstd + shared dictionary) is supported when a dictionary is supplied; `dcb` parses and returns `Unsupported` unless oxiarc-brotli grows shared-dictionary support.
9. Crate names stay `oxiarc-http`, `oxiarc-png`, `oxiarc-jpeg`, `oxiarc-tiff`, `oxiarc-image` (registered in the workspace 2026-09-07; publish order: jpeg, png, tiff, http, image before archive/cli).
10. No new `oxiarc-core::stream` module and no blanket `Decompressor` impl; cross-codec unification lives only in `oxiarc-http`'s private `CodingDecoder` trait. `Decompressor::decompress` keeps a whole-remaining-input contract (`FlushMode::Finish`); `AsyncDecompressorWrapper<Inflater>` is documented unsupported and re-pointed at `AsyncInflateReader`.
11. External oracles only (python3 zlib/gzip/Pillow/tifffile, libtiff `tiffcp`/`tiffinfo`, libjpeg `cjpeg`/`djpeg` when present); never a banned crate as a dev-dependency (`cargo deny check bans` walks dev-deps and stays a MUST gate).
12. `zlib_decompress_into` keeps its exact-slice semantics (Adler-32 read from the last 4 input bytes); PNG/TIFF never call it on slices that may carry trailing bytes — they use `WrappedInflate(Zlib)` with explicit `verify_checksum`/`TrailingPolicy`.
13. `max_output`/ratio guards bound ONE stream and reset with it; containers that reset per frame/strip (APNG, TIFF) carry their own file-level budget.
14. MSRV 1.85 is real: no let-chains (`if let … && …` needs 1.88) anywhere; local rustc 1.95 will not catch it.

### Agreed interface contracts (final; from the critique §2)
- `oxiarc_deflate::stream::InflateStream` — `new()`, `with_window_capacity(bytes)`, `inflate(&mut self, input: &[u8], output: &mut [u8], flush: FlushMode) -> Result<InflateProgress { consumed, produced, status: InflateStatus::{NeedInput, NeedOutput, StreamEnd} }>`, `set_dictionary(&[u8]) -> u32`, `reset()`, `reset_keep_history()`, `reset_for_next_member()` (preserves the bit accumulator), `align_to_byte()`, `is_finished()`, `at_sync_flush()`, `take_buffered_byte() -> Option<u8>`, `bits_consumed()`, `total_in()`, `total_out()`, `with_max_output(u64)`, `with_ratio_guard(f64, u64)`. Fast path (existing cached symbol loop) while `input_remaining >= 8 && output_space >= 258`, careful resumable per-symbol path otherwise; 32 KiB linear history; sticky fault latch cleared by `reset()`.
- `oxiarc_deflate::wrapper::WrappedInflate` — `new(InflateWrapper::{Raw, Zlib, Gzip, Auto})`, builders `multi_member`, `trailing_policy(TrailingPolicy::{Reject, AllowZeros, Stop})`, `with_max_output`, `with_ratio_guard`, `with_dictionary`, `verify_header_crc` (default true), `verify_checksum` (default true; PNG sets false), `strict_first_member` (default true); `inflate(...)` same shape; `is_finished`, `total_in`, `total_out`, `members_decoded`, `gzip_header() -> Option<&GzipHeaderInfo>`, `reset`. `Auto` = 2-byte zlib header sniff at offset 0 only (no Adler-32 retry).
- `oxiarc_deflate::{InflateReader<R: Read>, AsyncInflateReader<R: AsyncRead>}` — 64 KiB output staging buffer (mandatory), retry `Interrupted`, propagate `WouldBlock`, inner `Ok(0)` switches to `FlushMode::Finish` so truncation is an error, no-progress guard is an `Err` not a `debug_assert`, and the zlib "fewer than 6 unconsumed bytes at EOF after a complete member ⇒ stop" rule lives in the adapter.
- FlushMode per consumer: TIFF strip/tile and APNG frame = `Finish` on a complete slice (then `reset()`); PNG main IDAT chain and HTTP body = `None` until EOF then `Finish`; `RawInflateReader` (RFC 4978) = `None` always; `Decompressor::decompress` = `Finish`.
- `oxiarc_brotli::BrotliStream` / `oxiarc_zstd::ZstdStream` — `decode(&mut self, input, output, flush) -> Result<{Brotli,Zstd}Progress>` (fields mirror `InflateProgress`), `finish()`, `with_max_output(u64)`, `with_max_window(usize)`; zstd also `with_multi_frame(bool)`, `with_dictionary(Vec<u8>)`. Pre-decode budget enforcement (brotli MLEN exact; zstd conservative 128 KiB per compressed block then charge actual).
- `oxiarc_jpeg` ↔ `oxiarc_tiff` — `TableSet::{parse, emit}`, `Decoder::{load_tables, frame_header, decode_into_strided}`, `decode_abbreviated_into(tables, scan, &DecodeOptions{raw_components: true, ..}, out)`; TIFF never concatenates JPEG buffers (`oxiarc_jpeg::tiff::merge_jpeg_tables` is an interop helper only).
- Cross-crate additions for TIFF: `oxiarc_lzw::decompress_tiff_into(src, dst) -> Result<usize>` (prefix/suffix table, no per-code `Vec`), `oxiarc_zstd::{decompress_into, decompress_with_limit, decompress_multi_frame_with_limit}`, `oxiarc_lzma::xz` (framing moved from oxiarc-archive/src/xz and re-exported there).
- Encoders: PNG IDAT and TIFF deflate strips drive `Deflater::deflate(.., finish)` directly (cross-call LZ77 history verified); never `ZlibStreamEncoder` (it sync-flushes every 128 KiB).

### Wave 1 — foundations (parallel tracks; each track owns only the crates listed)
- [x] W1-A1 (done 2026-09-07; stream.rs/wrapper.rs, 381 deflate tests after A2; Fable gate A3 fix-then-ship → A4 + verifier) `oxiarc-deflate`: `stream.rs` (`InflateStream`) + `wrapper.rs` (`WrappedInflate`) + `BitCache::{refill_bulk, refill_bytes, align_to_byte, take_byte}` + `HuffmanTree::rebuild_from_code_lengths` in oxiarc-core; unit tests R1-R17 incl. byte-at-a-time feeding, 1-byte output buffers, proptest split points, truncation-at-every-offset with bounded call counts, multi-member gzip, all gzip header flags, zlib concatenation with 1-3 byte accumulator residue, cap/ratio enforcement mid-block (812 KB → 123 MiB single-block bomb regenerated by a committed generator), CAB-style reset/set_dictionary cycles.
- [x] W1-A2 (done 2026-09-07; InflateReader/AsyncInflateReader, all decode paths re-based, CAB gate 570 archive tests green) `oxiarc-deflate`: `InflateReader`/`AsyncInflateReader`; re-base `GzipStreamDecoder`, `ZlibStreamDecoder`, `RawInflateReader`, `async_deflate`, `Inflater as Decompressor` (Finish; sticky fault latch fixes the silent-truncation defect) on the new core with every public item and guarantee preserved (report §6 inventory); `decompressed_size()` re-documented as "produced so far"; python zlib/gzip oracle extended; `inflate_differential.rs` extended to all decode paths; bench gate: no group slower than 2 %. Named gate: `cargo nextest run -p oxiarc-archive cab`.
- [x] W1-A3 (done 2026-09-07; 10 findings fixed incl. a HIGH short-tail silent-truncation regression in the adapter; verifier caught and fixed one more) Serial gatekeeper review of A1+A2 (one pass), findings fixed.
- [x] W1-B (done 2026-09-07; ZstdStream 0.92x one-shot on entropy data, 2.1x on raw blocks; verifier fixed 7 more defects) `oxiarc-zstd`: incremental XXH64, `ZstdStream` push decoder with `with_max_output`/`with_max_window`/`with_multi_frame`/`with_dictionary`, real sliding-window ring (not "output Vec is the window"), `decompress_into`, `decompress_with_limit`, `decompress_multi_frame_with_limit`, fix stale literals table surviving `ZstdDecoder::reset` (frame.rs:440), re-base `ZstdStreamDecoder<R>` (truly incremental, cap honoured pre-decode), async adapter, tests (byte-at-a-time, random splits, truncation no-hang, cap, multi-frame, zstd-oracle).
- [x] W1-C (done 2026-09-07; BrotliStream; 5-7x faster on repetitive content, literal-dense/stored shapes below target → wave 3 BROTLI3) `oxiarc-brotli`: `BrotliStream` push decoder (meta-block headers parsed atomically with bit-cursor rollback, command loop symbol-resumable, real window ring, lazy window allocation, `with_max_window`), re-base `BrotliDecompressor<R>` and async adapter, `MetaBlockShape` differential vs `decompress_reporting_shapes`, tests + brotli-oracle; throughput ≥ 85 % of one-shot.
- [x] W1-D (done 2026-09-07; decompress_tiff_into 9.4-17.7x vs old path; oxiarc_lzma::xz incl. all 8 BCJ filters (RISC-V oracle-verified); verifier fixed 13 defects) `oxiarc-lzw` + `oxiarc-lzma`: `decompress_tiff_into` with prefix/suffix table decoder (≥ 3× the current per-code-`Vec` path; old-style `early_change=false` fallback), `compress_tiff` review for `LzwStreamMode`; move `.xz` container framing into `oxiarc_lzma::xz` (re-exported unchanged from oxiarc-archive; `xz` CLI oracle still green).
- [x] W1-E (done 2026-09-07; 6 defects incl. two hangs in Lzma2StreamDecoder; docs corrected; Known Issue 9 fixed) standalone defects + docs (oxiarc-core, oxiarc-lzma, oxiarc-lzhuf, READMEs of lz4/bzip2/snappy/lzw): `Lzma2StreamDecoder` swallows `WouldBlock` (:409), `StreamingLzhDecoder.carry` never trimmed (:332), `decompress_all` no-progress guard, `Decompressor`/`AsyncDecompressorWrapper` whole-input contract prose, overclaiming "streaming" docs corrected (audit §12), root TODO Known Issue #9 corrected (fuzz corpora are git-ignored, never committed).
- [x] W1-F1 (done 2026-09-07; byte-parity vs djpeg -dct int; 271 tests) `oxiarc-jpeg` decoder: markers, Huffman (9-bit fast lookup), islow-exact integer IDCT, fancy upsampling (libjpeg-exact), fixed-point YCbCr/YCCK/CMYK colour, baseline + extended 8/12-bit, progressive (Annex G incl. AC refinement/EOB runs), lossless SOF3 2..16-bit, restart markers, DNL, 1-4 components, all sampling factors, APPn/COM passthrough (JFIF/EXIF/Adobe/ICC), `TableSet`/`load_tables`/`decode_abbreviated_into`/`frame_header`/`decode_into_strided`, limits, no-panic corpus.
- [x] W1-F2 (done 2026-09-07; byte-identical to cjpeg incl. progressive/optimized/12-bit) `oxiarc-jpeg` encoder: baseline + extended 12-bit (SOF1, dynamic DQT Pq), standard + optimized Huffman (libjpeg tie-breaking), quality-scaled quant tables (byte-identical to libjpeg), box downsampling 4:4:4/4:2:2/4:2:0, edge replication, restart intervals, JFIF/Adobe headers, gray/YCbCr/RGB/CMYK, progressive encoding (libjpeg default scan script), `write_tables_only`/`encode_scan_only` (TIFF JPEGTables mode), lossless encoding.
- [x] W1-F3 (done 2026-09-07; arithmetic SOF9/10 byte-identical to libjpeg both directions, OJPEG, rayon, 492 tests) `oxiarc-jpeg` arithmetic coding (SOF9/10/11, DAC), OJPEG (Compression 6) reconstruction helpers, jpeg-oracle (byte-parity vs `djpeg`/`cjpeg -dct int` when present, Pillow tolerance cross-check otherwise), fuzz seeds, benches (targets ≥ 0.55×/0.50× of djpeg/cjpeg at this wave).
- [x] W1-G0 (done 2026-09-07; 264 tests, libtiff/Pillow/tifffile oracles; verifier fixed 7 defects) `oxiarc-tiff` core: byte-order layer, classic + BigTIFF headers, IFD parsing (all 18 types incl. LONG8/SLONG8/IFD8, inline/offset values, count overflow guards, lazy loading, IFD loop detection, SubIFD tree), tag table (baseline/extension/GeoTIFF/EXIF/GPS/XMP/ICC/IPTC/Photoshop/DNG numbers), `ImageInfo`, strip/tile geometry, sample unpacking (1/2/4/8/12/16/24/32/64-bit, FillOrder 2), predictors 1/2/3 (file-order arithmetic; planar stride rule), None + PackBits codecs, photometric conversions (MinIsWhite, palette, YCbCr matrix, CMYK passthrough), reader API (`Read+Seek`, multi-image navigation, limits), writer core (strips/tiles, None/PackBits, predictor, planar, classic/BigTIFF auto, multi-page, arbitrary tags incl. GeoTIFF passthrough, offsets patched), 40-row edge-case register as tests.
- [x] W1-H0 (done 2026-09-07; 111 tests after verification) `oxiarc-http` headers layer: `ContentCoding` (least-to-most preferred `Ord`), `parse_content_encoding`, `QValue` (u16 thousandths), `AcceptEncoding` builder (`to_header_value() -> Option<String>`), RFC 9110 `negotiate` with `*`/`q=0` table, `DecodeLimits` (max_output load-bearing, max_ratio defense-in-depth), error type, server-side `encode_body`; RFC verbatim examples as tests.

### Wave 2 — integration (after the Wave 1 dependencies land)
- [x] W2-PNG1 (done 2026-09-07; 236 tests, Pillow oracle both directions) `oxiarc-png` decoder: chunk reader (CRC via `oxiarc_core::Crc32`), IHDR validity table, all colour types × depths, unfilter kernels specialised per bpp, Adam7 (sub-byte correctness incl. 1×1, width<5, height==1 empty passes), `expected_raw` computed a priori (exact single allocation), incremental IDAT decode via `WrappedInflate(Zlib)` (`verify_checksum(false)` default, strict mode on), pull `Decoder<R: Read>`/`Reader` (`read_info`, `next_row`, `next_frame`, `Transformations::{EXPAND, STRIP_16, ALPHA…}` with `png` 0.18's exact output colour-type table), push `StreamingDecoder`, lenient defaults matching `png` 0.18 (ancillary CRC skip, palette OOR = opaque black, tRNS truncation) + strict mode, CgBI, limits.
- [x] W2-PNG2 (done 2026-09-07; 316 tests; encoder speed cliff fixed; remaining ratio/throughput gap is oxiarc-deflate encoder → wave 3 DEFENC) `oxiarc-png` encoder + ancillary + APNG + compat: filter strategies (None/Sub/Up/Avg/Paeth/adaptive MSAD/entropy), interlaced encoding, `Deflater` driven directly, `Encoder`/`Writer`/`StreamWriter`, all ancillary chunks (PLTE, tRNS, gAMA, cHRM, sRGB, iCCP, cICP, mDCv, cLLi, sBIT, bKGD, hIST, pHYs, sPLT, tIME, tEXt/zTXt/iTXt keyword rules, eXIf, oFFs/sCAL/pCAL, sTER, unknown-chunk retention), APNG (acTL/fcTL/fdAT sequence numbers, dispose/blend compositor to RGBA8/16), `png`-0.18-shaped compat at crate root + `v017` module, `parallel` feature (per-band filtering, DEFLATE serial), png-oracle (Pillow bidirectional, PngSuite-equivalent generated corpus + hand-built chunk writer), proptest, fuzz seeds, benches (≥ 0.9× decode / ≥ 0.85× encode vs `png` measured in a scratch project).
- [x] W2-TIFF1 (done 2026-09-07; every codec decode+encode, 388 tests; uncompressed-mode CCITT + JPEG encoder swap → wave 3 TIFFPOLISH) `oxiarc-tiff` codecs: LZW (`decompress_tiff_into`, old-style fallback), Deflate 8/32946 (`WrappedInflate(Zlib)` per strip with `TrailingPolicy`), ZSTD 50000, LZMA 34925 (`oxiarc_lzma::xz`), JPEG 7 (`TableSet` + `decode_abbreviated_into`, photometric × APP14 × subsampling table, RowsPerStrip % 8·Vmax on write), OJPEG 6 read (flavour (a) via libtiff-compatible reconstruction; (b)/(c) constructive), CCITT RLE/G3-1D/G3-2D/G4 decode + G4/G3 encode (T4/T6Options, FillOrder, uncompressed mode), explicit `Unsupported`/`FeatureNotCompiled` for WebP/JXL/LERC + `CodecRegistry` plugin trait.
- [x] W2-TIFF2 (done 2026-09-07; compat/rayon/mmap, CodecRegistry, benches) `oxiarc-tiff` completion: writer for every codec above, `compat` feature (`tiff`-0.11-shaped `Decoder`/`DecodingResult` with exactly 11 variants/`TiffError` 6 variants/`TiffEncoder`/`ImageEncoder`; `half` only here), `rayon` (parallel strips/tiles) and `mmap` features, tiff-oracle (libtiff `tiffcp -c none/lzw/zip/lzma/zstd/jpeg/packbits/g3/g3:2d/g3:2d:fill/g4` fixtures + `tiffinfo`, tifffile/Pillow), proptest, fuzz seeds, benches (LZW strip decode ≥ 3× old path; whole-image decode ≤ 1.25× `tiffcp`).
- [x] W2-HTTP `oxiarc-http` decoders (completed 2026-09-07): private `CodingDecoder` over `WrappedInflate` (gzip multi-member, `x-gzip`, deflate `Auto` sniff at offset 0), `BrotliStream`, `ZstdStream` (+ `dcz` dictionary), identity, `compress` token recognition; chained codings applied in reverse order; push `Decoder::feed_into`; `DecodedBody<R: Read + BufRead>`; `AsyncDecodedBody` (async-io); `finish()` error = failed response (documented); recipes as `examples/` for ureq 3 (default-features=false + manual Accept-Encoding), reqwest (`bytes_stream`), oxihttp; tests (RFC examples, per-coding round-trips from oxiarc encoders + python fixtures, byte-at-a-time, chunk invariance, limits, negotiation table), 7 fuzz targets, benches (bounded RSS, plus a throughput gate met at 0.96-0.98× of `oxiarc_deflate::gzip_decompress` — the literal “≥ 0.95× raw `inflate()`” figure is unreachable for *any* gzip decoder, since `gzip_decompress` is itself only 0.84× raw `inflate()`; see `oxiarc-http/TODO.md`).
- [x] W2-IMG (done 2026-09-08; 145 tests + 13 doctests after verification; 7 real defects found and fixed by the verify pass — AVIF magic-byte sniffing was dead code, `to_luma8`/`to_luma16` used BT.601 instead of `image`'s own sRGB/Rec.709 weights, `DynamicImage` conversions were 3-6x slower than necessary, a premultiplied-alpha float TIFF clamped HDR highlights, `read_image` neither panicked nor errored on a wrong-length buffer as documented, `PngEncoder::Level(n>9)` was unclamped, and a 9-15-bit JPEG decoded up to 16x too dark) `oxiarc-image` facade: magic sniffing (PNG/JPEG/TIFF + the other 12 `ImageFormat` variants recognised-but-`Unsupported`), `ImageFormat`, `open`/`load_from_memory`/`ImageReader`, `DynamicImage` (10 variants incl. `Rgb32F`/`Rgba32F`), `ImageBuffer<P>`/`Pixel`/`Primitive`, `to_rgba8`/`to_rgb8`/etc., `save`/`save_with_format`/`write_to`, `DynamicImage::from_decoder`, `image`-shaped `codecs::{png,jpeg,tiff}::{Encoder,Decoder}` names, migration guide. `#![forbid(unsafe_code)]`, no `half`/`image`/banned-crate dependency (`cargo tree` verified).

### Wave 3 — workspace integration and verification
- [x] W3-INFLATEPERF (done 2026-09-08) `oxiarc-deflate` inflate throughput: crate-private packed `DecodeTable` (symbol kind + code length + extra-bit count + payload in one `u32`, built by zlib's one-pass `inflate_table` algorithm, fixed tables built once per process, allocation-free rebuild), fast loop rewritten (one bulk refill and one availability check per iteration, one table lookup per symbol, up to three literals per 32-bit peek, accumulator and both cursors in locals, `#[inline(never)]` so it does not spill against the careful path), word-sized match copies (`memmove` only above 64 bytes), and the growable entry points decode into the tail of the buffer they fill (window written once). Measured by running the pre-track binary and the current one alternately on the same corpora, all figures medians (median across the interleaved rounds of each round's own median): on the four decode-bound shapes `inflate_into` is **1.04x-3.3x faster** and the worst-arm-vs-python ratio improves in all 36 measured rows (the two `memcpy`-bound shapes, stored blocks and long-match JSON, scatter within noise). Reference: CPython's Apple-tuned libz 1.2.12. Against the ">= 0.60x on every shape" gate: **met on every shape and level at 1 MiB** (0.65x-2.92x), met on four of six shapes at 64 KiB (`rgb8-image-rows` 0.46x at L6 and `many-blocks-text` 0.59x at L9 fall short on medians; on the load-robust best-of-round figure every 64 KiB shape clears 0.60x), and met on three of six at 16 MiB (PNG-filtered 0.51-0.60x, RGB8 0.58-0.67x, text 0.58-0.77x; five of six clear 0.61x on best-of-round). Re-measured 2026-09-08 (REMEASURE, three more full 54-row passes at load 60-71): PNG-filtered at 16 MiB lands at 0.47x-0.67x on medians and 0.48x-0.57x on best-of-round — same band, same verdict. The shortfalls are literal-heavy shapes and 20-40 microsecond measurements on a machine at load ~45, not regressions. Public API, output bytes and every documented guarantee unchanged (439 deflate tests + archive/png/tiff/http suites green); `oxiarc-http`'s allocation peak fell from ~216 KiB to 178 KiB. New `examples/inflate_ab.rs` A/B harness and `inflate_shapes` bench group.
- [x] DEFENC (done 2026-09-08; DEFENC + DEFENC-verify) `oxiarc-deflate` encoder rewritten as a faithful zlib `deflate.c`/`trees.c` port: byte-identical to CPython `zlib.compress` at every level 1-9 on 90/90 corpus×level pairs (was up to 179% larger); call-size no longer changes output or cost (was up to 13x, then up to 25x on the optimal parser before a second-pass fix); `Deflater::with_strategy(Strategy)` (all five byte-identical to CPython, one shipped-with-zero-coverage regression found and fixed: `Strategy::Fixed` emitted stored where zlib emits fixed); `with_optimal_parsing` never larger than the ladder (was up to 2% larger). A steady-state decoder-side allocation regression this surfaced (`HuffmanTree` rebuild not actually reusing its buffers, 79 allocations/64 calls) was fixed in the same pass (79 → 0). 432-454 tests across the two sessions; `cargo +1.85.0 build --all-features` real MSRV build clean.
- [x] BROTLI3 (done 2026-09-08; BROTLI3 + BROTLI3-verify, 347 tests) `oxiarc-brotli`: shared (custom LZ77) dictionaries both directions (`compress_with_dictionary`/`decompress_with_dictionary`/`with_dictionary` on `BrotliStream`/`BrotliDecompressor`/async) — established by probing `brotli 1.1.0 -D` rather than assumed; `Content-Encoding: dcb` (RFC 9842) framing (`oxiarc_brotli::dcb`, SHA-256 dictionary binding); push-decoder performance (0.14x→0.36x of one-shot on stored meta-blocks, its measured two-copy floor; 0.64x→1.23x literal-dense; 6.75x→9.7x repetitive; copy-dense at large windows 0.80x, just under the 0.85x target; re-measured 2026-09-08 at load 60-76 as 0.71x-0.76x min / 0.70x-0.79x paired, same verdict). One real defect found by the verifier and fixed: a shared-dictionary copy running past the dictionary's end could decode to silently wrong bytes depending on the caller's buffer size (the reference rejects it; both decoders now do too). Final oracle counts: **72/72** reference `-D` streams decode byte-identical, **96/96** oxiarc streams accepted by `brotli -d -D` (superseding the mid-track 45/48 figures, which undercounted because the oracle self-skip was checked wrong once).
- [x] LZW3 + LZWPERF (done 2026-09-08; LZW3/LZW3-verify + LZWPERF/LZWPERF-verify, 233 tests) `oxiarc-lzw`: the legacy UNIX `compress(1)`/`.Z` container (`oxiarc_lzw::z`, byte-identical to `compress -b N -c`, no EOI code by design); explicit `LzwBitOrder` + `TIFF_COMPAT_LSB`; code widths to 16 bits (`max_code()` was capped at 15, dictionary counter overflowed at 16); **three real defects found by LZW3-verify and fixed**: `z::ZReader` buffered the whole compressed stream instead of a bounded window (20.5 MB → 230 KB live heap on a 5.2 MB stream), a crafted 303-byte stream could panic the decoder (a code naming a slot a full table can never create), `z::ZWriter` could report `Ok` for a stream an inner-writer failure had silently corrupted. Then LZWPERF rebuilt the TIFF/GIF decoder to libtiff's own `LZWDecode` algorithm shape (packed `u64` code-table entries, a stateless bit window, KwKwK-before-emit): **1.25x-2.6x faster, 0.73-0.80x of libtiff 4.7.1's own decode throughput** (gate: ≥ 0.7x — met with margin, independently re-measured by LZWPERF-verify at 1.367x worst-case = 0.731x); GIF 5.8x-24x faster and, for the first time, validated against a reference (Pillow) in both directions. **Breaking**: `LzwConfig::GIF` is now genuinely LSB-first (was silently identical to `TIFF_OLD_STYLE`'s MSB value; no published release affected).
- [x] LZMA3 (done 2026-09-08; LZMA3/LZMA3-verify, 275 tests) `oxiarc-lzma`: `XzWriter` multi-block output (`with_block_size`, default 64 MiB, per-block `CheckType`); reusable `xz::XzDecoder` (keeps its LZMA2 dictionary/probability-model/coder-state allocation across same-dictionary-size streams, for TIFF's thousands-of-strips case; a `block_opener_permits_decoder_reuse` guard keeps the "every independent block resets its dictionary" rule enforced under reuse); SHA-256 moved to a new, independently-verified `oxiarc_core::sha256` module shared with `oxiarc-brotli`'s `dcb` and `oxiarc-http`'s `dcz`. The verify pass found the `.xz` block-check (CRC-32/64/SHA-256) comparison itself had zero direct test coverage (mutation-proven: `if false && computed != expected` passed the whole suite) and the reuse cache's dictionary-size key was untested despite a comment claiming otherwise — both closed with new tests, no production code changed.
- [x] JPEGSCALE (done 2026-09-08; JPEGSCALE/JPEGSCALE-verify, 591 tests) `oxiarc-jpeg`: reduced-/enlarged-scale decoding (`Scale` 1-16/8, libjpeg's `-scale M/8`; `M` in `{1,2,4,8}` byte-identical to `djpeg`, the other twelve within a measured tolerance); `compat::zune` and `compat::jpeg_decoder` migration facades. **The verifier found a critical bug the implementer's own gates could not reach**: the `rayon` parallel band merge placed every band after the first at the *unscaled* block pitch, corrupting every non-`Scale::FULL` decode of a restart-marker stream (invisible at full scale, and no scaled-decode fixture had ≥ 256 MCUs + restarts) — fixed, plus two compat-facade accessor/output mismatches (`compat::jpeg_decoder::info()` claiming `Rgb24` for a 12-bit colour frame `decode()` returned twice as many bytes for; `compat::zune`'s `output_buffer_size()` under-reporting by 25% for CMYK-through-`YCbCr`).
- [x] HTTPWIRE (done 2026-09-08; HTTPWIRE/HTTPWIRE-verify, 306 tests) `oxiarc-http`: `Content-Encoding: compress`/`x-compress` and `dcb` fully wired (dcb conditional on BROTLI3's shared-dictionary support landing, which it did the same day); `dcz` gained its RFC 9842 dictionary-binding preamble (previously any ordinary dictionary-referencing zstd frame decoded under `dcz`, with no check it named the *right* dictionary — closed, breaking within this same unreleased cycle only). **The verifier found the `compress` pull bridge could materialise ~128 MB for one `read()` call** on a body with a high expansion ratio (`oxiarc_lzw::z::ZReader` decodes a whole 16 KiB pull to completion before serving a byte) — fixed with a self-tuning pull-size meter (893,668 B peak after, ~8% *faster* from the smaller working set fitting cache) — and that every `dcb` error was mis-reported as `br`.
- [x] FUZZCLI (done 2026-09-08; FUZZCLI/FUZZCLI-verify) workspace integration: `oxiarc-http/tests/cross_crate_inflate.rs` (3 tests: PNG IDAT split across chunks, one TIFF deflate strip, an HTTP gzip body split across random reads, all through `WrappedInflate`; strengthened by the verifier to check ground truth — an independent RFC 2083 unfilter for PNG, the source rows for TIFF — not just two split granularities against each other); 15 fuzz targets (see below); `oxiarc-cli` PNG/JPEG/TIFF detect/info (`image_probe.rs`, hand-rolled magic+container walk since `oxiarc_tiff::is_tiff`/`oxiarc_jpeg::is_jpeg` don't exist); man pages (11×2 dirs) + completions (4 shells×2 dirs) regenerated. **The verifier found `image_format_hint` read the entire input (`read_to_end`) just to check 8 magic bytes** — 303 MiB peak RSS on an unrecognised 300 MB file, now 3.06 MiB (16-byte bounded prefix read) — plus an `oxiarc add` redundant full-archive pass on every success, an integer-overflow panic/silent-tiny-limit in `--memory-limit`'s SI-suffix parser (pre-existing since v0.2.8; see Security in the root CHANGELOG), and a missing IFD-chain loop guard in `fuzz_tiff_read`. Along the way, differential fuzzing `ZstdStream` against the legacy `decompress_multi_frame` found 4 real gaps in the legacy decode path — see ZSTD4 below.
- [x] ZSTD4 + ZSTD4-verify + ZSTDPERF + ZSTDPERF-verify + FUZZTIGHTEN (done 2026-09-08, 341 tests) `oxiarc-zstd`: legacy one-shot decoder hardening (`Dictionary_ID` validation, per-block decompressed-size ceiling, leading-garbage/skippable-frame-prefix handling now shared with `ZstdStream` so the two paths cannot drift — found by differential fuzzing, closed in shared code) plus a decode-throughput rewrite around the reference decoder's data layout (bulk bit-container refills, 4-stream lockstep Huffman, pattern-doubling match copies: 2-8x on per-byte-work-bound shapes, 0.5x-4x of `zstd -b -d` on 11/12 measured shapes, `text50m` level 3 the one miss at 0.43-0.55x across three independent measurement runs). Two more real defects found by the perf verifier: a reused `ZstdDecoder` could carry a failed frame's partial output into the next `decode_frame` call (silent corruption or a bogus checksum error), and offset code 31 (the RFC's maximum) computed its value in `usize`, overflowing on 32-bit targets (fixed in `u64`; see Security in the root CHANGELOG). `fuzz_zstd_stream`'s contract was then tightened back to a real two-directional differential (983,701 executions, zero divergences) now that the gaps it originally had to work around are closed.
- [x] TIFFPOLISH (done 2026-09-08; TIFFPOLISH/TIFFPOLISH-verify, 535 tests) `oxiarc-tiff` completeness/perf: decoder-pool finish (Deflate 0.98x→2.24x under `rayon`, Group 4 → 3.04x); JPEG encoder swap onto `oxiarc-jpeg` (659→333 lines, tag 347 byte-identical to libtiff's); CCITT Group 3/4 uncompressed mode (T.4 §4.2.1.3.2 — decode unconditional, encode opt-in, exact per-row cost comparison; libtiff 4.7.1 *parses* but cannot *decode* these files); a genuine `<= 1.25x tiffcp` perf gate met for uncompressed/PackBits/LZMA and missed for Deflate/LZW/JPEG/ZSTD (attributed: the gap is inside `oxiarc-lzw`/`oxiarc-zstd`, not this crate — ZSTD is the outlier at ~60x, the first place a follow-up should look); `--no-default-features` test failures inherited from Wave 1/2 fixed (feature-gated fixture generation). JPEG2C (same day) then closed a two-channel-chunky-JPEG gap TIFFPOLISH's own swap had opened, by adding `oxiarc-jpeg`'s `ColorSpace::Unknown(2)` (`JCS_UNKNOWN`), verified via `djpeg -verbose`'s header trace, per-channel `cjpeg`/`djpeg` decomposition, and — the strongest check — `tiffcp -c none` making libtiff's own embedded libjpeg decode the interleaved two-component scan byte-identically to this crate's decoder.
- [x] Cross-crate `tests/cross_crate_inflate.rs` (done — see FUZZCLI above; PNG IDAT split, TIFF strip, HTTP body split, all through `WrappedInflate`, strengthened with independent ground truth by FUZZCLI-verify) — no separate shared counting-allocator harness module was built; `oxiarc-deflate`/`oxiarc-http`/`oxiarc-lzw` each ship their own house-pattern counting-allocator test file instead (`allocation_steady_state.rs`, `allocations.rs`, `decode_memory.rs`/`z_memory.rs`), which is the pattern this workspace already used pre-Phase-8 (`oxiarc-brotli`/`oxiarc-snappy`'s `tests/memory_limit.rs`).
- [x] fuzz targets (done — 15 shipped, a superset of the 12 named here): `fuzz_inflate_stream`, `fuzz_wrapped_inflate`, `fuzz_zstd_stream`, `fuzz_brotli_stream`, `fuzz_http_decode`, `fuzz_http_headers`, `fuzz_png_decode`, `fuzz_png_limits`, `fuzz_jpeg_decode`, `fuzz_tiff_read`, `fuzz_tiff_ifd`, `fuzz_image_open`, plus `fuzz_inflate_reader`, `fuzz_png_streaming`, `fuzz_jpeg_tables`. `fuzz_image_open` has no dedicated seed corpus; `oxiarc-image/tests/adversarial.rs` (truncation/corruption at every offset across all three codecs) is the always-on substitute pending one. `fuzz_zstd_stream` restored to a real two-directional differential once ZSTD4 closed the gaps it originally had to carve around (see ZSTD4 above).
- [x] CLI: `oxiarc detect`/`info` recognise PNG/JPEG/TIFF by magic via a CLI-layer fallback (no `ArchiveFormat` expansion) — done, see FUZZCLI above; man pages + completions regenerated and byte-identity-verified against a fresh build by FUZZCLI-verify.
- [x] `deny.toml`: banned `png`, `tiff`, `jpeg-decoder`, `jpeg-encoder`, `zune-jpeg`, `zune-png`, `zune-core`, `mozjpeg`, `image` with `use-instead` pointers (`weezl` deliberately left unbanned — a legitimate dev-dependency oracle in `oxiarc-lzw`, used by both its pre-existing `tiff-oracle` suite and LZW3-verify's new `tests/bit_order_differential.rs`); `cargo deny check bans` clean (verified 2026-09-08, DOCS track).
- [x] Docs: per-crate README/TODO (house template), root README crate table/feature matrix + new HTTP/PNG/JPEG/TIFF/image sections with compiling quick-start snippets + CLI detect/info section, CHANGELOG `[0.4.2] - Unreleased` (Added/Changed/Security/Fixed, every crate and every behaviour change from every track's handoff), root TODO Test Coverage + Code Statistics re-measured (tokei, nextest, 2026-09-08), `~/work/pub_oxiarc.sh` CRATES order (jpeg, png, tiff, http, image inserted before archive/cli, matching Phase 8 owner decision 9 and independently re-derived from each crate's actual `Cargo.toml` dependencies — `oxiarc-http`'s `dev-dependencies` on `oxiarc-png`/`oxiarc-tiff` for `cross_crate_inflate.rs` are why `http` sorts *after* both, not just after their shared codec deps).
- [x] Gates — `cargo nextest run --workspace --all-features`: **5,155 passed, 0 failed, 0 skipped** (measured 2026-09-08; 5,130 by the DOCS track on a cold `target-DOCS`, re-measured at 5,155 by the FIX track after the FINALGATE gatekeeper's findings — and two decoder defects one of those fixes surfaced — added 25 regression tests).
- [x] Gates — `cargo test --doc --workspace --all-features`: **346 passed, 0 failed** (same run; 343 before the FIX track's three new doctests).
- [x] Gates — `cargo deny check bans`: **`bans ok`** (same run, with the new image-format bans in place).
- [x] Gates — every file < 2000 lines, zero `unwrap()` in `src/`, no let-chains: **verified per-crate by every individual track** (no violation reported or found by any verifier).
- [x] Gates — the literal workspace-wide commands, **run 2026-09-08 by the GATES track** on a quiescent tree (one scratch `CARGO_TARGET_DIR` per command; the repo's own `target/` was never used): `cargo fmt --all -- --check` **clean**; `cargo clippy --workspace --all-features --all-targets -- -D warnings` **0 warnings** (all 18 crates re-linted, not a cache read — every crate's `src/lib.rs` was `touch`ed first); `cargo clippy --workspace --no-default-features -- -D warnings` **0 warnings**; `cargo build --workspace --no-default-features` **exit 0**. Green in the same pass: `RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps --all-features`, `cargo deny check bans` and `check licenses`, `(cd fuzz && cargo check --all-targets)`, `cargo +nightly fuzz build` (35 targets), `cargo build --workspace --examples --all-features`, `cargo bench --workspace --no-run --all-features`, `cargo +1.85.0 check --workspace`. **The gates found two real defects, both fixed in the same pass**: a multi-line let-chain in `oxiarc-zstd/src/compressed_block.rs` that broke the declared MSRV 1.85 (invisible to the single-line let-chain regex every track used — see Known Issue #16), and twelve `-D warnings` rustdoc errors across `oxiarc-lzw`/`oxiarc-lzma`/`oxiarc-jpeg`/`oxiarc-deflate` (Known Issue #13 had predicted three, in one crate). Exact commands, outputs and per-crate counts: the GATES handoff.

### Out of this repo (follow-ups, not tracked here)
- P0 `ring` purge and P1 `deny.toml` standardisation across `~/work` (higher priority than P2/P3 ecosystem-wide, but not OxiArc work).
- Downstream adoption (repo-conventions report §15): keystone `scirs/Cargo.toml` ureq `gzip` feature (clears scirs/optirs/torsh/oxigeo); 17 actionable Tier-B projects; `oxihttp` client/server switch to `oxiarc-http`; sweep for `include_bytes!("*.png")` + `assert_eq!` golden tests before switching encoders.

## Known Issues

1. **[RESOLVED 2026-07-13] oxiarc-zstd: FSE decoder panics on valid zstd frames** (confirmed 2026-07-13 via OxiGDAL 0.1.7 release testing; the `fse.rs` `index out of bounds: the len is 32 but the index is 65526` panic on valid frames that reference `zstd` decoded fine). Fixed in the production-hardening campaign (ZSTD-01..08). The root cause ran deeper than the originally-suspected normalized-count reconstruction: the FSE backward bitstream was read FIFO/LSB-first instead of RFC 8878 LIFO/MSB-first — mirrored in the writer, which is why oxiarc↔oxiarc round-trips passed while 0/64 real frames decoded — compounded by the 4-stream Huffman jump table being read as offsets instead of sizes, missing Huffman weight validation/implied-last-weight deduction, and unguarded `entries[state]` indexing (the reported panic site). All fixed, with probability-sum validation and bounds-checked state indexing added to `FseTable`. Verified: 64/64 OxiGDAL Zarr v3 corpus frames + 101/101 wide reference-frame matrix decode byte-identical (each corpus chunk decodes to the expected 16384 bytes); 85/85 oxiarc-encoded frames accepted by `zstd -d`; 60,000 fuzz cases, 0 panics; permanent regression gate behind the `zstd-oracle` feature plus an always-run embedded corpus. Remaining checklist (external to this repo):
   - [x] repair the FSE bitstream/table construction (root cause fixed; counts validated; state indexing bounds-checked)
   - [x] add the failing frames as regression tests (embedded corpus + `zstd-oracle` differential gate)
   - [x] **STALE, superseded — not tracked further in this repo.** This line originally meant "release the still-unpublished 0.3.6 that carries the FSE fix." The branch has since moved through 0.3.6 → 0.4.0 → 0.4.1 (see the version history above), i.e. oxiarc-zstd has been released multiple times since the FSE fix landed; the fix has been out via crates.io for several release cycles. Left unchecked before only because nobody had re-read this line since 0.3.6.
   - [ ] **Out of this repo's scope (downstream/external).** OxiGDAL is a separate project in this workspace; bumping its oxiarc-zstd dependency and removing its `#[ignore]` on `test_compute_zarr_stats_demo_fixture` is OxiGDAL's own backlog item, not oxiarc's. Tracked here only for historical context — do not resurface this as oxiarc remaining work in future audits.
2. **[RESOLVED 2026-08-04] oxiarc-zstd encoder: custom block-optimal FSE sequence tables.** Sequence sections used to be restricted to the RFC 8878 predefined/RLE FSE tables — RFC-valid, but a ratio limitation on inputs whose symbol distribution differs from the RFC's fixed one. `FSE_Compressed_Mode` is now emitted: `oxiarc-zstd/src/fse_encoder.rs` carries reference-faithful ports of `FSE_normalizeCount` (including the `FSE_normalizeM2` fallback) and `FSE_writeNCount`, plus a `ZSTD_fseBitCost`-style model, and `compressed_block.rs` picks per category (literal length / offset / match length) whichever of RLE, predefined and custom costs fewest bits *including* the table description. Custom tables also lift the predefined offset table's 0..=28 code ceiling to the format's full 0..=31. Measured on a 1.5 MB structured-record corpus: 173,521 → 109,359 bytes at level 1 (-37%). Gated by the `zstd-oracle` differential suite, which now walks the produced frame, asserts a block really used mode 2, and requires reference `zstd -d` to reproduce the input byte for byte. Huffman literal compression remains wired in as before.
3. **[RESOLVED 2026-08-04] oxiarc-brotli encoder: block splitting and context modeling.** The encoder used to hardcode `NBLTYPESL = NBLTYPESI = NBLTYPESD = 1`, so it could never use the block-type switching and context maps the decoder has always supported. Literal block splitting now ships (`oxiarc-brotli/src/block_split.rs`): the literal stream is segmented, segment histograms are clustered by merge cost, adjacent equal labels collapse into runs, and the meta-block declares up to 8 literal block types with a per-type prefix code bound through a move-to-front + zero-run-length-coded context map. The encoder measures the meta-block **both** ways and keeps the smaller, so a split can cost encode time but never compression ratio. Active at quality 10–11 only; quality 0–9 emits byte-identical output to the previously reference-verified path, which is asserted by a test. Measured on a 180 KB two-population input: 145,638 → 125,264 bytes (-14%). Gated by the `brotli-oracle` suite, which walks the stream header, asserts `NBLTYPESL > 1`, and requires reference `brotli -d` to reproduce the input. **[COMPLETED 2026-08-04]** The remainder now ships too: insert-and-copy (`NBLTYPESI`) and distance (`NBLTYPESD`) block splitting reuse the same clustering machinery over their own alphabets, and per-context histogram assignment is live for both context-mapped categories (`NTREESL`/`NTREESD` may exceed their block-type counts, i.e. two contexts of one block type get different prefix codes — the `LSB6` mode is now exploited, not merely declared). The three categories are searched by *coordinate ascent*: each is tried against the current best plan for the other two, and a change is kept only when the fully-written meta-block actually gets smaller, so no category has to buy another's header cost to be usable. Measured on a two-regime 162 KB corpus: 62,719 → 60,659 bytes; on 240 KB of context-dependent text: 146,769 → 142,703. Gated by five new `brotli-oracle` tests that decode the produced stream through this crate's own decoder to read back the real `NBLTYPESI`/`NBLTYPESD`/`NTREESL`/`NTREESD` (a new `decompress_reporting_shapes` API), assert each feature actually reached the wire, and then require reference `brotli -d` to reproduce the input — so none of them can pass vacuously. Quality 0-9 remains byte-frozen for every category, asserted directly.
4. **CAB**: Quantum and LZX compression are not implemented — entries using them are listed, and extraction returns a clean `unsupported_method` error (never a silent raw-copy fallback). None/MSZIP folders are fully supported (window carried across CFDATA blocks, CFDATA checksums validated).
5. **TAR**: GNU old-format ('S'), PAX 0.1 (`GNU.sparse.*` attributes), and PAX 1.0 (`GNU.sparse.major=1`/`GNU.sparse.minor=0`, in-data-stream decimal-ASCII preamble map) sparse entries are all fully supported for *reading* in both the seekable and streaming readers. Writer-side sparse emission (for any of the three variants) remains out of scope — `TarWriter` writes sparse-source files as regular dense entries; see `oxiarc-archive/TODO.md`'s "TAR Improvements" note.
6. BWT has O(n² log n) worst-case for highly repetitive data (mitigated by 900KB block size limit).
7. Previously-tracked issues that remain RESOLVED: bzip2 parallel compression bit-alignment (pre-0.3.4); LZMA/LZMA2 liblzma interop (v0.3.4: distance-slot probability layout, state mapping, LZMA2 control-byte reset field, embedded LZMA2 EOS marker).
8. **`cargo bench` / `--all-targets` requires a C compiler (dev-only, does not affect the shipped libraries).** `criterion` 0.8+ (the workspace's benchmark harness, used only as a `dev-dependencies` entry) has a mandatory `[target."cfg(any(windows, unix))".dependencies.alloca]` dependency, and `alloca` itself depends on `cc`. Every member crate's *default* features remain 100% Pure Rust with zero C/C++/Fortran dependencies — verified by walking the full `Cargo.lock` crate list, which contains no other build-time C dependency — but `cargo build --workspace --all-targets` and `cargo bench` specifically need a working C toolchain on `PATH`, which breaks the hermetic-build property on minimal CI images for that surface only. Deliberately not downgrading `criterion` to a pre-0.8 version without this dependency: CLAUDE.md's "Latest Crates" policy requires using the latest available version, and criterion 0.5.x/0.7.x predate a number of upstream fixes. Documented in CONTRIBUTING.md's "Getting Started" and "Benchmarks" sections; revisit if/when `alloca` becomes optional upstream.
   **Addendum, verified 2026-09-08 (a second, independent reason `--all-targets` breaks on a strict-MSRV toolchain, distinct from the C-compiler one above):** `criterion` 0.8.2 itself declares `rust-version = "1.86"` in its own manifest — one version *above* this workspace's declared MSRV of 1.85. Confirmed empirically, not just read off the manifest: `CARGO_TARGET_DIR=<scratch> cargo +1.85.0 check -p oxiarc-deflate --benches` fails immediately with `error: rustc 1.85.0 is not supported by the following package: criterion@0.8.2 requires rustc 1.86`, before any workspace source file is even reached. So on a genuine rustc-1.85.0 toolchain (as opposed to the locally-installed rustc 1.95 every per-crate `cargo +1.85.0 build --all-features` check in this cycle actually ran under — none of those commands touch bench targets, since `--all-features` does not imply `--all-targets`/`--benches`), **no crate's bench target builds at all**, regardless of whether a C compiler is present. Same non-downgrade reasoning as above applies (`criterion` 0.7.x predates fixes this workspace wants); the practical mitigation is unchanged from what this issue already says — `cargo build`/`cargo test`/`cargo nextest run` without `--benches`/`--all-targets` are unaffected on 1.85.0, only bench targets are.
9. **[RESOLVED-AS-STALE 2026-09-07] Fuzz corpora were believed to be ~50 MB of committed seed inputs paid by every clone — re-verified, and they never were.** The previous entry claimed `fuzz/corpus/` "carries roughly 50 MB of committed seed inputs across 18 fuzz targets, so every clone of this repository pays that cost," and that shrinking it required `git rm --cached` or a full history rewrite. Re-checked 2026-09-07: `fuzz/.gitignore` excludes `/corpus` and `/artifacts` (alongside `/target`), `git ls-files fuzz/corpus` returns **zero** tracked files, and `git status --porcelain fuzz/` is clean even on a working tree whose `fuzz/corpus/` already holds ~50 MB on disk (regenerated locally by running the fuzz targets, e.g. via `cargo fuzz run`) — that data lives only in the local working copy, was never committed, and a fresh `git clone` of this repository pays none of it. There is therefore nothing tracked to shrink: no `git rm --cached`, no history rewrite, and no corpus-relocation decision is needed. `fuzz/artifacts/` (crash reproducers) is likewise git-ignored and, independently, empty in this working tree across every fuzz target.
10. **oxiarc-tiff CCITT Group 3/4 uncompressed mode parses but is not decodable by libtiff 4.7.1** (new in 0.4.2, `oxiarc-tiff`). `tiffinfo` correctly reports "Group 4 Options: uncompressed data (2 = 0x2)" and the geometry, but `tiffcp` refuses with "Fax4Decode: Uncompressed data (not supported)" — confirmed as libtiff's own limitation, not a bug in this crate's framing (a permanent regression test, `libtiff_parses_but_cannot_decode_our_uncompressed_mode_files`, fails loudly if a future libtiff learns the mode). This is exactly why the encoder never writes the mode unless asked (`ImageSpec::with_ccitt_uncompressed`); decode of a file that carries it is unconditional and does not depend on the option bit being set. Not tracked as a defect — recorded so a future auditor does not rediscover it as one.
11. **A handful of performance targets set for the 0.4.2 program are measured short, and are recorded here rather than silently dropped.** All are genuine — re-measured independently by at least one verifier in every case — and none blocks correctness; every one is attributed to its likely cause. **Every figure below was re-measured on 2026-09-08 by the REMEASURE track** (interleaved, medians, load average quoted per matrix, three independent runs wherever a row straddles its gate), because the originals were all taken while four to six other agents were compiling on this 8-core machine. Three of the four bullets were **confirmed**; the `oxiarc-tiff` one moved substantially and is rewritten below. Full tables and exact commands: `handoff/REMEASURE.md`.
    - `oxiarc-brotli`'s push decoder on copy-dense streams at large windows (half a million ~5-byte copies at a mean distance of ~116 KB) measures **0.71x-0.76x** of the one-shot decoder against a ≥ 0.85x target (re-measured 2026-09-08 at load 60-76, three runs of best-of-12 at the `64k/64k` gate shape: 0.76x, 0.71x, 0.72x on minima and 0.79x, 0.70x, 0.74x paired; BROTLI3-verify had recorded **0.80x** from a spread of 0.77x/0.80x/0.88x, i.e. at the optimistic end of its own scatter, and the implementer's own figure was 0.74x before a `CmdState` narrowing landed). **Same verdict either way — the shape misses, and it is the only one of the four that does**; the other three re-measure at 27.7x, 10.0x, 29.1x, 14.8x and 1.18x, all comfortably met. The stored-meta-block shape is **at its measured physical floor** (a purpose-built "copy floor" model in `examples/decode_profile.rs` reproduces the decoder's own timing to within ~2.5%) and should not be chased further. Next step recorded in `oxiarc-brotli/TODO.md`: narrow `CmdState` from 24 to 16 bytes.
    - `oxiarc-zstd`'s decode-throughput rewrite misses the `>= 0.5x of zstd -b -d` gate on exactly one of twelve measured shapes, a 50 MB text corpus at compression level 3 (0.43x-0.55x across three independent runs on a heavily loaded shared machine); the same corpus at level 19 clears the gate but with no margin (0.50x-0.51x). **Re-confirmed 2026-09-08** — ZSTDPERF-verify had asked for a quieter re-measurement of exactly these two rows, and two further 15-round interleaved runs (load 18→84 and 80→21) reproduce the straddle precisely: level 3 lands at 0.54x and 0.43x, level 19 at 0.53x and 0.50x. The machine never got quiet enough in that window to do better than confirm the published range, so **no `oxiarc-zstd` figure needed rewriting**. Ten of twelve shapes clear at 0.54x-4.0x (up to 8.3x on highly repetitive data). The remaining gap is not one hot spot — profiling attributes it to ring/window bookkeeping and the resumable command machinery, not the Huffman/FSE decode itself — and the structural fix (`decompress_into` writing into the caller's buffer as its own window, the way `libzstd` does, instead of a ring plus a copy-out) is recorded as scoped-but-not-attempted in `oxiarc-zstd/TODO.md`, since it is a third sequence executor and a rewrite risk for two rows inside this machine's measurement noise.
    - `oxiarc-deflate`'s resumable inflate core misses the `>= 0.60x of Apple's tuned libz` gate on one shape at 16 MiB, `png-filtered-rows` (0.51x-0.60x across two measurement passes at different load levels; every other shape/size combination clears, including 51 of 54 rows overall). **Re-measured 2026-09-08** across three more full 54-row passes at load 60-71: the same row lands at **0.47x-0.67x on medians and 0.48x-0.57x on best-of-round** — the same band, still missed, marginally lower at the top because these passes ran at a higher load than the originals' ~45. Not a regression versus the pre-0.4.2 decoder, which measured 0.36x-0.46x on the same row; closing the rest needs a wider decode step (SIMD, or two symbols per dependent load) rather than defect repair.
    - `oxiarc-tiff`'s `<= 1.25x of tiffcp` whole-image decode-throughput gate. **[MOSTLY RESOLVED — re-measured 2026-09-08 by the REMEASURE track; four rows crossed into "met" and one crossed out.]** *As TIFFPOLISH measured it* (medians of nine interleaved rounds at load 10-64, before the LZWPERF / INFLATEPERF / ZSTDPERF codec rewrites landed underneath this crate): met for uncompressed, PackBits and LZMA, missed for Deflate (1.58x), LZW (2.41x), JPEG (2.37x) and, by a wide margin, ZSTD (6.24x — "the outlier, and the first place a follow-up should look"). *Re-measured 2026-09-08* on the same geometry (4096x4096, 16-row strips, fixtures written by `tiffcp -m 0 -r 16 -c <codec>`), medians of **15 interleaved rounds**, **three independent runs** at load average **110 down to 20** on 8 cores, taking the median of the three run medians: **LZW 2.41x → 1.07x (RGB8) and 2.13x → 1.00x (Gray16), Deflate 1.58x → 1.14x (RGB8), ZSTD 5.07x → 0.96x (Gray16) — all four now MET**; ZSTD RGB8 6.24x → **1.65x** (still missed, but 3.8x better and no longer any kind of outlier — that claim is dead), Deflate Gray16 1.39x → 1.33x and JPEG 2.37x → 1.90x still missed; and **LZMA moved the wrong way, 1.15x-1.16x → 1.31x-1.32x, so a gate previously recorded as met is now missed.** Nothing in `oxiarc-lzma` changed in between, and **the reference arm settles it as a fixture difference rather than a regression**: libtiff did not change either, so where `tiffcp`'s own time is close the fixtures are comparable — and it is close for the three rewritten codecs (LZW 215.8→195.3 ms = 0.90x, Deflate 146.7→128.0 ms = 0.87x, ZSTD 76.1→90.7 ms = 1.19x), which is what makes *their* movement trustworthy, while for LZMA it **halved** (2026→952 ms = 0.47x) on nominally identical geometry with an unchanged decoder. Some of that is load — the uncompressed reference row moved by the same factor (69.6→33.5 ms) — but LZMA moved twice as far as any codec whose ratio the conclusions rest on, and this fixture compresses 2.5x/3.1x where the earlier perf sources compressed ~3.7x. Recorded as measured rather than explained away: **read this row as "not comparable", not as "LZMA regressed".** The measurement is trustworthy at that load because the codecs *no* track touched — PackBits (0.20x → 0.16x) and JPEG (2.37x → 1.90x) — reproduce their earlier band, while the movers sit 2.1x-5.3x outside it. Separately, the bilevel (CCITT) rows measure 1.87x-1.90x net of an unlike-work correction (`tiffcp` copies packed bits; this crate expands to one byte per pixel, which is the whole 5.30x uncompressed row), reproducing TIFFPOLISH's "~1.5-2x" — a packed-output decode path would close that gap and is recorded as unattempted, out of every current track's scope. Full tables and commands: `handoff/REMEASURE.md` §1.
12. **`oxiarc-tiff`'s reference-oracle test suite (`tests/tiff_oracle.rs`, `tests/tiff_oracle_codecs.rs`) fails under some *partial* feature-flag combinations that no Definition-of-Done gate in this program exercises** (pre-existing since W2-TIFF2, re-confirmed and left unfixed by TIFFPOLISH-verify, whose own §1.4 fixed the two *other* oracle tests that a "one codec off" build hits — this is the residual). Concretely: `--features "packbits,tiff-oracle"`, `"packbits,ccitt,tiff-oracle"` and `"packbits,jpeg,tiff-oracle,compat"` fail in `libtiff_reads_every_codec_we_write`/`pillow_and_tifffile_read_every_codec_we_write`/`tiffinfo_reads_what_we_write_without_warnings`/`ccitt_recodings_decode_byte_identically`, because those four tests write their fixtures from a hardcoded `Vec<ImageSpec>` rather than a feature-gated list. `default`, `--all-features`, `--no-default-features` and `default + tiff-oracle` — every combination any gate in this program actually runs — are all green. The fix is mechanical (turn each hardcoded `vec![]` into conditional `push`es, per the pattern `tests/corrupt_no_panic.rs` already uses) but touches the crate's own DoD-gated oracle coverage, so it was left recorded rather than refactored mid-verification-pass; see `oxiarc-tiff/TODO.md`.
13. **[RESOLVED 2026-09-08, and it was twelve errors in four crates, not three in one] rustdoc under `-D warnings`.** This entry originally recorded three pre-existing `oxiarc-jpeg` rustdoc errors (two unresolved links in `src/compat/zune.rs` — the crate imports `JpegError` under `#[cfg(test)]`, so it is out of scope in a doc build — and one `private-intra-doc-links` on `[ojpeg]` in `src/tiff/mod.rs`). The GATES track ran the workspace-wide gate (`RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps --all-features --keep-going`) and found **twelve** errors in **four** crates: those three, one in `oxiarc-lzw/src/z/io.rs` (`into_inner` linking to the private `READ_CHUNK`), one in `oxiarc-deflate/src/deflate.rs` (linking to the private `crate::encoder`), and seven in `oxiarc-lzma`'s `xz` module. The lzma cluster is worth remembering: `lib.rs` puts an **outer** `///` doc comment on `pub mod xz;` while `xz/mod.rs` also has `//!` inner docs, and rustdoc then resolves the merged fragments in the **crate root's** scope — so `[`decompress_into`]` did not resolve at all *and* `[`decompress`]` silently resolved to the unrelated crate-root `oxiarc_lzma::decompress` (the LZMA one-shot, not the XZ one). All twelve are fixed (doc text only, no API or behaviour change); the gate is now exit 0 for all 18 crates.
14. **`oxiarc-core/src/crc_simd.rs` emits ~30 warnings under `cargo +1.85.0 build`** (independently observed by both DEFENC-verify §6 and INFLATEPERF-verify §3, in different sessions), including a future-incompatibility lint (E0133) tied to the edition-2024 `unsafe_op_in_unsafe_fn` rule. Not this program's crate to fix (no Phase 8 track owns `oxiarc-core`'s SIMD module), and not new — this is existing code, and every `cargo +1.85.0 build -p <crate> --all-features` a Phase 8 track ran (a real MSRV smoke test, not a regex) exits 0 for its own crate with these as the *only* diagnostics printed, all attributed to a file outside that crate. Flagged here so a future MSRV audit of `oxiarc-core` starts from a known baseline rather than rediscovering it.
15. **[RESOLVED 2026-09-08] The literal workspace-wide `cargo fmt --all -- --check`, `cargo clippy --workspace --all-features --all-targets -- -D warnings` and `cargo build --workspace --no-default-features` have now been run.** The original entry was correct for its moment: every track's coordination rules scoped fmt/clippy/build to `-p <own-crate>` throughout parallel development, because eighteen crates were being edited concurrently in one working tree and a workspace-wide `-D warnings` run is only as green as the slowest-to-land crate at the instant it runs (FUZZCLI-verify §1.1, JPEG2C-verify §1.1, TIFFPOLISH-verify §8 each document exactly that). The GATES track ran all three on the quiescent tree after every implementation track had landed: **all three green, zero warnings**, with every crate's `src/lib.rs` touched first so clippy genuinely re-linted all 18 crates and all their targets rather than replaying a cache. See the Wave-3 gates bullet above for the full command list and Known Issues #13 and #16 for the two defects the pass turned up.
16. **The single-line let-chain grep every track used is not sufficient — use the multiline form.** `rg -n 'if let .*=.*&& |&& let ' */src` (the command in every Phase 8 track's Definition of Done) returns **empty** on `oxiarc-zstd/src/compressed_block.rs`, which nonetheless contained a genuine let-chain — `if let Some((symbol, &count)) = first` on one line and `&& count == total` on the next. It compiled silently on the local rustc 1.95 and broke the declared MSRV: `cargo +1.85.0 check --workspace` failed with ``error[E0658]: `let` expressions in this position are unstable``. It survived every per-crate track and every verifier in the cycle. Fixed 2026-09-08 (de-sugared to nested `if`s; `clippy.toml` pins `msrv = "1.85"`, so clippy's `collapsible_if` will not suggest the let-chain back). **Future audits must use the multiline scan**, over the whole tree rather than just `*/src`: `rg -U -n -e 'if let [^;{}]*\n\s*&&' -e 'while let [^;{}]*\n\s*&&' -e '&&\s*\n?\s*let ' --glob '*.rs' --glob '!**/target/**' .` (currently empty), and treat `cargo +1.85.0 check --workspace` as the real oracle — a regex is a smoke test, the MSRV compiler is the gate.

## Test Coverage

Per-crate counts measured 2026-09-08 (`cargo nextest run --workspace --all-features` + `cargo test --doc --workspace --all-features`, one cold `CARGO_TARGET_DIR`, 0 failed, 0 skipped across the whole workspace; nextest tests + doctests):

- oxiarc-core: 231 tests (200 + 31 doctests)
  - CRC-32/64 slicing-by-8, DualCrc, SIMD CRC32 (aarch64 PMULL), SHA-256 (FIPS 180-4, new in 0.4.2, shared by `oxiarc-brotli`'s `dcb` and `oxiarc-http`'s `dcz`), bitstream (buffered `BitReader`/`BitCache`), msb_bitstream, ringbuffer (bounded back-reference copy lengths), EntryBuilder, Serde serialization
- oxiarc-deflate: 507 tests (455 + 52 doctests)
  - Resumable `InflateStream`/`WrappedInflate`/`InflateReader`/`AsyncInflateReader` (new in 0.4.2: byte-at-a-time feeding, 1-byte output buffers, proptest split points, truncation at every offset, multi-member gzip, all gzip header flags, cap/ratio enforcement mid-block, a straddling-match buffer-edge corruption fix and a malformed-Huffman-table hang/panic fix, both introduced and closed within this cycle), a zlib-faithful encoder rewrite (byte-identical to CPython `zlib.compress` at every level 1-9, `Strategy`, `with_optimal_parsing` call-size invariance), async deflate, gzip module (multi-member decode), true streaming Compressor/Decompressor trait semantics, LZ77 tuning, memory pool, CPython zlib/gzip + gzip-CLI differential suite (`zlib-oracle`)
- oxiarc-lzhuf: 274 tests (269 + 5 doctests)
  - LH1/LH4-7 roundtrips (incl. beyond-window), truncation-guard regressions (lh4-7 short streams now Err), streaming decoder hardening, LZSS, Huffman trees, optimal parser, custom dictionaries, real-LHA corpus + `lha-oracle`
- oxiarc-bzip2: 108 tests (104 + 4 doctests)
  - MSB-first bit I/O, non-reflected CRC-32, BWT, MTF/symbol-map Huffman, multi-stream (concatenated) decode, de-randomisation (legacy blocks), multi-Huffman-table encoder (sendMTFValues), `decompress_with_limit`, reference `bzip2` differential suite (`bzip2-oracle`)
- oxiarc-lz4: 173 tests (158 + 15 doctests)
  - Frame format, XXHash32, LZ4-HC, per-sequence `max_output` enforcement (bomb regression), LASTLITERALS(5) invariant, block-dependent (linked) frames (`FrameDescriptor::with_block_independence`), parallel compression, dictionaries, `lz4` CLI differential suite (`lz4-oracle`)
- oxiarc-zstd: 356 tests (341 + 15 doctests)
  - RFC 8878 LIFO/MSB backward bitstream (FSE reader AND writer), `ZstdStream` push decoder (new in 0.4.2: real sliding-window ring, pre-decode memory ceilings, `with_max_window`/`with_multi_frame`/`with_dictionary`), decode-throughput rewrite around the reference decoder's data layout, legacy one-shot decode-path hardening shared with the streaming decoder (`Dictionary_ID`, per-block size ceiling, leading-garbage/skippable-frame handling, reused-decoder state reset — `tests/legacy_hardening.rs` + `tests/legacy_verify.rs`), an offset-code-31 `u64` overflow fix, FSE table probability-sum validation + bounds-checked states, Huffman weight validation + implied-last-weight, Huffman literals encoder + `FSE_Compressed_Mode` sequence encoder, dictionaries, multi-frame, OxiGDAL Zarr corpus regression, reference `zstd` differential suite (`zstd-oracle`)
- oxiarc-archive: 614 tests (583 + 31 doctests)
  - ZIP (GP-bit-0 encryption detection, AE-2 CRC=0, Info-ZIP check byte, civil-date DOS timestamps, name encoding, Zip64, data descriptors), TAR (GNU 'S' + PAX 0.1 sparse in both readers, PAX long names), XZ (multi-chunk LZMA2, block-header CRC validation, bounded header allocs; `.xz` container framing now lives in `oxiarc_lzma::xz`, re-exported unchanged), 7z (bounded stream counts/kCrc), CAB (MSZIP window carry, CFDATA checksum, folder cache, unknown-method Err), ISO 9660 (LEN_DR bounds), LZH (0x42 64-bit size in stream reader), archive repair, async readers (de-flaked temp paths), `zip-oracle`/`xz-oracle`/`lha-oracle` differential suites
- oxiarc-lzma: 291 tests (277 + 14 doctests)
  - LZMA/LZMA2 (persistent cross-chunk uncompressed position, stateful chunked encoder, `LzmaProperties` validation), reusable `xz::XzDecoder` + multi-block `XzWriter` (new in 0.4.2, incl. a dedicated `.xz` block-check-integrity suite the verify pass added after mutation-testing found the check itself untested), optimal parsing, range coder, BT4 match finder, parallel LZMA2, dictionaries, memory pool, liblzma golden vectors, live `xz` differential suite (`xz-oracle`)
- oxiarc-lzw: 254 tests (233 + 21 doctests)
  - The legacy `.Z` container (`oxiarc_lzw::z`, new in 0.4.2: byte-identical to `compress -b N -c`, a memory-amplification fix, a crafted-input panic fix, a silent-corruption-on-write fix, `z-oracle`), explicit `LzwBitOrder` + 16-bit codes, GIF (differentially verified against Pillow for the first time, `gif-oracle`), a decoder rebuilt to libtiff's `LZWDecode` algorithm shape (TIFF Clear Code per TIFF 6.0/libtiff semantics), `bitstream_msb`/`bitstream_lsb` writers, fallible `LzwConfig::new`, length-framed stream format, Pillow/libtiff differential suite (`tiff-oracle`)
- oxiarc-brotli: 370 tests (349 + 21 doctests)
  - RFC 7932 decoder/encoder (block-type switching, context maps + §7.1 LUTs, code-length VLC, distance ring, metadata blocks, two-level Huffman), shared (custom LZ77) dictionaries + `Content-Encoding: dcb` framing (new in 0.4.2, incl. a dictionary-overrun rejection fix), `BrotliStream` push decoder, byte-exact Appendix A dictionary (CRC-verified) + 121 transforms, strict trailing-garbage/padding rejection, reference `brotli` differential suite (`brotli-oracle`)
- oxiarc-snappy: 140 tests (132 + 8 doctests)
  - Block + framed formats, 64 KiB per-chunk cap enforcement, bounded-decode APIs, CRC32C, memory pool, dictionaries, async I/O, cramjam/python-snappy differential suite (`snappy-oracle`)
- oxiarc-szip: 47 tests (46 + 1 doctest)
  - CCSDS-121.0-B-2 §5.2 framing (option ID before all RSI first-block samples), zero-block/ROS/second-extension options, typed validation errors, embedded libaec fixtures + live libaec 1.1.4 differential suite (`libaec-oracle`)
- oxiarc-http: 327 tests (310 + 17 doctests) — **new crate, 0.4.2**
  - `ContentCoding`/`QValue`/`AcceptEncoding`/RFC 9110 `negotiate`, gzip/deflate/br/zstd decode+encode over `WrappedInflate`/`BrotliStream`/`ZstdStream`, legacy `compress`/`.Z` (a self-tuning pull-size meter closing a ~128 MB peak-allocation gap), `dcb`/`dcz` (RFC 9842, the latter's dictionary-binding preamble added and then independently verified against `zstd -D`), chained codings, `DecodedBody`/`AsyncDecodedBody`, `tests/cross_crate_inflate.rs` (PNG IDAT / TIFF strip / HTTP body all through the same core, checked against independent ground truth), `tests/allocations.rs` bounded-RSS gates, `http-oracle` differential suite
- oxiarc-png: 335 tests (303 + 32 doctests) — **new crate, 0.4.2**
  - Chunk reader + CRC-32, every colour type × depth, Adam7 (sub-byte edge cases), incremental `IDAT` via `WrappedInflate(Zlib)`, pull `Decoder`/`Reader` + push `StreamingDecoder`, `png`-0.18-shaped lenient defaults + strict mode, Apple `CgBI`, filter strategies, `Deflater` driven directly, every ancillary chunk, full APNG, `png`-0.18-shaped compat + `v017`, Pillow differential suite (`png-oracle`) both directions
- oxiarc-jpeg: 619 tests (591 + 28 doctests) — **new crate, 0.4.2**
  - Baseline/extended/progressive/lossless decode, 9-bit fast Huffman, exact-integer islow IDCT, arithmetic coding (SOF9-11, default-on), OJPEG; baseline/extended-12-bit/progressive/lossless encode byte-identical to `cjpeg`; reduced-/enlarged-scale decode (`Scale`, byte-identical at `M` in `{1,2,4,8}`, incl. a `rayon` band-merge corruption fix for restart-marker streams at every other `M`); `compat::{zune, jpeg_decoder}` migration facades (each with an accessor/`decode()` mismatch fixed by the verify pass); two-component (`ColorSpace::Unknown(2)`, `JCS_UNKNOWN`) frames; `jpeg-oracle` byte-parity suite (`cjpeg`/`djpeg`/`tjbench`)
- oxiarc-tiff: 573 tests (535 + 38 doctests) — **new crate, 0.4.2**
  - Classic + BigTIFF headers, all 18 IFD value types, every baseline codec (None/PackBits/CCITT G3/G4 incl. new uncompressed mode/LZW/Deflate/ZSTD/LZMA/JPEG/OJPEG), predictors, photometric conversions, decoder-scratch pooling under `rayon` (2-3x), `tiff`-0.11-shaped `compat`, `mmap`, `tiff-oracle` (own per-crate feature, distinct from `oxiarc-lzw`'s feature of the same name) against `tiffcp`/`tiffinfo`/Pillow/`tifffile`
- oxiarc-image: 158 tests (145 + 13 doctests) — **new crate, 0.4.2**
  - `image`-0.25-shaped `DynamicImage`/`ImageReader`/`ImageBuffer` over PNG/JPEG/TIFF, format sniffing (an AVIF dead-mask fix), `to_luma8`/`to_luma16` matching `image`'s own sRGB weights, integer-exact colour conversions (3-6x faster), TIFF `Rgb32F`/`Rgba32F` round trip (a premultiply-clamp fix), `codecs::{png,jpeg,tiff}` facades, `tests/adversarial.rs` (truncation/corruption at every offset, all three codecs — the always-on substitute for the not-yet-built `fuzz_image_open`), `tests/conversions.rs` golden table
- oxiarc-cli: 124 tests
  - End-to-end format×subcommand matrix, `--memory-limit` enforcement during decode (all formats, incl. a 0.4.2 overflow fix on the SI-suffix parser), `.br` extension fallback, PNG/JPEG/TIFF `detect`/`info` (bounded-prefix sniffing, new in 0.4.2), corrupt-input no-panic sweeps, path-traversal, exit codes
- **Total: 5,505 tests passing, 0 failed, 0 skipped** (5,159 via `cargo nextest run --workspace --all-features`, re-verified 2026-09-12, across what was 213 test binaries on 2026-09-08 and not recounted this pass + 346 doctests; 230 suites — 213 binaries + 17 crates with nonzero doctests, oxiarc-cli has none). Zero clippy warnings per crate (`--all-features --all-targets -D warnings`, also with `--no-default-features`), as independently verified by every track, and workspace-wide: `cargo fmt --all -- --check`, `cargo clippy --workspace` (both feature sets) and `cargo build --workspace --no-default-features` were run green on 2026-09-08 — see Known Issue #15 (RESOLVED). Oracle suites self-skip *internally* when their reference tool is absent (nextest's own `0 skipped` counts only harness-level skips, so it cannot see this) — the counts above are reproducible on a hermetic machine either way. This track's own 5,155-test run did not re-verify with `--nocapture` that every oracle leg actually exercised its external tool rather than self-skipping workspace-wide; that per-suite check was done live, individually, by each owning track instead — e.g. HTTPWIRE-verify confirms 12/12 oracle checks ran (not skipped), and LZWPERF reports live timings from its own oracle run. Every external oracle tool this workspace uses was confirmed present on this machine (§Requirements), so none of the self-skip branches should have triggered — but that is an inference from tool presence, not a re-run with output captured, and is recorded as such rather than overstated.

## Code Statistics (measured 2026-09-08; tokei Rust code lines per crate incl. tests/examples)

| Crate | Lines of Code |
|-------|---------------|
| oxiarc-core | ~7,086 (CRC-32/64 slicing-by-8, DualCrc, SHA-256, bitstream/msb_bitstream with buffered `BitReader`/`BitCache`, bounded ringbuffer copies, EntryBuilder, Serde) |
| oxiarc-deflate | ~23,129 (zlib-faithful encoder, resumable `InflateStream`/`WrappedInflate`/`InflateReader`, Zlib wrapper, Adler-32, async_deflate, gzip multi-member, zlib-oracle suite, `inflate_differential`/`encoder_behaviour`/`inflate_fast_boundary` suites) |
| oxiarc-lzhuf | ~9,782 (canonical MSB-first LZSS + Huffman: lh0/1/4/5/6/7/lhd, truncation guards, custom dictionaries, lha-oracle) |
| oxiarc-bzip2 | ~3,303 (MSB-first bit I/O, multi-stream decode, de-randomisation, multi-table sendMTFValues encoder, decompress_with_limit, bzip2-oracle) |
| oxiarc-lz4 | ~6,229 (frame format, XXHash32, LZ4-HC, per-sequence output caps, linked-block frames, lz4-oracle) |
| oxiarc-zstd | ~15,031 (RFC 8878 LIFO/MSB FSE bitstream both directions, `ZstdStream` push decoder, decode-throughput rewrite, legacy-path hardening, validated FSE/Huffman tables, dict, streaming, zstd-oracle) |
| oxiarc-lzma | ~13,417 (persistent-position LZMA2 decode, stateful chunked encoder, reusable `xz::XzDecoder`, multi-block `XzWriter`, validated LzmaProperties, xz-oracle) |
| oxiarc-archive | ~24,395 (ZIP GP-bit encryption detection + AE-2/timestamps, TAR sparse, XZ header CRC + bounded allocs (framing now re-exported from `oxiarc-lzma`), 7z count caps, CAB window/checksum/cache, ISO bounds, zip/xz/lha oracles) |
| oxiarc-lzw | ~9,638 (`.Z` container, explicit bit order + 16-bit codes, GIF gif-oracle, TIFF/GIF decoder rebuilt to libtiff's algorithm shape, tiff-oracle) |
| oxiarc-brotli | ~15,232 (full RFC 7932 decoder+encoder, shared dictionaries, `dcb` framing, `BrotliStream` push decoder, tables.rs, 122,784-byte Appendix A dict_data.bin + 121 transforms, two-level Huffman, brotli-oracle) |
| oxiarc-snappy | ~4,306 (block + framed formats, 64 KiB chunk-cap enforcement, bounded decode, CRC32C, snappy-oracle) |
| oxiarc-szip | ~1,902 (CCSDS-121.0-B-2 §5.2 framing, zero-block/ROS/second-extension, typed validation errors, libaec-oracle) |
| oxiarc-http | ~9,094 (**new crate**: `ContentCoding`/negotiation, gzip/deflate/br/zstd/compress/dcb/dcz decode+encode, cross-crate inflate proof, http-oracle) |
| oxiarc-png | ~15,144 (**new crate**: decoder, encoder, ancillary chunks, APNG, CgBI, `png`-0.18-shaped compat, png-oracle) |
| oxiarc-jpeg | ~28,077 (**new crate**: baseline/progressive/lossless/arithmetic decode+encode, scaled decode, compat facades, two-component frames, jpeg-oracle) |
| oxiarc-tiff | ~26,601 (**new crate**: every baseline codec incl. CCITT uncompressed mode, decoder pooling, `rayon`, `tiff`-0.11-shaped compat, tiff-oracle) |
| oxiarc-image | ~4,301 (**new crate**: `image`-0.25-shaped facade over PNG/JPEG/TIFF, no image-processing operations) |
| oxiarc-cli | ~7,844 (enforced --memory-limit during decode, .br fallback, PNG/JPEG/TIFF detect/info, e2e format×subcommand matrix) |
| **Total** | **~224,511 code lines** (678 Rust files across the 18 crates; workspace-wide incl. fuzz harnesses: ~225,714 total code lines, 714 Rust files) |

## Stubs to implement (added 2026-06-22 by /cooljapan-stub-check)

- [x] LZH compression (lh5) encoder now spec-compatible (completed 2026-07-07) — MSB-first canonical rewrite of the lh4/lh5/lh6/lh7 codec (new `oxiarc-core::msb_bitstream`; `oxiarc-lzhuf/src/{encode,huffman,decode,lzss,optimal,methods}.rs` + `streaming/{decoder,huffman}.rs`) producing genuine LHA-wire-format archives; `oxiarc-archive/src/lzh/{writer,header}.rs` needed **no** changes — the default level-2 writer already emits headers real `lha` reads. Validated byte-exact against 6 genuine real-LHA `.lzh` fixtures (levels 0/1/2, incl. a 1.24 MB multi-block file, cross-tool) AND a live `lha` (Lhasa 0.6.0) oracle at **both** the codec level (`oxiarc-lzhuf` `lha-oracle`: `lha t`/`x` round-trips) and the **archive** level (new `oxiarc-archive` `lha-oracle`: `lha l`/`t`/`x`/`pq` over small, ~150 KB multi-block, multi-file, Shift_JIS-named, empty and stored archives); reverse direction (real archive → `LzhReader`) added as always-run `tests/lzh_corpus_reader.rs`. Suites green (oxiarc-archive 389, oxiarc-lzhuf 163), clippy clean, oracle self-skips without `lha`. Residual (out of scope, non-blocking, pre-existing): the opt-in level-1/0 writer stores Unix time where canonical LHA uses DOS FAT time (cosmetic date only — `lha` still CRC-tests + extracts fine), and Lhasa does not read level-3 headers at all; the default level-2 path is fully clean. Prior fix (v0.3.4, 2026-07-06): an earlier, already-shipped beyond-window corruption bug (LZSS clobbering history past the window, a 16-bit per-block size field overflowing past 65535 bytes, wrong p-tree count-field width for np=16/17) was fixed under this same item with 8/16/64/100 KB CRC-16 regression tests; that fix stands independently — this canonical-format rewrite was the separate, now-closed real-tool-readability work.

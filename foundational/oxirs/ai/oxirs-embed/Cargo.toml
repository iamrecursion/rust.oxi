[package]
name = "oxirs-embed"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Knowledge graph embeddings with TransE, ComplEx, and custom models"
keywords = ["embeddings", "knowledge-graph", "machine-learning", "ai", "rdf"]
categories = ["science", "algorithms", "data-structures"]
readme = "README.md"
rust-version.workspace = true

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
# Core dependencies - using workspace versions for consistency
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
# ndarray and rand now provided by scirs2-core
rayon.workspace = true
nalgebra.workspace = true
uuid = { workspace = true }
tokio = { workspace = true }
async-trait = { workspace = true }
chrono = { workspace = true }
tempfile = { workspace = true }
# GraphQL and streaming dependencies - downgraded to avoid arrow conflicts
async-graphql.workspace = true
tokio-stream = { workspace = true }
futures-util = { workspace = true }
regex.workspace = true

# Federated learning dependencies
oxicode.workspace = true  # Replaces bincode per COOLJAPAN Policy
oxiarc-deflate.workspace = true
oxiarc-lz4.workspace = true
oxiarc-zstd.workspace = true

# Model validation dependencies
sha2.workspace = true
blake3.workspace = true
hex.workspace = true

# Model zoo dependencies
toml.workspace = true
once_cell.workspace = true

# Internal OxiRS dependencies
oxirs-vec.workspace = true

# SciRS2 AI/ML dependencies for enhanced embeddings with full feature support
# Note: array! macro is now in scirs2_core::ndarray_ext, no need for scirs2-autograd
# Note: rand and rand_distr replaced by scirs2_core::random
scirs2-core = { workspace = true, features = ["random", "array", "parallel", "simd", "profiling"] }
scirs2-neural = { workspace = true }
scirs2-linalg = { workspace = true }

# Web server dependencies (for API server feature)
axum = { workspace = true, optional = true }
tower = { workspace = true, optional = true }
tower-http = { workspace = true, features = ["cors", "timeout", "trace"], optional = true }

[dev-dependencies]
criterion = { workspace = true }
tempfile = { workspace = true }
tracing-subscriber = { workspace = true }
tokio-test = { workspace = true }

[features]
default = ["basic-models"]
basic-models = ["transe", "complex", "distmult", "hole"]
advanced-models = ["rotate", "conve", "tucker", "quatd"]
neural-models = []
transe = []
complex = []
distmult = []
hole = []
rotate = []
conve = ["neural-models"]
tucker = []
quatd = []
custom-models = []
ontology-aware = []
multi-modal = []
gpu-acceleration = ["gpu", "cuda"]
api-server = ["dep:axum", "dep:tower", "dep:tower-http"]
graphql = []  # GraphQL API support
onnx-export = []   # Placeholder — no dep yet, pure-Rust ONNX writer not mature
tf-export = []     # Placeholder — TensorFlow SavedModel export (future)
download = []      # Placeholder — HTTP checkpoint download (future, requires network permission)

# High-performance acceleration features
gpu = ["scirs2-core/gpu", "scirs2-linalg/gpu", "scirs2-neural/gpu"]
cuda = ["scirs2-linalg/cuda"]
opencl = ["scirs2-linalg/opencl"]
rocm = ["scirs2-linalg/rocm"]
metal = ["scirs2-linalg/metal"]
simd = ["scirs2-linalg/simd"]
parallel = ["scirs2-linalg/parallel"]

[[example]]
name = "conve_model_demo"
required-features = ["conve"]

[[example]]
name = "distributed_training_demo"
required-features = ["basic-models"]

[[example]]
name = "temporal_embeddings_demo"

[[example]]
name = "storage_backend_demo"
required-features = ["basic-models"]

[[example]]
name = "gpu_acceleration_demo"
required-features = ["gpu"]

[[example]]
name = "integrated_ai_platform_demo"
required-features = ["gpu"]

[[example]]
name = "load_pretrained"
